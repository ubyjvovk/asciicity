# Tiger Team state

Written for a cold-start PM who has read nothing else. Keep it current: update
after every review cycle and before ending any session, then commit.

## Mission
**AsciiCity** — a static browser minigame: first-person walk around the City of
London rendered as coloured ASCII glyphs with a green NAVIGATION HUD (reference:
the user's screenshot of a similar live app — dense coloured glyph buildings,
perspective floor grid, black sky, HUD rows SECTOR/WORLD/BEARING/ZONE). Stack
locked in `docs/architecture.md`: Vite + TS + three.js, GPU ASCII post-pass,
OSM building footprints via Overpass (`docs/data-format.md`). "Done" for wave 1
= playable at `npm run dev` and on GitHub Pages with ≥ 55 fps, e2e smoke green.

## Configuration notes
- Mode: single-branch (accepts merge straight into main; no staging worktree).
- Fleet (`tigerteam.toml`): `grok` ×2 (C3), `opus` ×2 (claude, login_auth, C3),
  `ds` ×2 (pi → DeepSeek V4 Flash on DeepInfra, C2). `glm` parked: DeepInfra
  only has GLM-5.2 (user: too slow; also 400'd via pi) — enable when GLM-5.3
  ships. `max_concurrent = 6`.
- Secrets in `<root>/.env` (DEEPINFRA_KEY, GITHUB_TOKEN). The supervisor must be
  restarted after `.env` changes (it was restarted 2026-08-23 23:40 for this).
- `test_cmd = bash scripts/test.sh` (vitest; self-installs node_modules);
  `verify_cmds = bash scripts/check.sh` (typecheck+unit+build+e2e) runs on the
  host in `_staging` — host has playwright chromium-1193 installed
  (`npx playwright@1.55.1 install chromium`, 2026-08-23).
- Worker image `tigerteam-agents:base` has node 22 + chromium-1193 baked
  (`/opt/pw-browsers`) → `@playwright/test` pinned to 1.55.1.
- GitHub: `[github] repo = ubyjvovk/asciicity`, `sync = true` (board → issues,
  tt:* labels), `watch = true`. `gh` CLI is not installed; use `tigerteam gh`.
- The cockpit tmux session is `tigerteam-asciicity` (pane %5 = supervisor).
- PM-owned files: `src/data/types.ts`, `src/world/mesh.ts`, `docs/architecture.md`,
  `docs/data-format.md`, `AGENTS.md`, `package.json`/lock after T-0001.

## Decision log (append-only)
- 2026-08-23 — Stack: Vite+TS+three, GPU ASCII pass (render at cell res → glyph atlas shader) — the only way to get "decent fps" at ~320×90 cells; fully specified in architecture.md §4.8 so it's a C2 ticket.
- 2026-08-23 — Area: City of London bbox `-0.106,51.506,-0.070,51.521`, origin Bank junction; tall buildings match the reference look; bbox is a CLI arg so Westminster can be fetched later.
- 2026-08-23 — Data is fetched once (`scripts/fetch-osm.mjs`) and COMMITTED as `public/data/city.json` (<6 MB) — no runtime Overpass dependency; synthetic city is the offline/test fallback.
- 2026-08-23 — Pure builders return `MeshData` (typed arrays) so geometry is unit-tested in node; browser-only code is thin and covered by the e2e smoke + PM visual review.
- 2026-08-23 — GLM worker dropped (user), Opus worker added (user: "quota to burn").
- 2026-08-23 — Integration ticket T-0010 is C3 (grok/opus only): most judgment lives there.
- 2026-08-24 — **Single-branch mode** (`staging = false`). Staging mode diverged `main`/`staging` on the very first accept (board commit lands on main, merge on staging → `--ff-only` can never advance main) AND worker worktrees are cut from HEAD=main, so the whole fan-out started without T-0001's files. Killed the 6 attempts (KILL.<instance>, strike-free), merged staging into main (77c4ca2), deleted the six stale branches/worktrees and the staging branch, lifted STOP. PM verifies every landing in-container before accept instead.
- 2026-08-24 — Process rule: COMMIT the board/scaffold BEFORE moving tickets into todo/ — workers claim within seconds and cut worktrees from HEAD.
- 2026-08-24 — Look tuning done by the PM (C3 judgment) in a `pm/tune` worktree with a GPU screenshot loop, not by workers: glyph density from `max(r,g,b)` (hue-independent), gamma 0.45 (≈ linear→sRGB), exposure 1.7. Shader in architecture.md §4.8 is the contract again.
- 2026-08-24 — SwiftShader blanks render targets ≤ 64 rows; e2e keeps SwiftShader (assertions tolerate it) but visual review uses the RTX 3090 (`--use-angle=gl-egl`).

- 2026-08-26 — **Wave 5 = 3D.** User: "London is flat — do something 3D; central Kyiv." Interpretation: real terrain. Design locked by the PM (architecture.md §4.9/§4.10, data-format.md §Terrain): SRTM 1″ from the public AWS skadi mirror (`N50E030.hgt.gz`, 4.7 MB, verified reachable; Kyiv bbox spans 89–215 m ASL: Dnipro 94, Maidan 156, Lavra 191, Sophia 197) → `terrain` grid (step 20 m, heights relative to the origin `datum`) + `waterLevels` (10th-percentile of DEM at ring vertices; river-bed nodes flattened to it). Sampler and heightfield share one triangulation so draped geometry never floats. Every builder takes a `HeightFn` (default `FLAT_HEIGHT`) so London stays byte-identical. Bridges: straight deck between abutments, `max(lerp, terrain)`; `makeGroundAt = max(terrain, deck)` for player/buses/sky; boats ride the flattened river bed + 1 m.
- 2026-08-26 — Multi-city (user: "don't remove London, let the user choose on startup"): one JSON per city (`city.json` London unchanged, `kyiv.json` new, `--lang en` names), `src/data/cities.ts` registry, `?city=`, picker on the start overlay when `?city=` absent. Esc pause menu (user request): `COPY LINK TO HERE` (`?city=…&at=lon,lat,bearing`, pure `buildShareUrl`) + `SWITCH CITY`.
- 2026-08-26 — Kyiv pedestrian bridges are OSM footways → converter now keeps footways with `bridge≠no` as `pedestrian` + `bridge: true` (data-format.md road table).

- 2026-08-26 — **Fog stays 0.0018 for Kyiv.** PM experiment (`pm/tune` worktree, `?fog=` override, GPU frames from Motherland/Lavra/Volodymyr Hill): halving the density to 0.0009 changes only a mid band; even with fog off the far bank is a thin strip just under the horizon at level pitch (154 m → 95 m over 2 km ≈ −1.7°). The vista is limited by viewpoint geometry, not fog; the relief reads best from below (funicular, Podil) and on the bridges. Revisit only with a different mechanism (e.g. brighter far-field exposure or a default downward pitch at hilltop presets).

- 2026-08-26 — **Wave 6 = movement + render styles.** User: walking too slow → walk 9 / sprint 27 (T-0048, done); fly mode (T-0049: F toggles, fly where you look, Space/C, noclip, ground clamp, 1 500 m ceiling, altitude-thinned fog, far 6 km, constant-speed landing); "ticket all the free/cheap renderers, R cycles, fold the theme switcher into renderers". Design (architecture §4.11, PM-owned `src/render/style.ts`): a style = fragment shader over the low-res scene target + cell/sub geometry + own uniforms; `StyleRenderer` (post.ts) replaces `AsciiRenderer`; twelve styles in `STYLE_ORDER` (ascii/gloom/solarized = the old themes, braille, blocks, teletext, dither, gameboy, pico8, edges, hatch, matrix); ≤ 640×360 scene target keeps ≥ 30 fps on integrated GPUs; `G` removed, `?theme=` kept as an alias. Core ticket T-0050 (C3) creates **stub modules** for every id so the eight style tickets have disjoint one-file scopes and the e2e style loop (12 screenshots to `e2e/__shots__/style-<id>.png`) works from day one.

- 2026-08-28 — **Wave 8 boarded from user notes** (contracts 0ac005e, board 4106604): (a) OG/social meta + committed `public/og.png` 1200×630 — links currently unfurl blank on X/Reddit; absolute URLs to https://ubyjvovk.github.io/asciicity/; (b) postcard export — `P` → PNG of the current frame + 28-px caption bar, `Shift+P`/menu → 3 s · 12 fps · ≤ 960 px GIF via `gifenc@1.0.3` (dep committed by the PM); capture = same-task `drawImage` after `post.render` (preserveDrawingBuffer stays false); (c) `amber` render style — user reference `ascii-another-example.png` (untracked, 3.2 MB, repo root): near-black frame, amber/olive glyphs, warm-white bloom, crushed darks; formula locked in §4.11 as ascii-family theme 3 (amberDensity black-point 0.06 + gamma·1.5, amber↔olive chroma by source greenness). Stub committed first so STYLE_ORDER = 13 and the e2e style loop covers amber from day one. Postcard/amber e2e live in NEW spec files (`e2e/postcard.spec.ts`, `e2e/amber.spec.ts`) so `smoke.spec.ts` has no writers this wave (its only wave-8 change, 12→13, was the PM's).

- 2026-08-28 — **San Francisco (user: "add SF as a destination next").** Two tickets, contracts a4eea26: T-0075 (C3) coastline support — SF Bay is `natural=coastline` ways (no polygons), invisible to the converter, so the Bay would be walkable ground. Locked algorithm in data-format.md "Coastline water": Overpass clause + clip→stitch→CLOCKWISE bbox-perimeter closure (keeps water, right of way direction, inside the ring); closed rings = islands, emitted as water rings with ODD-parity flattening in dem.mjs (Alcatraz keeps terrain+buildings); runtime collision intentionally stays "any ring = water" (islands unwalkable — fine, they're islands). T-0076 dataset: bbox −122.487,37.764,−122.383,37.835 (GGB→Embarcadero, Alamo Sq, Alcatraz; Twin Peaks/Bay-Bridge-east out), origin Union Square, DEM N37W123 (already W-safe: hgtTileName handles W001), ≤ 12 MB guard (blocked question if over), `?city=sf`, default spawn `ggb` ON the bridge deck (fixed coords must snap to the deck centre-line; e2e y > 20 catches a fallen-to-sea spawn — datum ≈ 25 m ASL, deck ≈ 67 m ASL), GGB towers as int'l-orange (0xc0362c) 227 m extras, presets ggb/transamerica/salesforce/coittower/ferrybuilding/paintedladies/lombard/pier39/unionsquare (`goldengate` is Kyiv's — collision avoided). presetsFor loosened to accept string so main.ts (in flight, T-0072/73) is untouched. No boats (no river centre-lines; ferries = later idea).

- 2026-08-28 — **Post-restart wave-8 close-out.** `scripts/make-og.mjs` now honours `PW_ARGS` (same convention as the PM probes) and hides `#toast` before capture — on a fast GPU the boot `RENDER: ASCII` toast is still fading when the FPS row first goes non-zero and it landed in the frame. og.png is a GPU capture from now on (regenerate with `PW_ARGS='--use-angle=gl-egl --ignore-gpu-blocklist --disable-dev-shm-usage --no-sandbox' node scripts/make-og.mjs`). Amber judged an acceptable first pass against `ascii-another-example.png` (amber/olive on near-black, 60 fps) — the reference has brighter warm-white bloom highlights and more olive in the darks; no blind PM tune, ask the user.

- 2026-08-28 — **Wave 9 boarded from user feedback ("GGB is just a plain road — real geometry"; "Alcatraz must be a landmark"; "huge cargo ships + sailboats, lights on at night").** Findings: the GGB roadway is `highway=motorway`, unmapped by the converter (only the two sidewalks survive) → motorway→primary + re-fetch (T-0077). T-0076's tower extras were placed from my memory: OSM pier centroids put the south tower at 37.8140 (I had 37.8107 = the anchorage) and the north at 37.8255 (I had 37.8199 = mid-span) → removed in T-0080, which synthesises the structure from a spec table (§4.16: legs with a portal arch the sidewalk walks through, struts, parabolic cables with 3 m mid-span clearance, hangers /16 m, 7.6 m truss deck box, anchorages; visual-only, no collision). Islands: runtime collision merged rings as pseudo-footprints → odd-parity water test in CollisionGrid (T-0078, §4.6) so Alcatraz is walkable → lighthouse fix `label: 'Alcatraz'` + `alcatraz` preset + `ggb` re-aim 260 m south of the south tower bearing 355 (T-0079). Ships: no river centre-lines in SF → curated lanes table + two instanced classes with separate unlit "lights" meshes toggled by `sunPosition(...).altitudeDeg < -6` (the stars' threshold; there is no time-of-day scene lighting otherwise) (T-0081, §4.17). Contracts 23f9c97. C3 tickets pinned `assignee: grok` (opus is 10–30× the price for equal C2/C3 quality here).

- 2026-08-29 — **Wave 10 boarded from user direction** ("keep opus — it's on a weekly quota; do the loading indicator; add Manhattan with 20 landmarks with good geometry; think about on-the-fly sector loading — Tokyo"; then "Manhattan in one piece first, chop it up when streaming lands"). Findings: Manhattan bbox (Battery→59th St, both bridges' far banks) = 52 126 buildings, 15 494 `building:part` ways → "good geometry" = parts support (`minH`, outline replacement, clamp 320→600 — One WTC/ESB/One Vanderbilt/432 Park/CPT exceed 320); DEM loader already mosaics tiles (W074+W075 fine); Brooklyn Bridge has one central promenade → bridge spec gets `sidewalks: [name] + deckWidth`. Loading indicator = streamed fetch + phases with `await nextFrame()` between builders (§4.18). Assignee pins dropped (opus is quota-priced for the user). Streaming (tiles + TileManager) deferred to wave 11 with Tokyo; Overpass rate-limited my Tokyo counts — estimate from density: central 8×5 km ≈ 150–250 k buildings ≈ 50–80 MB monolithic → streaming required.

- 2026-08-31 — **Wave 14 = Sydney (user: "next: let's add Sydney").** First southern-hemisphere city. PM recon before boarding: (a) `hgtTileName`/`fetchDemTiles`/`elevationAt` are sign-correct for `S34E151` — verified by a live probe (Observatory Hill 24.5, North Sydney 78.4, CBD 78.8 raw = roof-decked → `--dem-bare 1` required; NO dem.mjs ticket needed); (b) Overpass counts 19 815 buildings / 934 parts / 27 485 highways (bbox 151.183,-33.895,151.245,-33.833 ≈ 5.7×6.9 km, ~40 tiles, budget 12 MB); (c) Harbour Bridge deck names in OSM: `Bradfield Highway` (motorway), east walkway = `Cahill Walk` (named! also names the Circular Quay stretch — hump distortion risk flagged in T-0112), `Harbour Bridge Cycleway`; north pylon pair = `bridge:support` ways @ 151.2116,-33.8479, south pair unconfirmed (rate-limited) → worker reconciles ends against the dataset; (d) Sydney Tower is a `building:part` h=270 (renders via wave-10 parts, no extra needed); (e) Opera House is a building RELATION — converter ingests multipolygons (fetch-osm.mjs:45), safe. Contracts committed 2233514 (data-format "### Sydney", architecture **§4.16b ArchBridgeSpec** — two-rib parabolic-chord through-arch, hangers above/struts below, 4 granite pylons, deckHumps/makeBridgesObject union over both tables; `fetch-data:sydney` alias). Fleet note: grok scale 0 (user), so the C3 arch goes to opus.

## Board snapshot
- 2026-08-24 01:00 — **Wave 1+2 complete: 20/20 accepted.** Playable at `npm run dev`: real City of London data validates and loads, ASCII pass, HUD (6 rows), minimap, landmark row, CRT overlay, favicon, e2e smoke green in-container, GitHub Pages workflow on main. PM look-tuning merged (850ee7f): max-channel glyph density, gamma 0.45, exposure 1.7, brighter wall base, 3-px mipmapped floor grid. Verified on the RTX 3090 via `--use-angle=gl-egl`: 60 fps at 213×60 cells; screenshot in `docs/screenshot.png`.
- 2026-08-23 23:55 — wave 1 planned: T-0001 (P0 bootstrap) → T-0002…T-0009 fan-out (data, buildings, roads/ground, ascii, collision, controls, HUD) + T-0012 (Pages) → T-0010 (integration, C3) → T-0011 (e2e).
- 2026-08-24 00:10 — T-0001 accepted (ds-1, clean). Fan-out restarted after the staging incident; 6 instances on 9 claimable tickets.
- 2026-08-24 00:20 — T-0002 (ds-1) and T-0005 (grok-2) accepted first pass; both in-container check.sh green, all enumerated tests present. In flight: T-0003 ds-2, T-0004 grok-1, T-0006 opus-1, T-0007 opus-2, T-0008 ds-1, T-0009 grok-2.
- 2026-08-24 00:35 — T-0006 (opus-1), T-0007 (opus-2), T-0008 (ds-1) accepted first pass. Wave-2 note: Controls zeroes an axis when either key of the pair lifts (W held + S released ⇒ stop) — polish ticket later (track a held-key Set). In flight: T-0003, T-0004, T-0009, T-0012.
- 2026-08-24 00:55 — T-0012 (opus-1), T-0004 (grok-1), T-0003 (ds-2: 3118 buildings / 10307 roads / 43 places, 1.76 MB, real tower heights), T-0009 (grok-2) accepted first pass. 10/12 wave-1 tickets done; merged main gate green (80 unit tests). T-0013 (controls held-key polish) added and claimed by grok-2. T-0010 (integration, C3) now claimable.

- 2026-08-24 01:40 — 25/29 done. Wave 3 landed: minimap contrast, `?at=` presets + coordinates, Thames/dock water (data + render + collision), **Westminster dataset** (bbox −0.130…−0.070 / 51.497…51.521, origin still Bank, footways dropped: 9061 buildings / 7803 roads / 99 places / 31 water, 2.8 MB). User playtested and asked for a Big Ben start → T-0027 done, T-0028 (default spawn on Westminster Bridge @268°, verified visually via `?at=-0.12235,51.50085,268`: tower dead ahead) claimable; T-0025 touch, T-0026 landmark spawns, T-0029 landmark colours queued.

- 2026-08-24 02:20 — **29/30 done.** Big Ben default spawn (T-0028) + walkable bridges (T-0030: `Road.bridge` flag, `CollisionGrid` corridors override water) + touch controls (T-0025) + landmark colours (T-0029) merged. Verified on GPU: default `/` spawns mid-Westminster-Bridge at x≈−2331, ZONE WESTMINSTER BRIDGE, walking west works. Pushed to origin. Remaining: T-0026 (data-driven landmark spawns).

- 2026-08-24 02:45 — **30/30 done; board empty.** T-0026 data-driven landmark spawns merged (`?at=gherkin` → 70 m from 30 St Mary Axe facing it; verified on GPU). Wave-4 candidates (not queued, awaiting user): landmark spawn distance scaled by building height; tone down water saturation; bridge deck rendering/Tower Bridge; minimap zoom; day/night palette; sound; `?cell=` presets in the HUD help.

- 2026-08-24 04:20 — Wave 4 boarded after user discussion: T-0031 mobile HUD (P0: HUD was intercepting touches — 320px panel on a 390px phone; verified via touch-emulation harness that the joystick itself works), T-0032 idle joystick ring, T-0033 gloom mode (`G`/`?gloom=1` — Ctrl+0 is browser zoom so G chosen), T-0034 sun/moon/stars (`?time=`), T-0035 double-deckers, T-0036 Thames boats (rivers contract added to types.ts). main.ts chain serialised T-0031→33→34→35→36. Traffic is pass-through by user decision ("rideable later").

- 2026-08-24 15:05 — **Wave 4 complete: 38/38 accepted.** Landed since the last checkpoint: mobile HUD (click-through + compact) & idle joystick ring, theme system (G cycles cyber/gloom/solarized, hot cells keep the sun/moon/lit windows bright — user-driven two-pass contrast tuning), astronomically correct sun/moon/stars (`?time=`; one rework: sky must ride with the camera — spec bug), double-deckers on the road graph (visually verified via seeded ambush), Thames boats on OSM river centre-lines (verified from London Bridge). All gates green (257 unit tests + e2e).

- 2026-08-26 21:15 — **Wave 5 boarded (8 tickets, 38 done).** Contract commit be2e146, board 353d698. Graph: T-0039 dem.mjs (P0) · T-0041 validator+synthetic hills (P0) · T-0042 terrain.ts (P0) all claimable now → T-0040 pipeline + `kyiv.json` (needs 39, 41) · T-0043 drape builders (needs 42) → T-0044 traffic heights (needs 43) → T-0045 main.ts integration, `?city=`, Kyiv presets, ALT row, hills e2e (C3, needs 40/42/43/44) → T-0046 city picker + Esc pause menu + share link (needs 45). PM follow-ups after T-0045: GPU visual review from `?city=kyiv&at=maidan` / `hydropark` / `parkbridge`, look-tune the slope shade if hills read badly, then T-0046 review and push.

- 2026-08-26 21:35 — T-0041 (ds-1, $0.02) and T-0042 (grok-2, $0.16) accepted first pass; both gates green in-container (`WORKER_IMAGE=tigerteam-agents:base bash .tigerteam/scripts/in-container.sh <wt> -- bash scripts/check.sh`). terrain.ts is exactly §4.9. Minor: `makeGroundAt` allocates a `[x, z]` pair per call (contract signature) — negligible, revisit only if profiling says so. T-0039 (ds-2) still running; T-0043 now claimable.

- 2026-08-26 21:50 — T-0043 (grok-1, $0.15) and T-0039 (ds-2, $0.04; DEM at Maidan = 155.64 m from the real tile) accepted first pass. **Process note:** never run two in-container `check.sh` gates concurrently — the SwiftShader e2e starves and fails its "moved ≥ 0.5 m" assertion (T-0039 failed paired, passed solo). 42/46 done; T-0040 (pipeline + kyiv.json) and T-0044 (traffic) claimable.

- 2026-08-26 22:00 — T-0044 (ds-1, $0.01) accepted first pass; gate green solo. 43/46 done. Remaining chain: T-0040 (pipeline + kyiv.json) → T-0045 (C3 integration) → T-0046 (picker + pause menu).

- 2026-08-26 22:10 — T-0040 (opus-1, $5.34 — the one expensive attempt of the wave) accepted first pass. **`public/data/kyiv.json` committed**: 8183 buildings / 6740 roads / 193 places / 51 water / 19 rivers, terrain 323×270 @ 20 m, datum 155.6, heights −65.6…+59.1, Dnipro ring (5.2 km²) at −63.4 (≈ 92 m ASL), 81 bridge roads (Paton 1.5 km, Metro, Park/Klitschko pedestrian), 2.6 MB. PM spot checks: Sophia / St Michael / Lavra / Golden Gate / Saint Andrew's / Verkhovna Rada resolve by English name; 610 of 936 named buildings stay Cyrillic (no `name:en`). Known data quirk for a later look-tune: `Saint Sophia Cathedral` is h = 3 m in OSM (needs a Kyiv landmark height/colour table like T-0029). T-0045 (C3) now claimable.

- 2026-08-26 22:45 — **T-0045 accepted (opus-1, $11.05, 27 min) — Kyiv is playable.** Gate green in-container; GPU visual pass (RTX 3090, `node_modules/.pm-shot.mjs` must live under a worktree's `node_modules/` to resolve playwright): all probes 60 fps; ALT 155 m Maidan / 195 m Lavra / 99 m Hydropark / 108 m funicular; London control unchanged (six rows, y = 1.7, Big Ben). Hills read well from below (funicular: the hillside fills the upper half as a tilted grid) and across the river; from the Lavra plateau the valley disappears into fog before it can show. **Look-tune candidate (PM):** lower `FogExp2` density for terrain cities (0.0018 → ~0.0009) and check the void edge beyond the grid. Found: Parkovyi + Klitschko bridges are `highway=cycleway`+`bridge=yes` — never fetched → T-0047 filed (depends on T-0046 for docs/integration.md). Also noted by the worker: `docs/hud.md` describes a `div.hud-help` that hud.ts never creates (pre-existing); compact HUD breakpoint sized for 6 rows.

- 2026-08-26 23:05 — **Pushed main (367b761) → Pages deploy green; Kyiv live at https://ubyjvovk.github.io/asciicity/?city=kyiv** (user asked to try it). T-0046 attempt 1 (ds-1, $0.05): gate green, picker + pause menu verified visually on the GPU (probe: `node_modules/.pm-menu.mjs` in the worktree), but clicking the `#share` fallback input bubbles to the overlay's click-to-resume — **reworked** (stop propagation on `#menu`, hide the field until copied, add the check to the headless evidence). T-0047 waits on T-0046.

- 2026-08-26 23:20 — T-0046 accepted on attempt 2 (ds-1, $0.06 total): `#menu` container guard verified by the PM probe (overlay stays up after clicking `#share`), gate green. **46/47 done — the user-facing wave-5 scope is complete**: city picker on `/`, `?city=`, Esc pause menu with COPY LINK TO HERE / SWITCH CITY. Pushed to Pages. Only T-0047 (cycleway bridges + the two bridge presets) remains.

- 2026-08-26 23:40 — **Wave 5 complete: 47/47 accepted, board empty.** T-0047 (ds-1, $0.07) accepted first pass: Parkovyi (434 m) and Klitschko (209 m) cycleway bridges fetched, both presets verified on the GPU spawning on the deck (ZONE "PARKOVYI BRIDGE" 117 m / "BRIDGE OVER VOLODYMYRSKYI DESCENT" 157 m, walking works). Pushed to Pages. Wave cost ≈ $17 engine-reported, dominated by the two opus tickets (T-0040 $5.34, T-0045 $11.05); the seven DeepSeek/grok tickets together < $0.60. One rework in nine tickets (T-0046 `#share` click). No `events --wait` is armed (nothing in flight).

- 2026-08-26 23:59 — **Wave 6 boarded: 11 tickets.** T-0048 speeds accepted + pushed (6507b7a). In flight: T-0049 fly (ds-1). Queued: T-0050 render-styles core (C3, needs T-0049 for main.ts) → T-0051 braille · T-0052 blocks · T-0053 teletext · T-0054 dither/gameboy · T-0055 pico8 · T-0056 edges · T-0057 hatch · T-0058 matrix (all C2, one file each, parallel). Waiter armed.

- 2026-08-27 00:30 — T-0049 attempt 1 (ds-1, $0.10): gate green; GPU flight verified (`node_modules/.pm-fly.mjs` probe in the worktree: +150 m / 5 s Space, 60 fps at 150 m AGL, aerial view with thinned fog reads well, `F` falls at 30 m/s and lands). **Reworked** for one PM-side ambiguity: ALT must be the eye altitude (`datum + y − EYE_HEIGHT`), not the ground's. User asked (no ticket yet) how hard trees would be — answered: ~3 C2 tickets (OSM `natural=tree` nodes + tree_row + wood/park polygon fill at fetch time → `trees` list → one InstancedMesh like the buses; minimap wood fill); do after the render styles if they say go.

- 2026-08-27 00:50 — **T-0049 accepted on attempt 2** (ds-1, $0.13 total): ALT = eye altitude verified (304 M ASL at 150 m over Maidan); gate green; pushed 70544ab → Pages. Fly mode is live: `F`, Space/C, Shift = 90 m/s. Note: the armed `events --wait` did NOT deliver the attempt-2 landing (the supervisor nudge did) — killed it and re-armed; if it recurs, check `tigerteam events --peek` after each accept. T-0050 (render-styles core, C3) now claimable.

- 2026-08-27 01:20 — **T-0050 accepted first pass** (grok-1, $0.87, 26 min): `StyleRenderer` + 12 ids (3 real, 9 stubs), gate green; GPU cycle via `node_modules/.pm-styles.mjs` (in the worktree): all 12 at 56–60 fps incl. the depth-texture `edges` stub, toasts OK, ascii frame identical to pre-refactor. Worker findings worth keeping: three r185 throws on `depthTexture: undefined` (omit the key); `styleGrid` clamps cols/rows so every target ≤ 640×360 (2×2-cell styles become ~3×3 at 1080p — acceptable). Pushed 56c974f. **Eight style tickets T-0051…T-0058 now claimable in parallel** (6 slots).

- 2026-08-27 01:45 — T-0056 edges attempt 1 (opus-1, $2.68): gate green, look right, but my §4.11 first-difference depth rule lit every grazing surface (solid ground band). Contract revised to the second difference on inverse depth (exactly linear across planes); **reworked** with a pixel-fraction criterion (< 15 % edge pixels on the synthetic city). Six styles in flight.

- 2026-08-27 02:00 — **T-0052 blocks (ds-2, $0.04), T-0053 teletext (grok-2, $0.25), T-0054 dither+gameboy (grok-1, $0.22) accepted**; GPU frames from Maidan all 60 fps and each a distinct, correct look (ANSI quadrants; Ceefax 8-colour mosaic; Obra-Dinn 1-bit; 4-shade Game Boy). **Process incident:** their three in-container gates all reported `e2e: FAIL` while five worker containers were running (load 9.4/12 cores) — and I had chained the accepts before reading the gate lines, so all three merged unverified. Recovery: nothing had been pushed; merged-main gate on the HOST (`bash scripts/check.sh` — the container shim cannot run on the board root: `EROFS` on `node_modules/.vite-temp`) came back fully green, so the failures were starvation flakes. Rules from now on: never chain `accept` after a gate in one command; when a gate's e2e fails under fleet load, rerun on the host before judging; merged-main gate = host `check.sh`. Pushed after the green host gate.

- 2026-08-27 02:25 — T-0058 matrix attempt 1 (grok-2): worker flagged that my §4.11 formula makes the rain rise; GPU frame also showed a uniform 35 % glyph floor (sky = mush) and invisible trails. §4.11 matrix revised (falling rain, `S·(0.3+0.7·I) + 0.12·I` brightness, slower per-cell churn, `matrixBrightness` pure helper) and **reworked** with a 5–25 % non-black criterion. Pattern this wave: the *contract* is where the bugs are (edges, matrix) — every look needs one GPU frame before its spec is trusted.

- 2026-08-27 03:20 — Resumed after a usage-limit pause with a backlog (4 review, 1 blocked). **Accepted T-0056 edges (attempt 2: inverse-depth rule, clean wireframe), T-0055 pico8 (opus-2), T-0057 hatch (opus-1), T-0058 matrix (attempt 2: falling rain, dark sky)** — every one GPU-checked from Maidan at 60 fps, gates run on the HOST in each worktree (container e2e flaked again under load). 57 done. **T-0051 braille** struck out after two INCOMPLETE ds-1 attempts (exit 137, then exit 0 with no handoff; no commits) → `assignee: grok`, requeued via `answer`. Merged-main host gate running before push.

- 2026-08-27 03:50 — **Wave 6 complete: 58/58 accepted, board empty.** T-0051 braille accepted on attempt 3 (grok-1, $0.17 for the attempt): GPU frame = high-res coloured dot matrix at 60 fps. All twelve styles live after the merged-main host gate + push. Wave-6 cost ≈ $16.6 for 11 tickets (33 attempts incl. INCOMPLETE ones): the Opus lanes dominate again — T-0055 pico8 $6.21 (2 attempts), T-0057 hatch $5.35 (**7 attempts**, opus-1), T-0056 edges $2.75 — versus ≤ $0.47 for every grok/DeepSeek ticket. **Fleet note for the user:** Opus is 10–30× the price of grok for C2 work of equal quality here; consider `max_complexity = 3`-only routing for opus (`capability`/`assignee`) or scale 1.

- 2026-08-27 05:00 — **Wave 7 boarded from user feedback:** matrix contrast fixed by PM tune (density-ramp glyphs ±4 jitter, brighter body/head; pushed fbee06e); T-0059 landmark fixes (OSM heights are stubs: Sophia 3 m, Lavra belltower 8 m, Arch 3 m, no Motherland → curated table + synthetic monument + building-based presets + exact-name/height-scaled `landmarkSpawn`); T-0060 UI shell (minimap top-left, HUD top-right, ⚙ bottom-right on all platforms opening the settings menu with HUD/MINIMAP/CRT/STYLE/FLY toggles, H/M keys, localStorage + URL mirroring, `src/credits.ts` footer "built by @ubyjvovk"); T-0061 fast travel (LANDMARKS submenu); T-0062 dome/spire/tower caps; T-0063 floating labels (user: "special treatment for select landmarks so they are more recognizable"). Contracts: architecture §4.12/§4.13, `Building.shape` in types.ts.

- 2026-08-27 05:40 — T-0059 attempt 1 (ds-1, $0.13): table + Sophia verified on the GPU (gold cathedral + bell tower), but `landmarkSpawn` never checked `blocked` → `?at=lavra` spawns inside a footprint, Motherland extra rendered in the id palette (orange) instead of silver. **Reworked** (blocked(p, 6) filter through `resolveSpawn`; register extras' colours). Also: GH intake off (`watch=false`, hot-reloaded), Opus auto-STOP lifted after the quota reset (STOP.opus had been created at 00:55).

- 2026-08-27 06:05 — T-0064 minimap water accepted (opus-1, first pass; GPU: teal Dnipro band with the Park Bridge crossing it). 59 done. Note for the review of T-0060 (UI shell): the worker's container e2e style loop times out under load — judge on the host gate.

- 2026-08-27 06:40 — T-0060 attempt 1 (grok-1, $0.81): layout/toggles/persistence/credits verified on the GPU (`node_modules/.pm-shell.mjs` probe), but `#gear` sits under the canvas in hit-testing (Playwright: "canvas intercepts pointer events"; the worker had hidden it with a coordinate-box capture handler). **Reworked**: explicit z-index stacking for all chrome, drop the capture hack, plain `.click()` as e2e evidence.

- 2026-08-27 07:10 — T-0059 attempt 2 (ds-1): did the note exactly; still a wall at `?at=lavra` — 6 m clearance is meaningless when an 8.6 m building starts 6 m ahead (fills ±49° of a 70° view). **Second rework** with a mechanical rule: clear corridor toward the target, `blocked(pt + k·forward, 1.5) === false` for k = 4…40 m, widen to 300 m, else fixed fallback; twelve-preset evidence table; `assignee: grok`. Lesson: spawn rules must be stated as view-corridor tests, not point clearance.

- 2026-08-27 10:40 — **T-0059 accepted on attempt 3** (ds-1; corridor rule → Lavra spawn looks down a street at the 96 m gold belltower, Motherland a silver tower on the hill) and **T-0060 accepted on attempt 2** (grok-1; explicit z-index stacking; ⚙ clickable on desktop once pointer lock is released, tap on 390×844 opens the menu without moving the player). Note for probes: under pointer lock Chrome routes all mouse events to the locked element — release lock, wait for `pointerlockchange`, then hide the overlay before clicking chrome. T-0061/62/63 now unlocked.

- 2026-08-27 11:20 — T-0061 fast travel accepted first pass (opus-1): 20-entry LANDMARKS list in the ⚙ menu, tap-to-teleport verified on a 390×844 touch viewport (`node_modules/.pm-travel.mjs`), `__asciicity.travel()` works. T-0062 shapes (ds-2) and T-0063 labels (grok-1) still in flight.

- 2026-08-27 12:10 — T-0063 labels accepted (grok-1; tags float over Sophia/Golden Gate at 60 fps). Its merge conflicted with T-0061 in main.ts + e2e (import unions; one lost `});` — caught by tsc after the merge commit, fixed in 38fafbf: **check `tsc` BEFORE committing a resolved merge, and never let `cmd && echo; next` chains swallow a failure**). User asked about trees and the desktop gear → **wave 7b boarded**: T-0065 trees data (OSM nodes/rows + seeded wood/park fill, both datasets), T-0066 TreeField (two instanced meshes), T-0067 minimap woods, T-0068 gear touch-only + `ESC MENU` help. Contract: types.ts `trees`/`woods`, data-format §Trees, architecture §4.14 + §4.12 amendment.

- 2026-08-27 12:50 — T-0062 shapes accepted (ds-2, first pass; +918 vertices; Sophia wears a dome on the GPU frame). Across-the-river silhouettes are still fog-limited at ~1 km — the shapes matter most within ~500 m and from the air.

- 2026-08-27 13:30 — T-0065 trees data accepted (grok-1, first pass): Kyiv 23 214 trees / 351 woods (3.3 MB), London 10 160 / 96 (3.1 MB). The 40 k cap's "every k-th" rule halved Kyiv's forest fills (k = 2 for 2.7 k over the cap) — forests ≈ 1 tree / 1000 m²; if they look thin on the GPU, switch the cap to proportional thinning (keep fraction 40000/n) in a follow-up. T-0066 TreeField and T-0067 minimap woods now claimable.

- 2026-08-27 14:05 — T-0067 minimap woods accepted (ds-1, first pass; Mariinsky park reads as a dark-green fill). T-0066 TreeField (ds-2) in flight; T-0068 queued.

- 2026-08-27 14:40 — T-0066 TreeField accepted (ds-2, first pass; 23 214 instances at 60 fps). Trees are there but too dark for the glyph density curve (canopy L 0.22–0.38) — forests across the river read as a dim band. PM tune next: brighter canopies.

- 2026-08-27 15:20 — PM canopy tune merged (b0b488d): S 0.60–0.85, L 0.48–0.65, trunk 0x6b4a2e — Khreshchatyk's boulevard trees now read as bright green canopies. Gate lesson reconfirmed: the merged-main e2e (touch fast-travel tap) timed out while my GPU probes ran concurrently and passed on an idle host — **never run PM probes during a host gate** (23 k tree instances make the SwiftShader e2e slow enough that any extra load tips it).

- 2026-08-27 16:10 — T-0068 accepted (ds-1, first pass): credits are a 20 px footer bar (view 700 px at 720, 58 rows), gear hidden on desktop / visible above the bar on 390×844, help line ends `ESC MENU` (GPU probe `node_modules/.pm-footer.mjs`). 68 done; T-0069 Nelson's Column now claimable.

- 2026-08-27 17:00 — T-0069 Nelson's Column accepted (grok-1, first pass): 52 m ivory column on a 6 m plinth, `trafalgar` faces it (GPU frame). Cosmetic follow-up T-0070 (extra shares the OSM name → its tag showed the plinth's "Trafalgar Square" label). 69 done.

- 2026-08-27 17:40 — **Wave 7 complete: 70/70 accepted, board empty.** T-0070 tag-label follow-up accepted (ds-1). Wave 7 = user feedback round: matrix contrast (PM tune), landmark heights/colours + Motherland + Nelson's Column + corridor-safe presets, UI shell (minimap top-left, HUD top-right, touch-only ⚙ menu with toggles, H/M keys, settings persistence, credits footer bar), fast travel, domes/spires/tower caps, floating tags, minimap water + woods, trees (data + instanced render + PM canopy tune). Cost ≈ $14.8 over 12 tickets (17 attempts); Opus lanes = $10 of it (T-0061 fast travel $6.14, T-0064 minimap water $3.80). Two `accept` merge conflicts (main.ts / e2e) resolved by hand; one bad resolution slipped past a `&&`/`;` chain and was caught by tsc — resolve → `tsc` → commit, always.

- 2026-08-28 18:10 — **Wave 8 in flight.** T-0071 OG (ds-1), T-0072 postcard PNG (ds-2), T-0074 amber (grok-1) claimed within a minute of boarding; T-0073 GIF awaits T-0072 in todo/. Killed two stale `events --wait` orphans; exactly one waiter re-armed. Review notes: og.png is a SwiftShader capture — PM must eyeball it and, if mushy, regenerate on the GPU host (`node scripts/make-og.mjs` with PW_ARGS for angle/gl-egl); amber needs a GPU frame (London night + Maidan) + 60 fps check and constants may need a PM tune (pattern: contract-first looks always get one GPU review); gates one at a time, merged-main gate on the HOST.

- 2026-08-28 18:35 — SF boarded: T-0075 coastline (C3 → grok/opus lanes), T-0076 dataset (depends 0075). Board: 3 doing (T-0071 ds-1, T-0072 ds-2, T-0074 grok-1), 3 todo (T-0073 dep-gated, T-0075 claimable, T-0076 dep-gated). PM accept-time duty for T-0076: fill the counts line in data-format.md "### San Francisco" from the worker report; GPU frames from `?city=sf&at=ggb` (skyline from the deck — the postcard shot), `paintedladies`, `coittower`; verify Alcatraz reads as an island from pier39; flip defaultSpawn to `unionsquare` if the deck spawn is broken rather than reworking on looks alone.

- 2026-08-28 18:55 — Review round 1: **T-0074 amber ACCEPTED** (attempt 2, grok-1, $0.37 total; smoke adjacency one-liner rode in — main e2e green again). **T-0072 postcard PNG ACCEPTED** (attempt 2, grok-1 after ds-2's attempt 1; rework caught caption painted over the TOP of the frame + filename-from-label bug — §4.15 amended: PostcardMeta {cityId, cityLabel}, bar translate(0,h), preserveDrawingBuffer is TRUE in scene.ts). **T-0071 OG reworked** once: og.png showed `FPS 0` (screenshot before the 1-s FPS window; make-og must wait for a non-zero HUD FPS and fail on timeout). T-0073 GIF now claimable. **PM probe env note:** pm-shot timed out at ready on BOTH the T-0074 worktree and main while 2+ worker gates ran — GPU visual reviews (amber look-tune, og.png final, postcard caption on GPU) all DEFERRED to the quiet board; pkill pattern must be self-escaped (`vite --port 419[9]`).

- 2026-08-28 19:20 — **T-0073 GIF ACCEPTED** first pass (grok-1, $0.21; check.sh fully green — wall-clock 36-frame sampling with stall-duplication, rgb565 quantize, repeat 0, re-entrant promise; PNG path refactored into flushPng unchanged). **T-0075 coastline REWORKED**: confirmed-by-repro corner-ordering bug in closeCoastline (fixed [SE,SW,NW,NE] array order vs CW walk order — bowtie ring whenever endT ≥ 1; all worker fixtures ended on the east edge and dodged it). Postcard feature = fully live on main once pushed. Remaining wave 8: T-0071 OG rework (ds-1), T-0075 rework, then T-0076 SF dataset.

- 2026-08-28 19:50 — **T-0075 coastline ACCEPTED** (attempt 2, opus-1, $8.65 total — corner sort fix verified by PM re-running both repro rings on the branch; 104 tests). T-0076 SF dataset claimed by ds-1 (fetch + registry + presets), T-0071 OG claimed by grok-1. **PM erratum:** my T-0071 answer told the worker to "merge main into the branch" — PROTOCOL §6 forbids that (opus-1 correctly cited it on T-0075); ticket was claimed before I could correct the text. At T-0071 review: a merge-of-main commit on the branch = my instruction, not worker misbehavior; a flagged block = answer with "report the known base-commit smoke failure and hand off". Merged-main HOST gate + GPU visual pass (amber, og.png, postcard caption, SF bridge spawn) all deferred until the board is quiet.

- 2026-08-28 22:10 — **Resumed after an abrupt host restart.** T-0076 attempt 1 (ds-1) died with exit 143 at 17:55Z; the wrapper requeued it strike-free and ds-1 re-claimed at 21:01Z (attempt 2 in flight). Merged-main HOST gate green (608 unit + 17 e2e; ~50 s on the idle 32-core host). GPU visual pass done: amber London night + Maidan at 60 fps (look OK, bloom weaker than the reference — user's call); og.png regenerated on the GPU (the committed one showed SwiftShader's `FPS 11`, the first GPU take caught the boot toast → make-og hides `#toast`); postcard `P` → 1280×728 PNG with the 28-px caption bar (`ASCIICITY · LONDON` / site URL), `asciicity-london-<stamp>.png`, 56 fps (probe `node_modules/.pm-postcard.mjs`). **Pushed main → Pages: wave 8 live** (OG meta + og.png, postcard PNG/GIF, amber style, coastline converter). Exactly one `events --wait` armed.

- 2026-08-28 22:25 — **T-0076 blocked → answered (Option B).** ds-1's attempt 2 ($0.05) found a real T-0075 bug on the live SF data: `stitchChains` only appends (`tail == o[0]`), so the Embarcadero coastline way (whose successor, the Marina way, sits earlier in the Overpass result) was orphaned with an interior endpoint and `closeCoastline` threw "not on the bbox perimeter". Nothing committed by the worker (correct — every other deliverable needs the fetched names/heights/deck geometry). Decision: widen T-0076's scope to `scripts/osm-convert.mjs` (stitchChains only) + `tests/coastline.test.ts` (two reverse-order cases named in the answer) instead of a separate ticket; locked rule: attach after OR before a chain, loop to fixpoint, never reverse a piece. data-format.md step 3 amended on main (2c50dec). Re-claimed by ds-1 within seconds (attempt 3). Wave-8 Pages deploy for b74f446 being polled.

- 2026-08-28 22:45 — **T-0076 blocked again → answered (size guard).** Attempt 3 (ds-1, $0.03) landed the merge-aware `stitchChains` + two order tests (commit 1ea02ce on the branch, 16/16 coastline tests) and the real fetch now succeeds: 44 181 buildings / 10 935 roads / 92 places / 45 water / 1 river / 34 466 trees, terrain 461×396 @ 20 m, **14.6 MB > the 12 MB guard** → worker stopped correctly. PM check on the on-disk file: coords already 0.1 m (re-rounding saves 0 bytes), 422 footprints < 20 m², bbox 2.5–6× London/Kyiv by area → size is fidelity; **guard raised to 15 MB** (ticket + data-format.md). Ferry Building: OSM h 15 kept, 75 m clock tower becomes an extra at the footprint centroid (1236.9, −832.9); Coit colour-only fix; Transamerica/Salesforce as OSM. GGB deck exists only as the East/West Sidewalk pedestrian corridors → `ggb` snaps to the East Sidewalk line, tower extras offset 12 m toward the west sidewalk with a ≥ 3 m clearance unit test. sf.spec may use a 90 s ready timeout. Re-claimed by ds-1 (attempt 4).

- 2026-08-28 23:10 — **T-0076 SF dataset ACCEPTED (attempt 4, ds-1, $0.29 total over 4 attempts / 57 min) — wave 8 + San Francisco complete: 76/76 done, board empty.** Host gate in the worktree green (610 unit + 19 e2e incl. both SF boots); GPU pass from the worktree, all 60 fps: `ggb` spawns ON the deck (y 36.6, ALT 58 m ASL, water both sides, the orange south tower thin at ~640 m; downtown is beyond the 6 km far plane so no skyline from mid-span), `paintedladies` (park + row + skyline behind), `coittower` (ivory column among Telegraph Hill canopies), `pier39` (cluttered by the pier buildings — Alcatraz not readable from there), Alcatraz verified from the water 600 m south (`?at=-122.4230,37.8210,0`: land + cell blocks ringed by water — island parity works), Ferry Building Clock Tower verified from Justin Herman Plaza (`?at=-122.3952,37.7950,85`: ivory 75 m tower + tag above the terminal). Counts line filled in data-format.md (0d3bbee). Merged-main host gate green (624 unit + 19 e2e). Pushed → Pages. Probe note: `?at=lon,lat,bearing` on open water spawns at sea level (y ≈ −27) and renders fine — handy for island/shore checks.

- 2026-08-28 23:50 — **Wave 9 boarded: 5 tickets** (T-0077 converter+re-fetch ∥ T-0078 collision parity → T-0080 GGB structure (grok) → T-0079 Alcatraz+ggb re-aim, T-0081 Bay ships (grok)). main.ts writers serialised T-0078 → T-0080 → T-0081; landmarks.ts T-0080 → T-0079; docs/integration.md is shared by all five (expect trivial merge conflicts — resolve, `tsc`, commit). PM review plan: T-0077 counts + GGB roadway visible on the deck (GPU frame from `ggb`); T-0078 e2e London/Kyiv unchanged; T-0080 GPU frames from `ggb` (new spot: south tower + cables + portal) and from the water (`?at=-122.4700,37.8100,20`) — check the portal opening clears the sidewalk, cables meet the deck at mid-span, hangers visible; T-0079 `?at=alcatraz` walkable + tag; T-0081 `?at=pier39&time=22:30` (lit sailboats) and `?at=ggb&time=22:30` (a container ship under the span with lights); all 60 fps. Waiter armed.

- 2026-08-29 00:05 — **T-0078 collision parity ACCEPTED** (opus-2, first pass; 26 collision tests, Alcatraz lighthouse centroid in rings 40+43 → land, 300 m south → water; corridor check kept first) and **T-0077 motorway re-fetch ACCEPTED** (ds-1, $0.04, first pass; sf.json 14 691 673 bytes, roads 10 935 → 11 267, GGB roadway = 3 `primary` bridge roads; London/Kyiv untouched). Both host gates green solo. T-0080 GGB structure claimed by grok-1 seconds after T-0078 merged. Note from opus-2 worth keeping: the shore margin narrowed from "any ring cell" to "within r of a ring edge" — London/Kyiv e2e unchanged.

- 2026-08-29 00:35 — **T-0080 GGB structure ACCEPTED** (grok-1, $0.49, first pass; 648-line `bridge.ts`, 20 340 vertices, 27 tests, host gate green) — but the GPU pass exposed an UPSTREAM deck bug: `groundAt` along the East Sidewalk is sea-bed (−22 at the south tower, −28 at z −3255) because OSM splits that sidewalk into three `bridge: true` pieces and `BridgeDecks` lerps each piece between its own (underwater) endpoints; only the one-piece West Sidewalk is a real deck (that is why `ggb` reported the West Sidewalk zone). The bridge module inherits it: 3 348 deck/hanger vertices below sea level, south tower top 138 vs north 171. Accepted anyway (code is per contract; fix is upstream) and boarded **T-0082 (P0)**: `chainBridgeRoads` in terrain.ts (same-name, coincident endpoints, reversible, order-independent) — contract §4.9 "Bridge chaining". T-0079 now also depends on T-0082 (its `ggb` e2e `y > 20` sits on the East Sidewalk). PM probe lesson: `?at=lon,lat,bearing` is NOT snapped — 1–2 m off a 2 m sidewalk corridor drops the player to the sea bed; use the app's `project` (`src/geo.ts`) with ≥ 6 decimals, or the snapped preset. Numeric probe recipe: a throwaway `tests/zz-pm-probe.test.ts` in the worktree (`Terrain` + `BridgeDecks` + `makeGroundAt` like main.ts) run with `npx vitest run`, deleted after. In flight: T-0081 ships (grok-1), T-0082 (ds-1); T-0079 queued.

- 2026-08-29 01:05 — **T-0082 deck chaining ACCEPTED** (ds-1, $0.06, first pass; East Sidewalk now 30 → 46 m abutment to abutment). Erratum on the 00:35 note: the two "off-corridor" probes were ON the East Sidewalk — the y ≈ −16 they reported was the broken piece-lerp deck itself (the app's `project` is the same formula I used); `?at=lon,lat,bearing` with 5 decimals lands on a 2 m corridor fine. Post-fix GPU frames on merged main: deck y 33.8 / 39 on the East Sidewalk, tower legs + hangers + tag from 260 m south (the future `ggb` view), the east cable sweeping up beside the sidewalk at mid-span, south-tower lattice from the water. **New bug found from mid-span: no road ribbon on the deck** — `roads.ts` drapes bridge roads with its own per-piece `bridgeProfile`, so the motorway/sidewalk ribbons still dive to the sea → **T-0083** (roads.ts iterates `chainBridgeRoads`; roads.ts-only so it cannot collide with T-0081's main.ts). In flight: T-0079 (ds-1), T-0081 (grok-1), T-0083 queued.

- 2026-08-29 01:40 — **T-0083 ribbons ACCEPTED** (opus-1, $1.68 for a one-liner — the opus lane keeps grabbing C2 tickets; consider `max_complexity = 3`-only routing for opus). Ribbon still invisible from the East Sidewalk and from 90 m up, visible from the West Sidewalk → numeric probes: the ribbon mesh is right (37.4 vs box 36.9 at the camera) — the culprit is the deck box: OSM crosses the whole main span in ONE 884 m segment (z −3255 → −4139) over which the deck rises ~5 m, and bridge.ts builds a FLAT top per segment at the mid-height, so the south half of the span has the box up to 2.6 m above the ribbon. My §4.16 wording allowed it. Contract amended (sloped prisms, ≤ 50 m pieces) → **T-0084** (opus-1 claimed). Lesson: when a surface is missing in a frame, probe geometry per along-position — my first histogram mistook hanger bottoms (sampled locally) for the box top. Probe scripts: `node_modules/.pm-fly.mjs` (F + Space rise + pitch) and `.pm-shot2.mjs` (pitch in radians via `state.pitch`).

- 2026-08-29 02:05 — **T-0079 Alcatraz + ggb re-aim ACCEPTED** (ds-1, $0.16, first pass; 78 spawn/landmark tests, host gate green with the new sf.spec boot). Worker reconciliation accepted and folded into §4.13 (a): `alcatraz` is a COORDINATE preset (a building preset on the lighthouse resolves via the view-corridor rule to the shore road at y ≈ −2). GPU: `?at=alcatraz` spawns at 36 m ASL on the island facing the city, walking works (moved 7 m downhill); `?at=ggb` now frames the south tower (tag, legs, hangers) from the East Sidewalk 260 m south. In flight: T-0081 ships (grok-1, attempt 2 after a handoff-line hiccup), T-0084 deck-box prisms (opus-1).

- 2026-08-29 02:30 — **T-0081 ships REWORKED once** (grok-1, attempt 1 $0.38; host gate on the pre-rework commit green incl. the two ships e2e boots): module per §4.17, timezone-pinned e2e (`?time=` is browser-local — grok pinned `America/Los_Angeles`, worth copying for any future time-based e2e), but the four `InstancedMesh`es keep `frustumCulled` on and three computes their bounding sphere once from the initial poses — on a 9 km cargo lane a ship steaming under the span would vanish. Note asks for `frustumCulled = false` on all four + one named test. Alcatraz tag verified on the GPU from 120 m south (ivory lighthouse on the hilltop, "Alcatraz" floating over it). In flight: T-0081 rework (grok-1), T-0084 (opus-1).

- 2026-08-29 03:00 — **T-0081 ships ACCEPTED** (attempt 2, grok-1, $0.44 total; `frustumCulled = false` on all four instanced meshes + named test; host gate green). First four GPU frames (pier39/ggb/channel, night+day) showed NO ships — a node probe of the app's `ShipFleet` (deterministic seeds → same start poses as the app) put the nearest cargo 924 m from the deck spot (fog ≈ 800 m); the fleet is real, just sparse (3 cargo on 9 km). Frames taken 300 m from the probed positions: night, container ship east of Alcatraz = long low silhouette, container row, lit superstructure cells, red port light; day, two sailboats off the Marina as sail triangles at 200 m; all 60 fps. Ships float at the flattened Bay level (−28.4 = the ring's DEM percentile, 4.8 m under true sea level; consistent with the water mesh). Look-tune candidates for the user: cargo hull `0x7a2e2e` is near-black at night (realistic, but the "huge" impression comes only from the container stacks) — consider a lighter hull; cargo count 3 → 5 for a busier channel. In flight: T-0084 (opus-1).

- 2026-08-29 03:40 — **Wave 9 complete: 84/84 accepted, board empty, pushed 9858a17 → Pages.** T-0084 deck-box prisms accepted (opus-1, $6.98 — 25 m pieces, 23 472 vertices). Merged-main host gate green (660 unit + 22 e2e). Final GPU pass, all 60 fps: mid-span from the East Sidewalk = road ribbon along the deck beside the main cable and hangers, south tower with struts ahead; 90 m overhead = grey ribbon between orange deck edges with a container ship passing under the span; south-tower lattice from the water; `ggb` = tower + tag + hangers. **Wave-9 cost $13.16**: grok + DeepSeek $1.69 for five tickets incl. both C3s (T-0080 bridge $0.49, T-0081 ships $0.94 over 5 attempts); the opus lane $11.47 for three C2 tickets (T-0078 $2.81, T-0083 $1.68, T-0084 $6.98). Wave 9 delivered: motorway roadway on the GGB (T-0077), island parity + walkable Alcatraz (T-0078), Alcatraz tag/preset + `ggb` re-aim (T-0079), GGB structure (T-0080), Bay ships with night lights (T-0081), and three deck bugs found only by GPU review: fragmented-deck lerp (T-0082), ribbons per piece (T-0083), flat box tops (T-0084). Follow-up candidates recorded in Next actions.

- 2026-08-29 04:30 — **Wave 10 boarded: 4 tickets** (T-0085 loading indicator ∥ T-0086 building parts → T-0087 Manhattan dataset + 20 landmarks → T-0088 NYC bridges). Contracts 482ef5a (types.ts `minH`, package.json `fetch-data:nyc`, data-format "Manhattan" + "Building parts", architecture §4.18 + §4.13 wave-10 table + §4.16 one-walkway spec). main.ts writer: T-0085 only. PM accept duties: T-0085 GPU/boot check of the overlay phases on SF; T-0086 fixture byte-identity; T-0087 counts line in data-format.md, `sizeBytes` reconciliation if T-0085 lands later, GPU frames from `dumbo`, `brooklynbridge`, `empirestate`, `onewtc`, `timessquare` (parts massing must read: setbacks on the ESB/Chrysler); T-0088 GPU frame from the Brooklyn Bridge deck. Waiter armed.

- 2026-08-29 10:00 — **T-0085 loading indicator ACCEPTED** (opus-1, $6.67, first pass; host gate green incl. `e2e/loading.spec.ts`; PM probe with a throttled route on SF: `LOADING SAN FRANCISCO [....] 0% · 0.0 / 14.7 MB` → `BUILDING SAN FRANCISCO · BUILDINGS/ROADS/TREES/WATER/TRAFFIC` → `CLICK TO ENTER`; `__asciicity.loading` live). PM follow-up on main: `#overlay p { white-space: pre-line }` (777ecde) so the bar sits under the label. `sizeBytes` now in `CityInfo` (city 3 229 747 / kyiv 3 449 700 / sf 14 691 673) — update it whenever a dataset is re-fetched. Probe: `node_modules/.pm-loading.mjs <dir> <query>` (route-throttled boot, prints overlay text timeline). T-0086 parts in review (grok-2, $0.51).

- 2026-08-29 10:20 — **T-0086 building parts ACCEPTED** (grok-2, $0.51, first pass; 124 tests, host gate green, no data-file diff). Scope extra `tests/data.test.ts` (the old 320-clamp assertion) accepted as necessary. Docs made consistent on `[3, 600]` (e79e18f). T-0087 Manhattan dataset claimed by opus-1; T-0088 NYC bridges queued behind it.

- 2026-08-29 11:10 — **T-0087 Manhattan dataset ACCEPTED** (opus-1, $13.71, first pass; nyc.json 10 107 567 bytes — 41 152 buildings incl. 3 162 parts, DEM mosaic W074+W075 worked; 17 presets; 114 tests; host gate green incl. `e2e/nyc.spec.ts`). Two report findings became tickets: (1) **Brooklyn Bridge deck at ~3 m** — straight abutment lerp fails for street-level abutments → §4.9 deck humps (quadratic through the abutments peaking at `deckApexASL − datum`, driven by `deckHumps()` from the bridge spec; GGB gets its real 67 m too) → **T-0088 rewritten as C3** (humps + one-walkway spec + three NYC structures + `brooklynbridge` to mid-span; grok-2); (2) **"Grand Central Terminal" on a 423 m part** (outline replacement handed the name to One Vanderbilt's part) → parts rule 3 = smallest containing outline → **T-0089** (converter + nyc re-fetch; ds-1). GPU: Times Square canyon and the promenade approach read well; One WTC a tall pale mass; `empirestate` spawn is buried in the canyon (vantage, not geometry); **`dumbo` preset (my coordinate) faces a warehouse wall** → re-aim to Washington St facing the Manhattan Bridge after T-0088 (T-0090 polish). Counts line in data-format.md (T-0087 accept commit). Pushed.

- 2026-08-29 12:30 — **T-0088 NYC bridges + deck humps ACCEPTED** (grok-2, $1.39, first pass; 713 unit + 25 e2e in-worktree, host gate green). grok-2 went beyond the letter: densified humped roads to ≤ 50 m chords so OSM's long main-span segments don't flatten the parabola, recomputed cable sag against the humped deck (3 m clearance kept), skipped struts above the shorter towers, and listed the six OSM pylon ways. GPU on merged main: Brooklyn Bridge mid-span ALT 41 (north tower + tag + skyline), from the DUMBO shore both towers/deck/hangers/cable in profile, aerial at 161 m, Manhattan Bridge walkway at 41 m with the tower and cables, GGB east sidewalk unchanged at 59 (apex 67 at mid-span). Pushed. **T-0089** (parts rule 3b + re-fetch) re-claimed by opus-1 after the below-grade answer; **T-0090** preset polish queued behind it.

- 2026-08-29 13:05 — **T-0089 parts naming ACCEPTED** (attempt 2; ds-1 $0.11 for rule 3 + opus-1 $4.30 for rule 3b/re-fetch; host gate green). Blocked round was legitimate: the underground Grand Central platform relation (`layer=-2`) was the only outline containing an unrelated 209 m tower → rule 3b: below-grade outlines/parts are skipped entirely (they'd render as street-level boxes anyway). nyc.json 10 102 343 bytes, all 17 landmark names unique (One Vanderbilt keeps its own). Counts line refreshed (fc1b868). T-0090 preset polish claimed by ds-1 — last wave-10 ticket.

- 2026-08-29 13:40 — **Wave 10 complete: 90/90 accepted, board empty, pushed cc3509b → Pages.** T-0090 preset polish accepted (ds-1, $0.06; the worker nudged my `empirestate` point 70 m onto the real 5th Avenue centre-line — my coordinate was inside a footprint). Merged-main host gate green. GPU: `dumbo` = Washington St with the tagged Manhattan Bridge tower and cables overhead; `empirestate` = down the 5th Avenue canyon. **Wave-10 cost $26.74**: grok/DeepSeek $2.07 (T-0086 parts $0.51, T-0088 bridges $1.39, T-0090 $0.06, T-0089 attempt 1 $0.11) vs opus $24.68 (T-0085 $6.67, T-0087 $13.71, T-0089 attempt 2 $4.30) — user keeps opus (weekly quota). Wave 10 delivered: loading indicator (§4.18), building parts with `minH`/600 m clamp/outline replacement/below-grade skip, Manhattan (41 140 buildings, 17 presets, 20 landmarks, three suspension bridges with real 41 m decks via deck humps, GGB apex 67). Lessons: spec coordinates → verify against road vertices before writing them into a ticket (dumbo, empirestate, the GGB towers — three misses this week); "exactly one named X" criteria need a data check first (T-0089 blocked on the underground GCT relation).

- 2026-08-29 14:20 — **T-0091 boarded (P1) from a user bug**: pointer lock lost by a macOS screenshot cannot be regained without reload (keys work, Escape dead, no overlay). Code diagnosis: rejected `requestPointerLock()` swallowed at every call site, no `pointerlockerror` listener, no Escape handler outside the browser's lock-exit event → once the overlay's click hid it and the re-lock failed, the player is stranded. Not reproducible on Linux, so the fix is robustness (§4.7 amendment): lock-failure state with a `DRAG TO LOOK` prompt + one retry, Escape toggles the pause menu whenever unlocked, drag-to-look fallback in `Controls`, `__asciicity.pointer` for the headless e2e (which naturally exercises the failure path).

- 2026-08-29 15:10 — **T-0091 pointer-lock resilience REWORKED once** (grok-1 attempt 1, $0.62): everything per §4.7 and green in the container, but `e2e/pointer.spec.ts` failed deterministically on the host: `reportLockError` coalesced failures with a 50 ms wall-clock window, and Playwright's second overlay click on the fast host landed inside it → the retry's failure was dropped, overlay hidden, `dragLook` never set (a stranded state — the bug class we're fixing; a fast double-click would hit it). Diagnosed with `node_modules/.pm-pointer.mjs` (stubbed `requestPointerLock`, 300 ms between clicks → passes). Note: coalesce per attempt (`settleAttempt`), not per time window; ds-1 re-claimed. Lesson: never coalesce user-attempt failures by wall-clock; also, "passes in the container, fails on the host" can be a real timing bug, not a flake — replay with instrumentation before calling it flaky.

- 2026-08-29 16:05 — **T-0091 pointer-lock resilience ACCEPTED** (attempt 2, ds-1 $0.04 on top of grok-1's $0.62; host gate green incl. `e2e/pointer.spec.ts`; merged-main gate green: 723 unit + 26 e2e). Live behaviour: a refused `requestPointerLock()` (rejection, `pointerlockerror`, or a promise that settles without a lock) shows `POINTER LOCK UNAVAILABLE · DRAG TO LOOK`, one retry on the next click, then drag-to-look with the HUD help reading `DRAG LOOK`; Escape toggles the pause menu whenever the canvas is unlocked; `__asciicity.pointer = { locked, dragLook, failures, lastError }`. Failures are coalesced per attempt (`settleAttempt`), never by wall-clock. Pushed over HTTPS (SSH agent dead — see host-facts memory). **Ask the user for `window.__asciicity.pointer.lastError` after a macOS-screenshot repro** to target the real refusal reason (candidates: `document.exitPointerLock()` before re-requesting; requesting on `mousedown`). 91/91, board empty.

- 2026-08-29 16:40 — **CI Node-20 deprecation** (user question): the Pages run's annotation names the ACTIONS (`checkout@v4`, `setup-node@v4`, `configure-pages@v5`, `upload-artifact@v4`) — not the build, which already uses Node 22 (worker image 22, host 24; kept at 22 to match the gate container). Bumped `.github/workflows/pages.yml` to the Node-24 majors (checkout v7, setup-node v7, configure-pages v6, upload-pages-artifact v5, deploy-pages v5) in local commit 93fee4c — **UNPUSHED**: the HTTPS token push is refused (`workflow` scope missing) and the SSH agent is dead. Needs the user: reconnect the SSH agent and `git push origin main`, or a token with `workflow` scope. The run for that commit will confirm the annotation is gone.

- 2026-08-31 — **Kyiv bare-earth: 9×9 erode REJECTED by measurement; `ridge` mode designed (T-0117).** T-0115 (ds-2, $0.03) blocked with a model pre/post table: the Tokyo-tuned erode shaves REAL crests (Lavra −19.2 m, funicular −12.1 m vs the ≤ 8 m guard) while Khreshchatyk's north-end rise is genuine Lypky grade (raw SRTM +11.6 m) that no bare-earth filter should flatten. PM decision: new filter mode `--dem-bare ridge` = directional opening (8 half-rays × 4 nodes incl. centre; H1 = max over rays of ray-min; then the standard double smooth) — roofs have low ground every direction (corrected), crests keep the plateau ray (preserved); contract in data-format 3b; erode mode byte-unchanged for Tokyo. T-0115 bounds revised in Answers: Khreshchatyk Δ-gate on pts 1–6 only, pts 6–8 report + post ≤ pre + pt 8 ≥ 160 (the real rise must SURVIVE); crest ≤ 8 m unchanged. **Process lessons:** (a) when an answer changes a ticket's dependencies, EDIT FRONTMATTER BEFORE `tigerteam answer` — the requeue is instantly claimable and opus-2 claimed T-0115 in the seconds before the dep edit; (b) KILL.<instance> on a lane whose only claimable ticket is that one just triggers re-claim — dep-gate or STOP the lane first (the re-claimed attempt actually behaved perfectly: read the Answers, saw T-0117 missing, blocked itself recommending the dep patch).

- 2026-08-31 — **Review round: T-0117 ridge mode ACCEPTED (ds-2, first pass; erode path pinned byte-identical; the flush-corner-blob preservation is the CORRECT reading of the 3b contract — documented reconciliation, accepted). T-0112 arch REWORKED (opus-1 attempt 1 $9.86: code/tests per contract, but GPU review caught the arch ~400 m north — its south springing rose from open water, north half over Milsons Point streets. Root cause: the OSM bridge:support pair is the northern APPROACH VIADUCT, not the pylons; my 'within 20 m of the chain' criterion was satisfiable anywhere along 1.6 km. New anchor rule: ends = the chained deck's crossings with the LARGEST water ring's boundary +21 m landward; expected south [151.20945,-33.85434] north [151.21197,-33.85031], span 503±15 — PM-probed, midpoint ≈ the real bridge centre). T-0116 relation-rings REWORKED (attempt 1 good: Opera House lands 180 m/18.5 m podium, skips 24→6, isRoofPart extra accepted; widened T-0076-style because the PM probe found the coastline closure emitting TWO overlapping ~13 km² harbour rings → odd-parity calls the harbour LAND (walkable water, Fort Denison parity flipped). Rework: fix closeCoastline multi-chain closure + disjointness invariant + six parity probe points + one more refetch).** LESSONS: (a) vantage/structure PLACEMENT criteria must pin position along the feature, not just distance-to-polyline; (b) datasets need a WATER-PARITY criterion class (walk into the water) — transects and boot e2e never touch it; (c) probe recipe: minimal inline stitcher + largest-ring transitions beats importing TS from worktrees (extension-less imports break node type-stripping).

- 2026-08-31 — **T-0112 Harbour Bridge arch ACCEPTED (attempt 2, opus-1, $13.55 total).** The shoreline-crossing rework landed exactly: ends within 5 m of the PM-probed values, span 500.4 m, mid-span deck = the 49 m apex to 0.014 m, ends test replays the largest-ring rule from data (T-0116-refetch-safe), host gate green. GPU pass: mid-span ON the deck (ZONE BRADFIELD HIGHWAY, ALT 49, hangers framing the CBD view) and the postcard profile from the water east of the span — the full arch sweep with truss verticals, deck line and the "Sydney Harbour Bridge" tag at the crown, 60 fps. Probe note: The Rocks street level is walled in — the bridge profile vantage for T-0111's presets should be waterside (Kirribilli/Dawes Point water edge), not in-block.

- 2026-08-31 — **Answer round: T-0115 + T-0116 both blocked with model analyses; both answered.** (a) T-0115: ridge mode WORKS on Kyiv — crests preserved (Lavra drop 3.2 vs erode's 19.2, funicular/Volodymyr slightly RAISED), Lypky survives (pt8 178.8), 19 Dnipro rings ±1.9 — the only "failure" was my flat-core Δ≤3 gate hitting the REAL Maidan bowl (4.17) and European Sq rise (3.89). Final gate formula: `postΔ ≤ max(3, preΔ + 0.5)` — flat where raw is flat, never >0.5 lumpier than reality. Worker finishes + commits. Note: kyiv alias needs `--dem-bare ridge` (PM flips package.json at accept). Also accepted (one-off, not precedent): opus-1 fast-forwarded the zero-commit branch to main tip — pure pointer move, ruled equivalent to a fresh worktree. (b) T-0116: MY REWORK PREMISE WAS WRONG — Sydney has ZERO coastline ways; the duplicate giant rings are overlapping `natural=water` RELATIONS (Port Jackson ⊇ Sydney Harbour) and the islands exist only as ignored INNER members. ds-1 refused to fix the wrong mechanism (model behavior). Answer locks the water-path design: outer-ring dedup ≥0.1 km² by 50 m-grid coverage ≥0.85 against larger kept rings; inner-hole islands emitted only from kept relations + centroid dedup; six parity points + ≤15% pairwise overlap invariant; coastline.test.ts requirement VOID. LESSON: pattern-matching a symptom to a known failure (SF coastline) without checking the mechanism exists in THIS data cost a rework round — check the census first.

- 2026-08-31 — **T-0118 longest-chain hump rule ACCEPTED (opus-1, $3.76, first pass): 113 done.** `chainHumpApex` shared helper (reference-identity match — callers must pass the same chains array they iterate), roads.ts dedup gone, Sydney tile test pins harbour chain = 49−datum ± 1 while the Circular Quay chain reads plain lerp; a ≥2-chains guard will surface any future data change that joins them. GPU boot: default spawn back at ALT 17 (was floating ~45 between T-0112's accept and this fix — never pushed). Remaining in flight: T-0115 finish (ds-2), T-0116 water-path attempt 3 (ds-1); then ferries → landmarks → shells.

- 2026-08-31 — **T-0115 Kyiv ridge terrain ACCEPTED (attempt 5, ds-2; ~$0.10 ds total + $4.37 opus attempt 4 across the saga): 114 done.** All final-gate bounds pass (crests ≤ 3.2 m drop, Lypky survives at 178.8, 51 rings ≤ 1.9 m, per-pair formula green); host gate green; GPU: Maidan ALT 155 on KHRESHCHATYK, Podil→bluff vista intact (St. Michael's tagged on the crest above the wooded slope). PM follow-ups committed: kyiv alias → `--dem-bare ridge`, datasets-table bytes 632 439 / 3 604 059. Kyiv user-ask COMPLETE — remaining wave-14b: T-0116 attempt 3 (water dedup + islands + refetch, ds-1 in flight) → T-0113 ferries → T-0111 presets → T-0114 shells; then README + push + Pages.

- 2026-09-01 — **T-0116 blocked x2 → answered x3: the eastern shore was land-by-ACCIDENT.** Attempt 3 implemented the locked dedup+islands correctly (harbour water ✓, Fort Denison/Goat Island land ✓) but exposed the deeper truth: BOTH harbour relations' clipped rings enclose the Mrs Macquarie's/Botanic/Woolloomooloo peninsula (closure artifact of clipping shoreline-hugging relation boundaries), so attempt-1's double cover was masking it; dedup to one ring flips the peninsula to water. No inner ring, no coastline ways → no data source carves it. Decision: **per-city curated LAND-CARVE rings** (`LAND_CARVES` table + `--carve sydney` flag; appended as island-parity rings with aligned placeholder waterLevels; the render was always fine because water surfaces hide under terrain — PARITY is the only runtime contract). Gated by a diagnostic scan (parity of all non-bridge road vertices + building centroids, clustered; > 5 lobes = block again → PM specs full-relation-geometry fetching). Probe set extended (Bennelong/Kirribilli/Blues Pt land; Farm Cove/Woolloomooloo Bay water; Goat Island probe re-pointed to the real footprint (−1334,−1001) — my old coord was open water). PM duty at accept: add `--carve sydney` to the alias. Worker kept b3e265d (dedup+islands+fixtures) — that work stands.

- 2026-09-01 — **T-0116 attempt 4: the STOP condition fired — carve DROPPED, full-relation-geometry SPECCED (answer x4).** The mandated scan found 195 wet-land hits in 46 clusters spanning the eastern half + west-harbour lobes (Opera podium/Kirribilli/Blues Pt clean; Garden Island/Rose Bay/Elizabeth Bay wet) — the clipped Port Jackson ring's interior is chaotic, not harbour+one-peninsula. PM re-verified the zero-coastline claim independently against Overpass (0 ways — OSM closes this coastline at the Heads, outside bbox; the harbour is water-polygons-only). Spec: `--water-full 1` (sydney alias only) → one batched follow-up Overpass query for COMPLETE member geometry of all bbox-matched water relations; full outer+inner rings assembled, NO clip (a correct simple polygon is parity-correct in any window); b3e265d dedup/islands apply on the full rings; DEM tile set = bbox ∪ ring vertices (all still S34E151); validator may need a water-only bbox relaxation (scope grows if so); guards: 12 MB total + index.json ≤ 3 MB else block (out-of-bbox simplification would be next). Acceptance: parity set + Garden Island LAND + scan re-run ≈ 0 wet hits (justified pier list). 5th fetch authorized as final.

- 2026-09-01 — **T-0116 attempt 5 → answered x5: OSM's harbour polygons are label-grade; water becomes DEM-contoured.** `--water-full` worked (full 51.4 km² Port Jackson ring, 14 943 verts, index 1.02 MB, S34E151 only) but the peninsulas STILL read water — and the PM verified upstream with Overpass `is_in`: the REAL OSM polygons contain Mrs Macquarie's Pt (and exclude Town Hall) — the data itself swallows the foreshore; our assembly is faithful. All worker options rejected (ship-broken fails the ferries; 46 carve rings unmaintainable; coastline closes at the Heads — nothing to fetch). Final design answered: **`--water-dem`** — for kept outer rings ≥ 1 km², node mask = inside-sloppy-ring AND bareASL ≤ level+2.0, majority-vote + speck/puddle cleanup, marching squares (island holes emerge natively, replacing the emitted inner islands) + one Chaikin pass → rings REPLACE the giant + its islands; everything downstream (flatten/collision/render/minimap/ferry tests) consumes rings unchanged — zero src/ changes, no schema change. Threshold holes on open water = block with histogram (PM moves thresholds). Keep b3e265d + 1b7c197. PM at accept: add `--water-dem 1` (and `--water-full 1`) to the sydney alias. Saga tally: 5 blocks, every one model-quality; the ticket is converging, not thrashing.

- 2026-09-01 — **T-0116 attempt 6 → answered x6: composite water mask.** The DEM contour nailed the macro shore (5.78 km² true harbour; Mrs Mac/Kirribilli/Blues/Goat all correct) but the bare-earth filter ERASED the micro features: Fort Denison −3.5 ASL (erode pit), Bennelong −2, Garden Is 0.9, while smoothing RAISED the CQ cove to 2.0 — provably no single threshold passes both cove and islets. Answer: composite rules, all data-driven (no curation): threshold level+3.0 (PM's move); force-LAND any node whose cell holds a building centroid or non-bridge road vertex, applied post-majority (rescues Bennelong via the Opera House itself, Garden Is, Mrs Mac via her road; makes wharves walkable land); relation-inner island rescue (emit kept-body inners whose centroid reads mask-water — Fort Denison returns with exact OSM geometry); speck/puddle as built. Margins < 0.3 m at any AC probe get documented as fragilities. Worker iterated offline on saved raw elements (no extra Overpass) — good practice worth keeping.

- 2026-09-01 — **T-0116 attempt 7 → answered x7: composite mask CODE COMPLETE (b82082e, rules 1/3/5 unit-tested); blocker was infra — the fetch OOMs at ~2.5 GB against the fleet-default `workers.memory = 3g`** (attempts 5/6 fit under it at ~83%; the protected-node mask + retry-inflated Overpass responses tipped it). Fix: `[workers] memory = "6g"` committed (host 31 GB; one fetch ticket at a time). Worker's triage was model-grade: seven heap sizes, flag ablations, reverting a flatten rewrite that bought only 0.1 GB, refusing to ship the flagless output. Next attempt: verify cgroup ≥ 6g FIRST (if 3g persists, supervisor needs restart — block one-line), off-peak refetch, parity probes → tests → docs+counts+sizeBytes in one commit → review. F2 (streaming fetch) noted as a wave-15 hardening candidate.

- 2026-09-01 — **T-0116 ACCEPTED at attempt 9 (115 done) — the Sydney water saga closes.** Final ship: Opera House (180 m relation outline), multi-way ring assembly, --water-full, --water-dem composite mask (DEM-contoured 9.45 km² harbour, force-land by buildings/roads, rescued island inners), 138 rings, 15/15 parity probes (PM independently re-verified 7 on the worktree), wet-land scan 195 → 28 with every remainder justified (Harbour Tunnel + HMAS Onslow are correct positives). Accept hit a fetch-osm.mjs conflict (T-0117 resolveDemBare vs water flags — union resolution, tsc'd before commit) + a semantic test conflict (arch ends shore-crossing anchor 30 → 75 m: the DEM-contoured shoreline sits ~55 m from the OSM boundary the ends were derived from; on-polyline + span guards unchanged). Manual ritual per the T-0102 recipe; merged-main host gate green; alias += --water-full --water-dem. GPU: CQ cove renders as a real water band. **T-0119 boarded from a post-accept GPU probe: Bradfield is a DUAL carriageway (1630.6/1634.6 m twin chains) — T-0118's longest-chain rule humps only one → west carriageway at ALT 28 vs 49 (21 m cliff). Fix: apex to every chain ≥ 0.8 × longest.** LESSON: "no-op for existing bridges" claims need a chain-COUNT check per name, not an assumption. In flight: T-0111 presets (opus-1), T-0113 ferries (ds-2); T-0114 shells behind T-0111.

- 2026-09-01 — **T-0119 ACCEPTED (opus-1, first pass; 116 done): both Bradfield carriageways at ALT 49 on merged main (GPU re-probe).** The 0.8-ratio rule also correctly leaves 4 tiny same-name approach ramps unhumped. **T-0111 REWORKED once (harbourbridge only):** 11/12 presets + fixes accepted (new default = CQ west promenade with the Opera House tag across the cove — GPU-verified; mrsmacquarie/operahouse resolve right; Luna Park + Fort Denison absent from OSM, documented) — but `harbourbridge` resolved to the Milsons Point foreshore at ALT 6 UNDER the deck: the worker's own y>35 e2e was never executed (ran unit-only, no check.sh), and the ≤3 m Cahill Walk unit test matched the wrong piece; z −1225 isn't mid-span. Rework pins: crossing chain BY ARC LENGTH, arc-position 0.35–0.65, ground ≥ 40 local, bearing 165–200, check.sh lines pasted (one tolerated pre-existing arch unit failure in their stale-base worktree — fixed on main). LESSON: a report whose Commands section lacks the check.sh line means the e2e criteria were never exercised — check for it BEFORE the GPU pass.

- 2026-09-01 — **T-0113 ferries ACCEPTED (ds-2, first pass; 117 done): the harbour is alive.** 'ferry' class (double-ended, cream-over-green, twin wheelhouses), 6 sydney lanes (11 instances: 7 ferry + 4 sail), waypoints nudged to pass the every-25 m odd-parity water test; Parramatta lane passes 10 m from the bridge axis. GPU: a ferry visible on the cove from a 91 m aerial (cream superstructure + dark hull on blue water); night lights ambiguous at range — re-look when the board is quiet if the user reports them dim. Worker's "no Goat Island" flag was MY stale ticket coordinate ((−1710,−1139) = open water; the real island (−1334,−1001) is land and their water test guards it). Merged-main gate fully green post-accept. Remaining wave-14b: T-0111 rework (opus-1, harbourbridge preset) → T-0114 shells; then README six-cities + push + Pages + live verify.

- 2026-09-01 — **T-0111 ACCEPTED on attempt 2 (118 done): all twelve Sydney presets live.** The reworked `harbourbridge` stands mid-span (arc-frac 0.500, ALT 49, ZONE CAHILL WALK) looking 208° down the deck through the truss at the CBD — the worker's bearing deviation from my 165–200 hint was CORRECT (the 3 m walkway corridor physically requires walking the deck axis; matches the ggb/brooklynbridge convention) and the attempt-1 root cause was diagnosed precisely (old point = chain vertex 11 at arc-frac 0.73, on the foreshore past the north crossing). Full e2e actually run this time (4/4 sydney, 42 full) + a legit mrsmacquarie HUD-race flake fix rode along. Merged-main gate fully green. **T-0114 shells (C3, the last wave-14b build ticket) now claimable.** After it: README six cities + push + Pages + live verify; then wave-15 candidates for the user (streaming fetch hardening F2, Fort Denison satellite-ring dedup, night-light look pass on the ferries, ship lanes for Tokyo Bay).

## Next actions
0. **WAVE 14 + 14b COMPLETE (2026-09-01): 119/119 done, board empty — Sydney + Kyiv-bare-earth shipped.** T-0114 shells accepted first pass (opus-1; sphere-solved sails ~10.4 k verts on the real podium ring, suppressParts drops 3 stray ribbons; GPU: a great curved pale sail with the tag from the forecourt; across-the-cove peaks modest under fog — LOOK-TUNE CANDIDATE for the user, per the amber no-blind-tune rule). Merged-main gate green; README → six cities; pushed. Wave delivered: Sydney dataset (S-hemisphere first, 57 tiles), Harbour Bridge arch + deck humps (twin-carriageway rule), DEM-contoured harbour water (the 9-attempt T-0116 saga: relation rings → water-full → water-dem composite mask), 12 presets + landmark fixes, 6 ferry/sail lanes, Opera House shells, Kyiv ridge-mode terrain, workers.memory 6g. **Awaiting user:** play-test (Sydney vantages, ferry night lights, sail brightness at distance, Kyiv hills feel); wave-15 candidates: ferries for Tokyo Bay, streaming-fetch memory hardening (F2), Fort Denison satellite-ring dedup, sail/ferry look-tunes, GGB curved-approach deck box, Statue of Liberty, more cities.
1. (superseded) **Wave 14 running + 14b boarded (user: "yes, do the ferries and the opera house next … and yes Kyiv as well").** T-0110 dataset ACCEPTED (ds-1, $0.13, first pass; 57 tiles / 20 338 buildings / 6.48 MB; S-hemisphere transects sane vs bounds; host gate green; GPU boot frame OK — spawn lands ON the elevated Cahill Walk at 17 m ASL, reads fine, T-0111 relocates anyway). **Worker found the Opera House MISSING**: relation 9596872 has 16 outer ways; the converter only assembles single-way outers (24 relations skipped) → T-0116 boarded (reuse the WATER ring-stitcher for building relations + Sydney refetch + presence test). Graph now: T-0112 arch (opus-1, in flight) ∥ T-0115 kyiv bare-earth regen (independent, T-0108 recipe + bluff-crest ≤ 8 m drop guard — Kyiv's REAL relief must survive the 9×9 erode; block-don't-tune) ∥ T-0116 → T-0113 ferries (water-sample lane test; §4.17b ferry class) → T-0111 landmarks/presets (deps 0110+0112+0116) → T-0114 opera shells (§4.22, C3). Contracts 14c2e21 (§4.17b ferries, §4.22 shells, kyiv alias --dem-bare). PM accept duties unchanged from the wave-14 plan + T-0113: GPU night frame of a lit ferry from Circular Quay + one under the bridge; T-0114: GPU frames from operahouse/mrsmacquarie/circularquay — sail SHAPE is judged on the GPU; T-0115: verify pre/post table + spot-check funicular vista on the GPU; T-0116: verify Opera House footprint + no transect drift. Then README (six cities) + push + Pages. T-0110 dataset (C2, claimable) → T-0112 Harbour Bridge arch (C3 → opus lane, needs 0110) → T-0111 landmarks + 12 presets + Circular Quay default spawn (C2, needs 0110+0112). PM accept duties: T-0110 — verify transect numbers + tile invariants independently, fill/verify the data-format counts line, GPU boot frame from the default spawn; T-0112 — GPU frames from the deck mid-span, from Circular Quay (arch profile over the cove), from Luna Park side; check the Cahill Walk hump finding; T-0111 — GPU vantage pass on circularquay/operahouse/mrsmacquarie/harbourbridge/lunapark (sightline tests are necessary, not sufficient — the skytree lesson), then README six-cities line + push + Pages + live check. Wave-15 candidates for user: Sydney ferry lanes (§4.17 has no sydney entry — the harbour is empty water), Opera House shell geometry (extruded parts read as stepped boxes), Kyiv bare-earth refetch.
1. **WAVE 13a COMPLETE (2026-08-31): 109/109, bare-earth terrain live for Tokyo.** Three-round convergence on T-0108, all healthy: (1) Overpass outage → PM pivoted to IN-PLACE terrain regen from the cached N35E139 tile (only {terrain, waterLevels} may change — deep-compare guard; tiles byte-untouched; no Overpass needed at all); (2) 5×5 filter under-powered — worker PROVED raw SRTM has no sample < 24 m within 100 m of the Scramble (fully roof-decked) → T-0109 widened to 9×9 + double smooth; (3) my ≤ 30 absolute bound fought REAL Yoyogi-side rise → bound split (core ±120 m ≤ 30, fringe ≤ 33; deltas ≤ 3 the true gate). Final: crossing square 23.6 m ASL, spawn 26.9, deltas ≤ 2.2, Marunouchi mean 5.5, datum 6.2; GPU frame confirms level street (ALT 36→27). Absolute heights stay ~8 m above truth in block-decked districts — documented, only the ALT row shows it. T-0108's accept hit the SAME cleanup abort as T-0102 (root-owned container artifact — this time from the PM's own in-container gate run AFTER the pre-check; run the ownership check IMMEDIATELY before accept), recovered with the same recipe. LESSONS: transect acceptance bounds must respect real relief (flatness deltas are the gate, absolutes only for sanity); in-place dataset surgery with a changed-key-set guard beats refetching when only derived layers change. Pushed + deploying. Kyiv bare-earth refetch = optional wave-13b (ask user).
1. (superseded) **Wave 13a boarded (user: "go with option 2"): bare-earth DEM filter.** User caught a real data bug post-deploy: "is Shibuya crossing really on a hill?" — PM transects confirmed the Scramble reads 33–45 m ASL ±10 m lumpy (reality: flat ~16 m valley floor; Marunouchi 6–16 vs flat ~4). Root cause: SRTM is a radar SURFACE model — roofs contaminate street heights; the AWS skadi mosaic is bare-earth only for the US (why SF/NYC are clean and Tokyo isn't). Fix locked in data-format.md Terrain step 3b: `--dem-bare` boolean flag → erode (second-smallest of 5×5 window — robust to one bad low) then smooth (3×3 mean) on the ABSOLUTE grid before datum/water steps; default off, existing datasets untouched; tokyo alias now carries `--dem-bare 1`. T-0107 filter (opus-1, in flight) → T-0108 Tokyo refetch with before/after transect acceptance bounds (Scramble ≤ 28 m, adjacent deltas ≤ 3 m, Marunouchi mean ≤ 10 m). After accept: push + deploy + GPU re-check of the Scramble spawn. Kyiv could optionally take the filter later (milder contamination) — ask user.
1. **WAVE 12 COMPLETE (2026-08-31): 106/106, board empty, merged-main gate green.** Shipped: Tokyo bbox v2 (129 tiles / 170 619 buildings / 28.3 MB, Shibuya+Shinjuku districts), auto-matrix on Tokyo (§4.20 precedence), katakana matrix atlas (film look GPU-verified), Shibuya Scramble default spawn (T-0103 accepted: vertex 6.8 m from the geographic crossing centre, LANDMARK reads Q-FRONT in the frame, 815 unit + full gates green; worker caught a projection-constant drift by round-tripping project()), shinjuku preset, CarFleet + CARS toggle, README © 2026 @ubyjvovk (license TBD). Await user: play-test (matrix look, Scramble spawn, car density feel — cap 250 is ONE tunable constant), license decision. Wave-13 candidates: fetch summary "tiled total" double-count C1, car density tune, bus/car smooth handover, west-Tokyo Shinjuku vantage polish, ship lanes for Tokyo Bay, then the older parked list.
1. (superseded) **Wave 12: T-0102 + T-0104 ACCEPTED (105 done); T-0103 (Shibuya presets, opus-1) is the last ticket.** T-0102 Tokyo v2: 129 tiles / 170 619 buildings / 28.3 MB (PM re-verified: zero bridge leaks, Skytree 634 max, 642/587 named anchors near Shibuya/Shinjuku stations; v1 region byte-stable so all presets survived; note — the fetch summary's "tiled total" DOUBLE-COUNTS monolithic+tile bytes, pre-existing quirk, wave-13 C1 candidate). Its accept ABORTED mid-ritual: the worker's container left a root-owned `.tigerteam/worktrees/T-0102` nested INSIDE the worktree → `git worktree remove` Permission denied; merge had landed, so recovery = docker rm of the root-owned dir, rm+prune worktree, delete branch, hand-move review/→done/ + commit (the doing/→review moves were also uncommitted — accept owns board bookkeeping). CHECK `find <worktree> ! -user d` BEFORE accepting any container-run ticket. T-0104 defaultRender: clean accept; worker correctly flagged my ticket's false claim ("settings.ts already exports the URL parser" — it's private) and reimplemented explicit-URL detection in main.ts behaviour-identically, settings.ts byte-identical; precedence chain unit-tested per link; e2e proves tokyo boots matrix and `?render=ascii` wins. After T-0103: push (includes README ©-line + credit), deploy, verify live, GPU Shibuya vantage.
1. (superseded) **Wave 12 in flight — T-0105 katakana matrix + T-0106 cars ACCEPTED (103 done); T-0102 refetch + T-0104 defaultRender running; T-0103 queued on the refetch.** T-0105 (grok-2, first pass): 66 mirrored Unifont bitmaps vendored, no font dependency, before/after GPU frames confirm the film look (angular katakana vs the old ASCII brackets); README credit line added by PM. T-0106 (opus-2, first pass, ~12 min): CarFleet per §4.21, 32 unit tests, allocation-free update; minor unexplained dims deviation (4.5×1.8 vs contracted 4.2×1.7 — visually negligible, noted); density cap 250 may feel sparse on Tokyo's huge road network — ONE constant to tune on user feedback. Deps relaxed mid-wave at user prompt (T-0104/0106 freed; lesson: depends_on for data/interface deps only, let git 3-way handle different-region same-file edits). Fleet note: user set grok scale 2→0 in tigerteam.toml (committed as-is); grok-2 finished T-0105 before draining. Board wave-12 remainder: T-0102 → T-0103, T-0104; then push + deploy.
1. **Wave 12 boarded 2026-08-31 from user asks** ("entering Tokyo → matrix automatically; start at that shinjuku crossing" — user picked **Shibuya Scramble** as default via question, both districts covered; "matrix originally had kanji — adjust ours to be close to original"). Contracts: architecture.md §4.20 (per-city `defaultRender`, precedence `?render=` → city default → persisted → ascii; matrix katakana atlas — U+FF66–FF9D + digits, EMBEDDED mirrored 8×16 Unifont bitmaps so no font dependency, ink-coverage glyph order, credits line), data-format.md Tokyo bbox v2 `139.692,35.645,139.820,35.715` (west strip counted 2026-08-31: 57 539 buildings / 17 230 highways → ~170 k total, ~27 MB est, chunks 4x3; origin/tiles/presets stable). Tickets: T-0102 refetch (C2) → T-0103 shibuya/shinjuku presets + defaultSpawn shibuya (C2) → T-0104 defaultRender wiring (C2, serialized on cities.ts) ∥ T-0105 matrix katakana (C2, independent). PM review gates: matrix before/after GPU frames vs the film look; Shibuya crossing GPU vantage.
1. **WAVE 11 COMPLETE (2026-08-31): 100/100 done, board empty, pushed 4f9b9ae..abb9015 → Pages.** Sector streaming shipped (tiler + TileManager + all five cities tiled — the monolithic path survives only for `?synthetic=1`/tests) and **Tokyo is live**: 99 tiles / 17.8 MB / 112 743 buildings, 7 data-derived presets (skytree postcard vantage at 795 m verified on the GPU), both towers shaped/coloured. Wave cost $14.79 over 13 attempts ($11.88 of it = T-0098's opus rework — the cost lesson holds; ds/grok carried the rest for ~$3). Await user: play-test feedback (esp. Tokyo vantages + boundary walking feel), macOS `pointer.lastError`, SSH agent (pushes work via HTTPS token meanwhile). Parked polish (wave-12 candidates): bus smooth handover across tile rebuilds, fast-travel collision-free window until destination tiles load (T-0099 observed, accepted v1), Shinjuku/Shibuya west-Tokyo extension (bbox v2), Tokyo ship lanes (bay ferries), tag range vs fog, unionsquare/pier39 vantage polish, LANDMARK row naming, amber tune, ships hull/count, GGB curved-approach deck box, Statue of Liberty.
1. (superseded item kept below for history) **Wave 11 boarded 2026-08-30 (T-0093…T-0099): sector streaming + Tokyo.** Contracts live: data-format.md "Tiled datasets" + "Central Tokyo", architecture.md §4.19, `TileIndexData`/`TileData` in types.ts, clamp 600→650 (Skytree 634). Dependency spine: T-0093 tiler → {T-0094 TileManager core (C3 grok), T-0097 Tokyo dataset} → T-0095 main.ts integration + SF migration (C3 grok) → {T-0096 migrate NYC/London/Kyiv, T-0098 Tokyo city integration} → T-0099 streaming e2e. PM review gates: GPU frames at a tile boundary + on a global bridge after T-0095/96; Tokyo vantages (Skytree from Sumida, Tokyo Tower, Ginza) after T-0098. Pending user input: macOS `pointer.lastError`; SSH agent reconnect. Parked polish: bus smooth handover across rebuilds (buses teleport on tile-set change — accepted §4.19), ships hull/count, deck box on curved approaches (GGB), pier39 vantage, tag range vs fog, amber tune, LANDMARK row naming, Chrysler/One WTC presets as street vantages, Statue of Liberty (outside bbox), Shinjuku/Shibuya west-Tokyo extension (outside the wave-11 bbox).
1. Older candidates (await user): forest density (proportional cap thinning), touch fly controls, ANSI-16 `blocks` variant, PETSCII, more cities (Lisbon/Edinburgh/Tbilisi — one data ticket each, ~$0.3 each on DeepSeek), bus pitch on slopes, hilltop vistas (far-field exposure), `docs/hud.md` vs `hud.ts` help-node mismatch.
2. After the styles: push, then ask the user which looks to keep/tune; candidate polish: ANSI 16-colour variant of blocks, PETSCII atlas, touch fly controls, remember last style/city in localStorage.
2. Wave-6 candidates (await user): touch pause button (phones have no Esc), remember last city (localStorage), Kyiv landmark height/colour table (OSM has Saint Sophia at h = 3 m), bus pitch on slopes, `docs/hud.md` vs `hud.ts` help-node mismatch + compact breakpoint for 7 rows, more cities (bbox + origin + `--lang` + `--dem` are all CLI args — e.g. Lisbon, Edinburgh, Tbilisi).
2. User must set GitHub → Settings → Pages → Source: **GitHub Actions** for the deploy job to succeed (main pushed 2026-08-24).
3. PM visual harness: `node node_modules/.pm-shot.mjs <worktree> <out.png> ['?synthetic=1']` (copy lives in scratchpad `shot.mjs`); default args use the GPU (`--use-angle=gl-egl`). Never judge frames from SwiftShader at 60 rows (see architecture.md §8).
4. Later ideas: Westminster dataset option (bbox is a CLI arg), sound, day/night palette, `?cell=` presets in the HUD help.

## How to resume
1. Read this file.
2. `tigerteam status` (or `bash .tigerteam/scripts/board-status.sh`)
3. Process review/ first (`tigerteam accept` / `rework`), then blocked/ (`tigerteam answer`).
4. `git worktree list` — tigerteam/* entries are unmerged ticket branches.
5. Supervisor is normally already up in the cockpit; `tigerteam check` confirms. Re-arm `tigerteam events --wait` (exactly one for this root: `for p in $(pgrep -f 'tigerteam events --wait'); do readlink /proc/$p/cwd; done`).
6. Continue from Next actions.

- 2026-08-31 — **CI failure post-push + T-0101 deflake ACCEPTED (ds-1, first pass; 787 unit in-container, tsc clean; pushed 11d845d..b260812).** The wave-11 Pages run (11d845d) FAILED in `npm test`: `tests/landmarks.test.ts > sf fixes still target…` ran 9 886 ms on the shared runner vs vitest's 5 s default — the migration made test helpers fs-read 69 tile files per reconstruction, fine on our host (~1–2 s), fatal on CI. Deploy skipped → live site stayed pre-wave-11 (user: "Tokyo not in city menu tho?"). Fix: memoized `tests/sfCity.ts`/`tiledCity.ts` (per-process, shared references — worker audited ALL consumers for mutation and copy-fixed the two mutating call sites), `testTimeout: 30_000` per-file in the three dataset-heavy files. LESSONS: (a) local-green ≠ CI-green when tests are I/O-heavy — budget shared-runner slowness for dataset tests at boarding; (b) do NOT push STATE commits while a Pages run is in flight (the concurrency group cancels it — abb9015's run died that way).

- 2026-08-31 — **T-0098 Tokyo integration ACCEPTED (attempt 2 rework by opus-1 on top of ds-1's base): TOKYO IS PLAYABLE — `?city=tokyo`, picker TOKYO, default spawn tokyostation, 7 presets, both towers shaped/coloured.** The rework was exemplary reconciliation: opus-1 ran the sightline scan over ALL 99 tiles and PROVED my "postcard west/SW side" hint mechanically unsatisfiable (Tokyo Solamachi, 227×87 m, extends 90 m west of the anchor; ZERO clean vertices in the west/SW 550–900 m band, zero riverside ones at 300–500 m) → took the only clean candidates: `skytree` = Kita-Jūkken-gawa canal bank 795 m ENE bearing 286° (frame verified on the GPU: canal corridor + full thin spire rising through the frame top — the postcard), `sumida` = Sakurabashi-dōri 435 m NE bearing 241° (tower lattice dominates down the street corridor). Both carry the new buildings-only sightline unit tests (skytree clearance 2 m, sumida 3 m — projection round-trip cost one candidate 1.97 m of margin, worth remembering); skytree e2e text-poll replaced by position+tile assertions (ZONE Voronoi cell doesn't reach 795 m). Gates: 778 unit in-container, host check.sh green on the worktree AND on merged main (T-0100 was disjoint). 99/100 done; T-0099 (hardening) is the last open ticket. Wave-11 user-visible summary: five cities, all streamed, Tokyo new.

- 2026-08-31 — **T-0098 Tokyo integration REWORKED once (vantages only).** The landing itself was solid (ds-1 resumed an OOM-killed attempt, committed d86df65, re-verified: 776 unit + host gate green incl. e2e/tokyo.spec.ts; Tokyo Tower's 21 m OSM stub correctly overridden to 333 m orange per the curated-table rule; PM re-ran the full host gate + in-container suite green) — BUT the wrapper had struck it into blocked/ "(failed attempts)": after the complete attempt wrote `handoff: review`, two idle re-claims exited 0 with no handoff (nothing left to do) and the strike counter parked it. Recovered manually (rework refuses non-review lanes → append note + mv blocked/→todo/ + commit). PM GPU review found the real issue: **`skytree` preset at 156 m renders a 634 m tower as a featureless red wall** (70° vFOV shows the bottom ~110 m; the `clamp(70+1.2h,70,220)` distance rule is inadequate for supertalls — the worker optimized for the ZONE-text e2e criterion, classic criteria-shaping); `sumida` has a foreground structure blocking the river view. Rework note: relocate skytree to 550–900 m (postcard side across the Sumida), sumida stays 300–500 m, both with a NEW mechanical clear-sightline test (buildings-only CollisionGrid, ray samples every 10 m, blocked(pt,2)===false) and the skytree e2e text-poll replaced by position/tile assertions. Lesson recorded: **height-scaled vantage distance breaks above ~300 m towers — put a sightline test, not a zone-text test, in vantage criteria.** Also: negative local y east of the station is expected (Sumida lowlands 4–6 m ASL vs datum 9.5). Waiter processes get reaped by the harness after idle — PM now relies on supervisor nudges instead of a standing `events --wait`.

- 2026-08-31 — **T-0100 bare-flag parser fix ACCEPTED (grok-1, $0.18, 5m, first pass; 769 unit green in-container, tsc clean).** `parseArgs` exported; bare `--tiles` = true without consuming the next token; any other valueless `--key` throws loudly (the silent default-path fallthrough is gone); `--tiles 1`/`true` unchanged; negative-number values (`--bbox -74…`) unaffected (only `--` counts as a flag). Aliases stay on `--tiles 1` (both forms now work). `.d.ts` companion accepted (established pattern).

- 2026-08-31 — **T-0097 Central Tokyo dataset ACCEPTED (ds-1, attempt 2 after an exit-137 INCOMPLETE resume; full suite green on its pre-migration base — branch touches only public/data/tokyo + docs/fetch-osm.md, merge-safe).** Shipped: 99 tiles, **112 743 buildings, 17.8 MB total** (index 1.98 MB + tiles 16.65 MB — WAY under the 60 MB budget and under the 30–50 MB estimate: Tokyo footprints are simple), max h 634 = Skytree (clamp 650 ✓), 1 712 bridge roads global, zero tile leaks (PM re-verified all invariants independently), origin 33 m from OSM's Tokyo Station node (kept), datum 9.5 m, terrain 411×391, 214 water rings + 111 rivers (Sumida + bay). Landmarks include exact `name:en` "Tokyo Skytree" (3 944, −3 190) and "Tokyo Tower" (−1 959, 2 515) — T-0098 should use these strings. **Worker found a real T-0093 parser bug: bare `--tiles` is swallowed as the next arg's value → silently wrote monolithic city.json to the DEFAULT path.** PM response: all npm aliases switched to the `--tiles 1` value form (+ new `fetch-data:tokyo`), data-format.md Tokyo section finalized with counts + a warning note, and **T-0100 (P1/C1) boarded** to fix bare-flag parsing and make valueless flags error loudly. Overpass etiquette held: one 429 + one 504 retried, off-peak lull needed.

- 2026-08-31 — **T-0096 London/Kyiv/NYC migration ACCEPTED (grok-1, first pass; 764 unit + full host gate green on the worktree; all four cities boot tiled in e2e).** PM invariant check on the worktree data: tile building sums EXACT (9 061 / 8 183 / 41 140), zero `bridge: true` leaks into tiles, London correctly terrain-free, `index.bridgeRoads` all `bridge === true` (381/87/586). GPU frames: NYC brooklynbridge on tiles = promenade 41 m ASL + North Tower tag + skyline (correct); Kyiv maidan = Khreshchatyk 155 m ASL, buildings draped on global terrain (correct). PM follow-ups committed: data-format.md Datasets table rewritten for tiled dirs (+ per-city counts), package.json fetch aliases → `--tiles` dirs (all four), AGENTS.md data layout line. Monolithic path now exists ONLY for `?synthetic=1` + unit tests, exactly per plan (c). NOTE for T-0098/99 reviewers: e2e/nyc.spec.ts + landmarks.ts comments still say `nyc.json`/`sf.json` (cosmetic).

- 2026-08-31 — **T-0095 tiled boot + SF migration ACCEPTED (grok-1, first pass; 763 unit + full host gate green on the worktree — content-identical to merged main, so no duplicate gate run).** SF is the first streamed city: 69 tiles, sf.json deleted, registry `tiled: true` + `sizeBytes` 1 273 711 (index). PM GPU review: GGB spawn frame correct on tiles (deck y 36.7, 59 m ASL, orange towers, multi-tile vista, no seams, 60 fps); unionsquare frame is PIXEL-IDENTICAL to monolithic main (same y to 4 decimals — the red close-up wall there is pre-existing, not a migration artifact; candidate parked polish alongside pier39 vantage). Integration notes: boot gate has an idle-break so a twice-failed spawn tile cannot hang boot; rebuilds (zone/minimap/anchors/fleet) debounced ≤ 1/s via setTimeout(0); fast-travel to an unloaded area streams tiles AFTER arrival → brief collision-free window (accepted v1; T-0099 should observe it). Worker flags folded: PM updated `fetch-data:sf` npm alias to `--tiles --out public/data/sf` (package.json is PM-owned); landmarks.ts comments still say sf.json (cosmetic, T-0096's wave); `opts.tileRadius` in UrlOptions is redundant with parseTileRadius (cosmetic).

- 2026-08-31 — **T-0093 tiler ACCEPTED (ds-1, $0.14, 36m, first pass; 741 unit green in-container, tsc clean, worker ran full check.sh green).** PM real-data smoke before accept: tiled the committed sf.json twice → 69 tiles, byte-identical runs, union EXACT on every axis (44 181 buildings, 34 037 trees, 2 161 landmarks = named buildings), 343 bridge roads global, 10 924 non-bridge roads → 11 955 pieces (+9 % split overhead), 14.3 MB total (+2.5 % vs monolith). Three justified out-of-scope files accepted (two .d.ts decls per existing pattern; tests/osm-convert.test.ts clamp assertion my scope list forgot). NOTE: tile-city.mjs imports validateCity from the TS source via node type stripping (needs node ≥ 22.18; image 22.23.2, host 24 — fine, keeps the clamp single-sourced). splitRoad is a DDA walk; float drift at a boundary can at worst produce one extra piece with shared endpoints (benign). `--chunks NxM` split/dedupe are exported pure fns, tested without network; fetch-osm main() now guarded against import-time fetch.

- 2026-08-30 — **T-0094 TileManager core ACCEPTED (grok-1, $0.33, 19m, first pass; 738 unit green in-container, tsc clean).** Worker flagged a real §4.19 contradiction — a 2 000 m CIRCLE never reaches the 5×5 corners from a tile's interior (nearest corner ≈ 2 121 m) — and implemented the axis-aligned SQUARE metric (loadR/unloadR half-extents) + 3×3 always-wanted to satisfy the locked 5×5 test. Accepted the reconciliation; §4.19 amended (square metric, `?tileradius=<m>` → `{loadR: m, unloadR: 1.3·m}`, version bumps once per mutating take(), pending = in-flight + fetched-not-taken, collision base source key `'#base'` reserved). Implementation notes for future reviews: fetched-but-droppable tiles are pruned from the ready queue in take(); a fetched tile in the hysteresis band still gets added (harmless — it would be kept anyway); snapshot concatenates in numeric key order (deterministic for the bus seed). CollisionGrid refactor is per-source records + identity bucketPull, water constructor-only, verbatim semantics.

- 2026-08-30 — **Wave 11 boarded: sector streaming + Tokyo (T-0093…T-0099, two C3 pinned grok).** Contracts committed BEFORE tickets (process rule): data-format.md "Tiled datasets" (tile grid 1000 m, exclusive centroid assignment, roads split at boundaries with coincident endpoints, **bridge roads global in `index.bridgeRoads`** — never split, avoids the wave-9 piece-lerp class; landmarks/places global; per-tile `bytes`; `--chunks NxM` deduped fetch; clamp 600→650) + "Central Tokyo" (bbox `139.730,35.645,139.820,35.715` ≈ 8.1×7.8 km, origin Tokyo Station, `--lang en --dem 1` N35E139, budget 60 MB); architecture.md §4.19 (TileManager API with injected loader, load 2000 m / unload 2600 m hysteresis + 3×3 never-unload, ≤ 1 tile build per frame via `take()`, CollisionGrid `addSource`/`removeSource`, HUD/minimap/buses rebuilt from `snapshot()` ≤ 1/s — buses teleport, accepted; tiled boot: index → globals → spawn 3×3 → ready; `__asciicity.tiles` e2e surface); types.ts `TileIndexData`/`TileData`/`TileStat`/`LandmarkEntry`. **Fresh Overpass counts (2026-08-30, not rate-limited this time): 112 184 buildings / 510 parts / 46 594 highways** in the Tokyo bbox — 2.7× Manhattan, streaming confirmed necessary; Tokyo Tower verified in OSM at ≈ 139.747,35.658, Skytree ≈ 139.808,35.710 (station nodes). Tokyo has only 510 `building:part` ways — parts machinery mostly idle there, fine. Fleet unchanged (grok×2 C3, opus×2 C3, ds×2 C2); C3s pinned `assignee: grok` per the wave-9 cost lesson.

- 2026-08-29 22:00 — **T-0092 Cloudflare Web Analytics ACCEPTED (ds-1, $0.009, 5m45s, first pass): 92/92 done, board empty.** User-supplied beacon snippet inserted verbatim as the last element before `</body>` in `index.html` (token `810b…` lives only there; README `## Analytics` explains the one-line removal). Verified byte-for-byte against the user's snippet, `npm run build` carries the tag into `dist/index.html`, merged-main host gate green (723 unit + e2e). Pinned `assignee: ds` — right call for a C1: the opus lane would have billed $1–2 for the same diff. Pushed → Pages; analytics start counting once the deploy finishes (dev-server hits on localhost also report to the token — filter by hostname in the Cloudflare dashboard if that matters).
