---
id: T-0068
title: Gear button only on touch devices; ESC MENU help; credits as a real footer bar below the view
priority: P2
complexity: C1
depends_on: [T-0066]
scope: [src/main.ts, src/style.css, index.html, tests/hud.test.ts, e2e/smoke.spec.ts, docs/integration.md, docs/hud.md]
---

## Goal
User: "there's no way to press the gear icon on desktop" — under pointer
lock nothing is clickable and after Esc the menu overlay covers it, so it
is redundant there. Per the revised `docs/architecture.md` §4.12: show
`#gear` only on touch devices; on desktop the HUD help line ends with
`· ESC MENU`.

## Context
- **Footer bar** (user: "the bottom tagline should be its own footer line
  rather than overlaying the main view"): per the revised §4.12, `#credits`
  becomes a 20 px black bar at the bottom of the page; `#view` gets
  `height: calc(100vh - 20px)` (and `top: 0`), `applySize()` passes
  `window.innerHeight − CREDITS_BAR_PX` to `ascii.setSize`/camera aspect;
  `#gear` and `#toast` sit above the bar (`bottom: 20px + margin`). On the
  ≤ 700 px breakpoint the bar stays 20 px (text may ellipsize). e2e: assert
  the canvas bottom edge ≤ the `#credits` top edge and that `#credits`'s box
  height is 20.
- Detection: the same expression `main.ts` already uses to create
  `TouchControls`; set `gear.hidden = !touch` (and keep the CSS).
- Help line: desktop default becomes
  `'WASD MOVE · MOUSE LOOK · SHIFT RUN · F FLY · R STYLE · ESC MENU'`; the
  touch variant unchanged.
- e2e: the existing `smoke: panels (touch …)` keeps its gear tap; add to the
  desktop smoke: `#gear` is hidden and the HUD contains `ESC MENU`.
- Docs: `docs/integration.md`, `docs/hud.md`.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/hud.test.ts` exits 0 (help-line test updated)
- [ ] `bash scripts/check.sh` exits 0 with the e2e additions (gear hidden on desktop; ESC MENU; canvas/footer non-overlap on desktop and 390×844)
- [ ] Headless evidence: bounding boxes of `#view`, `#credits`, `#gear`, `#toast` on both viewports — no overlap with the bar
- [ ] JSDoc; docs updated
- [ ] No files changed outside scope

## Out of scope
- Any change to the menu itself or to touch behaviour.

## Worker report
