---
id: T-0045
title: Integrate terrain in main.ts — ?city= registry, Kyiv spawn presets, ALT row, hills e2e
priority: P1
complexity: C3
depends_on: [T-0040, T-0042, T-0043, T-0044]
scope: [src/main.ts, src/data/cities.ts, src/data/spawn.ts, tests/spawn.test.ts, tests/cities.test.ts, src/hud/hud.ts, src/hud/format.ts, tests/hud.test.ts, e2e/smoke.spec.ts, docs/integration.md, docs/hud.md]
---

## Goal
Kyiv is playable: `?city=kyiv` loads `data/kyiv.json`, the heightfield is
drawn, the player/camera/sky/buses walk on `groundAt` (terrain + bridge
decks), boats float on the river level, the HUD shows `ALT`, and named
Kyiv spawn presets exist. London (`?city=london`, and the default when no
`?city=` is given — the picker is T-0046) is byte-for-byte unchanged in
behaviour.

## Context
- Contract: `docs/architecture.md` §4.9, §4.10, §5 (updated 2026-08-26) and
  `docs/terrain.md`. Modules landed: `src/world/terrain.ts` (T-0042),
  draped builders (T-0043), fleets with `HeightFn` (T-0044), `kyiv.json`
  (T-0040), `syntheticCity(seed, blocks, hills)` (T-0041).
- `src/data/cities.ts` per §4.10: `CITIES = [london, kyiv]` with
  `file: 'data/city.json'` / `'data/kyiv.json'`, `defaultSpawn: 'bigben'` /
  `'maidan'`, labels `LONDON` / `KYIV`, blurbs `City of London & Westminster · flat`
  / `Central Kyiv · Dnipro hills, 120 m of relief`. `cityById` trims and
  lower-cases; unknown/absent → `undefined`.
- `parseUrlOptions` gains `city: string | null` and `hills: boolean`
  (`?hills=1`, synthetic only). `chooseCity`: synthetic → `syntheticCity(seed, 12, hills)`;
  else `cityById(opts.city) ?? CITIES[0]` → `loadCity(BASE_URL + city.file)`
  with the existing synthetic fallback on error.
- World assembly per §5 step 2: with `city.terrain` build `Terrain`,
  `BridgeDecks(city.roads, terrain.heightAt)`, `groundAt = makeGroundAt(terrain, decks)`,
  add `makeTerrainObject(city.terrain)`, keep `makeGround()` but set
  `position.y = terrain.min − 0.5`; pass `groundAt` to
  `makeRoadsObject`, `makeBuildingsObject`, `BusFleet`; `terrain.heightAt`
  to `BoatFleet`; `makeWaterObject(city.water, city.waterLevels)`. Without
  terrain: `groundAt = FLAT_HEIGHT` and every call site equals today's.
- Frame loop: `eyeY = groundAt(state.x, state.z) + EYE_HEIGHT`;
  `camera.position.set(x, eyeY, z)`; `sky.position.set(x, eyeY, z)`.
  `window.__asciicity` gains `y: number` (eye height) and `city: string`.
- Spawn: `resolveSpawn(param, origin, blocked, city, fallback)` per §4.10 —
  `fallback` defaults to `'bigben'`; `city` now `Pick<CityData, 'buildings' | 'roads' | 'bbox'>`;
  any preset/coordinate whose lon/lat is outside `city.bbox` uses the
  fallback preset (which must be a fixed-coordinate preset — throw otherwise).
  `main.ts` passes `cityInfo.defaultSpawn`.
- Kyiv presets to add to `SPAWN_PRESETS` (fixed-coordinate form
  `{ lon, lat, bearingDeg, label }` unless noted). Verify each `building`
  preset resolves via `landmarkSpawn` against `kyiv.json` in a test; if a
  building name does not match, use the fixed coordinate given instead:
  - `maidan`: 30.5240, 50.4500, 250 — "Maidan Nezalezhnosti, facing Hotel Ukraina"
  - `sophia`: building `Sophia` (fallback 30.5165, 50.4530, 270) — "Facing Saint Sophia Cathedral"
  - `michael`: building `Michael` (fallback 30.5210, 50.4553, 60) — "Facing St Michael's Golden-Domed Monastery"
  - `lavra`: 30.5560, 50.4350, 100 — "Pechersk Lavra, facing the Great Bell Tower"
  - `motherland`: 30.5610, 50.4275, 135 — "Facing the Motherland Monument"
  - `podil`: 30.5170, 50.4650, 180 — "Kontraktova Square, Podil"
  - `andriyivskyy`: 30.5165, 50.4586, 40 — "Top of Andriyivskyy Descent, facing St Andrew's Church"
  - `goldengate`: 30.5133, 50.4485, 20 — "Facing the Golden Gate"
  - `arsenalna`: 30.5455, 50.4443, 90 — "Arsenalna, the deepest metro station"
  - `parkbridge`: 30.5335, 50.4557, 90 — "Park Bridge, facing Trukhaniv Island"
  - `glassbridge`: 30.5297, 50.4553, 45 — "Klitschko glass bridge"
  - `mariinsky`: 30.5380, 50.4482, 90 — "Facing Mariinsky Palace"
  - `bessarabka`: 30.5209, 50.4420, 0 — "Bessarabska Square, looking down Khreshchatyk"
  - `funicular`: 30.5231, 50.4592, 210 — "Funicular lower station, looking up"
  - `hydropark`: 30.5770, 50.4459, 270 — "Hydropark, facing the right-bank hills"
  - `metrobridge`: 30.5600, 50.4423, 90 — "Metro Bridge over the Dnipro"
- HUD: `HudValues.alt?: string`; `Hud.update` renders `> ALT........ <alt>`
  between ZONE and LANDMARK only when `alt !== undefined` (six rows
  otherwise); `formatAlt(m) = \`${Math.round(m)} M ASL\`` in `format.ts`.
  `main.ts` sets `hudValues.alt = formatAlt(city.terrain.datum + groundAt(x, z))`
  when terrain exists.
- e2e: add a second test `smoke: hills` loading `/?synthetic=1&hills=1`
  that waits for `ready`, asserts `window.__asciicity.y` is within 0.5 of
  `1.7 + terrainHeightAt(syntheticCity(1, 12, true).terrain, 0, 0)` — compute
  the expected value in node inside the test — and that the HUD contains
  `ALT`; keep the existing test as is.
- Docs: `docs/integration.md` (`?city=`, `?hills=1`, Kyiv presets table,
  `__asciicity.y/city`, terrain assembly), `docs/hud.md` (ALT row).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/spawn.test.ts tests/cities.test.ts tests/hud.test.ts` exits 0
- [ ] New tests by name: `cityById('KYIV ')` → kyiv, `cityById(null)` → undefined; every `CITIES` entry's `defaultSpawn` is a fixed-coordinate preset inside that city's bbox (load both committed datasets' `bbox` in the test); `resolveSpawn('gherkin', kyivOrigin, () => false, kyivCity, 'maidan')` lands within 5 m of the maidan coordinate; `resolveSpawn('-0.1,51.5', …)` in Kyiv falls back likewise; London behaviour unchanged (existing tests untouched); every Kyiv preset's coordinate lies inside the Kyiv bbox; each `building` preset resolves via `landmarkSpawn` on `kyiv.json` (or was converted to a coordinate — say which in the report); `formatAlt(155.6)` → `156 M ASL`; `Hud.update` with `alt` renders 7 rows with ALT fourth, without it 6 rows
- [ ] `bash scripts/check.sh` exits 0 with both e2e tests passing
- [ ] Report the numbers from a headless run of `/?city=kyiv&at=maidan` via `window.__asciicity` (`y` ≈ 1.7 + local height, `city === 'kyiv'`) and of `/?city=london` (`y === 1.7`, `city === 'london'`) — Playwright script or the e2e runner, your choice
- [ ] JSDoc; `docs/integration.md` and `docs/hud.md` updated
- [ ] No files changed outside scope

## Out of scope
- The startup city picker and the Esc pause menu / share link (T-0046).
- Look tuning (PM), minimap changes, collision changes, mobile.

## Worker report
