---
id: T-0119
title: Deck humps — apex applies to every chain ≥ 0.8 × the longest (twin-carriageway fix)
priority: P1
complexity: C1
depends_on: []
scope: [src/world/terrain.ts, tests/terrain.test.ts, docs/architecture.md]
---

## Goal

T-0118's longest-chain rule assumed each bridge road name chains into one
polyline — but Sydney's `Bradfield Highway` is a DUAL carriageway: two
near-identical chains (1630.6 m and 1634.6 m). Only the longest gets the
49 m apex, so the west carriageway lerps at ~28 m ASL — a 21 m cliff
between the two halves of the same deck (PM GPU probe on main:
`?city=sydney&at=151.21050,-33.85230` → ALT 28; twenty metres east reads
49). Rule change: the apex applies to EVERY chain whose arc length is
≥ 0.8 × the longest matching chain's length.

## Context

- The rule lives in `chainHumpApex` (`src/world/terrain.ts`, T-0118).
  Change "is the longest" to "length ≥ 0.8 × the longest matching
  chain's length". Ratio check keeps the T-0118 behaviour everywhere it
  was right: Cahill Walk's Circular Quay chain (661 / 1503 = 0.44)
  stays un-humped; single-chain names are 1.0; the Bradfield twins
  (0.998) both hump.
- `roads.ts` already calls the shared helper — no second edit site.

## Acceptance criteria

- [ ] `chainHumpApex` per Context; the exported doc comment states the
      0.8 ratio.
- [ ] `tests/terrain.test.ts`: extend the T-0118 fixtures with a
      twin-chain case (two ~equal-length chains sharing a name → BOTH
      get the apex) while the existing long/short fixture still shows
      the short one un-humped; plus a Sydney tiles assertion: deck y at
      the midpoint of BOTH `Bradfield Highway` chains = (49 − datum)
      ± 1, and the Circular Quay `Cahill Walk` chain midpoint remains
      within 3 m of its no-apex profile.
- [ ] Existing bridge/arch/terrain tests green and unmodified (except
      the named extensions above).
- [ ] `docs/architecture.md` §4.9: the longest-chain sentence gains the
      0.8-ratio wording (one line).
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (full suite).
- [ ] No files changed outside scope.

## Out of scope

roads.ts (already delegated), bridge.ts, data files, spawn/presets, any
tuning of the 0.8 constant beyond implementing it.

## Worker report

(left empty — the worker fills this)
