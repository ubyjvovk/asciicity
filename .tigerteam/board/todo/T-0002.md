---
id: T-0002
title: Geo projection, city.json validation, synthetic city and loader
priority: P1
complexity: C2
depends_on: [T-0001]
scope: [src/geo.ts, src/data/validate.ts, src/data/synthetic.ts, src/data/load.ts, tests/geo.test.ts, tests/data.test.ts, docs/data-format.md]
---

## Goal
Implement the data layer: WGS84 → local-metre projection, a strict validator
for `CityData`, the deterministic synthetic test city, and the fetch-based
loader. Everything here is pure and unit-tested in node.

## Context
- Contract: `docs/architecture.md` §3 and §4 (module list for `src/geo.ts`,
  `src/data/*`), `docs/data-format.md` (coordinate formulas, schema rules,
  synthetic-city spec). Types are in `src/data/types.ts` (PM-owned, do not edit).
- Exports (exact names):
  - `src/geo.ts`: `M_PER_DEG_LAT = 110574`, `M_PER_DEG_LON_EQ = 111320`,
    `project(lon: number, lat: number, origin: {lat: number; lon: number}): Vec2`,
    `unproject(x: number, z: number, origin): {lon: number; lat: number}`.
  - `src/data/validate.ts`: `validateCity(raw: unknown): CityData` — returns the
    same object (typed) when valid; throws `Error` whose message contains the
    JSON-ish path of the first problem, e.g. `buildings[3].poly`, `roads[0].cls`.
    Checks: `v === 1`; origin/bbox finite; every rule under "Schema (v: 1)" in
    data-format.md including unique ids per array and `h` in `[3, 320]`.
  - `src/data/synthetic.ts`: `syntheticCity(seed = 1, blocks = 12): CityData`
    exactly per data-format.md "Synthetic city" (mulberry32 PRNG; include the
    PRNG as `mulberry32(seed): () => number` export).
  - `src/data/load.ts`: `loadCity(url: string, fetchImpl: typeof fetch = fetch): Promise<CityData>`
    — `fetchImpl(url)`, throw `Error('city data: HTTP <status>')` when `!ok`,
    else `validateCity(await res.json())`.
- Append a short "Validation errors" subsection to `docs/data-format.md`
  listing the error message format and the checks performed.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/geo.test.ts tests/data.test.ts` exits 0
- [ ] `tests/geo.test.ts` covers: origin projects to `[0, 0]`; a point 0.01° north of origin has `z ≈ -1105.74` and `x ≈ 0`; a point east has `x > 0`; `unproject(project(p))` round-trips within 1e-9 degrees; values are rounded only by callers (project returns unrounded numbers)
- [ ] `tests/data.test.ts` covers: `validateCity(syntheticCity())` returns the input; rejects (message contains the path) — missing `v`, `v: 2`, a polygon with 2 points, a NaN coordinate, unknown road `cls`, duplicate building id, `h = 400`, a road with 1 point, a place with empty name; synthetic determinism (`JSON.stringify` equal for the same seed, different for seed 2); synthetic counts (`blocks*blocks` buildings, `2*(blocks+1)` roads, 1 place, every 5th building named); `loadCity` with an injected fetch returning `{ ok: true, json: async () => data }` resolves to validated data, and `{ ok: false, status: 404 }` rejects with a message containing `404`
- [ ] Every exported function has a JSDoc line; `docs/data-format.md` gains the "Validation errors" subsection
- [ ] `bash scripts/check.sh` exits 0 (typecheck must pass)
- [ ] No files changed outside scope

## Out of scope
- Fetching real OSM data (T-0003); rendering; changing `src/data/types.ts`.

## Worker report
