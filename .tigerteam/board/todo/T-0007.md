---
id: T-0007
title: Footprint collision grid with wall sliding
priority: P1
complexity: C2
depends_on: [T-0001]
scope: [src/world/collision.ts, tests/collision.test.ts, docs/collision.md]
---

## Goal
Keep the player out of buildings: a spatial hash of footprints, point-in-
polygon and edge-distance tests, and a `resolve` that slides along walls
instead of stopping dead.

## Context
- Contract: `docs/architecture.md` §4.6 (exports, cell size, `blocked`,
  `resolve` order of fallbacks). Types from `src/data/types.ts`.
- Grid: `cellOf(v) = Math.floor(v / cell)`; a footprint is inserted into every
  cell its axis-aligned bounding box (expanded by 1 m) touches. `blocked`
  queries the cell of `p` plus the 8 neighbours.
- Pure module: no three.js imports needed.
- Write `docs/collision.md` (algorithm, radius, complexity).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/collision.test.ts` exits 0
- [ ] `tests/collision.test.ts` covers: `pointInPolygon` inside/outside a square, inside/outside the notch of a concave "U" polygon, works for both windings; `distToSegment` at an endpoint, perpendicular foot, and beyond the segment; `blocked` true inside a footprint, true 0.5 m outside an edge with `r = 0.6`, false 1 m outside, false far away; a footprint spanning several 25 m cells is found from every cell it covers; `resolve` returns `to` when free, slides to `[to.x, from.z]` when only the z-move is blocked, slides to `[from.x, to.z]` when only the x-move is blocked, returns `from` when cornered; a 5000-building synthetic-style grid (generate rectangles in the test) answers 10 000 `blocked` queries in under 200 ms
- [ ] Every exported function/class has a JSDoc line; `docs/collision.md` written
- [ ] `bash scripts/check.sh` exits 0
- [ ] No files changed outside scope

## Out of scope
- Player movement (T-0008), scene wiring (T-0010), road/kerb collision.

## Worker report
