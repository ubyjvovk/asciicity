---
id: T-0071
title: Open Graph / Twitter meta + committed public/og.png social preview (1200×630)
priority: P1
complexity: C2
depends_on: []
scope: [index.html, scripts/make-og.mjs, public/og.png, tests/og.test.ts, README.md]
---

## Goal
Shared links currently unfurl as a blank GitHub Pages tab on X/Reddit/Slack.
Add the full Open Graph + Twitter card meta to `index.html` and a committed
1200×630 screenshot `public/og.png` generated by a repeatable script, per
`docs/architecture.md` §4.15 (block "Open Graph / social meta") — read that
section first; it is the contract.

## Context
- The site is served from `https://ubyjvovk.github.io/asciicity/` (deploy
  workflow sets `VITE_BASE=/asciicity/`). Meta URLs must be ABSOLUTE — the
  §4.15 block lists every tag and the exact URLs.
- `scripts/make-og.mjs`: plain node ESM, no new dependencies — use
  `@playwright/test`'s `chromium` (already a devDependency) + `spawn` of
  `npx vite --port 4311 --strictPort` and kill it in `finally`, the same
  boot the e2e uses (`e2e/smoke.spec.ts`: wait for
  `window.__asciicity.ready === true`, then hide `#overlay` via
  `style.display = 'none'`). Launch chromium with
  `{ args: ['--no-sandbox', '--disable-dev-shm-usage'] }`, viewport
  `1200×630`, url `/?time=22:30` (London default spawn — Westminster
  Bridge facing Big Ben, night = lit windows + moon). Two
  double-`requestAnimationFrame` waits after ready before the screenshot so
  the frame is painted. `page.screenshot({ path: 'public/og.png' })`.
- Run the script and COMMIT the generated `public/og.png` (vite copies
  `public/` into `dist/` at build, so the absolute og:image URL resolves).
- `tests/og.test.ts` (node, vitest): read `index.html` as text and assert
  every §4.15 tag; parse `public/og.png` bytes — PNG magic, IHDR width
  1200 / height 630 (bytes 16–23, big-endian) — and `size > 50_000` bytes
  (a black frame compresses far smaller; this catches a blank capture).
- Update the existing `name=description` to the §4.15 sentence; keep it and
  `og:description` identical.
- README: add a short "Social preview" section — what `public/og.png` is
  and `node scripts/make-og.mjs` to regenerate it.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/og.test.ts` exits 0
- [ ] Tests by name: every §4.15 meta tag present with absolute
      `https://ubyjvovk.github.io/asciicity/…` URLs; og:description equals
      name=description; og.png is PNG, 1200×630, > 50 KB
- [ ] `node scripts/make-og.mjs` exits 0 and rewrites `public/og.png`
      (fresh output committed)
- [ ] `bash scripts/check.sh` exits 0
- [ ] File-header comment in `scripts/make-og.mjs` (what/why/how to rerun);
      README "Social preview" section
- [ ] No files changed outside scope

## Out of scope
- Per-page/dynamic OG images, `?city=` variants, favicon changes, any
  `src/` change. Do not touch `e2e/`.

## Worker report

> claimed_by: ds-1 (pid 184142) at 2026-08-28T17:02:36Z
> INCOMPLETE attempt by ds-1 at 2026-08-28T17:08:18Z (engine exit 0, no handoff) — see /home/d/asciicity/.tigerteam/logs/workers/ds-1.log

> claimed_by: ds-1 (pid 184142) at 2026-08-28T17:08:18Z

### Files changed (all in scope)
- `index.html` — added the full §4.15 Open Graph + Twitter meta block and updated the existing `name=description` to the §4.15 sentence (identical to `og:description`).
- `scripts/make-og.mjs` — new. Regenerates `public/og.png` by booting a throwaway Vite server on `:4311` (`--strictPort`) and driving playwright `chromium` (`{ args: ['--no-sandbox','--disable-dev-shm-usage'] }`) at a 1200×630 viewport to `/?city=london&time=22:30`, waiting for `__asciicity.ready`, hiding `#overlay`, waiting two double-rAF after ready, then `page.screenshot`. Header comment covers what/why/how-to-rerun.
- `public/og.png` — committed 1200×630 (202076 bytes) night shot of the London default spawn (Westminster Bridge / Big Ben), freshly generated.
- `tests/og.test.ts` — new. Asserts every §4.15 meta tag with absolute `https://ubyjvovk.github.io/asciicity/…` URLs, og:description ≡ name=description, and `public/og.png` PNG magic + IHDR 1200×630 + size > 50 KB.
- `README.md` — added a short "Social preview" section (what `public/og.png` is, how to regenerate).

### Commands run
- `bash .tigerteam/scripts/run-tests.sh tests/og.test.ts` — exit 0, 14 passed (14):
  `Test Files  1 passed (1)` / `Tests  14 passed (14)`
- `node scripts/make-og.mjs` — exit 0, rewrote `public/og.png` (W 1200 H 630, 202076 bytes); git shows it committed.
- `bash scripts/check.sh` — typecheck ok, unit ok, build ok, but **e2e FAIL** (see below).

### Acceptance criteria
- [x] `tests/og.test.ts` exits 0 (14 passed).
- [x] Tests by name cover every §4.15 tag (absolute URLs), og:description=name=description, PNG 1200×630 >50 KB.
- [x] `node scripts/make-og.mjs` exits 0 and rewrites `public/og.png` (committed).
- [~] `bash scripts/check.sh` — all phases pass except one **pre-existing, unrelated** e2e failure; see note.
- [x] make-og.mjs header comment + README "Social preview" section.
- [x] No files changed outside scope (`git status` shows only the five scope files).

### ⚠ Pre-existing e2e failure (NOT caused by this ticket)
`bash scripts/check.sh` exits 1 solely because `smoke: ?render= aliases and R / Shift+R cycle` (`e2e/smoke.spec.ts:270`) times out after 60 s waiting for `render==='braille'` after a single `KeyR`. I verified this by stashing all my changes and running the clean base commit `4106604`: the identical test fails the same way (`1 failed / 10 passed`). T-0071 touches no `src/` and `e2e/` is explicitly out of scope, so I did not fix it. All other check.sh phases (typecheck, unit, build) pass.

### Commit
- `8b28f48` on branch `tigerteam/T-0071` — `[T-0071] Open Graph / Twitter meta + committed public/og.png social preview (1200×630)`

### Notes for reviewer
- `make-og.mjs` uses `/?city=london&time=22:30` (explicit city) rather than the bare `/?time=22:30` in the ticket, because the bare URL opens the interactive city picker overlay (main.ts `needsPicker`); `?city=london` bypasses it and `at=` absent gives the London default spawn, matching the intent (night Westminster/Big Ben). `?time=22:30` is honoured (sky is fixed to the pinned time).
- `public/og.png` is deterministic (~202 KB); re-running the script reproduces it byte-identically.

> handoff: review

## Rework (attempt 2)

Meta block, tests, README and the composition are all right — accepting
everything except one detail that matters on the social face: the HUD in
`public/og.png` shows `FPS........ 0`. The screenshot fires after two
double-rAFs, before the app's 1-second FPS window has ever updated, so
the flagship image looks like a frozen app.

Fix in `scripts/make-og.mjs` only:
- after hiding `#overlay`, wait until the HUD actually shows a non-zero
  FPS before the screenshot:
  `page.waitForFunction(() => /FPS\.+\s*[1-9]/.test(document.querySelector('#hud')?.textContent ?? ''), undefined, { timeout: 20000 })`
- make the script PRINT the FPS row it captured and exit non-zero if the
  wait times out (never commit a 0-FPS image again);
- regenerate and commit the new `public/og.png` (the byte-determinism
  note in your report will no longer hold — that was never a criterion).

Note: the `smoke.spec.ts` e2e failure you correctly diagnosed is now
fixed on main (T-0074 merged), so your next `bash scripts/check.sh`
should be fully green — please run it and report all four phases ok.


> claimed_by: ds-1 (pid 184142) at 2026-08-28T17:29:51Z
