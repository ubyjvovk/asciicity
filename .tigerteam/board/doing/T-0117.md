---
id: T-0117
title: dem.mjs — `ridge` bare-earth mode (directional opening, crest-preserving)
priority: P2
complexity: C2
depends_on: []
scope: [scripts/dem.mjs, scripts/dem.d.ts, scripts/fetch-osm.mjs, tests/dem.test.ts, docs/fetch-osm.md]
---

## Goal

Add a second bare-earth filter mode, `ridge`, that removes roof
contamination without shaving real bluff crests: T-0115 measured the
wave-13 9×9 erode cutting 19 m off the Lavra crest and 12 m off the
funicular bluff (real relief) while still under-flattening streets. The
`ridge` mode is a directional opening — roofs have low ground in every
direction, crests keep a high ray along their plateau.

## Context

- **The contract is `docs/data-format.md` step 3b, "`ridge` mode"** —
  the exact algorithm (8 half-rays × 4 nodes including the centre,
  `H1 = max over rays of ray-min`, then the existing double 3×3 smooth),
  its properties, and the CLI shape. Read it first.
- T-0115's blocked report (`.tigerteam/board/blocked/T-0115.md` — or
  `todo/` once answered) carries the Kyiv measurements motivating this.
- Implementation home: `scripts/dem.mjs` beside `erode`/`smooth` — a new
  pure `ridgeOpen(heights, cols, rows)` (same array→array signature,
  reads only the input grid). `Dem`'s `bareEarth` option and
  `buildTerrain` currently treat it as boolean; make the option
  `false | true | 'ridge'` (`true` = the wave-13 erode path, byte-for-
  byte unchanged), and pick the first pass accordingly. Update
  `scripts/dem.d.ts` (established pattern).
- CLI: `scripts/fetch-osm.mjs` currently forwards `--dem-bare` as a
  boolean. Accept `--dem-bare ridge` (the T-0100 parser already
  consumes a value token); `--dem-bare`/`--dem-bare 1`/`true` keep
  meaning the erode mode. Any other value throws loudly.
- Do NOT touch any committed dataset — this ticket is code + tests only.
  Kyiv's regen re-runs afterwards as T-0115 (already answered to depend
  on this ticket).

## Acceptance criteria

- [ ] `ridgeOpen` implemented per the step-3b contract, exported, pure.
- [ ] `tests/dem.test.ts` fixtures (synthetic grids, one per contract
      property): (a) isolated 3-node-wide high blob on a flat plain →
      cut to plain height before smoothing; (b) a straight ridge/plateau
      edge spanning the grid → preserved exactly before smoothing;
      (c) flat grid → fixpoint; (d) `ridgeOpen(H) ≤ H` element-wise on a
      random grid; (e) border clipping — a blob in a corner is still
      cut; existing erode/smooth tests untouched and green.
- [ ] `buildTerrain`/`Dem` accept `bareEarth: 'ridge'` (ridge → double
      smooth) while `true` reproduces the wave-13 path byte-identically
      (a test runs both modes on one fixture and asserts they differ
      only where expected — i.e. `true`'s output is unchanged vs a
      pinned expectation).
- [ ] `fetch-osm.mjs` flag handling per Context, with a unit test for
      `--dem-bare ridge`, `--dem-bare 1`, bare `--dem-bare`, and a
      rejected `--dem-bare bogus`.
- [ ] `docs/fetch-osm.md`: the `--dem-bare` flag row documents both
      modes in one or two sentences.
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (full suite).
- [ ] No files changed outside scope.

## Out of scope

Regenerating any dataset (T-0115 does Kyiv after this lands), changing
the erode/smooth implementations or parameters, package.json aliases
(PM-owned), tuning ray length/count beyond the contract.

## Worker report

(left empty — the worker fills this)

> claimed_by: ds-2 (pid 1487920) at 2026-08-31T21:57:16Z
