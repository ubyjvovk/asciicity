---
id: T-0066
title: TreeField — instanced low-poly trees seated on the terrain
priority: P1
complexity: C2
depends_on: [T-0065]
scope: [src/world/trees.ts, tests/trees.test.ts, src/main.ts, docs/world.md]
---

## Goal
Render `city.trees` as two instanced meshes (canopies + trunks) per
`docs/architecture.md` §4.14 — pure `buildTreeInstances` unit-tested,
`TreeField` browser wrapper, one line in `main.ts`.

## Context
- Follow §4.14 exactly (geometry, placement, colours, materials). Pattern
  reference for instancing: `BusFleet` in `src/world/traffic.ts`.
- Pure tests (`tests/trees.test.ts`): `buildTreeInstances` with
  `heightAt = () => 10` and one tree `[0, 0, 12, 4]` → canopy matrix
  translation `(0, 10 + 12 − 3.2, 0)` and scale `(4, 3.2, 4)`, trunk scale y
  `= 8.8` with base at y = 10; colours in the HSL ranges of §4.14 (convert
  back and check hue/sat/lightness bounds over 200 seeded trees); seeded
  determinism; `count` and array lengths for 0 and 3 trees.
- `main.ts`: add the field when `city.trees?.length` (before buildings),
  pass `groundAt`; nothing else.
- Docs: `docs/world.md` Trees section.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/trees.test.ts` exits 0
- [ ] Tests by name as in Context
- [ ] `bash scripts/check.sh` exits 0
- [ ] Headless evidence: `/?city=kyiv&at=hydropark` boots with `__asciicity.ready` and no console errors; report `TreeField.count` for Kyiv and London (expose it on `window.__asciicity.trees`)
- [ ] JSDoc; docs updated
- [ ] No files changed outside scope

## Out of scope
- Collision with trunks, wind, LOD, minimap (T-0067), look tuning (PM).

## Worker report

> claimed_by: ds-2 (pid 484850) at 2026-08-27T10:08:36Z
