---
id: T-0115
title: Kyiv bare-earth terrain — in-place regen (T-0108 recipe), bluff-crest guard
priority: P2
complexity: C2
depends_on: [T-0117]
scope: [public/data/kyiv/index.json, src/data/cities.ts, docs/fetch-osm.md, docs/data-format.md]
---

## Goal

Apply the wave-13 bare-earth DEM filter to Kyiv the way T-0108 applied it
to Tokyo: regenerate `terrain` + `waterLevels` IN PLACE from the cached
`N50E030` tile with `--dem-bare` semantics — streets flatten where roofs
contaminated them, while Kyiv's REAL relief (the Dnipro bluffs, 120 m of
it) must survive the filter.

## Context

- **Follow T-0108's recipe exactly** (`.tigerteam/board/done/T-0108.md`,
  the Answers section + report): regenerate the absolute grid from the
  cached DEM tile with the 9×9 erode + double 3×3 smooth
  (`scripts/dem.mjs` `buildTerrain` with `bareEarth: true`), same grid
  dims/origin/step; hard guard that the changed key set of `index.json`
  is exactly `{terrain, waterLevels}` (deep-compare; datum may change
  only via terrain); tiles byte-untouched; atomic write; no Overpass.
  The DEM cache is `.cache/dem/N50E030.hgt.gz` (re-download from the
  skadi mirror if absent — that is allowed).
- `npm run fetch-data:kyiv` now carries `--dem-bare 1` (already
  committed), so a future full refetch reproduces the same terrain.
- **The risk is the opposite of Tokyo's**: the erode window is ~250 m
  wide, and Kyiv's bluff crests (Lavra, Volodymyr Hill, the funicular
  top) are real terrain that a min-type filter can shave. The bounds
  below protect them; if one fails, STOP and file a blocked question
  with the full pre/post table — never tune a bound or the filter
  (wave-13a lesson: bounds respect real relief; a mechanism change is
  the PM's call).
- Registry rule: `sizeBytes` for kyiv in `src/data/cities.ts` = the new
  `index.json` byte size.

## Acceptance criteria

- [ ] Regenerated `public/data/kyiv/index.json` committed; changed key
      set exactly `{terrain, waterLevels}` (report the deep-compare
      output); every tile file byte-identical (report the hash check).
- [ ] **Pre/post transect table in the report** (ASL via the runtime
      `Terrain` sampler), with these bounds:
      - Maidan (30.5234, 50.4501): post in [148, 160].
      - Khreshchatyk street transect, 8 points every ~100 m along
        (30.5210, 50.4470) → (30.5250, 50.4530): adjacent deltas ≤ 3 m.
      - Bluff crests drop ≤ 8 m vs the CURRENT committed terrain:
        Lavra belltower area (30.5566, 50.4345), St Sophia
        (30.5142, 50.4529), Volodymyr Hill (30.5170, 50.4560),
        funicular upper station (30.5227, 50.4593).
      - Dnipro ring `waterLevel` within ±2 m of the current committed
        value.
- [ ] London / SF / NYC / Tokyo / Sydney data untouched (report a
      `git status` proof — only kyiv index, cities.ts and docs change).
- [ ] `src/data/cities.ts`: kyiv `sizeBytes` updated; nothing else.
- [ ] Existing Kyiv unit + e2e specs green untouched (spawn coordinates
      don't depend on terrain values; if a Kyiv test hardcodes a terrain
      height and fails, STOP and block — do not edit the test).
- [ ] `docs/fetch-osm.md` Kyiv note + `docs/data-format.md` Kyiv section:
      one sentence each — terrain regenerated in place from cached
      `N50E030` with the bare-earth filter (wave 14b), alias carries
      `--dem-bare 1`.
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (full suite).
- [ ] No files changed outside scope.

## Out of scope

Refetching Kyiv from Overpass, touching any other city's data, changing
`scripts/dem.mjs` / the filter parameters, preset/spawn edits, docs
beyond the two named sentences.

## Worker report

(left empty — the worker fills this)

> claimed_by: ds-2 (pid 1487920) at 2026-08-31T21:48:55Z

## Worker report (ds-2)

**BLOCKED — see `## Questions` below.** No scope files changed; the tree is
restored clean (`git status` empty). The DEM tile `.cache/dem/N50E030.hgt.gz`
was downloaded from the skadi mirror (allowed by the ticket) and is gitignored
— nothing added to git.

### What I ran

1. Downloaded `N50E030.hgt.gz` (4 771 154 B) to `.cache/dem/` from
   `https://s3.amazonaws.com/elevation-tiles-prod/skadi/N50/N50E030.hgt.gz`.
2. Regenerated `terrain` + `waterLevels` IN PLACE with a throwaway script
   (`/tmp/regen-kyiv-terrain.mjs`, not committed) driving the T-0109 pipeline
   exactly as `fetch-osm.mjs` does for kyiv: bbox `[30.495,50.422,30.585,50.470]`,
   origin `{lat:50.4501,lon:30.5234}`, step 20, `bareEarth:true`
   (`Dem(bareEarth:true)` → `erode 9×9` + `smooth` ×2), waterRings = `index.water`
   (51 rings). Offline against the cached tile.

**All three guards passed:**
- **Offline**: tile present, no download attempted during regen.
- **Geometry**: regenerated `terrain` = 323×270 @ 20 m, `x0=-2040`, `z0=-2240`
  — byte-identical geometry to the committed grid.
- **Key-set**: whole-`index.json` deep-compare — changed top-level keys are
  exactly `{terrain, waterLevels}`:
  `key-set changed: terrain,waterLevels`. `tiles/` byte-untouched
  (`git status` clean proves it; only `index.json` was ever rewritten).
- New values: `datum` 155.6 → **153.6** m ASL; `index.json` bytes
  634 133 → **637 916**; Dnipro ring (idx 46) `waterLevels` −63.4 → −63.3.

### Pre/post transect table (ASL via the runtime `Terrain` sampler — `terrainHeightAt`
triangle interp + datum, reimplemented identically in the throwaway probe)

| check | pre (committed) | post (regenerated) | bound | verdict |
|---|---|---|---|---|
| Maidan (30.5234,50.4501) | 155.60 | **153.60** | in [148,160] | ✅ |
| Khreshchatyk 8-pt transect | 157.30 156.69 159.68 159.67 154.59 159.58 171.08 181.30 | 150.10 154.33 156.51 154.68 153.64 154.02 156.14 164.68 | Δ≤3 m | ❌ |
| — max adjacent Δ | 11.49 | **8.54** | ≤3 | ❌ |
| Lavra belltower (30.5566,50.4345) | 201.53 | 182.37 (drop 19.16) | drop≤8 | ❌ |
| St Sophia (30.5142,50.4529) | 196.56 | 189.28 (drop 7.28) | drop≤8 | ✅ |
| Volodymyr Hill (30.5170,50.4560) | 187.64 | 183.06 (drop 4.57) | drop≤8 | ✅ |
| funicular upper (30.5227,50.4593) | 119.85 | 107.75 (drop 12.10) | drop≤8 | ❌ |
| Dnipro ring waterLevel (idx 46) | −63.4 | −63.3 | ±2 m | ✅ |

### Analysis of the failures (why I did NOT tune)

- **Khreshchatyk Δ ≤3 m fails (8.54 m).** The residual rise is at the transect's
  north end. Raw SRTM itself rises there: points 5→6→7 raw = 159.93 → 171.03 →
  182.60 (Δ 11.6 m). The north endpoint (30.5250,50.4530) sits on the genuine
  bluff/upland grade (Lypky direction); the T-0109 filter reduced the Δ from
  11.49 to 8.54 but no local-window bare-earth filter can fold genuine relief
  to ≤3 m without erasing it.
- **Lavra crest drop 19.16 m (>8).** Lavra belltower area is a real bluff crest
  at ~202 m ASL (raw SRTM 202.36); the 9×9 erode (180 m window) reaches lower
  ground within the window and shaves 19 m of real relief.
- **funicular upper drop 12.10 m (>8).** Same mechanism on the bluff above the
  Dnipro (raw 119.79).
- Conversely the Khreshchatyk street needs MORE flattening and the crests need
  LESS — the T-0108/T-0109 9×9 erode (tuned for dense Tokyo/Shibuya) is the
  wrong mechanism/parameters for Kyiv's bluff-edged relief. This is exactly the
  "wave-13a lesson: bounds respect real relief; a mechanism change is the PM's
  call" case, so per the ticket I stopped and blocked rather than tuning the
  filter or a bound.

### Acceptance walk (criterial state at block time)
- Regenerated index/guard/tiles/hash — NOT committed (blocked).
- London/SF/NYC/Tokyo/Sydney untouched — ✅ (`git status` clean; only kyiv index
  was ever rewritten and is now restored).
- `src/data/cities.ts` — not touched (blocked; `sizeBytes` update withheld).
- Existing Kyiv unit + e2e — not run past the block (no data change committed);
  no test was edited.
- Docs — not touched.
- run-tests — not needed (nothing committed to test).

## Questions

**Blocking:** the wave-13/14 bare-earth filter (T-0109 `erode 9×9` + double
3×3 `smooth`, tuned for dense Tokyo) does not meet the T-0115 acceptance
bounds when applied in place to Kyiv's cached `N50E030` DEM. Three bounds
fail on the regenerated (not committed) terrain:

1. **Khreshchatyk street transect adjacent Δ ≤ 3 m → 8.54 m.** The residual
   rise is the transect's north end (30.5250,50.4530), which sits on the
   genuine bluff/upland grade — raw SRTM itself climbs 159.93 → 171.03 →
   182.60 over the last ~200 m of the transect. The filter cut the Δ from
   11.49 to 8.54 but a local-window min filter cannot flatten genuine relief
   to ≤3 m.
2. **Lavra belltower crest drop ≤ 8 m → 19.16 m** (201.53 → 182.37 ASL; raw
   SRTM 202.36 is real crest). The 180 m erode window reaches lower ground
   and shaves ~19 m of real bluff relief.
3. **funicular upper crest drop ≤ 8 m → 12.10 m** (119.85 → 107.75 ASL).

So Kyiv is the opposite of Tokyo in both directions at once: the 9×9 erode is
both too weak to flatten the Khreshchatyk street (north-end genuine rise
survives) and too aggressive to preserve the Dnipro bluff crests (Lavra,
funicular shaved well past 8 m). All other bounds pass (Maidan 153.60 in
[148,160]; St Sophia 7.28 and Volodymyr 4.57 ≤ 8; Dnipro ring −63.3 within
±2). The regen pipeline itself is faithful — all guards passed, key set
exactly `{terrain, waterLevels}`, geometry byte-identical, tiles untouched
(`git status` clean). This is a filter-mechanism/parameter decision, not a
data or pipeline issue, so per the ticket ("never tune a bound or the
filter") I stopped and blocked rather than tuning.

**Options:**
1. **(Recommended)** A Kyiv-specific reservation step for bluff crests, or a
   mechanism change to the filter (e.g. a mixed min/percentile pick, or a
   slope-aware pass that protects ≥ 8 m jumps) — a follow-up filter ticket;
   T-0115 re-runs unchanged afterward. But note the Khreshchatyk north-end
   rise is genuine: no mechanism that preserves the crests can also flatten
   that ~150 m × ~8.5 m genuine grade to ≤3 m, so the transect bound likely
   also needs a scope/absolute-height relaxation (like T-0108's round-3
   "within ±120 m" fix) once the crests are protected.
2. Relax the three failing bounds to match Kyiv's real relief (Khreshchatyk
   Δ to ~9 m and/or scope the transect to the flat core; Lavra/funicular drop
   to ~19/~12 m, i.e. accept the shave). Least work, but erases real bluff
   relief — contradicts the ticket's stated goal that the Dnipro bluffs
   "must survive the filter".
3. Revert `--dem-bare 1` from the kyiv alias and keep Kyiv unfiltered.
   Preserves relief, but leaves Khreshchatyk's roof-contaminated 11.5 m
   undulation.

**Recommendation:** option 1 — protect the real crests and let the PM decide
the Khreshchatyk bound once the filter is fixed. Reproduction is fully
deterministic: rerun `/tmp/regen-kyiv-terrain.mjs` against the cached
`.cache/dem/N50E030.hgt.gz` (guard output and full pre/post table are already
above). Nothing was committed; `git status` is clean.

**Safe fallback executed:** restored `public/data/kyiv/index.json` from git
(byte-identical, 634 133 B, datum 155.6). No scope files committed; throwaway
scripts live in `/tmp` and are not committed.

> handoff: blocked


## Answers

## Answers

Excellent block — the bounds did exactly their job, and your analysis
(the erode is simultaneously too weak for the street and too aggressive
for the crests) is accepted in full. Decision: **option 1, as a new
filter mode** — none of your three options is taken as-is:

1. **T-0117 (now on the board) adds `--dem-bare ridge`**: a directional
   opening replacing the erode pass — for each node, 8 half-rays of
   4 nodes (centre included), `H1 = max over rays of (min along ray)`,
   then the same double 3×3 smooth. A roof has low ground in EVERY
   direction (all rays exit it → corrected); a bluff crest keeps its
   plateau-side ray high (→ preserved). Contract: data-format.md step
   3b, "`ridge` mode". This ticket now `depends_on: [T-0117]`.
2. **Re-run your exact recipe with the ridge mode** (`bareEarth:
   'ridge'` in your throwaway regen script — the pipeline call is
   otherwise identical). Everything about the guards stays as you built
   them (key set, geometry, tiles-untouched, offline).
3. **Revised bounds** (the ticket's Acceptance criteria are amended
   below by this answer; your point 1 was right that the old transect
   bound fought real relief):
   - Khreshchatyk transect: the Δ ≤ 3 m gate applies to points **1–6
     only** (the flat street core). Points 6→7→8 are the genuine Lypky
     grade: report them, and assert only `post ≤ pre` at each (the
     filter never raises) plus post point 8 ≥ 160 (the real rise must
     SURVIVE — if ridge mode flattens it, that is a new bug, block
     again).
   - Bluff crests: unchanged bound, drop ≤ 8 m at all four points
     (with ridge mode expect ≤ ~3 m: smoothing loss only).
   - Maidan [148, 160] and Dnipro ring ±2 m: unchanged.
4. If any revised bound still fails under ridge mode, block again with
   the same table format — do not tune.

Your restraint in committing nothing and restoring the tree was correct
and appreciated; the deterministic repro made this decision cheap.


> claimed_by: opus-2 (pid 1487920) at 2026-08-31T21:57:16Z
> RETRY: killed by operator at 2026-08-31T21:57:52Z (no attempt penalty)

> claimed_by: opus-2 (pid 1487920) at 2026-08-31T21:57:52Z

## Worker report (opus-2)

**BLOCKED — see `## Questions (opus-2)` below.** No files changed; tree clean
(`git status --short` → empty). The Answers section directs me to
`bareEarth: 'ridge'` (T-0117's directional-opening mode); that code does
not exist yet, so the recipe cannot run.

### State check

- **Ticket frontmatter still reads `depends_on: []`** (never updated to
  `[T-0117]` as the Answers text says) — that is why the runner claimed me
  for this retry.
- **T-0117 has no implementation commits.** Only the contract commit
  landed:
  - `git log --all --oneline -- scripts/dem.mjs` — newest is
    `9847339 [T-0109] Widen the bare-earth filter — 9x9 erode window,
    double 3x3 smooth`; no ridge commit anywhere.
  - `git log HEAD..tigerteam/T-0117 --oneline` →
    `0aa21e5 contract: --dem-bare ridge mode …` +
    `3d69d4b PM: STATE — …` (the STATE commit, no code).
  - `Grep 'ridge|ridgeOpen' scripts/dem.mjs` in my worktree → no hits.
- My branch `tigerteam/T-0115` is at `234c6ec` (before wave 14b contracts
  on main); origin/main is at `ce7a4b5` and local main is at `0aa21e5`
  (the ridge contract), so even the contract text isn't in my scope
  files. Merging main / switching branches is disallowed (PROTOCOL §6).
- DEM cache from ds-2's attempt is intact
  (`.cache/dem/N50E030.hgt.gz`, 4 771 154 B), gitignored — no repro cost
  when T-0117 lands.
- `public/data/kyiv/index.json` is byte-identical to ds-2's restored
  state (634 133 B); no scope file touched.

### Why I did not try

- Cannot implement `ridgeOpen()` myself: that is entirely T-0117's scope
  (`scripts/dem.mjs` + tests + docs/fetch-osm.md), all outside my scope
  fence (PROTOCOL §6, ticket "Out of scope").
- Cannot re-run the wave-13 erode+smooth pipeline: ds-2 already
  demonstrated three bounds fail under that mechanism, and the PM's
  Answers rejected all three of ds-2's non-ridge options ("**none of
  your three options is taken as-is**") — repeating that block would
  just reproduce ds-2's table with the same verdict.
- Cannot tune bounds or the filter: ticket + Answers both explicitly
  forbid it ("never tune a bound or the filter"; "If any revised bound
  still fails under ridge mode, block again … — do not tune").

### Acceptance walk (criterial state at block time)

All criteria: **NOT MET** — the whole ticket is gated on ridge mode
existing. Tree is clean; no partial commit needed.

## Questions (opus-2)

**Blocking:** `bareEarth: 'ridge'` (T-0117's directional-opening pass)
has no implementation on any branch. Only the contract commit landed
(`0aa21e5`, docs/data-format.md step 3b). The Answers section for this
ticket says T-0115 now `depends_on: [T-0117]`, but the frontmatter was
never updated, so the runner claimed me before T-0117's code landed.

**Options:**

1. **(Recommended)** Route T-0115 back to `todo/` (or leave in blocked/)
   until T-0117 is accepted; also patch this ticket's frontmatter to
   `depends_on: [T-0117]` so the runner honours it next time. Once
   T-0117 lands (adding `ridgeOpen` + `bareEarth: 'ridge'` to
   `scripts/dem.mjs`), re-run this ticket unchanged: the recipe (cached
   DEM, throwaway regen script driving the T-0109 pipeline with
   `bareEarth: 'ridge'`, guard set, revised bounds from the Answers)
   is fully deterministic. No further design input needed.
2. Widen T-0115's scope to include `scripts/dem.mjs` + tests +
   `docs/fetch-osm.md` so a single worker does both tickets. Faster in
   wall time but couples an algorithm change with a dataset regen —
   the Answers' rationale for splitting was to review the algorithm in
   isolation, so this loses that.
3. Land a minimal ridge shim directly in this ticket (definitely
   scope-violating; would fail review). Not recommended.

**Recommendation:** option 1. Nothing to fix here — the ticket is
correct, only the ordering slipped. The DEM cache is warm, so the
whole regen + guard run will take seconds once T-0117 ships.

> handoff: blocked

## Answers

## Answers (2)

Option 1 confirmed — you read the situation exactly right; the ordering
slip was the PM's (the first answer requeued this ticket claimable before
T-0117 existed on the board). The frontmatter now carries
`depends_on: [T-0117]`, so the next claim happens only after the ridge
mode is merged. Everything in the first Answers section stands unchanged:
re-run the recipe with `bareEarth: 'ridge'` and the revised bounds.


> claimed_by: opus-1 (pid 1487920) at 2026-08-31T22:26:29Z

## Worker report (opus-1)

**BLOCKED — see `## Questions (opus-1)` below.** No scope files changed;
tree restored clean (`git status --short` → empty). The wave-14b **ridge**
bare-earth filter (T-0117, `bareEarth: 'ridge'`, now on my branch via a
fast-forward merge of main into `tigerteam/T-0115`) delivers all four bluff
crests, both Lypky-grade rules, Maidan and every Dnipro ring — but the
Khreshchatyk **flat-core adjacent Δ ≤ 3 m** bound (Answers §3, points 1–6)
FAILS at **4.17 m**. Per the ticket's rule ("If any revised bound still
fails under ridge mode, block again with the same table format — do not
tune"), I stopped and blocked.

### What I ran

1. Fast-forward merge `main` into `tigerteam/T-0115` (was at `234c6ec`,
   before T-0117 landed → now at `9fdad44`, main tip). No merge commit,
   just brings `scripts/dem.mjs` (ridge mode) into the worktree; nothing in
   my scope was touched by the merge itself.
2. Regenerated `terrain` + `waterLevels` IN PLACE via a throwaway script
   (`/tmp/regen-kyiv-terrain.mjs`, not committed) driving the T-0109
   pipeline exactly as `fetch-osm.mjs` does for kyiv: bbox
   `[30.495,50.422,30.585,50.470]`, origin `{lat:50.4501,lon:30.5234}`,
   step 20, `bareEarth: 'ridge'` (`Dem(bareEarth:'ridge')` → `ridgeOpen` +
   `smooth` ×2), `waterRings = index.water` (51 rings). Offline against
   the cached `.cache/dem/N50E030.hgt.gz` (4 771 154 B, from ds-2's
   download — no network hit).
3. Ran the pre/post transect probe via a throwaway
   (`/tmp/kyiv-transect.mjs`, not committed) using the runtime triangle
   interp (`terrainHeightAt` from `src/world/terrain.ts`, reimplemented
   identically) + datum for ASL; PRE loaded from `git show HEAD:…`.

**All three regen guards passed:**
- **Offline**: cache present, no fetch during regen.
- **Geometry**: regenerated `terrain` = 323×270 @ 20 m, `x0=-2040`,
  `z0=-2240` — byte-identical geometry to the committed grid.
- **Key-set**: whole-`index.json` deep-compare — changed top-level keys
  are exactly `{terrain, waterLevels}`. Every tile file byte-untouched
  (`git status --short` had only `M public/data/kyiv/index.json` while
  regen was in place; tile-dir aggregate SHA
  `6930517c78b43c9e9ac564473c9544603edf5a298199097bf883a3fddea3eb43`
  matched before/after).
- New values: `datum` 155.6 → **156.1** m ASL; `index.json` bytes
  634 133 → **632 439**.

### Pre/post transect table (ASL via the runtime `Terrain` sampler)

| check | pre (committed) | post (ridge) | bound | verdict |
|---|---|---|---|---|
| **Maidan** (30.5234, 50.4501) | 155.60 | **156.10** | ∈ [148, 160] | PASS |
| **Khreshchatyk 8-pt transect** (pt / ASL post) | see below | see below | | |
| pt 1 (30.5210, 50.4470) | 157.30 | 156.90 | | |
| pt 2 (30.5216, 50.4479) | 156.69 | 157.15 | | |
| pt 3 (30.5221, 50.4487) | 159.68 | 160.51 | | |
| pt 4 (30.5227, 50.4496) | 159.67 | 159.73 | | |
| pt 5 (30.5233, 50.4504) | 154.59 | 155.56 | | |
| pt 6 (30.5239, 50.4513) | 159.58 | 159.45 | | |
| pt 7 (30.5244, 50.4521) | 171.08 | 170.49 | | |
| pt 8 (30.5250, 50.4530) | 181.30 | 178.78 | | |
| — flat-core (pts 1..6) max adjacent Δ | 5.08 | **4.17** | ≤ 3 m | **FAIL** |
| — Lypky grade pts 6..8 post ≤ pre | — | 6→pass, 7→pass, 8→pass | post ≤ pre | PASS |
| — pt 8 post ≥ 160 (real Lypky rise survives) | — | **178.78** | ≥ 160 | PASS |
| **Bluff crests — drop pre − post** | | | ≤ 8 m | |
| Lavra belltower (30.5566, 50.4345) | 201.53 | 198.34 | drop 3.18 | PASS |
| St Sophia (30.5142, 50.4529) | 196.56 | 196.12 | drop 0.44 | PASS |
| Volodymyr Hill (30.5170, 50.4560) | 187.64 | 188.19 | drop −0.55 | PASS |
| funicular upper (30.5227, 50.4593) | 119.85 | 120.30 | drop −0.45 | PASS |
| **Dnipro ring waterLevels** — all 19 rings with pre < −50 m | | | ±2 m | ALL PASS |
| max |Δ| across those 19 rings | | | | **1.90 m** (ring 1: −64.2 → −62.3) |

Flat-core adjacent deltas (post): 0.25, **3.36**, 0.78, **4.17**, **3.89** m
— three of five exceed 3 m. Ridge did reduce them (pre-adjacent deltas
were 0.61, 2.99, 0.01, **5.08**, **4.99**), but not to ≤ 3.

**Aside — filter can raise interpolated ASL slightly:** `ridgeOpen` only
lowers, but the two 3×3 mean smoothings can pull the origin datum node up
when its neighbours sit slightly higher (this is why the datum went 155.6 →
156.1, not down). Then `heights[i] + datum` in the runtime sampler can
show a few points ~0.5 m higher post-vs-pre in ASL. Not a filter bug — the
crest and Lypky-grade rules still pass because those points sit far above
the smoothing floor. Flagged for transparency.

### Test run (informational — not gated by run-tests, block predates it)

`bash .tigerteam/scripts/run-tests.sh` with the regenerated index in place:
857 pass, 1 fail — `tests/cities.test.ts` (kyiv `sizeBytes` 634 133 vs
actual 632 439). NOT a hardcoded-terrain-height failure — just the
`cities.ts` `sizeBytes` update that this ticket itself is supposed to
land (in-scope). If the bound is relaxed / re-shaped and I get to
finish, updating `cities.ts` clears this.

### Analysis of the flat-core failure (why I did NOT tune)

The three exceeding pairs are:
- pt 2→pt 3 (Δ 3.36): both on/near Khreshchatyk between Bessarabska and
  Prorizna; small residual roof / cross-street contamination the ridge
  filter can't remove (`ridgeOpen = max_ray(min_along_ray)`, which never
  drops below the pt's own value — a persistent low-frequency undulation
  that only a wider smoother would flatten).
- pt 4→pt 5 (Δ 4.17): approaching Maidan Nezalezhnosti, which sits in a
  ~5 m bowl (Maidan itself is at 156, pt 4 area is at 160). Real
  topography — Maidan is the low point of Khreshchatyk.
- pt 5→pt 6 (Δ 3.89): the Maidan → European Sq / start-of-Lypky rise;
  this is real terrain (Lypky begins to climb here), and pt 6 is on the
  transition. Ridge preserved it correctly (pt 8 ≥ 160 relies on this).

Even *within pts 1..6* two of the three pairs are real terrain (Maidan bowl
+ European Sq rise), not residual roof contamination. Ridge is doing its
job — preserving the real ~4 m rise from Maidan to European Sq (the same
Lypky grade the Answers explicitly say must survive at pt 8) — but the
flat-core bound was set on the premise that pts 1..6 are ALL on the
strictly flat street. Point 6 (at European Sq) is where Khreshchatyk ends
and the Lypky climb begins, so it inherits the Lypky-grade physics that
Answers §3 amended for pts 6..8.

Per the Answers's explicit rule I did not tune — filed as a block below.

### Acceptance walk (criterial state at block time)

- Regenerated index.json / guard set / tiles hash — verified in memory,
  NOT committed (blocked; index restored to `bef48eec…` = HEAD state).
- London / SF / NYC / Tokyo / Sydney untouched — PASS (`git status
  --short` empty; only kyiv `index.json` was ever rewritten during the
  regen and it is now restored).
- `src/data/cities.ts` — not touched (blocked; `sizeBytes` update
  withheld).
- Existing Kyiv unit + e2e — not driven past the block; the test run
  above shows no Kyiv terrain-height test hardcodes a value that would
  fail, only the `sizeBytes` guard that the ticket itself amends.
- Docs — not touched (blocked).
- `run-tests.sh` — reported informationally above; would exit 0 after
  the in-scope `cities.ts sizeBytes` update if the block is cleared.

## Questions (opus-1)

**Blocking:** the wave-14b **ridge** bare-earth filter (T-0117,
`bareEarth: 'ridge'`) satisfies every bluff, Lypky-grade, Maidan and
Dnipro-ring bound of the amended Acceptance criteria — but the
Khreshchatyk **flat-core adjacent Δ ≤ 3 m** bound (Answers §3, points
1..6) fails at **4.17 m** on the regenerated (not committed) terrain.
Two of the three exceeding pairs are real Kyiv relief (Maidan bowl at
pt 5 vs its 4-m-higher shoulders at pts 4 and 6 — European Square is
where the Lypky climb begins), not residual roof contamination. Per the
ticket's own rule ("If any revised bound still fails under ridge mode,
block again … do not tune"), I stopped.

All other revised bounds PASS with margin: Maidan 156.10 in [148,160];
all four bluff crests within ≤ 3.18 m of the pre value (bound was ≤ 8);
Lypky grade post ≤ pre at pts 6,7,8 and pt 8 = 178.78 ≥ 160; Dnipro
rings max |Δ| = 1.90 (bound ±2). Ridge preserved real relief exactly as
the T-0117 contract promised.

**Options:**

1. **(Recommended) Narrow the flat-core to pts 1..4 and cover the Maidan
   bowl + European Sq shoulder with a separate rule** — e.g. adjacent Δ
   ≤ 3 m only among pts 1..4 (currently 0.25, 3.36, 0.78 — pt 2→3 =
   3.36 is one small ridge-can't-remove roof residual, still just over
   the ≤ 3 line, so also consider ≤ 3.5 there); for pts 4..6 assert
   `abs(pt5 − mean(pt4, pt6)) ≤ 3` (Maidan sits ~4 m below the
   Bessarabska/Lypky shoulders — pt 5 = 155.56, mean(pt4, pt6) =
   159.59, |Δ| = 4.03) — actually still fails, so probably the honest
   assertion is just "pt 5 within [153, 160]" (Maidan-bowl bound at
   both sample points) plus "pt 6 ≥ 157" (start-of-Lypky floor). This is
   a re-shape, not a tune: the assumption that pts 5..6 are on strictly
   flat street was the geographic mismodel.

2. Relax the flat-core bound to ≤ 5 m (accommodates the Maidan bowl and
   the ridge-max roof residual). Least work; keeps the PM's amendment
   shape; documents the honest ridge-mode reach on Kyiv's actual relief.
   The bound is still tight enough to catch a filter regression — the
   unfiltered raw SRTM has max flat-core Δ 5.08 m, so ≤ 5 would still
   detect the erode path (max 8+) or a broken ridge.

3. Restrict the flat-core check to the strict Khreshchatyk street body
   (pts 1..4), and add pts 5..6 to the Lypky-grade rule (post ≤ pre +
   pt 6 ≥ some floor). This matches Kyiv geography — Maidan and
   European Sq are the transition, not the flat core — and is a
   principled fix to the mismodel.

4. Move the transect endpoints so pts 1..6 all land on the strictly
   flat street body (e.g. shrink to (30.5215, 50.4475) → (30.5240,
   50.4510) — Bessarabska to Maidan) and re-run.

**Recommendation:** option 3 (or option 4). Option 1 is right in spirit
but produces a fussy multi-rule table. Option 3 is the cleanest
correction to the geographic model: pts 1..4 are Khreshchatyk street
proper (bound ≤ 3 m); pts 5..8 are the Maidan-and-beyond sequence which
follows the Lypky-grade rule (post ≤ pre, pt 8 ≥ 160). Ridge already
delivers post ≤ pre at pt 5..8 (155.56 ≤ 154.59? — actually no, 155.56 >
154.59, so we'd need the same 0.5 m ASL-slop tolerance discussed in the
"Aside" paragraph above, or a pt-5 bowl-value bound).

Reproduction is fully deterministic: rerun `/tmp/regen-kyiv-terrain.mjs`
followed by `/tmp/kyiv-transect.mjs` against
`.cache/dem/N50E030.hgt.gz` (guard + table output is above). Nothing was
committed; `git status --short` is clean.

**Safe fallback executed:** restored `public/data/kyiv/index.json` from
git (byte-identical, 634 133 B, datum 155.6, SHA
`bef48eec3b59adae1f350817776c2df13781735a3d483a7c2d8a97aaf548890a`).
The `main → tigerteam/T-0115` fast-forward stays (it only brought
`scripts/dem.mjs` + friends into the worktree; nothing in my scope was
touched). Throwaway scripts live in `/tmp` and are not committed.

**Note on the kyiv fetch alias (out of my scope, package.json):**
`npm run fetch-data:kyiv` still carries `--dem-bare 1` (erode). Once
this ticket lands under ridge mode, the alias should be flipped to
`--dem-bare ridge` so a future full refetch reproduces the committed
terrain byte-for-byte. `package.json` isn't in my scope, so I did not
touch it — flagging for the next iteration or a follow-up ticket.

> handoff: blocked

## Answers

## Answers (3)

Ruling on the flat-core failure: **your attribution is accepted — this is
a pass in substance, and the bound gets its final, relief-respecting
form.** The Maidan bowl (pt 4→5) and the European Square rise (pt 5→6)
are real topography the filter MUST keep (the same physics as the Lypky
rule); only pt 2→3's 3.36 m is residual contamination, and ridge reduced
it versus raw.

**Final flat-core gate (replaces "pts 1–6 Δ ≤ 3 m"):** for each adjacent
pair among pts 1–6, `postΔ ≤ max(3, preΔ + 0.5)` — flat where the raw
DEM is flat, and never more than 0.5 m lumpier than the raw relief where
the relief is real. Your measured values pass every pair (0.25→ok,
3.36 ≤ 3.49, 0.78→ok, 4.17 ≤ 5.58, 3.89 ≤ 5.49). Every other bound
stands exactly as measured (all PASS in your table).

**Finish the ticket:** commit the ridge-regenerated `index.json`, update
kyiv `sizeBytes` (632 439) in `src/data/cities.ts`, add the two doc
sentences (say "regenerated in place with `--dem-bare ridge`" — and note
the kyiv alias must read `ridge`, not `1`: if `package.json` on your
branch still says `--dem-bare 1`, flag it in the report and leave it,
package.json is PM-owned and the PM will flip it at accept), run the
full suite, and hand off to review with the final table (including the
new formula's per-pair evaluation) in the report.

On the fast-forward of `tigerteam/T-0115` to main tip: accepted THIS
time because the branch had zero commits (a pure pointer move, no merge
commit — equivalent to a fresh worktree from HEAD). It is not a
precedent; PROTOCOL §6 still forbids merging main into a branch that
carries work.

The datum-raise aside (155.6 → 156.1 via smoothing) is understood and
fine — thank you for flagging it.


> claimed_by: ds-2 (pid 1487920) at 2026-08-31T22:40:21Z
