---
id: T-0089
title: Converter — parts belong to the smallest containing outline (no more "Grand Central Terminal" on a 423 m tower); re-fetch nyc.json
priority: P2
complexity: C2
depends_on: [T-0087]
scope: [scripts/osm-convert.mjs, tests/osm-convert.test.ts, public/data/nyc.json, src/data/cities.ts, tests/landmarks.test.ts, docs/integration.md]
---

## Goal
T-0087 found "Grand Central Terminal" on a 423 m building part: a large
outline containing One Vanderbilt's parts handed its name to the tallest
of them. Contract: `docs/data-format.md` "Building parts" rule 3 as amended
(a part belongs to the SMALLEST outline containing its centroid; a name
transfers only to a part that belongs to that outline and is unnamed; an
outline whose parts all belong to smaller outlines is dropped without
transferring). Re-fetch Manhattan.

## Context
- `scripts/osm-convert.mjs` `applyBuildingParts` (T-0086): compute ring
  areas once; when several outlines contain a part's centroid keep the
  smallest; transfer names per the rule. Keep byte-identical output when
  no parts exist (existing fixtures).
- `tests/osm-convert.test.ts`: `'a part inside two nested outlines belongs to the smaller one and takes its name'`,
  `'an outline whose parts all belong to smaller outlines is dropped without renaming anything'`,
  `'a named part keeps its own name'`.
- Re-fetch: `npm run fetch-data:nyc`; guard 22 MB; report counts and the
  new size; update `sizeBytes` in `src/data/cities.ts` for `nyc`; verify
  with `node -e` that exactly ONE building is named `Grand Central
  Terminal` (report its h) and that every T-0087 landmark name still
  resolves (list them with h). If a landmark name disappears, STOP and
  file a blocked question rather than editing landmarks.ts.
- `tests/landmarks.test.ts`: add `'nyc: exactly one building carries each fixed landmark name'`.
- `docs/integration.md`: one line under Building parts.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/osm-convert.test.ts tests/landmarks.test.ts` exits 0
- [ ] Tests by name: the four cases above; all pre-existing cases unchanged
- [ ] `bash scripts/check.sh` exits 0 (nyc e2e boots still green)
- [ ] `public/data/nyc.json` re-fetched and committed ≤ 22 MB; `sizeBytes` updated; report lists the landmark name check
- [ ] JSDoc on changed functions; docs/integration.md line
- [ ] No files changed outside scope (other data files, `landmarks.ts`, `spawn.ts`: no diff)

## Out of scope
Bridges (T-0088), SF re-fetch, spawn/landmark table edits.

## Worker report

> claimed_by: ds-1 (pid 3900) at 2026-08-29T10:10:13Z

### Status: BLOCKED — see `## Questions` below.

### What I did (committed, safe partial work)

- `scripts/osm-convert.mjs` — rewrote `applyBuildingParts` to implement the amended
  rule 3 exactly as documented in `docs/data-format.md`: each part's HOME outline is its
  SMALLEST (by area) containing outline (`ringArea` computed once per outline); an
  outline that contains ≥1 part centroid is dropped; its `name` transfers only to the
  TALLEST part that belongs to it AND has no name of its own; an outline whose parts all
  belong to smaller outlines is dropped without transferring. Byte-identical when
  `parts` is empty (early return unchanged). JSDoc updated.
- `tests/osm-convert.test.ts` — added the three required cases:
  - `'a part inside two nested outlines belongs to the smaller one and takes its name'`
  - `'an outline whose parts all belong to smaller outlines is dropped without renaming anything'`
  - `'a named part keeps its own name'`
  All 77 osm-convert tests pass.

### Why I did NOT commit the rest (the blocker)

I re-fetched (`npm run fetch-data:nyc`, exit 0) and ran the landmark check. The converter
fix works for the primary bug — One Vanderbilt's 423 m part is no longer named
"Grand Central Terminal" (One Vanderbilt keeps its own name, 427 m) — **but the output
still contains TWO buildings named "Grand Central Terminal"**, so the acceptance check
"exactly ONE building is named Grand Central Terminal" FAILS:

```
id 265947358 h 45.8   (the real surface terminal way, building=train_station)
id 137860916 h 209    (a 50-level unnamed glass tower, building:part)
```

Root cause (traced in the raw OSM): the second one is claimed by the relation
`11171793` "Grand Central Terminal" (`building=train_station`, `layer=-2`,
`location=underground`, `underground=yes`) — the UNDERGROUND terminal platform
multipolygon. Its outer ring is the only outline containing the 209 m tower's centroid
there is no smaller containing outline, so under the documented rule the tower
legitimately "belongs" to it and takes its name. This is the same class of mislabel the
ticket is meant to kill, and it happens regardless of my change: the PRE-EXISTING committed
`nyc.json` (T-0087) already has two "Grand Central Terminal" buildings (45.8 terminal +
423 m One Vanderbilt part). So "exactly one" was never achievable on this OSM snapshot
with the documented rule alone.

Re-fetch summary (fresh current OSM): 41152 buildings / 10482 roads / 187 places /
97 water / 2 rivers / 27424 trees, terrain 287×430 @ 20 m, 9.87 MB (9871 KB).
`sizeBytes` for nyc is therefore NOT updated (I did not commit a failing nyc.json).
`tests/landmarks.test.ts` new case `'nyc: exactly one building carries each fixed
landmark name'` and `docs/integration.md` line are held until the rule is decided.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh tests/osm-convert.test.ts` → `Test Files 1 passed (1)`, `Tests 77 passed (77)`
- `npm run fetch-data:nyc` → exit 0, summary above
- Node checks for GCT count / sizes / containment, old-vs-new comparison

### Commit
- `b2fe6b7` on branch `tigerteam/T-0089` — rule fix + 3 incremental tests only.

### Reviewer should look closely at
- The 209 m tower (`id 137860916`) named "Grand Central Terminal" in the re-fetched data — same class of mislabel the ticket targets.
- Old-vs-new GCT output (both 2 entries) — the fix swaps WHICH second building is mislabeled, it does not reduce the count to 1.

## Questions

**Blocking: the acceptance criterion "exactly ONE building named Grand Central Terminal"
is not satisfiable with the documented rule 3 on the live OSM data.** Both before and
after my (contract-faithful) fix, the nyc dataset contains two OSM entities literally
named "Grand Central Terminal": the surface terminal way `265947358` (h 45.8) and the
underground train_station relation `11171793` (`layer=-2`, `underground=yes`) whose loose
ring is the only containing outline for an unrelated 209 m surface glass tower — so the
rule forces that tower to be labelled GCT. Options I see:

- **Option A (my recommendation):** amend the contract so below-grade building
  outlines/parts are not emitted as part-holders (skip outlines with `layer<0` /
  `location=underground` / `underground=yes`). Then the terminal relation no longer
  claims the 209 m tower (kept unnamed, no outline), and exactly ONE building (the
  45.8 m surface terminal) is named GCT. Cost: a data-format.md amendment + a converter
  filter; affects 14 underground building outlines / 6 parts in the nyc fetch (all
  subway-station footprints — needs a PM check that dropping them from geometry is
  desired, else keep geometry but exclude them from part-claiming).
- **Option B:** accept the contract as-is; the output has 2 GCT buildings. Relax the
  acceptance/landmarks check to "exactly one building that is NOT a building:part" (so
  the 45.8 m terminal is the single named outline) or drop ✓Grand Central Terminal from
  the fixed-name test. Leaves a mislabelled 209 m tower.
- **Option C:** a targeted contract tweak — an outline only transfers its name if it is
  the laser-facing above-grade structure... heuristic; least principled.

Please pick A or B (I recommend A) and tell me whether to keep the 209 m tower unnamed
while still rendering the underground station outlines. I'll then re-fetch, update
`sizeBytes`, add the landmarks test + `docs/integration.md` line, and finish the commit.

> handoff: blocked

## Answers

### PM answer (2026-08-29)

Good trace. **Option A, and stricter:** below-grade structures are not
buildings at all. Contract amended on main — `docs/data-format.md`
"Building parts" rule 3b (do not edit the doc, do not merge main):

- An outline OR part whose tags have `layer` < 0, `location=underground`
  or `underground=yes` is SKIPPED entirely: not emitted, and never a
  part-holder. (They are subway-station footprints that would render as
  boxes on the street; dropping the 14 outlines / 6 parts is desired.)
- Everything else in rule 3 stands (smallest containing outline; name to
  the tallest unnamed part that belongs to it).

Then: re-fetch, expect exactly ONE "Grand Central Terminal" (the 45.8 m
terminal) and the 209 m tower unnamed; update `sizeBytes`; add the
landmarks test and the docs line; add one converter test named verbatim
`'below-grade outlines and parts are skipped and never claim parts'`
(fixture: an underground relation ring containing a surface part → the part
is kept unnamed). Report the landmark-name check as the ticket asks. Hand
off with `> handoff: review` as the last line.


> claimed_by: opus-1 (pid 3900) at 2026-08-29T10:40:26Z
