---
id: T-0072
title: Postcard PNG export — P key + pause-menu SAVE PNG download the current frame with a caption bar
priority: P1
complexity: C2
depends_on: []
scope: [src/export/, src/main.ts, src/hud/hud.ts, tests/postcard.test.ts, e2e/postcard.spec.ts, docs/hud.md]
---

## Goal
One key gives the player an artifact that lives outside the tab: `P` (and a
pause-menu button for touch) downloads the current frame as a PNG with a
28-px caption bar. `docs/architecture.md` §4.15 is the contract — read it
first. This ticket is the PNG half; `recordGif` is T-0073 (do NOT implement
it — leave it out of the interface for now).

## Context
- New module `src/export/postcard.ts` exactly per §4.15: `createPostcard`,
  `afterRender()` hook, `snapPng(download = true)`. The WebGL canvas is not
  preserved between frames, so the copy (`drawImage` onto an offscreen 2d
  canvas) MUST happen inside `afterRender()`, which `main.ts` calls
  immediately after `post.render(scene, camera)` in `frame()` (~line 930).
  `snapPng` sets a pending flag and resolves from the next `afterRender`.
- Caption bar constants (colours, font, layout) live in §4.15. City label:
  upper-cased city label from the registry (`src/data/cities.ts`); the
  meta callback in `main.ts` closes over the loaded city.
- Key: extend the existing `keydown` listener in `main.ts` (~line 859,
  where `R`/`H`/`M` live): `KeyP` without shift → `snapPng()`. Ignore
  repeats (already handled). `Shift+P` is reserved for T-0073 — no-op now.
- Pause menu: add a `SAVE PNG` `menuButton` directly under the `STYLE:` row
  (see `menuButton(...)` uses around `src/main.ts:749–780`): dismiss the
  overlay exactly the way CLICK TO RESUME does, wait two
  `requestAnimationFrame`s, then `snapPng()`.
- Toast `POSTCARD SAVED` (existing `toast.show`).
- Test hook per §4.15: `window.__asciicity.postcard('png')` → Promise<Blob>
  without download ('gif' rejects until T-0073).
- Help line (`src/hud/hud.ts`): insert `P POSTCARD` before `ESC MENU`;
  document the key in `docs/hud.md`.
- Pure + unit-tested in `tests/postcard.test.ts`:
  `postcardFilename('london', new Date(2026, 7, 28, 14, 22, 33))` ===
  `'asciicity-london-20260828-142233.png'` (zero-padded, local time); the
  caption layout helper (given width → bar rect + left/right text
  positions).
- e2e in NEW file `e2e/postcard.spec.ts` (copy the boot/ready helpers from
  `smoke.spec.ts`; never edit smoke.spec.ts):
  1. `/?synthetic=1&cell=3x6`: `__asciicity.postcard('png')` resolves; blob
     bytes start with the 8-byte PNG magic; IHDR width === canvas width and
     height === canvas height + 28.
  2. Pressing `P` fires a `download` event with
     `suggestedFilename()` matching `/^asciicity-\w+-\d{8}-\d{6}\.png$/`.
  3. Touch viewport 390×844 (see the gear-menu e2e in smoke.spec.ts for
     the pattern): tap `#gear` → tap `SAVE PNG` → `download` event fires.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/postcard.test.ts` exits 0
- [ ] Tests by name: filename format (padding + local time), caption
      layout; e2e cases 1–3 above green
- [ ] `bash scripts/check.sh` exits 0
- [ ] JSDoc on every export of `src/export/postcard.ts`; `docs/hud.md`
      documents `P` and the menu button
- [ ] No files changed outside scope

## Out of scope
- GIF recording, `Shift+P` behaviour, gifenc (all T-0073). OG image
  (T-0071). No `preserveDrawingBuffer` change.

## Worker report

> claimed_by: ds-2 (pid 184142) at 2026-08-28T17:02:36Z

**Files changed** (commit `d1ecaea` on `tigerteam/T-0072`):
- `src/export/postcard.ts` (new) — `createPostcard` + `afterRender()` +
  `snapPng()`, plus pure `postcardFilename` / `captionLayout` helpers and
  the caption constants (bar height 28, `#000`/`#333` border, `#ffb000`/
  `#7a7a7a` text). Captures the WebGL canvas in `afterRender()` (same task
  as the render) and appends the caption bar; downloads via temp `<a>` +
  `URL.createObjectURL`.
- `src/main.ts` — imports `createPostcard`; builds the postcard after the
  toast with a meta callback closing over the loaded city (`cityInfo?.label`
  else uppercased URL id); calls `postcard.afterRender()` right after
  `post.render(scene, camera)` in `frame()`; adds `KeyP` (no shift) to the
  existing `keydown` handler → `snapPng(true)`, `Shift+P` no-op; adds a
  `SAVE PNG` `menuButton` directly under the `STYLE:` row that dismisses the
  overlay like CLICK TO RESUME, waits two `requestAnimationFrame`s, then
  `snapPng(true)`; adds the `window.__asciicity.postcard('png')` test hook
  (`'gif'` rejects, reserved for T-0073); inserts `P POSTCARD` before
  `ESC MENU` in the desktop help line.
- `src/hud/hud.ts` — default (and desktop) help line now lists `P POSTCARD`
  before `ESC MENU`.
- `tests/postcard.test.ts` (new) — filename format (zero-padded local time,
  lower-cased city) and caption-layout cases.
- `e2e/postcard.spec.ts` (new) — PNG magic + IHDR dims (canvas + 28), `P`
  key download, touch gear → SAVE PNG download. `smoke.spec.ts` untouched.
- `docs/hud.md` — documents `P`, `Shift+P` reservation, the `SAVE PNG` menu
  button, and the updated help line.

**Commands run / summary:**
- `bash .tigerteam/scripts/run-tests.sh tests/postcard.test.ts` → exit 0,
  `Test Files 1 passed (1), Tests 5 passed (5)`.
- `bash scripts/check.sh` → `typecheck: ok`, `unit: ok`, `build: ok`,
  `e2e: FAIL`. All 3 postcard e2e pass; 12 of 14 smoke e2e pass; the single
  failure is a **pre-existing smoke test** (`smoke.spec.ts:270`
  `?render= aliases…`): it presses `R` from `solarized` and waits for
  `braille`, but `STYLE_ORDER` (styles/index, T-0074 amber) now inserts
  `amber` between them so `R` yields `amber`. Reproduced deterministically in
  isolation (`npx playwright test e2e/smoke.spec.ts -g "render= aliases"` →
  1 failed). Out of T-0072 scope — `smoke.spec.ts` must not be edited; the
  amber change is elsewhere in this wave.

**Doc changes**: `docs/hud.md` — added a "Postcard export (T-0072)"
section and updated the help-line description.

**Commit**: `d1ecaea` (branch `tigerteam/T-0072`).

**For the reviewer**: the check.sh e2e gate does NOT exit 0 purely due to the
pre-existing `smoke.spec.ts:270` amber-vs-braille mismatch noted above; every
postcard unit + e2e criterion is green. Also note the §4.15 contract says the
canvas is "not preserved between frames", while scene.ts still ships
`preserveDrawingBuffer: true` — the afterRender capture is safe either way,
so I followed the ticket and did not change that flag.

> handoff: review

## Rework (attempt 2)

Close — module design, wiring, menu/touch path and the e2e are all right.
Two fixes before accept (§4.15 on main was amended today to match; read
the updated section — your worktree's copy is stale):

1. **Caption bar paints over the TOP of the frame.** In `afterRender()`
   you `drawImage(canvas, 0, 0, w, h)` then `paintCaption(ctx, …)`, and
   `paintCaption` fills `(0, 0, width, 28)` — no translate. Result: the
   bar covers the frame's top 28 px and rows `h…h+28` of the output stay
   empty. The bar must sit BELOW the frame: `ctx.save();
   ctx.translate(0, h); paintCaption(…); ctx.restore();` (or pass a y
   offset). Add e2e evidence so this cannot regress: in the existing
   PNG test, decode the blob (createImageBitmap → offscreen canvas →
   getImageData) and assert the bottom bar contains amber caption text —
   ≥ 50 pixels in rows `[height−28, height)` with `r > 150 && g > 90 &&
   b < 100`.

2. **Filename must use the city id, not the label.** `postcardFilename`
   currently gets `meta().cityLabel` — for San Francisco (boarded, lands
   soon) that yields `asciicity-san francisco-….png`. Per amended §4.15:
   `PostcardMeta` is now `{ cityId: string; cityLabel: string }`;
   filename from `cityId`, caption text from `cityLabel`. In `main.ts`
   the meta callback closes over the loaded city id (`cityId ??
   'synthetic'`); unit-test the split (label `'SAN FRANCISCO'`, id
   `'sf'` → `asciicity-sf-….png`, caption `ASCIICITY · SAN FRANCISCO`).

While you're in there (no new criterion): show the `POSTCARD SAVED`
toast only when at least one waiter requested a download — the silent
test-hook capture should stay silent.

Done looks like: both fixes in, the new e2e/unit assertions green,
`run-tests.sh tests/postcard.test.ts` + your three e2e cases green.
`check.sh`'s smoke `?render= aliases` failure remains known/out-of-scope
(T-0074 fixes it) — note its status in the report as before.


> claimed_by: grok-1 (pid 184142) at 2026-08-28T17:20:34Z

## Worker report (attempt 2)

**Files changed** (commit `02a1da6` on `tigerteam/T-0072`):
- `src/export/postcard.ts` — `PostcardMeta` is now `{ cityId, cityLabel }`;
  `afterRender` translates `(0, h)` before `paintCaption` so the 28-px bar
  sits below the frame; filename uses `cityId`; `POSTCARD SAVED` toasts only
  when a waiter asked for a download; exported `captionLeft` helper.
- `src/main.ts` — meta callback closes over `cityId ?? 'synthetic'` plus the
  registry label (upper-cased id for synthetic).
- `tests/postcard.test.ts` — id-vs-label split: `'sf'` →
  `asciicity-sf-20260828-142233.png`, caption `ASCIICITY · SAN FRANCISCO`.
- `e2e/postcard.spec.ts` — existing PNG test now decodes the blob and asserts
  ≥ 50 amber pixels in rows `[height−28, height)`.
- `docs/hud.md` — documents bar-below-frame, filename from city id, caption
  from label, silent test-hook toast.

**Commands run / summary:**
- `bash .tigerteam/scripts/run-tests.sh tests/postcard.test.ts` → exit 0,
  `Test Files 1 passed (1), Tests 7 passed (7)`.
- `bash scripts/check.sh` → `typecheck: ok`, `unit: ok` (567 tests),
  `build: ok`, `e2e: FAIL`. All 3 postcard e2e pass (PNG magic + IHDR +
  amber-bar ≥ 50, `P` download, gear → SAVE PNG). 13 of 14 smoke e2e pass;
  the single failure is the known pre-existing
  `smoke.spec.ts:270` `?render= aliases…` (presses `R` from `solarized`
  expecting `braille`; `STYLE_ORDER` now inserts `amber` — T-0074). Out of
  T-0072 scope; `smoke.spec.ts` was not edited.

**Doc changes**: `docs/hud.md` — caption-below-frame, cityId vs cityLabel,
silent test-hook toast.

**Commit**: `02a1da6` (branch `tigerteam/T-0072`). Parent attempt-1 commit
`d1ecaea` is still on the branch.

**For the reviewer**: both rework items are in. Caption paint is
`drawImage` then `ctx.translate(0, h)` then `paintCaption` (bar-local
layout unchanged). Filename never sees the label. Toast is skipped for
`snapPng(false)` / `__asciicity.postcard('png')`. Worktree
`docs/architecture.md` §4.15 is still the stale copy (PM-owned, not in
scope); implementation matches the amended contract on main.

> handoff: review
