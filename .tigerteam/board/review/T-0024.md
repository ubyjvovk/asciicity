---
id: T-0024
title: Render water as flat blue polygons and block walking into it
priority: P1
complexity: C2
depends_on: [T-0023, T-0022]
scope: [src/world/water.ts, tests/water.test.ts, src/main.ts, docs/world.md, docs/integration.md]
---

## Goal
Draw `city.water` rings as a flat dark-blue surface just above the ground and
keep the player on land.

## Context
- Contract: `CityData.water?: Vec2[][]` (`src/data/types.ts`). Use the
  PM-owned `MeshBuilder`/`toGeometry` (`src/world/mesh.ts`) and
  `THREE.ShapeUtils.triangulateShape` exactly as roofs do in
  `src/world/buildings.ts` (read it: winding normalisation, upward winding).
- `src/world/water.ts` exports:
  `buildWaterMesh(rings: Vec2[][]): MeshData` (pure; y = `0.02`, normal
  `(0,1,0)`, uv `(0,0)`, colour `0x163a6b` linear via `THREE.Color`, one
  group 0; rings with < 3 points or |area| < 1 skipped) and
  `makeWaterObject(rings): THREE.Mesh` (`MeshBasicMaterial({ vertexColors: true })`).
- `main.ts`: when `city.water?.length`, add the water object to the scene and
  include the rings in collision as fake footprints:
  `new CollisionGrid([...city.buildings, ...city.water.map((poly, i) => ({ id: -1 - i, h: 1, poly }))])`.
  Keep everything else as is.
- Docs: `docs/world.md` (water section), `docs/integration.md` (startup
  sequence mentions water + collision).
- Verify visually with the throwaway Playwright recipe from T-0010 using
  `?at=tower` (looks south towards the river) and describe it.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/water.test.ts` exits 0
- [ ] `tests/water.test.ts` covers: a 10×10 square ring → 2 triangles (6 vertices) at y = 0.02 with upward winding and normal (0,1,0); a clockwise ring gives the same vertex count as its reverse; a degenerate ring is skipped; colour equals `new THREE.Color(0x163a6b)`; a single group `{start:0,count:N,materialIndex:0}`
- [ ] `main.ts` wiring as specified (review); `bash scripts/check.sh` exits 0 (incl. `e2e: ok`)
- [ ] Every new export has a JSDoc line; both docs updated
- [ ] No files changed outside scope

## Out of scope
- Animated water, reflections, bridges, changing the data pipeline.

## Worker report

> claimed_by: grok-1 (pid 3364115) at 2026-08-24T00:01:54Z

### Files changed
- `src/world/water.ts` — `buildWaterMesh` triangulates water rings at y=0.02 (roof-style winding + `ShapeUtils.triangulateShape`, skip <3 pts / |area|<1) with linear `0x163a6b` and one group 0; `makeWaterObject` wraps it in `MeshBasicMaterial({ vertexColors: true })`.
- `tests/water.test.ts` — five named cases from the ticket (10×10 square, clockwise vs reverse, degenerate skip, colour, single group).
- `src/main.ts` — when `city.water?.length`, `scene.add(makeWaterObject(city.water))` and `CollisionGrid` includes fake footprints `{ id: -1 - i, h: 1, poly }`.
- `docs/world.md` — new Water section (normalise, skip, triangulate, colour, material, collision lives in `main.ts`).
- `docs/integration.md` — startup sequence step 4/5 now mention water mesh + fake-footprint collision; layer recipe lists water.

### Commands run
`bash .tigerteam/scripts/run-tests.sh tests/water.test.ts` — exit 0
```
 ✓ tests/water.test.ts (5 tests) 6ms

 Test Files  1 passed (1)
      Tests  5 passed (5)
   Start at  00:07:29
   Duration  170ms
```
log: `.tigerteam/logs/tests/20260824-000727-153.log`

`bash scripts/check.sh` — exit 0
```
typecheck: ok
unit: ok
build: ok
e2e: ok
log: .tigerteam/logs/tests/check-20260824-000743-250.log
```

Throwaway Playwright (not committed; T-0010 recipe + `page.mouse.move(640,360)` then click) against `npx vite --port 5173` with swiftshader/no-sandbox args, URL `/?at=tower&cell=3x6&crt=0`. `cell=3x6` so SwiftShader does not blank the lower half. Screenshot `/tmp/t0024-shots/tower.png` (not committed).

### Visual (`?at=tower`)
Spawned at Tower Hill (`x≈879.83, z≈420.18, yaw=π`, HUD `BEARING 180 DEG / SOUTH`, `ZONE TOWER HILL`, `LANDMARK Bowyer Tower`). Looking south: orange/red ASCII façades (Tower of London) across the mid-ground; the lower ~40% of the frame is a flat dark-blue/cyan glyph field — the Thames — sitting on the green ground grid, with a few green road ribbons still crossing it (bridges are out of scope). Sprint-south from `z=470` (already on the north bank) did not increase `z` — water fake footprints block walking onto the river.

### Docs
- `docs/world.md` — Water section as specified.
- `docs/integration.md` — startup sequence water mesh + `CollisionGrid` fake footprints; “adding a new world layer” now lists water.
- JSDoc on `buildWaterMesh` and `makeWaterObject`.

### Commit & branch
- Branch: `tigerteam/T-0024`
- Commit: `92ab20768d43f06e68ee4517b62aba33fc7322c8`

### Reviewer notes
- `docs/architecture.md` §7 still says “the whole city is three meshes”; PM-owned, not touched. Water is a fourth draw call when present.
- `docs/integration.md` spawn paragraph (step 6) still describes the old walk-+x-from-origin spawn; that predates T-0022 and was left alone.
- Green road ribbons still draw across the Thames (roads are not clipped to land). Out of scope.
- UVs are `(0,0)` as specified (not asserted as a named test case).

> handoff: review

