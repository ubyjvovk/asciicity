---
id: T-0109
title: Widen the bare-earth filter — 9x9 erode window, double 3x3 smooth
priority: P1
complexity: C1
depends_on: []
scope: [scripts/dem.mjs, scripts/dem.d.ts, tests/dem.test.ts, docs/fetch-osm.md]
---

## Goal
T-0108's offline drive proved the T-0107 filter under-powered for dense
Shibuya: raw SRTM there has no sample below ~24 m ASL inside any 100 m
window, so the 5×5 erode left the Scramble at 39.6 m with 5.6 m
node-to-node steps. Per the amended contract: widen the erode window to
9×9 and run the 3×3 mean smooth TWICE.

## Context
- **Read first**: data-format.md §Terrain step 3b (amended for this
  ticket) and T-0108's blocked-report numbers in
  `.tigerteam/board/todo/T-0108.md` (raw grid + failed transects).
- `scripts/dem.mjs`: `erode` window radius 2 → 4 (9×9, still
  second-smallest, same border clipping and <2-values fallback);
  `smooth` applied twice wherever the bare-earth path runs (either a
  `passes` arg or two calls at the call site — pick one, document it).
  `scripts/dem.d.ts` updated if a signature changes.
- `tests/dem.test.ts`: adjust the wave-13 tests for the new window —
  plateau/valley preservation cases need features wider than 9×9 (use
  13×13+); spike-removal cases now also cover a 4×4 spike cluster
  (second-smallest of 81 floors it); the default-path byte-identical
  regression and no-mutation/determinism tests must still pass unchanged.
- Expected effect at the Scramble (from T-0108's raw grid): local
  second-smallest within 180 m ≈ 24–25 m ASL; after double smooth the
  crossing lands ≈ 25–28 m and every adjacent-node delta ≤ 3 m. T-0108
  re-verifies against real data; this ticket needs synthetic tests only.
- No dataset changes here — T-0108 (already re-specced) does the Tokyo
  regeneration after this lands.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/dem.test.ts` exits 0 and
      the full `bash .tigerteam/scripts/run-tests.sh` stays green.
- [ ] Named tests updated/added: 4×4 spike cluster removed; plateau ≥
      13×13 preserved (state tolerance); lone low outlier still cannot
      crater (second-smallest of 9×9); double-smooth output has max
      adjacent-node delta ≤ the single-smooth output on a synthetic step
      edge; filter-off path byte-identical (regression untouched).
- [ ] `docs/fetch-osm.md` `--dem-bare` paragraph reflects 9×9 + double
      smooth.
- [ ] No files changed outside scope.

## Out of scope
`public/data/` (T-0108), `fetch-osm.mjs` flag plumbing (unchanged),
percentile/ladder redesigns (this is the locked minimal widening).

## Worker report

> claimed_by: ds-1 (pid 912208) at 2026-08-31T10:45:30Z

**Files changed** (all in scope):
- `scripts/dem.mjs` — `erode` window radius 2 → 4 (9×9 second-smallest, same
  border clipping and <2-values fallback); the bare-earth path in
  `buildTerrain` now applies `smooth` TWICE (`smooth(smooth(eroded))` — picked
  the two-calls-at-the-call-site option, documented in the `smooth`/`erode`
  JSDoc). `buildTerrain` doc updated (erode → smooth → smooth).
- `scripts/dem.d.ts` — no signature changed (both `erode`/`smooth` keep
  `(heights, cols, rows)`); added a short doc comment noting 9×9 + double
  smooth for TS consumers.
- `tests/dem.test.ts` — updated wave-13 tests: 2×2 spike cluster test
  replaced by a **4×4 spike cluster** test (second-smallest of the 9×9 window
  floors it); plateau and valley preservation cases widened to 13×13 features
  in 19×19 grids (both double-smoothed, centre = 100/−100 tolerance 0); lone
  −30 outlier still cannot crater (comment/name updated to 9×9); border-clip
  finite test now runs the double-smooth pipeline; added a new test proving
  double-smooth max adjacent-node delta ≤ (in fact <) single-smooth on a
  synthetic step edge. The filter-off byte-identical regression,
  no-mutation/determinism tests, and all other buildTerrain tests are
  unchanged and green.
- `docs/fetch-osm.md` — the `--dem-bare` paragraph now says **9×9** erode
  window and the 3×3 mean smooth applied **twice** (with the 180 m window and
  the ≤ 3 m Shibuya step rationale).

**Commands run** (via the wrapper only):
```
bash .tigerteam/scripts/run-tests.sh tests/dem.test.ts
  Test Files  1 passed (1)
      Tests  33 passed (33)
  exit: 0

bash .tigerteam/scripts/run-tests.sh
  Test Files  49 passed (49)
      Tests  833 passed (833)
  exit: 0
```

**Doc changes**: `docs/fetch-osm.md` `--dem-bare` paragraph updated as above
(docs/fetch-osm.md); `scripts/dem.d.ts` doc comment (compiler-facing, ts
consumers).

**Commit**: `9847339` on branch `tigerteam/T-0109`.

**Acceptance walkthrough**:
1. run-tests.sh dem green (33) + full run green (833/49 files) — PASS.
2. Named tests present & green: 4×4 spike cluster removed; plateau ≥ 13×13
   preserved (tolerance 0); lone low outlier cannot crater (second-smallest
   of 9×9); double-smooth max adjacent delta ≤ single on a synthetic step
   edge (asserted <, measured 22.2 vs 33.3); filter-off byte-identical
   regression unchanged — PASS.
3. docs/fetch-osm.md `--dem-bare` paragraph reflects 9×9 + double smooth —
   PASS.
4. Changed file set = exactly the 4 scope files — PASS (git status clean
   apart from scope).

**Notes / not run**: synthetic tests only per the ticket (T-0108 re-verifies
against real Tokyo). I did not run `scripts/check.sh` — no build/config or
`src/main.ts` files were touched (scripts/dem.mjs is pure node, tests, and
one doc); the `.d.ts` change is comment-only so `tsc --noEmit` is unaffected.
No `## Questions` needed.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 7m 3s | 1.31M (1.23M) | 19.75k | $0.03 | engine | review |

total: 1 attempt · 7m 3s · 1.31M in (1.23M cached) / 19.75k out · $0.03 (accepted 2026-08-31T10:57:59Z)
