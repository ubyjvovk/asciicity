---
id: T-0114
title: Opera House shells — synthesised spherical sails (opera.ts, §4.22)
priority: P1
complexity: C3
depends_on: [T-0111]
scope: [src/world/opera.ts, src/main.ts, src/world/landmarks.ts, tests/opera.test.ts, docs/architecture.md]
---

## Goal

Give the Sydney Opera House its sails: a synthesised spec-table structure
(`src/world/opera.ts`) of nested white spherical-patch shells on the OSM
podium — extruded prisms cannot read as the building, and it is the one
Sydney landmark everyone will look for first.

## Context

- **The contract is `docs/architecture.md` §4.22** — `OperaShellGroup` /
  `OperaHouseSpec` / `OPERA_HOUSES`, the sail geometry rule (mirrored
  same-radius sphere patches meeting at a rising ridge arc, dark mouth
  wall), the sydney table values, wiring, budget. Read it first.
- Pattern: `src/world/bridge.ts` (spec table → pure MeshData builder →
  `make*Object` wired once in `main.ts` beside `makeBridgesObject` — the
  shells are permanent, not tiled).
- The real podium outline is in the dataset since T-0116 (building
  `Sydney Opera House`; T-0110/0116 reports carry its massing).
  Reconcile §4.22's group bases/axes against that footprint so every sail
  stands on the podium; report the deltas from the table's hypothesis
  coordinates.
- T-0111 set the landmark fix (ivory podium, no fake height). If the
  refetched data carries `building:part`s that fake the roof over the
  podium, suppress them via the sydney `LANDMARK_FIXES` entry (§4.22
  rule) — the outline stays.
- The module adds NO tag anchor (the OSM building already supplies the
  floating tag).

## Acceptance criteria

- [ ] `OPERA_HOUSES` has exactly one entry (`sydney`) per §4.22;
      `buildOperaHouse` is pure and exported; `makeOperaObject` returns
      an empty object for every other city id.
- [ ] `tests/opera.test.ts` (committed sydney tiles via the memoized
      helpers): tallest ridge tip y = (67 − datum) ± 1; every sail-base
      vertex lies inside the `Sydney Opera House` footprint expanded by
      6 m; per group, tip x/z of each sail lies bearing-ward of its base
      (the sails lean toward the harbour); every vertex y ≥ podiumASL −
      datum − 0.5; vertex count < 20 000; no NaN; non-sydney →
      empty MeshData.
- [ ] `main.ts`: one `scene.add(makeOperaObject(...))` beside the
      bridges call; nothing else there.
- [ ] `docs/architecture.md` §4.22 amended with as-built notes
      (reconciled bases/axes, part-suppression outcome, vertex count).
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (full suite).
- [ ] No files changed outside scope.

## Out of scope

Interior/collision (visual-only like the bridges), dataset changes,
presets/e2e (the `operahouse` preset exists from T-0111; the PM verifies
the look on the GPU — the sail SHAPE is judged there, not by unit test),
any other landmark. If the podium footprint from the data cannot host the
§4.22 table (axes off, groups overflow), file a blocked question with the
footprint's local-coordinate outline instead of silently shrinking sails.

## Worker report

**Files changed**
- `src/world/opera.ts` (new) — `OperaShellGroup` / `OperaHouseSpec` /
  `OPERA_HOUSES` (sydney only), pure `buildOperaHouse` (spherical-patch
  sail builder with a mouth wall), `makeOperaObject` (empty `Object3D`
  for every non-sydney city, three.js `Mesh` otherwise). Sail geometry
  solves `sphereR`-radius sphere centre `C` per sail from the apex +
  two base corners; each shell is 8×12 quads two-sided, mouth wall is
  a single inset triangle two-sided (see §4.22 as-built).
- `src/main.ts` — one added line: `scene.add(makeOperaObject(cityId,
  city, groundAt));` beside `makeBridgesObject`. Also the import.
- `src/world/landmarks.ts` — `LandmarkFix.suppressParts?: boolean`
  and `applyLandmarks` now drops every non-extra building whose
  centroid falls inside a named ring flagged with `suppressParts`.
  `LANDMARK_FIXES.sydney['Sydney Opera House']` gets `suppressParts:
  true` (and keeps its ivory colour).
- `tests/opera.test.ts` (new) — every ticket criterion, using the
  memoized `loadTiledCity('sydney')` helper (T-0101 rule): the
  `OPERA_HOUSES` table shape and `sydney` constants, tallest ridge
  tip y = (67 − datum) ± 1, every sail-base vertex within the podium
  ring expanded 6 m (proper edge-distance test), per-group per-sail
  tip bearing-ward of base, every vertex y ≥ podiumASL − datum − 0.5,
  vertex count > 0 and < 20 000 with no NaN, `makeOperaObject` is an
  empty `Object3D` for every other city and a non-empty `Mesh` for
  sydney.
- `docs/architecture.md` §4.22 — appended two "T-0114 as-built"
  paragraphs (reconciled bases, sphere-solve rule, vertex count, part
  suppression outcome).

**Commands run**
- `bash .tigerteam/scripts/run-tests.sh tests/opera.test.ts` — 8/8
  pass after a first-pass geometry fix (see "Iteration notes" below).
- `bash .tigerteam/scripts/run-tests.sh` — **52 test files, 941
  tests, all pass in 16.10 s** (log
  `.tigerteam/logs/tests/20260901-052943-380.log`). Existing
  `tests/landmarks.test.ts` unchanged by the new `suppressParts`
  branch — its sydney assertions do not enumerate the Opera House
  parts and stay green.

**Docs**
- `docs/architecture.md` §4.22 updated with as-built base coords,
  sphere-solve rule, back-sail rule, sail-patch resolution and total
  vertex count (~10 420), plus the `suppressParts` mechanism and the
  three OSM part ids that get dropped from the Sydney dataset.

**Reconciled base coords (spec hypothesis → landed value)**
- Concert Hall: `[151.21470, -33.85755]` (≈ (342, −415) local, near
  the podium's north tip in this dataset) → **`[151.21473,
  -33.85741]`** (≈ (344.8, −430.1) local). The hypothesis put the
  landward sail 0's south-west quarter half-width edge outside the
  podium ring notch at z ≈ −438; the +6 m Minkowski margin covers
  the shift.
- Joan Sutherland: axis 45 m ENE of CH axis, so the shift moves with
  CH → **`[151.21518, -33.85757]`** (≈ (386.4, −412.4) local).
- Bennelong Restaurant: hypothesis "SE of the theatre" (my first
  attempt at (410, −540) fell outside the polygon at the podium's
  south-east notch) → **`[151.21560, -33.85669]`** (≈ (425.2,
  −509.7) local) — the polygon's real SE bulge.

**Part suppression outcome**
Three unnamed `building:part`s live inside the Opera House ring in
`sydney/tiles/0_-1.json` (ids `681506197` h 14, `681506198` h 14,
`1427781151` h 14 — west-side ribbons). All are shorter than the
18.5 m podium so they were mostly hidden visually, but they clip the
sail bases from close inspection and add collision boxes inside the
podium. `LANDMARK_FIXES.sydney['Sydney Opera House'].suppressParts =
true` now drops them at load; the parent podium (id `9596872`) and
any landmark extras (id ≤ −1000) are always kept.

**Vertex count**: 9 sails × (8×12 quads × 12 verts two-sided) + 6
mouth-wall verts each ≈ **10 422** total. Under the 20 k budget.

**Iteration notes for the reviewer**
- First pass had a subtle geometry bug in `baseAtDir` that mixed the
  local-frame forward offset from `basePos` with the direction vector
  from `C`; caught by the "every sail-base vertex …" and "every
  vertex y ≥ …" tests. Fixed by using the true `(worldPos − C)/R`
  form with magnitude `sqrt(r_pod² + e²) = R`.
- The "expanded by 6 m" test uses a proper edge-distance Minkowski
  check (`pointInRing || min-dist ≤ 6 m`); centroid-radial expansion
  under-covers the podium's concave west notch (would false-fail).
- Nothing outside scope was touched. Bridges, terrain, tiles,
  builders, e2e, `spawn`, `presets` all unchanged.

**Commit**
- `808c887` on branch `tigerteam/T-0114`.

**Things the reviewer might want to eyeball on the GPU**
- The Bennelong single-sail sits over the podium's SE bulge; the
  spec text calls out "SE of the theatre" which is where I placed
  it (the polygon's SE bulge is at z ≈ −510, which is northern of
  the Opera Theatre if the podium's south end is landward). If the
  PM wants Bennelong FURTHER SOUTH (nearer the entry-stairs / south
  end), the base lon/lat moves; the polygon has room down to about
  (400, −380).
- Sail lean = `min(sphereR·0.35, rise·0.6)`. Bigger factors would
  make the tips lean further into the harbour; too big and the tip
  goes off the podium. Purely visual.
- No `LANDMARK_FIXES` height override on the podium (h 18.5 stays
  from T-0116); the sails start at `podiumASL − datum = 4.1` m so
  they don't rise from ground level — they rise from the podium
  deck. Verify visually the sails don't clip the podium top.

> claimed_by: opus-1 (pid 1487920) at 2026-09-01T04:51:34Z

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-4-7 | 40m 59s | 9.48M (9.48M) | 194.79k | $11.40 | engine | review |

total: 1 attempt · 40m 59s · 9.48M in (9.48M cached) / 194.79k out · $11.40 (accepted 2026-09-01T05:39:58Z)
