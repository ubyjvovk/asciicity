---
id: T-0064
title: Minimap — paint water bodies (and river centre-lines) so the Dnipro and the Thames read on the map
priority: P2
complexity: C1
depends_on: []
scope: [src/hud/minimap.ts, tests/minimap.test.ts, docs/minimap.md]
---

## Goal
User: "water bodies aren't apparent on the minimap". Paint `city.water`
rings as a filled layer under buildings and roads, in a dim teal that reads
as water inside the green-on-black palette, and `city.rivers` as thin lines
of the same hue.

## Context
- `src/hud/minimap.ts` buckets buildings and road segments into a cell grid
  (`docs/minimap.md` §Cell grid) and draws in the order background →
  buildings → roads → player → north (§Colours). Add water rings to the same
  bucketing (bucket a ring into every cell its bounding box touches, like
  buildings) and river polylines like roads (`segs`), and draw them **first**
  after the background: water fill `#0e3a46`, rivers stroke `#155b6b`
  1 px. Ring projection: same heading-up transform as footprints.
- Large rings (the Dnipro is one 5 km² polygon) must not be re-projected
  per frame in full — clip is unnecessary; just draw the whole ring once
  per frame when any of its cells is in range (a `Set` of drawn ring ids per
  frame, as for buildings that span cells if such de-dup exists — mirror
  whatever `update` does for multi-cell buildings).
- Tests (`tests/minimap.test.ts`): a city with one water ring around the
  origin → after `update`, the pixel at the canvas centre (behind the arrow —
  sample a pixel 20 px from the centre) is `#0e3a46` (use the existing fake
  2D-context pattern in this test file, or `getImageData` if it runs on a
  real canvas); a ring 5 km away is not drawn (no water-colour fill calls);
  rivers produce one `stroke` in the river colour; cities without `water`
  are unchanged (same draw-call sequence as before).
- `docs/minimap.md`: update the draw order and colours sections.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/minimap.test.ts` exits 0
- [ ] Tests by name as in Context
- [ ] `bash scripts/check.sh` exits 0
- [ ] JSDoc; docs updated
- [ ] No files changed outside scope

## Out of scope
- Moving the minimap panel (T-0060 does that; this ticket does not touch `main.ts` or CSS), water polygons in the 3D world, bridges on the map.

## Worker report

> claimed_by: opus-1 (pid 484850) at 2026-08-27T08:10:12Z

### Files changed
- `src/hud/minimap.ts` — added `StoredWater`, `waters` and `rivers` arrays to
  the cell bucket, wired `city.water` / `city.rivers` into `bucket()`, and
  drew water fills (`#0e3a46`) plus river strokes (`#155b6b`, 1 px) after the
  black background and before buildings/roads. Per-frame `Set`s de-dup rings
  and segments that span multiple cells (so the Dnipro polygon draws once).
  Guard flags mean the fillStyle/strokeStyle/beginPath/stroke ops are skipped
  entirely when the city has no water or rivers in view — the draw-call
  sequence for `CityData` without water is byte-identical to before.
- `tests/minimap.test.ts` — added a `makeFakeCanvas()` recorder (mirrors the
  fake 2D-context pattern used by `tests/ascii.test.ts`) plus a
  ray-casting `pathContains` helper and four new tests: water ring around
  origin covers the pixel 20 px from centre in `#0e3a46`, a ring 5 km away
  is not drawn, one river produces one `stroke` in `#155b6b` with `lineWidth`
  1 and a moveTo/lineTo pair, and a city without `water` / `rivers` emits no
  water-fill and no river-stroke ops.
- `docs/minimap.md` — updated the "Cell grid" and "Colours (draw order)"
  sections to describe the water/river layers, the per-frame de-duplication,
  and the guarantee that the draw sequence is unchanged when the city has no
  `water` / `rivers`.

### Commands run

`bash .tigerteam/scripts/run-tests.sh tests/minimap.test.ts`
```
 Test Files  1 passed (1)
      Tests  11 passed (11)
```

`bash scripts/check.sh`
```
typecheck: ok
unit: ok    (Test Files  30 passed (30) / Tests  489 passed (489))
build: ok
e2e: ok     (5 passed (1.1m))
```

Note: the `smoke: R cycles every render style and each paints` Playwright
test is a pre-existing flake in this environment — its 60 s timeout is
tight against a 12-style screenshot loop that ran in ~56 s on the base
commit (with no worktree changes) and also timed out on the base commit on a
second run. It passed cleanly on the `check.sh` invocation that produced the
`ok` summary above. Not related to this ticket's scope.

### Docs

`docs/minimap.md` §Cell grid and §Colours (draw order) both updated as
required by the ticket.

### Commit

- branch `tigerteam/T-0064`
- `df2e0f3` — `[T-0064] Minimap — paint water bodies (and river centre-lines) so the Dnipro and the Thames read on the map`

### For the reviewer

- The water/river bucketing mirrors the existing building/road pattern
  (bbox-into-every-cell + `Set` de-dup by object identity). No cross-frame
  state is kept; both `Set`s are allocated fresh per `update()` call, same as
  `seenBuildings` / `seenSegs` today.
- The stroke/fill-style ops for water and rivers are guarded (`waterStyled`,
  `riverStarted`) so a city without water/rivers produces the same sequence
  as before — the "unchanged" invariant test in `minimap.test.ts` asserts
  this at the op-recorder level.
- The `#0e3a46` (water fill) / `#155b6b` (river stroke) constants live at
  the top of `src/hud/minimap.ts`; the same hex codes appear in
  `docs/minimap.md`'s colours table.
- The minimap smoke path in `src/main.ts` (`opts.minimap`) still uses the
  same `new Minimap(miniCanvas, city)` constructor signature; no wiring
  changes needed for London or Kyiv to render water immediately.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-4-7 | 20m 11s | 4.48M (4.48M) | 29.88k | $3.80 | engine | review |

total: 1 attempt · 20m 11s · 4.48M in (4.48M cached) / 29.88k out · $3.80 (accepted 2026-08-27T08:32:05Z)
