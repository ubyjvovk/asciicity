---
id: T-0101
title: Deflake dataset-heavy unit tests on slow runners (memoize tiled-city test helpers, raise data-test timeouts)
priority: P0
complexity: C1
depends_on: []
scope: [tests/sfCity.ts, tests/tiledCity.ts, tests/landmarks.test.ts, tests/spawn.test.ts, tests/sf-data.test.ts]
---

## Goal
CI (GitHub Actions `pages` run for 11d845d) failed on
`tests/landmarks.test.ts > sf fixes still target their names after the nyc
table lands` with `Test timed out in 5000ms` (it ran 9 886 ms on the shared
runner; the whole file took 11 s). Cause: since the wave-11 migration the
test helpers reconstruct a city by fs-reading `index.json` + all tile files
(SF = 69 files ≈ 15 MB) — and they do it AGAIN for every test that calls
them. Fix the waste and give data-driven tests slack; the deploy is blocked
on this.

## Context
- `tests/sfCity.ts` and `tests/tiledCity.ts` are the helpers (T-0095/96).
  Add module-level memoization: each city's parsed index / globals / full
  reconstruction is computed ONCE per process and returned by reference
  afterwards (tests only read). Vitest workers are per-file, so this
  collapses repeat loads within each test file.
- Belt and braces: in the dataset-heavy test FILES listed in scope, set a
  per-file `testTimeout` of 30 000 (e.g. vitest's
  `describe`/`it` timeout arg or a top-of-file
  `vi.setConfig({ testTimeout: 30_000 })` — pick the idiom already used in
  the repo if one exists) so a cold-cache first test on a slow runner still
  passes. Do NOT touch the global vitest config.
- CI log for reference: run `pages` on sha `11d845d`, job `build`, step
  `Run npm test`.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/landmarks.test.ts tests/spawn.test.ts tests/sf-data.test.ts` exits 0,
      and the full `bash .tigerteam/scripts/run-tests.sh` stays green.
- [ ] Memoization is observable: a named test (in the helper's own small
      test block or an existing file) asserts two calls to the loader
      return the SAME object reference.
- [ ] `tests/landmarks.test.ts` full-file wall time drops vs before on the
      same machine (report both numbers).
- [ ] No behavioural change to any assertion; no `src/` or config change.
- [ ] No files changed outside scope.

## Out of scope
`vitest.config.ts` / global timeouts, `src/`, datasets, CI workflow files
(PM-owned), converting helpers to async.

## Worker report

> claimed_by: ds-1 (pid 912208) at 2026-08-31T07:07:07Z

**Files changed (all in scope):**
- `tests/sfCity.ts` — module-level memoization of `loadSfIndex` / `loadSfGlobals` /
  `loadSfCity`: each is parsed once per process and returned by reference after;
  callers must treat them as read-only. JSDoc updated.
- `tests/tiledCity.ts` — same memoization, per city id (Map caches), for
  `loadTiledIndex` / `loadTiledGlobals` / `loadTiledCity`. JSDoc updated.
- `tests/landmarks.test.ts` — `vi.setConfig({ testTimeout: 30_000 })` per-file
  timeout; `tokyoTowersCity()` now copies the memoized globals' `buildings` /
  `roads` arrays before appending tiles so it never mutates the shared object.
- `tests/spawn.test.ts` — `vi.setConfig({ testTimeout: 30_000 })` per-file
  timeout; the two Tokyo blocks (3×3-tile preset grid, buildings-only
  sightline) now copy the memoized globals' arrays before appending tiles.
- `tests/sf-data.test.ts` — per-file 30 s timeout + two named memoization
  tests: "sf loaders return the same object reference across calls" and
  "tiled loaders return the same object reference across calls per city"
  (acceptance criterion 2).

**Commands run / results (via the run-tests wrapper only):**
- `bash .tigerteam/scripts/run-tests.sh tests/landmarks.test.ts tests/spawn.test.ts tests/sf-data.test.ts` → exit 0, `Test Files 3 passed (3)`, `Tests 118 passed (118)`, `Duration 5.08s` (116 pre-change + 2 new).
- Full `bash .tigerteam/scripts/run-tests.sh` → exit 0, `Test Files 47 passed (47)`, `Tests 787 passed (787)`, `Duration 12.28s`.
- `npx tsc --noEmit` → exit 0 (no src/ or config changes; test files typecheck).

**Wall-time comparison for `tests/landmarks.test.ts` (same machine, this
worktree, `date +%s.%N` around the wrapper):** before = 4.077 / 4.069 s
(avg 4.07); after = 3.971 / 4.041 / 4.014 / 3.967 / 4.055 / 3.971 / 3.986 s
(avg 4.00). Drop ≈ 2 %. Modest because this file already loads each city once
at module scope; the larger win is `spawn.test.ts`, which called
`loadTiledCity('kyiv')` three times (now once) plus SF/NYC/London/Tokyo loads.
On a cold cache / shared CI runner the one-time loads are far slower, which is
what the per-file 30 s timeout (6× the default) covers as belt-and-braces.

**Doc changes:** no doc file is named by this ticket. The memoization contract
is documented in the helpers' JSDoc (module headers + per-function lines).

**Commit:** `d11c390` on branch `tigerteam/T-0101`.

**For the reviewer to look at closely:**
- Memoization returns shared references. I audited every consumer of the
  helpers across all test files for mutation and found exactly two mutating
  call sites (Tokyo blocks in `spawn.test.ts` and `tokyoTowersCity` in
  `landmarks.test.ts`) — both now copy the globals' arrays first. The full
  suite (including bridge/collision/data/osm-convert/roads/ships/terrain,
  which also use these helpers) stays green.
- `loadTiledTile` / `loadSfTile` are intentionally NOT memoized (ticket
  scoped memoization to index / globals / full reconstruction only).
- Out of scope, noticed but not touched: the `sf fixes still target their
  names` test in `landmarks.test.ts` has an O(n²) `SF.buildings.find` loop
  over ~44 k buildings — the original CI timeout was likely driven by that
  on a loaded runner plus cold-cache imports; the 30 s timeout covers it.
  Rewriting the loop to a `byId` map would be a further speedup but would
  touch an assertion-adjacent line, so it is left for a follow-up.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 15m 33s | 3.8M (3.63M) | 29.53k | $0.09 | engine | review |

total: 1 attempt · 15m 33s · 3.8M in (3.63M cached) / 29.53k out · $0.09 (accepted 2026-08-31T07:28:12Z)
