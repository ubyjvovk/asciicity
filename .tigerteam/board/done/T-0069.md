---
id: T-0069
title: Nelson's Column — plinth height fix + 52 m column extra; trafalgar preset faces it
priority: P2
complexity: C1
depends_on: [T-0068]
scope: [src/world/landmarks.ts, src/data/spawn.ts, tests/landmarks.test.ts, tests/spawn.test.ts, docs/world.md, docs/integration.md]
---

## Goal
User: "Trafalgar Square looks wrong without a proper Nelson's Column". OSM
maps the column as its 338 m² plinth at the default 14 m. Per
`docs/architecture.md` §4.13 (London paragraph, 2026-08-27): fix the plinth
to 6 m (label `Trafalgar Square`), add a 5×5 m, 52 m ivory extra named
`Nelson's Column` at (−0.12793, 51.50776), and make the `trafalgar` preset
`{ building: "Nelson's Column", city: 'london', label: 'Trafalgar Square, facing Nelson\'s Column' }`
with the current coordinates kept as its fallback.

## Context
- `LANDMARK_FIXES.london` and `EXTRA_BUILDINGS.london` in
  `src/world/landmarks.ts` — same shapes as the Kyiv entries (`label` is
  supported by `LandmarkFix`; the tags module uses it).
- Tests: `applyLandmarks(city.json, 'london')` → the OSM "Nelson's Column"
  has `h === 6`, exactly one extra named `Nelson's Column` with `h === 52`
  and a 4-point square of side 5 within 1 m of the projected point, ids ≤
  −1000, idempotent; `resolveSpawn('trafalgar', …)` against the real London
  data + `CollisionGrid` resolves via `landmarkSpawn` (not the fallback),
  100–180 m from the extra's centroid (52 m → `targetDist` 132), corridor
  clear per the T-0059 rule, facing it within 10°.
- Docs: `docs/world.md` landmark table (London rows), `docs/integration.md`
  preset row.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/landmarks.test.ts tests/spawn.test.ts` exits 0
- [ ] Tests by name as in Context
- [ ] `bash scripts/check.sh` exits 0; JSDoc untouched or updated; docs updated
- [ ] No files changed outside scope

## Out of scope
- Any other London landmark, shapes, main.ts.

## Worker report

> claimed_by: grok-1 (pid 484850) at 2026-08-27T11:02:39Z

### Files changed
- `src/world/landmarks.ts` — `LandmarkFix.label`; London plinth fix `{ h: 6, color: 0xe8e0c8, label: 'Trafalgar Square' }`; 5×5×52 m ivory extra at (−0.12793, 51.50776) with no shape; extras may share an OSM name (skip only existing extras, id ≤ −1000); OSM-name fixes no longer squash extras; optional `ExtraBuilding.shape` so Motherland stays `tower` without giving Nelson a cap.
- `src/data/spawn.ts` — `trafalgar` is a hybrid building preset on `"Nelson's Column"` with the previous WGS84 fallback; `landmarkSpawn` prefers an extra (`id ≤ −1000`) on exact-name collision so targetDist uses h=52 (132 m), not the 6 m plinth.
- `tests/landmarks.test.ts` — OSM plinth `h === 6`; exactly one extra named Nelson's Column, h=52, side-5 square within 1 m, id ≤ −1000; idempotent (second apply keeps one extra at h=52); London building count +1.
- `tests/spawn.test.ts` — no-city `trafalgar` still uses the coordinate fallback; real London + `CollisionGrid` resolves via `landmarkSpawn` (not fallback), 100–180 m from the extra centroid, T-0059 corridor clear, facing within 10°.
- `docs/world.md` — London landmark table (plinth + extra); silhouette/idempotency notes.
- `docs/integration.md` — `trafalgar` preset row + data-driven list.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh tests/landmarks.test.ts tests/spawn.test.ts` — exit 0
  - `Test Files  2 passed (2)`
  - `Tests  63 passed (63)`
- `bash scripts/check.sh` — exit 0
  - `typecheck: ok` / `unit: ok` / `build: ok` / `e2e: ok`
  - log: `.tigerteam/logs/tests/check-20260827-111033-286.log`

### Doc changes
- `docs/world.md`: London table rows for the 6 m plinth (label Trafalgar Square) and the 52 m extra; extras may share an OSM name; Nelson extra has no shape.
- `docs/integration.md`: `trafalgar` is named-building on Nelson's Column with the old Whitehall coordinate as fallback; listed with the other data-driven presets.
- JSDoc updated on `LandmarkFix`, `ExtraBuilding`, `applyLandmarks`, `landmarkSpawn`.

### Commit
- hash: `91374ac56b19b03278166c0964a393d48064c0ec`
- branch: `tigerteam/T-0069`

### Reviewer notes
- OSM "Nelson's Column" (id 150920358, 338 m² plinth) and the extra share the exact name. `applyLandmarks` must not skip the extra just because the OSM name exists, and must not re-apply the plinth's `h: 6` onto the extra on a second pass. `landmarkSpawn` must prefer the extra or `targetDist` is 77 m (plinth) instead of 132 m.
- `ExtraBuilding.shape` is optional and not in the architecture.md ExtraBuilding signature. Needed so Motherland keeps `tower` (previously hardcoded on every extra) while Nelson stays flat-roof per §4.13 "(no shape)". Kyiv extra gained an explicit `shape: 'tower'` only for that reason.
- Out of scope / tags: `src/hud/tags.ts` looks up `LANDMARK_FIXES[name]`, so both the plinth and the extra would render the tag "Trafalgar Square". Not touched.
- GPU visual of `?at=trafalgar` facing the 52 m shaft is for the PM; verified here through `resolveSpawn` + `CollisionGrid` only.

> handoff: review


## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | grok-1 | grok-4.6-build | 11m 6s | 2.23M (1.83M) | 32.6k | $0.33 | engine | review |

total: 1 attempt · 11m 6s · 2.23M in (1.83M cached) / 32.6k out · $0.33 (accepted 2026-08-27T11:16:11Z)
