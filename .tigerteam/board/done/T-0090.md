---
id: T-0090
title: Manhattan preset polish — dumbo on Washington St (Manhattan Bridge framing the skyline), empirestate down 5th Avenue
priority: P2
complexity: C2
depends_on: [T-0088, T-0089]
scope: [src/data/spawn.ts, tests/spawn.test.ts, docs/integration.md]
---

## Goal
Two Manhattan presets face the wrong thing (PM GPU review of T-0087):
`dumbo` (my coordinate on Main Street) spawns against a warehouse wall,
and `empirestate` (building preset) lands in the 32nd-Street canyon where
the tower is hidden. Re-aim both as fixed coordinates.

## Context
- `dumbo`: Washington Street at Water Street, DUMBO — start from
  −73.98960, 40.70330, bearing 350 (north up Washington St: the Manhattan
  Bridge tower fills the frame with the Empire State Building framed under
  the span — the classic shot). VERIFY against nyc.json: the point must be
  on the street (not inside a footprint — resolve it through
  `CollisionGrid` with buildings + water like main.ts and assert not
  blocked), and the nearest road name should be `Washington Street`.
  Nudge along the street if needed and record the final coordinate.
- `empirestate`: convert to a coordinate preset in the middle of 5th
  Avenue at 38th Street — start from −73.98380, 40.75080, bearing 200
  (down the avenue; the tower rises at the end of the canyon 450 m away).
  Same not-blocked verification; nearest road `5th Avenue`. Keep the
  label "Facing the Empire State Building".
- `tests/spawn.test.ts`: `'dumbo and empirestate resolve to unblocked street points with the expected nearest road'`
  (load nyc.json, build `Terrain` + `CollisionGrid` as main.ts does,
  assert `blocked(...) === false` and the nearest road names above).
- `docs/integration.md`: update the two rows.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/spawn.test.ts` exits 0
- [ ] Tests by name: the case above; existing cases unchanged (update `presetsFor('nyc')` expectations if kinds changed)
- [ ] `bash scripts/check.sh` exits 0
- [ ] docs/integration.md rows updated
- [ ] No files changed outside scope

## Out of scope
Other presets, landmark fixes, data.

## Worker report

> claimed_by: ds-1 (pid 3900) at 2026-08-29T10:51:27Z

## Worker report

**Commit** `6b5b03d` on branch `tigerteam/T-0090`.

### Changes (scope only)
- `src/data/spawn.ts` — `dumbo`: re-aimed to Washington Street at Water
  Street, DUMBO, lon −73.98960, lat 40.70330, bearing 350° (north up
  Washington St so the Manhattan Bridge tower frames the skyline).
  `empirestate`: converted from a building preset to a fixed coordinate
  preset, lon −73.98284, lat 40.75080, bearing 200° (down the avenue),
  label kept "Facing the Empire State Building".
- `tests/spawn.test.ts` — dropped the now-coordinate `empirestate` from the
  "NYC building presets carry their verbatim OSM names" case; added the
  T-0090 case "dumbo and empirestate resolve to unblocked street points with
  the expected nearest road".
- `docs/integration.md` — updated the `dumbo` and `empirestate` rows in the
  Manhattan presets table.

### Coordinate reconciliation (important for review)
The PM's given `empirestate` starting point (−73.98380, 40.75080) does **not**
satisfy its own acceptance criteria against nyc.json: it projects to local
(565, −1647.6), which is **blocked** (inside a footprint) and whose nearest
named road is **West 37th Street** (~19 m), with 5th Avenue ~71 m away. I
nudged it along the street as the ticket's "nudge if needed" process implies:
keeping the PM's latitude 40.75080, I snapped the longitude onto the actual
5th Avenue centre-line at that latitude → **−73.98284** (local ~(646, −1647.6)).
That point is unblocked, nearest named road 0 m = **5th Avenue**, and faces
roughly down the avenue (5th Ave heads ~209° there; ESB is ~355 m away at
~222°). The ticket's "450 m / bearing 200" figures are approximate — the hard
criteria (unblocked, nearest road 5th Avenue, 5th Ave area at 38th, label
unchanged) all hold. `dumbo` needed only the re-aim given (already unblocked,
nearest road Washington Street). If the PM prefers a different exact
5th/38th point (e.g. the intersection vertex (655.7, −1665.2) =
−73.98273, 40.75096), it also passes — say the word and I'll flip it.

### Verification
- `bash .tigerteam/scripts/run-tests.sh tests/spawn.test.ts` → exit 0,
  `Test Files 1 passed (1)`, `Tests 69 passed (69)`.
- `bash scripts/check.sh` → `typecheck: ok`, `unit: ok`, `build: ok`, `e2e: ok`.

### Out-of-scope notes
- The PM-spec `empirestate` coordinate is off the avenue in the data —
  recorded above; resolved by nudging onto the avenue rather than flagging.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 11m 58s | 2.18M (1.94M) | 27.99k | $0.06 | engine | review |

total: 1 attempt · 11m 58s · 2.18M in (1.94M cached) / 27.99k out · $0.06 (accepted 2026-08-29T11:05:58Z)
