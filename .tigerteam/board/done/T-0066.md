---
id: T-0066
title: TreeField — instanced low-poly trees seated on the terrain
priority: P1
complexity: C2
depends_on: [T-0065]
scope: [src/world/trees.ts, tests/trees.test.ts, src/main.ts, docs/world.md]
---

## Goal
Render `city.trees` as two instanced meshes (canopies + trunks) per
`docs/architecture.md` §4.14 — pure `buildTreeInstances` unit-tested,
`TreeField` browser wrapper, one line in `main.ts`.

## Context
- Follow §4.14 exactly (geometry, placement, colours, materials). Pattern
  reference for instancing: `BusFleet` in `src/world/traffic.ts`.
- Pure tests (`tests/trees.test.ts`): `buildTreeInstances` with
  `heightAt = () => 10` and one tree `[0, 0, 12, 4]` → canopy matrix
  translation `(0, 10 + 12 − 3.2, 0)` and scale `(4, 3.2, 4)`, trunk scale y
  `= 8.8` with base at y = 10; colours in the HSL ranges of §4.14 (convert
  back and check hue/sat/lightness bounds over 200 seeded trees); seeded
  determinism; `count` and array lengths for 0 and 3 trees.
- `main.ts`: add the field when `city.trees?.length` (before buildings),
  pass `groundAt`; nothing else.
- Docs: `docs/world.md` Trees section.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/trees.test.ts` exits 0
- [ ] Tests by name as in Context
- [ ] `bash scripts/check.sh` exits 0
- [ ] Headless evidence: `/?city=kyiv&at=hydropark` boots with `__asciicity.ready` and no console errors; report `TreeField.count` for Kyiv and London (expose it on `window.__asciicity.trees`)
- [ ] JSDoc; docs updated
- [ ] No files changed outside scope

## Out of scope
- Collision with trunks, wind, LOD, minimap (T-0067), look tuning (PM).

## Worker report

> claimed_by: ds-2 (pid 484850) at 2026-08-27T10:08:36Z

## Worker report

**Files changed** (branch `tigerteam/T-0066`):
- `src/world/trees.ts` (new) — `buildTreeInstances` (pure geometry + seeded canopy
  colours) and `TreeField` (two `THREE.InstancedMesh`es: canopies + trunks),
  per architecture.md §4.14.
- `tests/trees.test.ts` (new) — 8 tests covering every case in the ticket Context.
- `src/main.ts` — import + `new TreeField(city.trees, groundAt)` added before the
  buildings when `city.trees?.length`; `window.__asciicity.trees` exposes the count.
- `docs/world.md` — new “Trees” section.

**Acceptance walk-through:**
- `bash .tigerteam/scripts/run-tests.sh tests/trees.test.ts` → `1 passed (8 tests)`.
- `bash scripts/check.sh` → `typecheck: ok / unit: ok / build: ok / e2e: ok`.
- Headless probe (dev server + Playwright chromium, SwiftShader):
  `/?city=kyiv&at=hydropark` → `ready: true`, `trees: 23214`, `y: -55`, no console
  errors; `/?city=london` → `ready: true`, `trees: 10160`, no console errors.
  Counts match the T-0065 data (Kyiv 23 214 / London 10 160).
- JSDoc on every export; docs/world.md updated. No files changed outside scope.

**Tests by name** (all present): canopy translation `(0, 10+12−3.2, 0)` + scale
`(4, 3.2, 4)`; trunk scale y `8.8` with base at `y=10` (+ x/z scale `1` guard);
HSL ranges over 200 seeded trees (hue 95–130°, sat 0.45–0.70, light 0.22–0.38,
checked via linear→sRGB round-trip); seeded determinism (same seed identical,
different seed changes palette); count/lengths for 0 and 3 trees; TreeField
two-mesh contract + empty case.

**Commits:** `245d152` on `tigerteam/T-0066`.

**Reviewer notes:**
- §4.14 says the hue is `95 + 35·rand`° — `THREE.Color.setHSL` takes hue in the
  0–1 range, so the implementation divides by 360 (also documented in world.md).
- §4.14’s `TreeInstances.matrices` comment reads `16·count`, but two instanced
  meshes need `2·16·count`; the layout is canopy matrices `[0, 16·count)` then
  trunk matrices `[16·count, 32·count)` (flagging per protocol; the doc comment
  in the PM-owned architecture.md was left untouched).
- `matrices` geometry is PRNG-independent (the seed drives colours only); the
  “different seed differs” test therefore asserts on `colors`, not `matrices`.
> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-2 | deepseek-ai/DeepSeek-V4-Flash-0731 | 16m 39s | 1.96M (1.82M) | 25.62k | $0.05 | engine | review |

total: 1 attempt · 16m 39s · 1.96M in (1.82M cached) / 25.62k out · $0.05 (accepted 2026-08-27T10:27:52Z)
