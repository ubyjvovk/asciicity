---
id: T-0043
title: Drape buildings, roads and water over terrain (HeightFn parameters)
priority: P1
complexity: C2
depends_on: [T-0042]
scope: [src/world/buildings.ts, src/world/roads.ts, src/world/water.ts, tests/buildings.test.ts, tests/roads.test.ts, tests/water.test.ts, docs/world.md, docs/roads-ground.md]
---

## Goal
Every world builder accepts a `HeightFn` and drapes its geometry over it,
per `docs/architecture.md` §4.2 (buildings: base = min, roof = max + h) and
§4.5 (roads: 10 m sub-segments with per-corner heights, bridge decks from
`bridgeProfile`, `ROAD_LIFT = 0.15`; water: `levels[i] + 0.3`). With the
default `FLAT_HEIGHT` the output is the old flat world (roads rise from
0.05 to 0.15 — update the existing assertion).

## Context
- Read §4.2 and §4.5 of `docs/architecture.md` (updated 2026-08-26) and
  `docs/terrain.md` (landed by T-0042). Import `bridgeProfile` from
  `src/world/terrain.ts`; `HeightFn`/`FLAT_HEIGHT` from `src/data/types.ts`.
- Signatures (keep existing positional params, add trailing optionals):
  `buildBuildingsMesh(buildings, heightAt = FLAT_HEIGHT)`,
  `makeBuildingsObject(buildings, windowTex, heightAt = FLAT_HEIGHT)`,
  `buildRoadsMesh(roads, heightAt = FLAT_HEIGHT)`, `makeRoadsObject(roads, heightAt = FLAT_HEIGHT)`,
  `buildWaterMesh(rings, levels?)`, `makeWaterObject(rings, levels?)`;
  export `ROAD_LIFT`.
- Roads: sub-segment count `n = max(1, ceil(len / 10))`; for ordinary roads
  sample `heightAt` at each of the four quad corners (left/right offsets
  included); for `bridge: true` roads the deck height at fraction `f` along
  polyline segment `i` is `lerp(ys[i], ys[i+1], f)` with
  `ys = bridgeProfile(road.pts, heightAt)`, identical for both edges.
- Buildings: `base = min`, `top = max` of `heightAt` over ring vertices;
  walls `base … top + h`, wall `v = (y − base) / 24`, roof at `top + h`.
- Docs: `docs/world.md` (buildings + water sections), `docs/roads-ground.md`
  (roads) — describe the new parameters and the rules.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/buildings.test.ts tests/roads.test.ts tests/water.test.ts` exits 0
- [ ] Existing tests still pass (roads' `0.05` expectation updated to `ROAD_LIFT`); new tests by name — buildings: with `heightAt = (x, z) => x` on a 10×10 square at x∈[0,10], h=5: wall min y = 0, wall max y = 15, roof y = 15, wall `v` runs 0 → 15/24; default `heightAt` gives identical geometry to the previous behaviour (compare against a call with `FLAT_HEIGHT`); roads: a 100 m primary segment yields 10 quads (60 vertices), corner y = `heightAt(corner) + 0.15` under `heightAt = (x, z) => 0.1·x + 0.2·z` (check all four corners of the first quad), a 3-point bridge road over a dip has its middle vertex at `lerp + 0.15` (both edges equal), default → every y = 0.15; water: with `levels = [2]` every y = 2.3, without levels every y = 0.02, `levels` shorter than `rings` throws an `Error` naming `levels`
- [ ] `bash scripts/check.sh` exits 0 (incl. `e2e: ok`); JSDoc; docs updated
- [ ] No files changed outside scope

## Out of scope
- `main.ts`, terrain.ts, traffic, collision, mesh.ts, types.ts.
- Mitred corners, road banking, water animation.

## Worker report

> claimed_by: grok-1 (pid 484850) at 2026-08-26T20:24:17Z

### Files changed
- `src/world/buildings.ts` — trailing `heightAt = FLAT_HEIGHT`; per ring `base = min` / `top = max`, walls `base … top+h`, roof at `top+h`, wall `v = (y − base) / 24`.
- `src/world/roads.ts` — export `ROAD_LIFT = 0.15`; 10 m sub-segments; ordinary corners `heightAt + ROAD_LIFT`; `bridge: true` decks from `bridgeProfile` + lerp; `makeRoadsObject` threads `heightAt`.
- `src/world/water.ts` — `levels?[i] + 0.3`, else `0.02`; throws `Error` naming `levels` when the array is shorter than `rings`.
- `tests/buildings.test.ts` — sloped 10×10 square (wall y 0…15, roof 15, v 0→15/24) + default vs `FLAT_HEIGHT`.
- `tests/roads.test.ts` — 0.05→`ROAD_LIFT`; 100 m → 10 quads / 60 verts; linear `heightAt` first-quad corners; 3-point bridge over a dip; default y = 0.15. Vertex counts that the 10 m split necessarily changed (3-point 12→90, mixed degenerate 6→60) were updated so existing cases still pass.
- `tests/water.test.ts` — `levels = [2]` → y = 2.3; without levels y = 0.02; short `levels` throws `/levels/`.
- `docs/world.md` — buildings HeightFn / base-top rules; water `levels` + throw.
- `docs/roads-ground.md` — `ROAD_LIFT`, 10 m split, ordinary vs bridge draping.

### Commands
```
bash .tigerteam/scripts/run-tests.sh tests/buildings.test.ts tests/roads.test.ts tests/water.test.ts
```
```
 ✓ tests/buildings.test.ts (15 tests) 16ms
 ✓ tests/roads.test.ts (11 tests) 11ms
 ✓ tests/water.test.ts (8 tests) 7ms

 Test Files  3 passed (3)
      Tests  34 passed (34)
   Start at  20:30:22
   Duration  461ms (transform 84ms, setup 0ms, import 189ms, tests 34ms, environment 0ms)
```
```
bash scripts/check.sh
```
```
typecheck: ok
unit: ok
build: ok
e2e: ok
```

### Docs
JSDoc on the new/changed exports (`ROAD_LIFT`, builder signatures). `docs/world.md` (buildings + water) and `docs/roads-ground.md` (roads) describe the parameters and draping rules.

### Commit
`396d1c286ef66398fbd449d021b1936ce5bf35e0` on `tigerteam/T-0043`

### Reviewer notes
- `roads.ts` now imports `bridgeProfile` from `terrain.ts`, which already imports `ROAD_WIDTH` from `roads.ts`. Both uses are runtime-only (not at module init), so the ESM cycle is live-binding-safe; splitting `ROAD_WIDTH` is out of scope.
- `main.ts` still calls the builders without `heightAt` / `levels` (T-0045). Default `FLAT_HEIGHT` / omitted `levels` keeps London byte-identical except roads now sit at `y = 0.15` instead of `0.05`, as specified.
- Did not touch terrain.ts, types.ts, mesh.ts, traffic, collision, or main.ts.

> handoff: review


## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | grok-1 | grok-4.6-build | 7m 15s | 1.09M (972.29k) | 26.68k | $0.15 | engine | review |

total: 1 attempt · 7m 15s · 1.09M in (972.29k cached) / 26.68k out · $0.15 (accepted 2026-08-26T20:33:46Z)
