---
id: T-0067
title: Minimap — fill woodland/park rings between water and buildings
priority: P2
complexity: C1
depends_on: [T-0065]
scope: [src/hud/minimap.ts, tests/minimap.test.ts, docs/minimap.md]
---

## Goal
Paint `city.woods` rings on the minimap in `#0b2f18`, layered after water
and before buildings (architecture §4.14; T-0064 is the pattern — mirror
its bucketing and per-frame de-dup exactly).

## Context
- Same bucketing/draw approach as the water rings from T-0064; colour
  `#0b2f18`; draw order background → water → woods → buildings → roads.
- Tests: a city with one wood ring around the origin paints the wood colour
  at a sample pixel; woods draw after water (a wood ring inside a water
  ring wins the pixel); cities without `woods` unchanged.
- Docs: `docs/minimap.md` colours/draw order.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/minimap.test.ts` exits 0
- [ ] Tests by name as in Context
- [ ] `bash scripts/check.sh` exits 0; JSDoc; docs updated
- [ ] No files changed outside scope

## Out of scope
- Tree dots on the map, 3D trees, any other file.

## Worker report

> claimed_by: ds-1 (pid 484850) at 2026-08-27T10:08:36Z

Implemented woodland/park rings on the minimap, mirroring the T-0064 water
bucketing/draw pattern exactly.

**Files changed**
- `src/hud/minimap.ts` — added `StoredWood` + `woods` cell-bucket field,
  `WOODS_FILL = '#0b2f18'`, `insertWood` bucketing for `city.woods` rings, and a
  per-frame de-duplicated woods draw pass placed after the water pass and
  before buildings (draw order background → water → woods → buildings → roads).
- `tests/minimap.test.ts` — new `describe('Minimap woods')` with the three
  Context cases: one wood ring at origin paints `#0b2f18` at a sample pixel;
  a wood ring inside a water ring paints after water (wins the pixel); cities
  without `woods` produce no wood-colour fill.
- `docs/minimap.md` — added the Woods row to the colours/draw-order table and
  updated the prose so `woods` fill between water and buildings and are
  skipped (like water/rivers) when absent.

**Commands run**
- `bash .tigerteam/scripts/run-tests.sh tests/minimap.test.ts` → exit 0;
  `Test Files  1 passed (1)` / `Tests  14 passed (14)`.
- `bash scripts/check.sh` → typecheck ok, unit ok, build ok, e2e ok.

**Docs**: `docs/minimap.md` colours/draw order updated. JSDoc added on the new
`WOODS_FILL` constant.

**Commit**: `d78def1` on branch `tigerteam/T-0067`.

**For the reviewer**: no out-of-scope changes. Woods reuse the same polygon
projection/de-dup path as water; nothing else touched.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 4m 40s | 237.71k (204.29k) | 5.29k | $0.0076 | engine | review |

total: 1 attempt · 4m 40s · 237.71k in (204.29k cached) / 5.29k out · $0.0076 (accepted 2026-08-27T10:15:20Z)
