---
id: T-0070
title: Landmark tags — an extra building's tag uses its own name; a fix's label applies only to the OSM building
priority: P3
complexity: C1
depends_on: [T-0069]
scope: [src/hud/tags.ts, tests/tags.test.ts, docs/hud.md]
---

## Goal
At `?city=london&at=trafalgar` both floating tags read "Trafalgar Square":
the 52 m extra shares the OSM name "Nelson's Column", so `landmarkAnchors`
resolves its label through the fix table (`label: 'Trafalgar Square'`) meant
for the plinth. Extras (ids ≤ −1000) must be tagged with their own `name`;
the fix's `label` applies to the OSM building only.

## Context
- `src/hud/tags.ts` `landmarkAnchors`: when `building.id <= -1000`, use
  `building.name` as the label regardless of the fix table.
- Test: a city with an OSM building `X` (fix `{ label: 'Plinth' }`) and an
  extra also named `X` (id −1000) yields two anchors labelled `Plinth` and
  `X` respectively.
- `docs/hud.md` tags section: one sentence on the rule.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/tags.test.ts` exits 0 with the new case
- [ ] `bash scripts/check.sh` exits 0; docs updated
- [ ] No files changed outside scope

## Out of scope
- Anything else.

## Worker report

> claimed_by: ds-1 (pid 484850) at 2026-08-27T11:16:20Z

Files changed (all in scope):
- `src/hud/tags.ts` — in `landmarkAnchors`, when `b.id <= -1000` the label
  is now `b.name` regardless of the fix table; the fix `label` still applies
  to the OSM building sharing that name.
- `tests/tags.test.ts` — new case "an extra named like a fixed building uses
  its own name, not the fix label": OSM `X` (fix `label: 'Plinth'`) plus an
  extra also named `X` (id −1000) yields two anchors labelled `Plinth` and
  `X` respectively.
- `docs/hud.md` — one sentence on the rule in the tags section.

Commands run:
- `bash .tigerteam/scripts/run-tests.sh tests/tags.test.ts` → Test Files 1
  passed (1); Tests 4 passed (4).
- `bash scripts/check.sh` → typecheck: ok; unit: ok; build: ok; e2e: ok.

Doc changes: `docs/hud.md` tags section updated.

Commit: `bcde0b2` on branch `tigerteam/T-0070`.

Acceptance criteria walked: run-tests exit 0 with new case ✓; check.sh exit 0
+ docs updated ✓; no files changed outside scope ✓.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 4m 47s | 188.85k (172.29k) | 2.59k | $0.0051 | engine | review |

total: 1 attempt · 4m 47s · 188.85k in (172.29k cached) / 2.59k out · $0.0051 (accepted 2026-08-27T11:22:34Z)
