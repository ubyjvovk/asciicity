---
id: T-0051
title: Render style — braille (2×4 dot cells)
priority: P1
complexity: C2
depends_on: [T-0050]
assignee: grok
scope: [src/render/styles/braille.ts, tests/styles/braille.test.ts, docs/styles/braille.md]
---

## Goal
Replace the stub `src/render/styles/braille.ts` with the real **braille (2×4 dot cells)** look,
exactly as specified in `docs/architecture.md` §4.11 "braille". One file,
one shader, pure helpers unit-tested in node.

## Context
- Read `src/render/style.ts` (PM-owned: `RenderStyle`, `STYLE_PRELUDE`
  helpers — `sampleSub`, `cellMean`, `bright`, `shaped`, `tintOf`,
  `linearDepth`) and `src/render/styles/ascii.ts` (the reference style:
  how the atlas, `makeUniforms` and the fragment fit together) before
  writing code. Keep the module's `export const STYLES` and the id(s) the
  stub declares; keep cell/sub/needsDepth per §4.11.
- The shader must mirror the pure helpers term for term (the tests are the
  spec for the shader); pure helpers never touch `document`.
- Atlases are procedural (canvas rectangles/arcs/lines), never font glyphs,
  unless §4.11 says otherwise; build them in `makeUniforms` via
  `ctx.makeCanvas`; `dispose` frees the texture.
- `docs/styles/braille.md`: the algorithm in prose, the pure exports, and a
  note on what the shader does per pixel.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/styles/braille.test.ts tests/post.test.ts` exits 0
- [ ] Tests by name: `brailleBits` of all-zero → 0, all-one → 255, only top-left dot → 1, only bottom-right → 128, left column rows 0–2 → 7; thresholds table equals §4.11 exactly; `brailleDots(bits)` inverse of `brailleBits` for 20 random bit patterns; a gradient across the 8 samples lights more dots as it brightens (monotone count)
- [ ] `bash scripts/check.sh` exits 0 (the e2e style loop still visits every id and saves `e2e/__shots__/style-<id>.png` — mention the file for this style in the report)
- [ ] Headless evidence: `/?synthetic=1&render=<id>&cell=3x6` renders > 2 % non-black pixels and `__asciicity.render` is the id; no WebGL shader compile errors in the console (quote the console tail)
- [ ] JSDoc on every export; `docs/styles/braille.md` written
- [ ] No files changed outside scope

## Out of scope
- Any other style, `post.ts`, `style.ts`, `main.ts`, HUD, docs outside `docs/styles/`.

## Worker report

> claimed_by: ds-1 (pid 484850) at 2026-08-26T23:25:53Z
> INCOMPLETE attempt by ds-1 at 2026-08-27T00:10:54Z (engine exit 137, no handoff) — see /home/d/asciicity/.tigerteam/logs/workers/ds-1.log

> claimed_by: ds-1 (pid 484850) at 2026-08-27T00:10:54Z
> INCOMPLETE attempt by ds-1 at 2026-08-27T00:23:16Z (engine exit 0, no handoff) — see /home/d/asciicity/.tigerteam/logs/workers/ds-1.log

## Answers

Not a question — the ticket struck out after two INCOMPLETE DeepSeek attempts (engine exit 137, then exit 0 with no handoff; the branch has no commits). Reassigned to `grok` (`assignee: grok`), whose sibling styles (teletext, dither) landed cleanly. Nothing in the spec changes; two notes for the next attempt:

- Start from the current stub `src/render/styles/braille.ts` on `main` (T-0050) and the reference `styles/ascii.ts`; the procedural 256-tile atlas is 16×32 px per tile → a 4096×32 canvas (one row), same layout the ascii atlas uses.
- Keep the work small and commit early: pure helpers + tests first, then the shader; the whole file is ~150 lines. If the container is slow, `run-tests.sh tests/styles/braille.test.ts` is enough until the final `check.sh`.


> claimed_by: grok-1 (pid 484850) at 2026-08-27T00:43:10Z

Replaced the T-0050 ascii-copy stub with the §4.11 braille look. Pure helpers (`BRAILLE_THRESHOLDS`, `brailleBits`, `brailleDots`, `buildBrailleAtlas`) are node-safe; the fragment mirrors `brailleBits` term for term (`shaped(bright(sample)) > T[r][c]`, Unicode bit order, `tintOf(mean) · clamp(shaped(bright(mean))·0.7+0.4,0,1) · mask`). Atlas is a 4096×32 procedural row of 256 16×32 tiles (arcs, not glyphs), same `LinearFilter` + `flipY` layout as ascii.

### Files changed
- `src/render/styles/braille.ts` — real braille `RenderStyle` (id `braille`, cell 6×12, sub 2×4, `needsDepth: false`) plus the four pure exports.
- `tests/styles/braille.test.ts` — the named cases from the ticket, plus an atlas fake-canvas check (256 tiles, 1024 dots, r=2.5).
- `docs/styles/braille.md` — algorithm, pure exports, per-pixel shader walk-through.

### Commands run
`bash .tigerteam/scripts/run-tests.sh tests/styles/braille.test.ts tests/post.test.ts` exit 0:
```
 ✓ tests/styles/braille.test.ts (9 tests) 25ms
 ✓ tests/post.test.ts (4 tests) 4ms
 Test Files  2 passed (2)
      Tests  13 passed (13)
```

`bash scripts/check.sh` exit 0:
```
typecheck: ok
unit: ok
build: ok
e2e: ok
log: .tigerteam/logs/tests/check-20260827-004657-186.log
```
(416 unit tests; e2e 5 passed. Style-loop screenshot: `e2e/__shots__/style-braille.png`.)

Headless probe (Playwright Chromium + SwiftShader, Vite on :4174) `/?synthetic=1&render=braille&cell=3x6`:
```
PROBE_RESULT {"render":"braille","ready":true,"frac":0.355859375,"w":1280,"h":720}
--- console tail ---
[debug] [vite] connecting...
[debug] [vite] connected.
--- console count --- 2
--- shader/webgl/error lines ---
(none)
```
35.6 % non-black (> 2 %), `__asciicity.render === 'braille'`, no WebGL/shader compile errors.

### Docs
`docs/styles/braille.md` written (algorithm table, pure exports, shader per pixel). JSDoc on every export. Did not touch `docs/render.md` (still says "Stub until T-0051" — out of scope).

### Commit
`2016d52b1ea8af74846c1517b57b57d1e7548007` on `tigerteam/T-0051`.

### Notes for reviewer
- GPU visual: `e2e/__shots__/style-braille.png` is the SwiftShader e2e frame; PM GPU review from `?city=kyiv&render=braille` as planned. I cannot view the PNG here.
- Shader uses `float(bool)` (strict `>`, matching `brailleBits`) rather than `step` (`>=`). Mean of the eight already-fetched samples is `cellMean` for a 2×4 cell (avoids a second 8-sample loop).
- Out of scope noticed, not touched: `docs/render.md` still lists braille as a stub.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 45m 1s | 2.08M (902.66k) | 44.66k | $0.13 | engine | todo |
| 2 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 12m 22s | 862.4k (544.77k) | 11.37k | $0.04 | engine | blocked |
| 3 | grok-1 | grok-4.6-build | 7m 20s | 1.09M (885.12k) | 20.45k | $0.17 | engine | review |

total: 3 attempts · 1h 4m · 4.03M in (2.33M cached) / 76.48k out · $0.34 (accepted 2026-08-27T00:52:21Z)
