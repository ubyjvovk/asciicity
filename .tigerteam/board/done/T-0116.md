---
id: T-0116
title: Converter — assemble multi-way outer rings for building relations; refetch Sydney (Opera House lands)
priority: P1
complexity: C2
depends_on: [T-0110]
scope: [scripts/osm-convert.mjs, tests/osm-convert.test.ts, public/data/sydney/, docs/data-format.md, src/data/cities.ts]
---

## Goal

The Sydney Opera House is missing from the shipped dataset: OSM maps it as
a building relation whose outer boundary is **16 separate ways**, and the
converter only emits building relations whose outer members are single
closed ways (T-0110 report: one of 24 skipped relations). Teach the
building path to stitch multi-way outer members into closed rings — the
water/woods paths already do exactly this — then refetch Sydney so the
Opera House (and the other skipped relations) land.

## Context

- T-0110's Worker report (`.tigerteam/board/done/T-0110.md`) documents the
  find: relation 9596872, `building=yes`, name `Sydney Opera House`,
  16 outer ways, skipped.
- `scripts/osm-convert.mjs` already contains ring assembly from unordered,
  possibly-reversed way members for water ("Assemble water rings from OSM
  ways/relation members", ~line 257/312) and woods — reuse that helper for
  the building-relation path rather than writing a second stitcher. Keep
  the existing rules: each assembled outer ring becomes one building; the
  first ring keeps the relation id, later rings get unique ids; tags
  (name, height, colour inputs) come from the relation.
- An open (unstitchable) outer boundary stays skipped and counted — never
  emit a partial ring.
- Refetch: `npm run fetch-data:sydney` (same alias/bbox — nothing else
  changes). Expect small drifts vs T-0110's fetch (live Overpass); that is
  fine. The 12 MB guard still applies.
- `sizeBytes` in `src/data/cities.ts` must be updated to the new
  `index.json` size (registry rule).
- The wave-13a in-place-surgery rule does NOT apply here — this is a
  legitimate full refetch (the building set itself changes).

## Acceptance criteria

- [ ] Unit tests (`tests/osm-convert.test.ts`) with a synthetic
      multi-way-outer building relation fixture: ways out of order, one
      reversed, two disjoint outer rings → two buildings, correct ids,
      name/height from the relation tags; an open boundary → skipped, not
      emitted; existing single-way relation behaviour unchanged.
- [ ] Refetched `public/data/sydney/` committed, validator-clean, ≤ 12 MB
      total; report the new counts (buildings / skipped relations —
      expect noticeably fewer than 24 skipped) and the tile invariants
      (zero bridge leaks, tile count, bytes).
- [ ] **`Sydney Opera House` exists in the committed dataset** — a unit
      test on the tiles (memoized helpers) asserts a building with that
      exact name near local coords for `151.2153, -33.8568`, footprint
      spanning ≥ 100 m along its long axis; report its h and vertex
      count.
- [ ] T-0110's transect + invariant tests (`tests/sydney.test.ts`) still
      green against the refetched data — if a transect bound fails from
      Overpass/DEM drift, STOP and file a blocked question (do not touch
      bounds).
- [ ] `docs/data-format.md` "### Sydney": Shipped-counts line refreshed;
      one sentence in the conversion-rules section documenting multi-way
      outer-ring assembly for building relations.
- [ ] `src/data/cities.ts`: sydney `sizeBytes` = new index.json size;
      nothing else changed there.
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (full suite).
- [ ] No files changed outside scope.

## Out of scope

Refetching any other city (their committed data stays byte-identical —
the converter change only affects future fetches), landmark fixes,
presets, the shells module (T-0114), ships. If ring assembly surfaces a
second converter gap (e.g. inner holes — we ignore holes today by
existing rule), report it; do not widen the change.

## Worker report

**Summary** — Taught the building-relation path to assemble multi-way outer
members into closed rings (reusing `assembleRingsInternal`), skipped
`building:part=roof` sheets so the authoritative Opera House outline survives,
refetched Sydney, and refreshed the registry/docs. Full gate green (typecheck +
854 unit + build + e2e). The Sydney Opera House (relation 9596872) now lands
as a single ~180 m building named exactly.

**Files changed** (branch `tigerteam/T-0116`, isolation worktree; commit
`d555e3a`)
- `scripts/osm-convert.mjs` — building-relation branch now assembles all
  `role=outer` members via `assembleRingsInternal` (the water/woods stitcher);
  each closed ring becomes one building (first keeps the relation id, later
  rings `.id*1000+n`); an unstitchable outer stays skipped and counted. Added
  `isRoofPart`: `building:part=roof` sheets are dropped as parts so they
  cannot replace the real multipolygon outline via `applyBuildingParts`.
- `tests/osm-convert.test.ts` — new `describe('osm-convert building relations
  — multi-way outer rings (T-0116)')` with 4 named tests (out-of-order + one
  reversed → 1 ring; two disjoint rings → ids [700,700001] + relation
  name/height; open boundary → skipped/counted; single closed-way relation
  unchanged) and a `committed public/data/sydney` block asserting the Opera
  House exists (exact name, ≥ 100 m long axis, near 151.2153,-33.8568) via the
  memoized `loadTiledCity`.
- `public/data/sydney/` — refetched via `npm run fetch-data:sydney` (57 tiles;
  index 914 166 B + tiles 5 875 407 B ≈ 6.48 MiB ≤ 12 MB guard; zero bridge
  leaks into tiles; only 17 of 57 tiles changed content).
- `docs/data-format.md` — refreshed the "### Sydney" shipped-counts line and
  rewrote the Buildings `relation` conversion bullet to document multi-way
  outer-ring assembly + the roof-part rule.
- `src/data/cities.ts` — `sydney.sizeBytes` 913937 → 914166 (new index.json
  size); nothing else.

**Commands run**
- `npm run fetch-data:sydney` (live Overpass; one 429 retry): `20322
  buildings, 9818 roads, 106 places, 89 water, 1 rivers, 28962 trees (17333
  filled, 0 dropped), terrain 291x347 @ 20 m (0 voids), 6386 KB (skipped 6
  relations, dropped 0 open water chains), 57 tiles (12124 KB tiled total)`.
  Skipped relations: **24 → 6**.
- `bash .tigerteam/scripts/run-tests.sh` — `Test Files 50 passed (50), Tests
  854 passed (854)`. `tests/sydney.test.ts` (T-0110 transects + invariants)
  passed unchanged.
- `bash scripts/check.sh` — `typecheck: ok, unit: ok, build: ok, e2e: ok`.

**Doc changes** — data-format.md: Sydney counts line + building-relation
conversion rule (both above).

**Opera House (report for AC)** — building id 9596872, name exactly `Sydney
Opera House`, `h = 18.5` (from `building:levels=5`), **57 vertices**, long axis
**~180 m**; the advertised point (151.2153, -33.8568) projects inside its bbox.

**Reviewer, please look at** — the `isRoofPart` rule is a converter decision
beyond the literal "stitch rings" scope but REQUIRED by the ≥ 100 m AC: OSM
maps the Opera House's podium fill as `building:part=roof` (and `building=roof`)
sheets whose centroids sit inside the 180 m relation outline; without skipping
the roof PARTS, `applyBuildingParts` drops the outline and transfers the name to
a 56 m slab, and no ≥ 100 m `Sydney Opera House` exists. I skip only
`building:part=roof` as parts (2 buildings in Sydney); `building=roof` outlines
are left untouched (their suppression is a LANDMARK_FIXES / §4.22 concern, per
out-of-scope). Other cities' committed data are untouched. I chose not to
narrow/alter `applyBuildingParts` further.

**Out-of-scope noticed but not touched** — no other converter gaps surfaced
beyond the roof-part case above.

> claimed_by: ds-1 (pid 1487920) at 2026-08-31T21:48:55Z

> handoff: review

## Rework (attempt 2)

## Rework (attempt 2)

Attempt 1's work is GOOD and stays — the ring assembly, `isRoofPart`
(accepted as necessary, keep it exactly as scoped), tests, docs and the
Opera House landing are all right. The rework widens scope the way
T-0076 did, because a PM probe found a SECOND converter bug in the same
pipeline, and the dataset cannot ship with it:

**The coastline closure emits two near-duplicate giant harbour rings.**
In the shipped (attempt-1) sydney index: rings idx 85 (4 273 verts,
12.65 km²) and idx 86 (4 663 verts, 13.26 km²) BOTH contain open-harbour
points, e.g. local (277, −918) (mid-harbour off Kirribilli). Under the
odd-parity water rule (collision T-0078, islands, T-0113's coming ferry
tests) their overlap counts as LAND: the player can walk on the harbour,
and Fort Denison (inside both giants + its own island ring = 3 = odd)
flips to water. The T-0110/0116 transect criteria never caught it
because none of them walks into the water. (Terrain flattening is
per-ring painted, so ALT/render look fine — the defect is parity.)

**What to do:**

1. Diagnose `closeCoastline`'s multi-open-chain closure on the real
   Sydney chains (the harbour shore arrives as at least two open chains;
   suspicion: each open chain is closed along the bbox perimeter without
   splicing in the OTHER chains' endpoints encountered on the walk, so
   two overlapping full-harbour rings emerge — but trust your diagnosis
   over mine). Fix so the closure yields NON-OVERLAPPING water rings:
   one ring per connected water body, islands nested exactly once.
2. Unit tests in `tests/coastline.test.ts` (this rework ADDS that file
   to your scope): a synthetic fixture with two open chains exiting the
   same bbox edge reproducing today's overlap, asserting the closure
   emits pairwise-disjoint rings (no sampled interior point of one ring
   inside another); ALL existing coastline/SF fixtures byte-unchanged
   and green (SF's committed data must remain reproducible).
3. Refetch Sydney once more (etiquette: off-peak, retry on 429) and
   assert on the committed data, in `tests/osm-convert.test.ts` or
   `tests/sydney.test.ts`:
   - odd-parity WATER at local (277, −918) [mid-harbour], (92, −365)
     [Circular Quay cove], (−30, −990) [under the bridge];
   - odd-parity LAND at (0, 500) [CBD], (1368, −730) [Fort Denison],
     (−1710, −1139) [Goat Island];
   - no two water rings overlap (pairwise sampled-disjointness over
     rings ≥ 0.1 km²).
4. Keep every attempt-1 criterion green (Opera House presence test,
   transects, guards, counts line + `sizeBytes` refreshed again).

Scope for this rework additionally includes
`scripts/osm-convert.mjs` (already), `tests/coastline.test.ts`, and
`tests/sydney.test.ts`. Everything else from the original ticket
stands. Done looks like: all attempt-1 criteria + the six parity points
+ the disjointness invariant green on the refetched data, full suite +
run-tests green.


> claimed_by: ds-1 (pid 1487920) at 2026-08-31T22:20:28Z

## Rework (attempt 2) — BLOCKED — diagnosis contradicts the rework's premise

**Summary — the rework's premise (`closeCoastline` + open coastline chains) is
factually wrong for this dataset, so I am blocking instead of guessing at a
mechanism that does not exist.** I ran the actual Sydney fetch and inspected the
real pipeline. There are **zero `natural=coastline` ways** in the Sydney bbox —
`closeCoastline` never runs on this city (its `coastOpen` input is empty). The
two near-duplicate giant rings (committed index idx 85: 4 273 verts / 12.65 km²;
idx 86: 4 663 verts / 13.26 km²) are **not** coastline closures; they are
assembled from **overlapping `natural=water` multipolygon relations** by the
water (`assembleRingsInternal`) path:

- rel **15522136 `Port Jackson`** (water=harbour) — 484 outer / **35 inner**
- rel **1252425 `Sydney Harbour`** (water=harbour) — 193 outer / **14 inner**
- rel **1249186 `Parramatta River`** — 135 outer / **7 inner**
- rel **2013600 `Lane Cove River`** — 74 outer / 0 inner

Port Jackson and Sydney Harbour are the same harbour body mapped as two
separate overlapping multipolygons → two overlapping outer rings → the
odd-parity rule (oldest rule, architecture.md §4.6) reads their overlap as LAND.

**Why this cannot be fixed by `closeCoastline` / why the acceptance needs a
water-path change:**
1. There are no coastline ways, so criterion "close two open chains exiting the
   same bbox edge" has no real object — a `tests/coastline.test.ts` case built
   on that premise would test a bug that isn't the one in the data.
2. Meeting the parity target (**Fort Denison (1368,−730) and Goat Island
   (−1710,−1139) = LAND**) requires, once the harbour collapses to a single
   ring, an island ring nested inside it (so count = harbour(1)+island(1)=2 =
   land). Those islands come from the relations' **inner members** (35 + 14 + 7
   inner hole rings), which the converter currently **ignores entirely** ("inner
   holes — we ignore holes today by existing rule" — the original ticket's
   explicit out-of-scope). Today Fort Denison/Goat Island read as land only by
   the accident of being inside both giants (count 2); a correct single-ring
   harbour flips them to water unless inner holes are emitted. There is no
   `Fort Denison`/`Goat Island` name anywhere in the data; the holes are bare
   island outlines.
3. "One ring per connected water body" for overlapping relations needs a
   dedup/merge design (keep-largest-in-overlap-cluster vs geometric union) that
   the ticket does not specify and that affects the shared water assembly for
   every future fetch (London/Kyiv/SF committed data stays byte-identical only
   because we do not refetch them — no converter-byte golden test exists).

**Evidence (rerunnable):**
- Parity of committed `public/data/sydney/index.json` water rings (odd = water):
  ```
  node -e '<pip over SYD.water for each point, count containing rings>'
  mid-harbour (277,-918)      count=2 LAND   (rings 85,86)
  Circular Quay (92,-365)     count=2 LAND   (rings 85,86)
  under bridge (-30,-990)     count=2 LAND   (rings 85,86)
  CBD (0,500)                 count=0 LAND   ✔
  Fort Denison (1368,-730)    count=2 LAND   (rings 85,86 only — no island ring)
  Goat Island (-1710,-1139)   count=2 LAND   (rings 85,86 only — no island ring)
  ```
- Raw fetch element census: `way["natural"="coastline"]` → **0** elements;
  water relations with outer/inner counts as listed above.

**Options (please pick):**
- **A (recommended, matches the rework's stated output).** Fix in the **water**
  path of `scripts/osm-convert.mjs`: (1) assemble inner-member (hole) rings per
  relation and emit them as island rings; (2) dedup overlapping same-body outer
  rings to one ring per connected water body (drop a ring that a kept ring
  covers above a threshold, or keep max-area per connected-overlap cluster);
  (3) dedup the emitted island rings too (the same island appears as an inner in
  Port Jackson AND Sydney Harbour). Tests live in `coastline.test.ts`/`sydney.test.ts`
  as the rework scoped, asserting the 6 parity points + pairwise
  disjointness ≥ 0.1 km². Note: this is the "inner holes" widening the original
  ticket listed as out-of-scope — the rework's "islands nested exactly once"
  makes it required.
- **B.** Narrow: only dedup the overlapping harbour relations so harbour = water,
  and accept that Fort Denison/Goat Island then read as water by ring-parity
  (they would still be un-walkable via their building/footprint collision). This
  fails the rework's literal "odd-parity LAND" for the two islands.
- **C.** PM-fixed design for the dedup/cluster thresholds and inner-hole
  handling; I implement exactly that.

**What I did NOT do:** no source changes this attempt (working tree clean;
attempt-1 commit `d555e3a` intact). I did not fabricate a `closeCoastline` fix
that the data cannot exercise.

## Questions

1. The rework's mechanism (`closeCoastline`, open coastline chains) does not
   apply — Sydney has zero coastline ways; the overlapping giant rings come
   from overlapping `natural=water` relations (Port Jackson + Sydney Harbour +
   Parramatta River). Confirm you want the fix in the **water** path instead?
2. Which dedup design for "one ring per connected water body" — (A) drop rings
   covered ≥T by a larger kept ring, (B) keep max-area per connected-overlap
   cluster, (C) geometric union? Recommend A/B with threshold climb-on-refetch
   verified against the 6 parity points.
3. Confirm the "inner holes" widening is now in scope (Fort Denison / Goat
   Island LAND require emitting the relations' inner-member island rings).

> handoff: blocked

## Answers

## Answers (2)

Your diagnosis is accepted in full and the rework's premise is
retracted — the PM probe found the two giant rings and pattern-matched
them to the SF coastline closure without checking that Sydney has any
coastline ways at all. It has zero; the duplication is overlapping
`natural=water` relations (Port Jackson ⊇ Sydney Harbour), and the
islands live only in ignored inner members. Good refusal to fix the
wrong mechanism. Answers to your three questions:

**1. Confirmed: the fix lives in the WATER path** of
`scripts/osm-convert.mjs`. The `tests/coastline.test.ts` requirement
from the rework note is VOID — do not create synthetic coastline
fixtures. New unit fixtures live in `tests/osm-convert.test.ts` (a
water-relations describe block).

**2. Dedup design — your option A with these locked parameters:**
- Applies to assembled OUTER rings with |area| ≥ 0.1 km² only (small
  ponds skip the O(n²) work).
- Sort those rings by |area| descending; walk down; for each candidate
  compute `coverage` = the fraction of 50 m-grid cells over the
  candidate's bbox whose centres are inside the candidate AND inside
  any already-KEPT ring. Drop the candidate iff `coverage ≥ 0.85`;
  otherwise keep. Deterministic, no polygon booleans.
- Expected outcome on Sydney: Port Jackson kept, Sydney Harbour dropped
  (≈ full coverage); Parramatta River / Lane Cove River kept (adjacent
  bodies, low coverage). If the six parity points still fail after this
  (e.g. two KEPT rings overlap more than expected), block again with
  the coverage numbers — do not tune the 0.85/50 m constants.

**3. Inner holes are now in scope — with this shape:**
- Emit inner-member rings ONLY from relations at least one of whose
  outer rings was KEPT (a dropped duplicate relation contributes no
  islands — that kills most cross-relation island duplicates for free).
- Assemble inners with the same stitcher; an open inner chain is
  skipped and counted, never emitted partially.
- Residual island dedup: skip an inner ring whose centroid lies inside
  an already-emitted island ring.
- Islands are emitted as bare water rings (no names, no landmarks) and
  ride the existing wave-8/9 odd-parity conventions in collision and
  dem flattening unchanged.

**Acceptance restated** (replaces the rework note's points 1–3; point 4
— keep all attempt-1 criteria green — stands):
- Unit fixtures: two overlapping same-body relations → one outer kept;
  two adjacent low-overlap bodies → both kept; a shared island inner →
  emitted once; an open inner → skipped + counted; existing water/
  riverbank tests unchanged and green.
- Refetched Sydney committed: the six parity probe points
  (WATER at (277, −918), (92, −365), (−30, −990); LAND at (0, 500),
  (1368, −730) Fort Denison, (−1710, −1139) Goat Island) asserted in
  `tests/sydney.test.ts` via odd-parity over the shipped rings.
- Disjointness invariant: for kept rings ≥ 0.1 km², pairwise 50 m-sample
  overlap ≤ 15 %.
- No other city's committed data changes.


> claimed_by: ds-1 (pid 1487920) at 2026-08-31T22:40:21Z

## Attempt 3 — rework implemented; BLOCKED on a transect regression the locked design cannot avoid

### What I did (per the PM's Answers verbatim, commit `b3e265d` on `tigerteam/T-0116`)
Implemented the water-path fix exactly as answered and verified it against a
real refetch before touching the shipped data:

- `scripts/osm-convert.mjs` — water path now assembles OUTER rings per body
  (standalone ways one group, each relation another, so provenance is kept),
  dedups overlapping outer rings ≥ 0.1 km² by the locked rule (sort |area|
  desc; drop a candidate iff `coverage ≥ 0.85` over its 50 m bbox grid vs
  any already-kept ring), emits INNER members as island rings (only from
  relations with ≥ 1 kept outer; same stitcher; open inner → skipped +
  counted via a new `droppedOpenInnerChains`; residual island centroid dedup).
- `tests/osm-convert.test.ts` — new `water relations` describe with the four
  named fixtures (overlap→1; adjacent→both; shared inner→once; open
  inner→skipped+counted). All pass; existing water/riverbank tests unchanged.
- `docs/data-format.md` — new "Water relations" conversion subsection
  documenting the dedup + inner-island rule.
- Ran `npm run fetch-data:sydney`: `96 water rings` (was 89), single kept
  harbour ring = 13.26 km², Goat Island + Fort Denison emitted as island
  rings, index 849 337 B / 57 tiles / ~6.4 MB ≤ 12 MB.

### Verified GOOD against the refetched data (so the fix works as answered)
- Parity: mid-harbour (277,−918) WATER, CQ cove (92,−365) WATER, under-bridge
  (−30,−990) WATER, CBD (0,500) LAND, Fort Denison (1368,−730) LAND (harbour
  + island ring), Goat Island LAND (harbour + island ring).
- Disjointness: only one ring counters ≥ 0.1 km² → trivially ≤ 15 %.

### BLOCKING finding — the dedup waterlogs the eastern shore (breaks a must-stay-green attempt-1 transect)
With the single kept harbour ring, **Mrs Macquarie's Point (the `sydney.test.ts`
transect point, must stay 0–12 m ASL LAND) now reads WATER** and flattens to
−0.6 m ASL:

```
kept ring 82 (13.26 km²) vs 50 m-grid parity:
  Mrs Macquarie's Pt (1017,−287)   WATER (count 1)   ← was LAND (count 2) in attempt-1
  Royal Botanic Garden (1100,−200) WATER (count 1)
  Woolloomooloo (1200,−600)        WATER (count 1)
  mid-harbour (277,−918)           WATER (count 1)   ← the fix (was LAND) ✓
  Fort Denison (1368,−730)         LAND (count 2) ✓   Goat Island LAND (count 2) ✓
```

Root cause: **both** harbour relations (Port Jackson 484 outer/35 inner,
Sydney Harbour 193 outer/14 inner) close through the open ocean EAST of the
bbox (between North/South Heads), so after the +300 m east clip **both**
outer rings enclose the eastern peninsula (Mrs Mac's Pt / Botanic Garden /
Woolloomooloo) as "inside". In attempt-1 the peninsula read as LAND **only
by the accident of double coverage** (inside both giants → count 2 = even).
Dedup to a single ring fixes the harbour (odd → water) but drops that
accident, so every point that was land-by-double-coverage becomes water —
the eastern shore among them. There is **no** inner-member hole for the
peninsula (I enumerated ALL in-bbox inner rings: only Goat Island 61 004 m²,
Fort Denison 3 030 m², one 10 805 m² east-side islet, plus < 30 m² specks),
and Sydney has **zero** `natural=coastline` ways, so no data source carves
the eastern shore out of the ring. **No single-ring representation can be
both "harbour = water" and "eastern shore = land"** under the locked
converter rules. This is a regression from MY change (not Overpass/DEM
drift), and it fails the attempt-1 transect AC — so I stopped rather than
tune bounds or constants.

I **reverted** the refetched `public/data/sydney/`, `src/data/cities.ts`
sizeBytes and the `tests/sydney.test.ts` parity block (they depend on the
blocked data), and kept only the self-contained converter+tests+doc change
(committed `b3e265d`). Full suite green vs the reverted (attempt-1) data:
`Test Files 50 passed (50), Tests 858 passed (858)`.

## Questions

1. The locked dedup (drop one of two overlapping harbour rings) cannot keep
   the eastern shore as land — Mrs Macquarie's Pt / Botanic Garden /
   Woolloomooloo read land UNTIL dedup, then flip to water. The peninsula has
   no inner ring and Sydney has no coastline ways. How do we represent the
   eastern shore? Options:
   - **A.** Ship the data with the eastern shore as water (would need the
     transect bound relaxed/removed for Mrs Mac's Pt — I was told not to
     touch bounds).
   - **B.** Source a land/hole layer for the eastern shore (e.g. OSM
     `place=island`/`natural=land`/park polygons in the harbour as a new
     converter water/lane for inner-of-infills), widening scope to add
     peninsulas.
   - **C.** Keep BOTH harbour rings but change the parity semantics so
     overlap reads WATER (not land) — affects the shared odd-parity rule for
     every city; needs design sign-off.
2. Note also: the PM's Goat Island probe coordinate (−1710, −1139) is open
   water west of Goat Island (reads WATER, correctly); the real Goat Island
   (OSM way 397373570 `place=islet`) projects onto the emitted 61 000 m²
   island ring near (−1334, −1001) and reads LAND. Please confirm the test
   coordinate should be the real island footprint.
3. If (B): confirm the source layer and that the `natural=wood`/park
   polygons in the harbour (Royal Botanic Garden is `leisure=park`) qualify —
   they already come through the woods path, so the simplest fix might be to
   exclude wood/park rings from the odd-parity water count. Is that
   acceptable?

> handoff: blocked

## Answers

## Answers (3)

Again the right block. Confirmed: attempt-1's eastern shore was
land-by-accident (double cover), and no existing data source carves the
peninsula out of the clipped harbour ring. Decisions:

- **Option A rejected** — Mrs Macquarie's Point is the postcard preset
  (T-0111) and the Botanic Garden is a preset too; the eastern shore
  must be land.
- **Option C rejected** — "overlap = water" is union semantics; it
  destroys island parity everywhere (an island inner ring inside a
  harbour ring is count 2 and would read water).
- **Q3 (parks excluded from parity) rejected** — parks/woods are render
  layers, not hydrology; Woolloomooloo isn't a park anyway.
- **Q2 confirmed** — my Goat Island coordinate was open water; re-point
  the LAND probe to the real islet footprint (≈ (−1334, −1001)) and
  KEEP (−1710, −1139) as an additional WATER probe.

**The fix: per-city curated LAND-CARVE rings (your option B, made
mechanical).** Parity is the only thing the runtime consumes — ring
geometry may overhang land as long as parity nets out (attempt-1 proved
it: the render was fine because water surfaces hide under terrain). So
we carve, we don't re-clip:

1. **Refetch once** (4th and final fetch for this ticket; etiquette as
   before). Keep it uncommitted in the worktree while you work.
2. **Diagnostic scan** on that data, and put the result in the report:
   parity-test every ROAD VERTEX (excluding `bridge: true` roads) and
   every building centroid against the deduped ring set. Cluster the
   wet-land hits (simple ≤ 200 m-linkage grouping is fine). Expected:
   one compact cluster = the eastern peninsula, possibly a couple more
   small shore lobes (check Bennelong Point/the Opera House, Kirribilli,
   Blues Point explicitly — report their parity even if clean).
   **If the wet-land clusters are NOT compact (> 5 lobes or spanning
   whole shores), STOP and block with the cluster map** — that would
   mean the closure artifact is pervasive and the PM will spec
   full-relation-geometry fetching instead.
3. **Carve mechanism** in `scripts/osm-convert.mjs`: a
   `LAND_CARVES: Record<string, ring[]>` table (WGS84 polygons, the
   SHIP_LANES/SUSPENSION_BRIDGES pattern) + a `--carve <key>` flag
   (the sydney alias gains `--carve sydney` — flag it in the report;
   package.json is PM-owned, the PM flips it at accept, use the flag
   explicitly for your fetch). At output time each carve ring is
   appended to `water` (with an aligned placeholder `waterLevels`
   entry) so it flips its interior to land by the ordinary island
   parity — dem flattening then skips those nodes (even parity), which
   is exactly what we want. Implemented as a small exported pure
   function so it is unit-testable without a fetch.
4. **Derive the sydney carve ring(s)** from your scan clusters: convex
   hull of each cluster's points expanded ~40 m, then trimmed so no
   carve edge crosses real water (your judgment, verified by the probe
   set below). Report the final vertex lists.
5. **Final parity set** (asserted in `tests/sydney.test.ts`):
   - WATER: (277, −918), (92, −365), (−30, −990), (−1710, −1139), plus
     one point mid-Farm-Cove and one mid-Woolloomooloo-Bay (the carve
     must not swallow the coves — pick from the data, report coords).
   - LAND: (0, 500), Fort Denison, Goat Island (≈ (−1334, −1001)),
     Mrs Macquarie's Pt (1017, −287), a Botanic Garden point, a
     Woolloomooloo street vertex, a Bennelong Point/Opera House podium
     point, a Kirribilli street vertex.
   - Global: zero non-bridge road vertices read water, modulo a
     reported-and-justified exception list (piers/wharf aprons only).
   - Disjointness invariant from Answers (2) unchanged.
6. Everything from Answers (2) that landed in `b3e265d` (dedup, inner
   islands, fixtures, docs) stays exactly as built — this adds carve on
   top. Docs: extend the new "Water relations" subsection with the
   carve rule + the sydney table's rationale (closure artifact of
   clipped harbour relations).

Done looks like: refetched + carved dataset committed, scan + clusters
+ carve vertices + parity table in the report, all fixtures + the full
suite green, counts line + `sizeBytes` refreshed.


> claimed_by: ds-1 (pid 1487920) at 2026-08-31T23:28:12Z

> claimed_by: ds-1 (pid 1487920) at 2026-08-31T23:28:12Z

## Attempt 4 — BLOCKED on a PERVASIVE wet-land artifact (the carve STOP condition from Answers 3)

### What I did
Per the Answers-(3) plan I ran the 4th (final) Sydney fetch and ran the
mandated **diagnostic scan** on the deduped data before touching any carve
rings. No converter/code change this attempt — the working tree is clean at
the attempt-3 commit `b3e265d`, and the freshly fetched `public/data/sydney/`
has been reverted (it depends on the blocked carve). The tiles from the new
fetch are **byte-identical** to the committed attempt-1 tiles; the only
worktree delta the fetch produced was `index.json`'s water section (the
b3e265d dedup + inner-island change), confirming buildings/roads/trees are
stable between fetches and the whole defect lives in the water rings alone.

Fetch: `npm run fetch-data:sydney` → `20322 buildings, 9818 roads, 106
places, 96 water, 1 rivers, 28962 trees (17333 filled, 0 dropped), terrain
291x347 @ 20 m (0 voids), 6323 KB (skipped 6 relations, dropped 0 open water
chains), 57 tiles (12061 KB tiled total)`. 96 water rings = the single kept
harbour ring + island/inner rings (Goat Island 61 004 m², Fort Denison 3 030
m², etc.).

### The scan result — NOT the compact single-eastern-peninsula case; this is the STOP condition
Scan (per Answers-3 ¶2): odd-parity test of every NON-bridge road vertex and
every building centroid against the deduped 96-ring set.

- **195 wet-land hits** (points that read water but carry a road/building),
  forming **46 separate 200 m-single-linkage clusters**, spanning
  **x ∈ [−2552, +2927], z ∈ [−2607, +1716]** — the entire eastern half of the
  bbox plus western-harbour shore lobes, not one peninsula.
- The single kept harbour ring (**idx 82, 13.26 km², 4 663 verts**, bbox
  x[−2888,+3442] z[−3429,+1597]) encloses a chaotic, deeply-interleaved
  swath: verified-in-ring-82 points include the far-west cluster centre
  (−1006,796), the eastern roads lobe (820,684), Pyrmont (−102,−487), the SE
  suburbs (1977,−2502) and the far north-east (2927,1270). CBD (0,500) and
  Observatory (−500,0) are correctly out.
- Named-area parity (checked explicitly as requested): **Opera podium
  (151.2153,−33.8568) LAND (count 0 ✓ clean), Kirribilli LAND ✓ clean, Blues
  Point LAND ✓ clean** — but Garden Island (1479,−365), Rose Bay (3143,962)
  and Elizabeth Bay (1571,851) read **WATER**, and the ring zigzags through
  those eastern suburbs creating dozens of isolated slivers.
- Answers-(2) parity probes on this data: mid-harbour (277,−918) WATER ✓,
  CQ cove (92,−365) WATER ✓, under-bridge (−30,−990) WATER ✓, west-Goat
  (−1710,−1139) WATER ✓, CBD (0,500) LAND ✓, Fort Denison (1368,−730) LAND ✓
  (harbour+island), Goat Island (−1334,−1001) LAND ✓ (harbour+island). So
  those six WOULD pass — but the **global criterion "zero non-bridge road
  vertices read water" fails ~195 times**, and Answers-3 §5 needs Mrs
  Macquarie's Pt / Botanic Garden / Woolloomooloo as land, all of which read
  water.

### Why I am not carving (the carve was predicated on compactness)
Answers-3 §2 says literally: *"If the wet-land clusters are NOT compact
(> 5 lobes or spanning whole shores), STOP and block with the cluster map —
that would mean the closure artifact is pervasive and the PM will spec
full-relation-geometry fetching instead."* 46 lobes spanning the full eastern
map and the west-harbour shore is the pervasive case. A carve ring per lobe
would (a) overlap actual harbour water and break the six water probes, and
(b) effectively require reconstructing the true coastline — exactly the
mechanism the PM reserved to spec. The flood is inherent to bbox-clipping the
huge Port Jackson `natural=water` polygon whose eastern boundary runs in the
open ocean east of the heads (the peninsulas bounded by ocean on one side and
the harbour on the other end up enclosed; their separating coast is not a
`natural=coastline` way — Sydney has zero coastline ways — and is not an inner
hole of the relation).

### Rerunnable evidence
```
node /tmp/scan.mjs    # wet-land census: 195 hits / 46 clusters, bbox spanned
node /tmp/cluster.mjs # cluster table (top: n=45 @ x[-1126,-886] z[331,1261];
                      #   n=27 @ (820,684); n=18 @ (-102,-487); n=17 @ (-73,-1393);
                      #   n=12 @ (-1,-140); + 41 smaller)
node /tmp/analyze.mjs # ring 82 area/bbox/verts + six-probe parity table
node /tmp/verify.mjs  # named points in-ring82 (flood extent)
node /tmp/named.mjs   # Opera/Kirribilli/Blues clean, Garden/Rose/Elizabeth Bay water
```

## Questions

1. The carve plan's compactness premise does not hold: **46 wet-land clusters
   spanning the whole eastern half of the map**, so a carve ring (convex hull
   of cluster points) cannot separate land from harbour water without
   overlapping real water and breaking the six water probes. This is the
   explicit STOP condition from Answers-3 ¶2. Please spec the
   **full-relation-geometry** path you reserved for this case.
2. Precise mechanism to confirm with the spec: the flood comes from
   bbox-clipping the Port Jackson `natural=water` polygon whose outer ring
   runs east through the open ocean (Sydney has **zero** `natural=coastline`
   ways, and the eastern peninsulas are not inner holes). What is the desired
   water source for the eastern shore — fetch `natural=coastline`
   ways+relations for the peninsula coast, un-clip the harbour ring, or
   something else? 
3. Note for the PM re-checking probes: Opera podium, Kirribilli and Blues
   Point already read LAND (count 0) and are clean; the failures are Garden
   Island/Rose Bay/Elizabeth Bay and the ~195 scattered road/building wet
   points — the global "zero non-bridge road vertices read water" criterion
   is the one that materially fails.

> handoff: blocked

## Answers

## Answers (4)

The STOP was right and the mechanism is confirmed — I re-ran the
coastline census independently against Overpass (0 ways / 0 relations in
the bbox): OSM maps this harbour purely as water polygons whose coastline
closes at the Heads, outside our box. Bbox-clipping a shoreline-hugging
relation boundary can never be parity-correct here. The carve plan is
DROPPED (never built — nothing to remove). Here is the reserved
**full-relation-geometry** spec:

**1. New fetch flag `--water-full 1`** (the sydney alias gains it — flag
in the report, PM flips package.json at accept; pass it explicitly for
your fetch):
- For every `natural=water` / `waterway=riverbank` RELATION matched by
  the existing bbox query, issue ONE follow-up Overpass request for the
  complete member geometry of all those relations together
  (`rel(id ...); way(r); out geom;` — a single batched request, normal
  retry etiquette).
- Assemble each relation's FULL outer and inner rings (the existing
  stitcher), apply NO bbox clip to them, round 0.1 m as usual.
- Standalone water WAYS keep today's path unchanged. Cities without the
  flag are byte-stable on refetch (London's Thames stays clipped).

**2. Everything from `b3e265d` still applies, now on full rings**: the
≥ 0.1 km² coverage dedup (Port Jackson ⊇ Sydney Harbour → one kept
ring; adjacent Parramatta / Lane Cove kept), inner-member islands from
kept relations, centroid island dedup, open chains skipped + counted.
A correct simple polygon is parity-correct in any window — that is the
whole point: peninsulas are excluded by the boundary path itself.

**3. DEM rule**: `waterLevels` samples ring vertices, and full rings
have vertices outside the bbox — the DEM tile set becomes the tiles
touched by (bbox ∪ water-ring vertices). For Sydney everything stays in
`S34E151` (harbour spans ~151.0–151.35 E, −33.79–−33.87 S), so expect
zero new downloads; state the tile set in the report.

**4. Validator**: if `validateCity` rejects out-of-bbox water vertices,
relax that bound for water rings ONLY (scope then includes
`src/data/validate.ts` + its tests; report it). No other element type
may leave the bbox.

**5. Size**: full rings will grow the water section (expect roughly
0.5–1.5 MB). The 12 MB total guard stands; additionally, if
`index.json` alone exceeds 3 MB, STOP and block with the ring
vertex-count table (the PM will then spec out-of-bbox simplification).
Renderer/minimap notes for your report only: out-of-bbox water surfaces
hide under terrain / clip at the canvas — no code change expected.

**6. Acceptance** (replaces the carve items; all else from Answers 2–3
stands):
- The Answers-3 §5 parity set, plus Garden Island (≈ (1479, −365))
  LAND.
- The diagnostic scan re-run on the final data: wet-land hits ≈ 0 —
  every remaining hit individually listed and justified (piers/wharf
  aprons only).
- Dedup/disjointness invariant, Opera House presence test, transects,
  counts line, `sizeBytes`, docs ("Water relations" subsection gains
  the `--water-full` rule and drops any carve wording) — all green on
  the committed data.
- Full suite + run-tests green.

This is the 5th and final fetch of the saga — thank you for the
discipline across it; the scan-before-carve call saved us from shipping
a whack-a-mole table.


> claimed_by: opus-1 (pid 1487920) at 2026-08-31T23:50:48Z

## Attempt 5 — `--water-full` implemented per Answers 4; BLOCKED — the fixed premise is invalidated by real OSM data

### What I did (per the PM's Answers 4, commit `1b7c197` on `tigerteam/T-0116`)
Implemented the full-relation-geometry spec exactly, and verified the
mechanism works before touching the shipped data:

- `scripts/fetch-osm.mjs` — added `--water-full` (bare/`1`/`true`). When
  set: (i) collect the ids of every `natural=water` / `waterway=riverbank`
  RELATION in the primary bbox response; (ii) issue ONE follow-up Overpass
  request `rel(id …); (._; way(r); >;); out geom;` for their complete
  member geometry; (iii) splice the full-geometry way into every relation
  member that references it; (iv) pass `waterFull: true` to
  `convertOverpass`. Standalone water WAYS are untouched.
- `scripts/osm-convert.mjs` — `convertOverpass` accepts `opts.waterFull`.
  When true, the water-relation outer AND inner rings assemble from the
  spliced full geometry and skip `clipRingToBox` (steps 1–3 of the "Water
  relations" subsection are otherwise identical: same dedup, same island
  emission, same island centroid dedup, same open-chain accounting).
  Datasets fetched without `--water-full` are byte-stable on refetch.
- `tests/osm-convert.test.ts` — new named test
  `waterFull skips bbox clipping for RELATION rings only (standalone ways
  still clip)`: a relation whose outer ring vertex sits 20 km east of the
  origin keeps that far-east vertex under `waterFull: true`; a matching
  standalone way is still bbox-clipped. Sanity check that without the flag
  the relation ring gets clipped, no ring reaches the far east.
- `docs/data-format.md` — new bullet **4** in "Water relations" subsection
  documenting the `--water-full` flag, the follow-up query, the "no bbox
  clip" rule, the DEM-tile expectation, and the byte-stable guarantee for
  other cities.

### Refetch: mechanism verified GOOD, parity NOT fixed
`node scripts/fetch-osm.mjs … --water-full 1` (etiquette: 30 s waits
between chunks; no 429s): `20322 buildings, 9818 roads, 106 places,
117 water, 1 rivers, 28962 trees (17332 filled, 0 dropped), terrain
291x347 @ 20 m (0 voids), 6516 KB (skipped 6 relations, dropped 0 open
water chains), 57 tiles (12254 KB tiled total)`.
- index.json = **1 046 883 B (≈ 1.02 MiB)** — well under the 3 MB block.
- Total = **12 254 KB (~ 12 MB)** at the 12 MB guard (57 tiles as before).
- DEM tile set: **S34E151 only** — no new downloads (as the PM predicted).
- The water section grew from 89 → 117 rings and the giant harbour ring
  (idx 82) went from 4 663 verts / 13.26 km² (clipped, attempt-3) to
  **14 943 verts / 51.42 km² (full)** with bbox
  x[-19 907, 8 252] z[-11 876, 1 598] — the outer boundary now closes
  through the true OSM `natural=water` outer, ~ 20 km west and ~ 8 km
  east of the origin. The `--water-full` mechanism works.

### Six-probe parity (Answers 2 + 3 + 4) on the fetched (uncommitted) data
- **WATER (want, pass):** mid-harbour (277, −918) ✓ (in ring 82 only),
  CQ cove (92, −365) ✓, under-bridge (−30, −990) ✓, west of Goat Island
  (−1710, −1139) ✓.
- **LAND (want, mixed):** CBD (0, 500) ✓ (count 0), Fort Denison
  (1368, −730) ✓ (harbour + island 90), Goat Island real (−1334, −1001)
  ✓ (harbour + island 86), Kirribilli (700, −1500) ✓ (count 0).
- **LAND (want, FAIL):** Mrs Macquarie's Pt (1017, −287) **WATER**
  (count 1, ring 82); Botanic Garden (1100, −200) **WATER**;
  Woolloomooloo (1200, −600) **WATER**; Bennelong/Opera (1400, −450)
  **WATER**; Garden Island (1479, −365) **WATER** — all inside ring 82.

### Wet-land census matches attempt-4 exactly (STOP conditions still hold)
Non-bridge road vertices + building centroids parity-tested against the
deduped 117-ring set: **195 wet-land hits, 46 clusters, bbox spanned
x[−2552, +2927] z[−2607, +1716]** — this is Answers-3 ¶2's pervasive-flood
STOP condition, verbatim, and it is the same 46/195 fingerprint I reported
under attempt-4 with the CLIPPED harbour ring. The topology of the harbour
polygon has changed (13 → 51 km², closes through real OSM ocean instead
of the bbox edge), but the parity outcome for the eastern peninsulas is
identical.

### Root cause — OSM data limit, not code
The Answers-4 premise ("A correct simple polygon is parity-correct in any
window — peninsulas are excluded by the boundary path itself") does NOT
hold for OSM's Sydney harbour polygons. Evidence, all rerunnable against
the fresh fetch:

1. Ring 82 is a **simple polygon**: signed area −51.418 km² (CW winding),
   zero consecutive-duplicate vertices, robust point-in-polygon
   (7 different offset probes) unanimously reports Mrs Mac / Bennelong /
   mid-harbour as INSIDE and CBD as OUTSIDE.
2. Ring 82's boundary **does** trace the shoreline near Mrs Macquarie's
   Point in detail (46 vertices within 500 m of the tip, e.g. (1016.6,
   −249.2), (1031.8, −250.8), (1059.7, −246.9) — the ring hugs the water
   line ≈ 40 m north of the LAND tip at (1017, −287)). So the boundary
   IS accurate NEAR the tip.
3. **BUT** ring 82 also contains 367 m straight-line segments that CUT
   ACROSS peninsula bases. Concrete example: segment between vertices
   12792 (1465.3, −43.6) and 12793 (1603.1, −383.5) is a single-OSM-way
   segment 367 m long, projecting onto lon 151.227 → 151.228 lat −33.861
   → −33.858 — this cuts across the Elizabeth-Bay / Woolloomooloo /
   Bennelong-Point peninsula base. Combined with the ocean closure east
   of the Heads (ring extends to x = 8 252 east and closes across),
   the ray from Mrs Mac (1017, −287) due east crosses ring 82 exactly
   3 times (at x ≈ 1 564, 1 786, 5 370) — odd = INSIDE.
4. Sydney Harbour (id 1252425, 26.15 km², 6 030 verts CCW) exhibits
   **the same problem**: its assembled outer big ring also encloses Mrs
   Mac and mid-harbour. So neither harbour polygon is peninsula-accurate,
   and using both together (skipping dedup) makes mid-harbour LAND
   (count 2 = even, wrong) even if it makes Mrs Mac LAND.
5. No other data source in the current pipeline carves the peninsulas:
   `leisure=park`/`natural=wood` polygons in the east cover only the
   parks proper (largest is wood 468 = 13.14 ha, bbox x[627, 1110]
   z[−251, 791]) — none reaches Mrs Mac's tip at z = −287 (the tip is
   Mrs Macquarie's Road, not a park), none covers Garden Island or
   Bennelong Point.

### Why every option under the current spec fails
- **Keep dedup, `--water-full`** (attempted): Mrs Mac / Botanic Garden /
  Woolloomooloo / Bennelong / Garden Island all read WATER — this run.
- **Drop dedup, keep both harbour rings**: Mrs Mac reads LAND (count 2)
  but mid-harbour also reads LAND (count 2). Six-probe fails on
  mid-harbour, CQ cove and under-bridge — worse than attempt-1.
- **Use only Sydney Harbour (smaller polygon)**: SH also encloses the
  peninsulas (evidence: SH big-ring PIP shows Mrs Mac IN, mid-harbour
  IN). No improvement.
- **Curated per-cluster LAND-CARVE (Answers 3)**: STILL 46 clusters
  spanning the whole eastern half of the map (the STOP condition of
  Answers-3 ¶2, verbatim). Convex-hull carves would either overlap the
  real harbour (breaking the WATER probes) or leave dozens of slivers.

### What I did NOT change
Kept commit `1b7c197` (code + tests + docs) — the mechanism is correct
per spec and worth having on the branch for the next attempt. Sydney
`public/data/sydney/` and `src/data/cities.ts` `sizeBytes` **unchanged**
(reverted to the attempt-1 committed state). `tests/sydney.test.ts` was
never modified. Full suite green vs the reverted (attempt-1) data:
`Test Files 50 passed (50), Tests 859 passed (859)` — the new water-full
unit test passes alongside every attempt-1/2 test.

### Rerunnable evidence
```
node scripts/fetch-osm.mjs --bbox 151.183,-33.895,151.245,-33.833 \
  --origin 151.2110,-33.8613 --lang en --dem 1 --dem-bare 1 \
  --chunks 2x2 --water-full 1 --tiles 1 --out public/data/sydney
node /tmp/parity.mjs      # six-probe + Garden Island parity
node /tmp/scan.mjs        # wet-land census: 195 hits / 46 clusters
node /tmp/detailed-pip.mjs  # robust PIP: Mrs Mac / Bennelong IN,
                            # CBD OUT (consistent across 7 offsets)
node /tmp/near-mac.mjs    # ring 82 vertices within 500 m of Mrs Mac
```

## Questions

1. The Answers-4 premise (full-relation geometry → peninsulas excluded by
   the boundary path) does not hold for Sydney: OSM's Port Jackson and
   Sydney Harbour outer boundaries contain long straight-line segments
   (>350 m) that cut across peninsula bases, so the full simple polygon
   genuinely encloses the eastern peninsulas as water. Neither harbour
   polygon is peninsula-accurate, and there is no other single OSM layer
   that carves them (the `leisure=park` polygon set covers only the
   parks proper, not Mrs Mac's tip or Bennelong Point). What next? Options
   as I see them:
   - **A.** Ship Sydney as attempt-1 (unchanged committed data — the
     eastern peninsulas read LAND *by accident* of double-cover, ring
     parity is technically wrong on many edges, but the runtime plays
     fine and the postcard presets work). Accept the ~195 non-bridge
     road vertices reading water as a documented limitation.
     Requires: relax Answers-3 §5 parity criteria and the "wet-land ≈ 0"
     acceptance; do NOT ship the `--water-full` code path (or ship it
     off by default).
   - **B.** Add a per-city LAND-CARVE table anyway, with the wet-land
     cluster centroids as ring seeds (the mechanism Answers-3 proposed).
     46 clusters would mean ~ 46 hand-tuned rings; a small team could
     curate one carve ring per cluster. Not the "elegant simple polygon"
     Answers-4 hoped for, but it does fix parity.
   - **C.** Widen the water-source scope: fetch `natural=coastline` at a
     much larger radius (Sydney has none in the query bbox but the OSM
     mainland coastline does exist further out) and re-derive the
     shoreline from coastline ways instead of the harbour polygon. Big
     scope change (and requires a way to compose coastline rings with
     the harbour polygon).
   - **D.** Punt Sydney's peninsula parity — ship what we have, move on.
2. If **A**, please confirm which parity criteria to drop / relax and
   whether to KEEP or REVERT commit `1b7c197` (I recommend KEEP: the
   `--water-full` mechanism is correct per spec and useful for future
   cities where the OSM harbour polygon IS peninsula-accurate).
3. If **B**, please spec the exact carve derivation (convex-hull of each
   cluster's points, expanded ~ 40 m? or a different smoothing?) and
   whether the seed data lives in `scripts/osm-convert.mjs`
   (`LAND_CARVES: Record<city, ring[]>`) or a JSON alongside the fetch
   alias — the mechanism from Answers-3 §3–4 is intact.
4. If **C**, please spec the coastline radius, the composition rule
   (coastline ways as inner rings of the harbour polygon? emitted as
   separate rings alternating parity?), and confirm which cities are
   affected.

> handoff: blocked

## Answers

## Answers (5)

Your evidence settled it, and I double-checked it upstream: Overpass's
own area engine (`is_in`) puts Mrs Macquarie's Point INSIDE both Port
Jackson and Sydney Harbour, and puts Town Hall outside — the real OSM
polygons genuinely swallow the foreshore peninsulas (label-fidelity
mapping), and your assembly is faithful. Rulings on your options:
**A/D rejected** (the ferries ticket needs true water parity — walkable
harbour and terrain poking through the water mesh are not shippable),
**B rejected** (46 curated rings is a maintenance disaster), **C
rejected on the facts** (the real coastline closes across the Heads
outside the bbox — there is nothing peninsula-shaped to fetch).
**Q2: KEEP both `b3e265d` and `1b7c197`** — dedup, islands and
`--water-full` all remain in the final design.

**The fix — DEM-contoured water rings (fetch-side only, no runtime or
schema change).** The dataset already carries shoreline truth: the
bare-earth terrain grid. Peninsulas read 5–20 m ASL, water ~0. So the
converter replaces each SLOPPY giant ring (and its inner islands) with
rings contoured from the terrain grid, gated by the sloppy polygon:

1. New flag `--water-dem 1` (sydney alias gains it; flag it in the
   report, PM flips package.json at accept; pass it explicitly).
2. After dedup/island emission and after the terrain grid is built but
   BEFORE water flattening, for each kept OUTER ring with |area| ≥
   1 km² (the giants — smaller rings are trusted accurate and pass
   through untouched):
   - `level` = the ring's 10th-percentile DEM value (as today).
   - Node mask over the terrain grid: water iff the node is inside the
     giant ring (plain single-ring test) AND its bare-earth ASL ≤
     `level + 2.0`.
   - Clean-up, in order: one 3×3 majority-vote pass; flip any
     4-connected LAND component ≤ 8 nodes containing zero road
     vertices/building centroids to water (specks on open water); drop
     any WATER component < 6 nodes (puddles).
   - Marching squares at node resolution → boundary rings (the outer
     shoreline AND island holes emerge natively as separate rings —
     Goat Island/Fort Denison are ≥ 8 m ASL, so they carve
     themselves); one Chaikin corner-cut pass to soften the 20 m
     staircase; round 0.1 m.
   - These rings REPLACE the giant ring and its previously emitted
     inner-island rings (the contour's own hole rings take over that
     job — no double-counting). `waterLevels` for each emitted ring =
     `level` of its source body.
3. Everything downstream (flattening, collision parity, render,
   minimap, the ferries' coming water tests) consumes rings exactly as
   today — zero src/ changes.
4. Report: the mask's water-node ASL histogram, ring/vertex counts
   before/after, and any land-holes remaining on open water after
   clean-up. **If open-water holes persist (DEM noise above the
   threshold mid-harbour), STOP and block with the histogram** — the
   threshold is mine to move, not yours.
5. Acceptance (final form): the full parity set — WATER: mid-harbour,
   CQ cove, under-bridge, west-of-Goat, mid-Farm-Cove,
   mid-Woolloomooloo-Bay; LAND: CBD, Fort Denison, Goat Island
   (footprint), Mrs Macquarie's Pt, Botanic Garden, Woolloomooloo,
   Bennelong/Opera podium, Garden Island, Kirribilli, Blues Point —
   plus the global scan (≈ 0 wet non-bridge road vertices, every
   remainder listed and justified), unit fixtures for the mask rules
   (threshold, majority vote, speck/puddle flips, marching-squares
   hole emission — synthetic grids, no fetch), 12 MB guard, counts
   line + `sizeBytes` + the "Water relations" doc subsection gaining
   the `--water-dem` rule, full suite green.
6. No further fetch should be needed if you still have the attempt-5
   fetch output or its raw inputs — the contour step consumes the
   already-fetched rings + terrain. If you must re-fetch, one more is
   authorized (same etiquette).

This closes the design space: rings stay the universal contract, and
where OSM's polygons are label-grade, the DEM — which we already trust
for everything else — supplies the shoreline.


> claimed_by: ds-1 (pid 1487920) at 2026-09-01T00:20:14Z

## Attempt 6 — `--water-dem` implemented per Answers 5; BLOCKED — the 20 m bare-earth DEM erases Sydney's small harbour features, so the locked spec cannot satisfy the AC parity set

### What I did (per Answers 5, commit `e5ca6b4` on `tigerteam/T-0116`)
Implemented the DEM-contoured water-ring spec faithfully and verified it
against a real Sydney fetch + the saved raw Overpass elements (offline
iteration — no repeated Overpass hits).

- `scripts/osm-convert.mjs` — new pure helpers `waterMaskFromRing`,
  `majorityVoteGrid`, `protectedNodesFrom`, `cleanupMask`,
  `traceWaterBoundary`, `chaikin`, `contourWaterRings`, plus the
  `--water-dem` pipeline in `convertOverpass`: each kept OUTER ring with
  |area| ≥ 1 km² is node-masked (`inside giant AND height ≤ level + 2.0`,
  `level` = the ring's 10th-percentile DEM via bilinear sampling, matching
  buildTerrain's formula — verified: my level −4.43 == buildTerrain −4.50
  for ring#82), majority-voted, speck/puddle-cleaned, marched-squares into
  shoreline + island-hole rings, Chaikin'd, and REPLACES the giant + its
  inner islands. Each contour ring's `waterLevels` = its source body's
  level (flattening done in-module by the exact odd-parity rule, so
  `scripts/dem.mjs` stays byte-identical).
- `scripts/fetch-osm.mjs` — `--water-dem 1` flag (bare/1/true), passed as
  `waterDem: true`. Datasets fetched without the flag are byte-stable.
- `tests/osm-convert.test.ts` — `osm-convert water-dem DEM contour
  (T-0116)` describe: mask threshold (inside+≤level+t), outside-ring rule,
  majority-vote lone-node removal + region survival, speck flip (unprotected
  ≤8-node land → water; protected stays), puddle drop (<6 nodes), marching-
  squares single-ring trace + island-hole emission, `contourWaterRings`
  elevated-island parity (island = 2 rings = land, open water = 1), Chaikin,
  and a `convertOverpass` integration test proving `--water-dem` replaces a
  sloppy 16 km² giant with a shoreline + island hole.
- `docs/data-format.md` — "Water relations" rule 4 documents `--water-dem`
  (mask rules, level, replacement, byte-stability, and the blocked-Sydney
  finding below).

Full suite green vs the committed attempt-1 data: `Test Files 50 passed
(50), Tests 869 passed (869)`.

### The mechanism works; the DATA cannot satisfy AC (STOP condition from Answers 4/5 ¶4)
The DEM contour works on the real fetch: the sloppy 13.26 km² Port Jackson
giant is replaced by a 5.78 km² DEM-shoreline ring, the dedup/island rules
from `b3e265d` still apply, and the big peninsulas read LAND correctly.
But the AC parity set fails 4 of 12 probes, and the cause is the DEM data,
not the code or the threshold:

```
waterDem pipeline: water rings=122, main ring 5.78 km²; main level=-4.43 local, threshold level+2=-2.43 (datum 3.90)
PASS W mid-harbour (277,-918)   -> WATER (DEM -0.5 ASL)
FAIL W CQ cove (92,-365)        -> LAND  (DEM 2.0 ASL)     ← want WATER
PASS W under-bridge (-30,-990)  -> WATER
PASS W west-of-Goat (-1710,-1139)-> WATER
PASS L CBD (0,500)              -> LAND  (DEM 66.1 ASL)
FAIL L Fort Denison (1368,-730) -> WATER (DEM -3.5 ASL)    ← want LAND
PASS L Goat Island (-1334,-1001)-> LAND  (DEM 4.0 ASL)
PASS L Mrs Macquarie Pt (1017,-287)-> LAND (DEM 2.7 ASL)
FAIL L Bennelong/Opera (1400,-450)-> WATER (DEM -2.0 ASL)  ← want LAND
FAIL L Garden Island (1479,-365)-> WATER (DEM 0.9 ASL)     ← want LAND
PASS L Kirribilli (700,-1500)   -> LAND  (DEM 3.9 ASL)
PASS L Blues Point (-600,-300)  -> LAND
8 pass, 4 fail (threshold NOT moved; locked constants).
```

Crucially, these are the **original** (unflattened) bare-earth DEM values,
NOT my flattening — I checked the `buildTerrain(waterRings: [])` grid
directly: Fort Denison is a −3.5 m ASL pit (the whole neighbourhood is
−3 to −4 ASL, no elevated node anywhere near), Bennelong Point −2 m,
Garden Island 0.9 m, Circular Quay cove 2.0 m. The T-0108/T-0109 bare-earth
filter (9×9 erode → double 3×3 smooth) has **erased** Sydney's small
harbour points/islands (Fort Denison ~55 m, Bennelong ~100 m, Garden Island)
down to sea level and **raised** the shallow cove above the water line.

**No single elevation threshold can satisfy the set** (CQ cove needs
threshold ≥ 2.0 ASL, yet Fort Denison is −3.5 ASL and Bennelong −2 ASL —
always underwater regardless of threshold). So the locked `--water-dem`
constants cannot produce the AC water/land parity map on this grid. Per
Answers 4/5 ¶4 ("If open-water holes persist… STOP and block — the
threshold is mine to move"), I am blocking rather than tuning constants.

### What I did NOT do
Did not move the threshold/level constants or the majority/speck/puddle
numbers; did not touch `src/` or `scripts/dem.mjs` (out of scope); did not
commit the refetched Sydney data (it fails the parity + transect ACs) — the
worktree `public/data/sydney/` is back at the committed attempt-1 state;
`src/data/cities.ts` untouched. `scripts/fetch-osm.mjs` + `--water-dem`
mechanism + tests + docs are committed (`e5ca6b4`); `b3e265d` (dedup/innner
islands) and `1b7c197` (`--water-full`) remain as the PM ruled.

### Rerunnable evidence
```
node /tmp/evidence.mjs   # AC parity probes vs waterDem pipeline (uses /tmp/sydney-elements.json)
node /tmp/demcheck.mjs   # ORIGINAL bare-earth ASL at each probe + Fort Denison neighbourhood scan
```
(`/tmp/sydney-elements.json` = raw Overpass elements, 56 350, saved offline.)

## Questions

1. The locked `--water-dem` premise — "the bare-earth terrain grid already
   carries shoreline truth" — does not hold at Sydney's small-feature scale:
   the T-0108/T-0109 bare-earth filter has ERASED Fort Denison (−3.5 ASL),
   Bennelong Point (−2 ASL) and Garden Island (0.9 ASL) to/below sea level
   and RAISED Circular Quay cove to 2.0 ASL, so no single elevation
   threshold can satisfy the AC set (CQ cove needs threshold ≥ 2.0 while
   Fort Denison is always underwater regardless of threshold). The big
   peninsulas (Mrs Mac, Goat Is, Kirribilli, Blues Pt, CBD) read LAND
   correctly — the DEM handles the macro shore, not the micro features.
2. How should the ~4 erased small features be restored? Options:
   - **B1 (targeted DEM relief overlay):** contour from a less-smoothed
     elevation source for the water mask only (e.g. the raw SRTM before the
     bare-earth erode, or a finer `step` for the mask) so Fort Denison /
     Bennelong / Garden Island survive and CQ cove stays low. Widens scope
     to a second DEM variant; the big-peninsula correctness must be
     re-verified.
   - **B2 (small-feature carve overlay on the working DEM contour):** keep
     the DEM contour for the macro shore, then force-correct ONLY the
     ~4 erased features via a curated table (CQ cove = forced water; Fort
     Denison / Bennelong / Garden Island = forced land) — the compact,
     ~4-entry LAND-CARVE the Answers-3 46-lobe objection does not apply to.
     Needs the name/geometry source for those features (OSM `place=island` /
     `place=islet` / `natural=coastline` fetch at a larger radius, since the
     bbox has none).
   - **B3:** relax the 4 failing AC parity probes (CQ cove, Fort Denison,
     Bennelong, Garden Island) — but they are the PM's own AC; and a
     walkable Fort Denison / floating Opera House is not shippable per the
     answers.
3. My recommendation: **B2** — it is smallest, keeps the working DEM macro
   contour, and fixes exactly the features the DEM erases, with a compact
   curated overlay (the Answers-5 carve rejection was about 46 whack-a-mole
   lobes, not 4). If you prefer B1, please specify the elevation variant and
   whether it must stay consistent with the rendered bare-earth grid.

> handoff: blocked

## Answers

## Answers (6)

Correct block again — a single elevation threshold provably cannot pass
both the CQ cove (2.0 ASL, smoothed UP by the quay walls) and Fort
Denison (−3.5 ASL, an erode pit). Your B2 instinct (compact targeted
rescue) is right, but no curated geometry is needed: every failing
feature already has an authoritative data source in the pipeline.
**Composite mask rules (replaces the Answers-5 mask step; everything
else in `e5ca6b4` — contour, marching squares, Chaikin, replacement —
stays exactly as built):**

1. **Base threshold raised once, by me: `level + 3.0`** (was + 2.0).
   That puts the CQ cove (2.0 ASL vs cutoff ≈ 2.47 ASL) in the water
   with a real margin instead of a knife-edge.
2. **Majority vote** on that raw mask, as built.
3. **Force-LAND override (new):** any node whose cell (node ± half a
   step) contains a BUILDING centroid or a NON-BRIDGE road vertex is
   land, applied AFTER the majority vote so it cannot be eroded. This
   mechanically rescues Bennelong Point (the Opera House itself),
   Garden Island (naval buildings + roads), Mrs Macquarie's tip
   (Mrs Macquarie's Road), Woolloomooloo, the Botanic paths, and makes
   the CQ wharf strips land (walkable piers — desirable). The
   protected-speck logic you built already half-does this; this makes
   protection generative, not just preservative.
4. **Speck/puddle cleanup** as built (forced nodes count as protected).
5. **Relation-inner island rescue (new):** under `--water-dem`, do not
   drop the kept body's inner-island rings wholesale — emit any inner
   ring whose centroid reads WATER in the final mask (the contour
   missed it). Fort Denison's 3 030 m² inner ring comes back with its
   exact OSM geometry; parity contour(1) + island(1) = land. An inner
   already carved by a contour hole stays dropped (no double ring).
6. Everything else per Answers 5 (waterLevels, replacement, ≥ 1 km²
   gate, byte-stability without the flag).

**Acceptance:** unchanged — the full parity set (all 12 + Farm Cove /
Woolloomooloo Bay WATER probes and the extended LAND list), the global
scan (≈ 0 wet non-bridge road vertices; wharf/pier exceptions now
mostly vanish since piers force land — list what remains), unit
fixtures extended for rules 3 and 5 (synthetic: a building on a
below-threshold node → land; an inner ring on mask-water → emitted;
one on a contour hole → not), the guards, counts line, `sizeBytes`,
docs updated to the composite rule, full suite green.

**Also report:** the margin (ASL vs cutoff) at every AC probe — any
probe passing by < 0.3 m gets a note in data-format.md as a known
fragility; and confirm your offline raw-elements iteration means no
new Overpass hit is needed (if one is, it is authorized).

Rejected alternatives for the record: a curated 4-entry table (your
B2 as literally proposed) loses to the same fixes sourced from data we
already ship — buildings, roads and relation inners are maintained by
OSM, not by us; a second DEM variant (B1) is a two-grid tuning swamp;
relaxing the probes (B3) ships a floating Opera House.


> claimed_by: ds-1 (pid 1487920) at 2026-09-01T01:04:31Z
> RETRY: provider quota/rate limit hit at 2026-09-01T01:18:19Z (no attempt penalty)

> claimed_by: opus-1 (pid 1487920) at 2026-09-01T01:18:24Z

## Attempt 7 — Answers-6 composite mask implemented (rules 1/3/5); BLOCKED — refetch OOMs at 2.5 GB inside the container's 3 GB cgroup memory limit

### What I built (commit `b82082e` on `tigerteam/T-0116`, on top of `e5ca6b4`)
Exactly the Answers-6 composite mask rules — no other converter behaviour changed:

- `scripts/osm-convert.mjs`:
  - Rule 1: `DEM_THRESHOLD` raised **2.0 → 3.0 m** (PM-locked, with the CQ-cove
    rationale in the doc comment).
  - Rule 3 — **force-LAND override** in `contourWaterRings`: applied AFTER the
    majority-vote pass, before speck/puddle cleanup, so any node whose cell
    contains a building centroid or non-bridge road vertex reads land and
    cannot be eroded. Returns the final cleaned mask alongside the boundary
    rings (`{ rings, mask }`) so the giant's post-cleanup mask is available
    for rule 5.
  - Rule 5 — **relation-inner island rescue** in the `waterDem` branch of
    `convertOverpass`: an inner island ring whose centroid lies inside a
    giant is now saved (`islandInGiant[]`) instead of dropped; after the
    per-giant contour it is emitted iff the giant's final mask reads WATER
    at the inner's centroid (contour missed it — e.g. Fort Denison's 3 030 m²
    OSM inner ring). One already carved by a contour hole reads LAND in the
    mask and stays dropped (no double ring). Rescued rings share the giant's
    (10th-percentile OSM) `waterLevels`.

- `tests/osm-convert.test.ts` — three new named tests in the existing
  `osm-convert water-dem DEM contour (T-0116)` describe (per Answers-6's
  "unit fixtures extended for rules 3 and 5" line):
  1. `rule 3: a building/road node below threshold is FORCED LAND after the
     majority vote` (synthetic all-sea-level grid, one protected node reads
     LAND, an unprotected below-threshold node stays WATER).
  2. `rule 5: an inner island ring on mask-WATER is rescued and emitted
     (parity LAND)` (synthetic 4×4 km giant relation with a 300×300 m inner
     island on all-sea-level DEM; the contour carves no hole but the inner
     is rescued, so the island centre reads count=2=LAND while open water
     reads count=1=WATER, and `waterLevels.length === water.length`).
  3. `rule 5: an inner island on a contour HOLE (elevated DEM) is NOT
     double-emitted` (same fixture but DEM elevated over the island → the
     contour carves its own hole; exactly two rings and no third — the OSM
     inner is dropped).

Full unit-test suite green (with the shipped attempt-1 data reverted, i.e.
`public/data/sydney/` unchanged): `Test Files 50 passed (50), Tests 872
passed (872)`. All rule-1/3/5 mechanism tests pass. `tests/sydney.test.ts`
(T-0110 transects + invariants) unchanged.

### BLOCKING finding — the mandated refetch cannot complete inside this container's memory budget

Answers 6 §6 authorised one more fetch. **Every attempt of that fetch —
seven separate `node --max-old-space-size=<H>` runs across H ∈ {8192, 16384,
2200, 2400, 2500, 2600, 2800} — OOMs before the summary line prints.** The
failure mode is deterministic: after 30–160 s (whenever `V8` hits its heap
ceiling), `FATAL ERROR: Ineffective mark-compacts near heap limit` /
`Reached heap limit`, `EXIT=134`. Full stack + GC lines saved for every
attempt (one representative in the evidence block below).

Diagnosis, all rerunnable:

- **Container cgroup memory is capped at 3.0 GB** (`cat /sys/fs/cgroup/memory.max
  → 3221225472`; `memory.high` = `max`; `memory.swap.max` = 3221225472 but V8
  heap does not spill into swap). Peak RSS I ever observed before OOM was
  2.97 GB. This is the ceiling.
- **Node's effective heap ceiling in-container is ~2.5 GB.** `--max-old-space-size
  ≥ 2600` OOMs before that heap is even filled (RSS hits the cgroup wall and
  gets SIGKILL — I confirmed `EXIT=137` at 8192/16384). `--max-old-space-size
  ≤ 2500` OOMs *cleanly* (V8 gives up the heap: `EXIT=134`). Somewhere in
  between there is no room for the operation.
- **The `sydney` fetch with `--water-full 1 --water-dem 1 --chunks 2x2`
  requires more than 2.5 GB of retained heap.** Sources:
  1. `all.push(...res.elements)` accumulates every chunk's parsed elements
     (Sydney bbox — 4 chunks, seam-overlap not yet deduped) in a single array.
  2. The `--water-full` follow-up query `rel(id …); (._; way(r); >;); out geom;`
     returns the **complete member geometry** of Port Jackson (484 outer / 35
     inner), Sydney Harbour (193 / 14), Parramatta River (135 / 7), Lane Cove
     River (74 / 0) — a single JSON response that the previous worker measured
     at 51 km²/14 943-vert output; JSON.parse temporarily 3–5× inflates.
  3. `--water-dem` inside `convertOverpass` builds a second, un-flattened
     terrain grid (`buildTerrain({waterRings: []})` allocates `absolute`,
     `eroded`, two `smooth` passes), a `protectedNodes` mask over ~53 k road
     vertices + 20 k building centroids, per-giant `contourWaterRings` masks,
     and a from-scratch flatten of the height grid over every kept + rescued
     + contour ring.
- **This is a new-since-attempt-5/6 environmental constraint.** Attempts 5
  and 6 (same tigerteam/T-0116 branch, same `--max-old-space-size` numbers)
  both completed the same fetch under this container image. Nothing in
  `scripts/fetch-osm.mjs` or the water pipeline has grown since then; the
  only mechanical delta is my rule-3 override and rule-5 rescue (`+81` lines
  of osm-convert.mjs), which barely allocate.
- **Overpass was extra-hostile today**, likely made worse by the retries
  extending the heap-live window: on top of the OOMs I saw a run of `HTTP
  500 Internal Server Error` (five separate fetches after the OOM window on
  overpass-api.de returned 500 after the first 429/504 retry). This is
  server-side (`kumi` fell over similarly), and Answers-6 etiquette allows
  one more fetch — but no amount of Overpass patience helps when the
  container SIGKILLs before the summary line.

I also tried, before blocking, the memory-side rescues I had within scope:

- **`node --optimize-for-size`** — refused: `--optimize-for-size is not
  allowed in NODE_OPTIONS`.
- **`node --jitless`** — refused: `WebAssembly is not supported in this
  environment, but is required for TypeScript` (Node 22 loader dependency).
- **`--max-semi-space-size=16`** — same OOM at ~2.4 GB.
- **Chunks `2x2` → single-query** — OOM at ~28 s / 2.5 GB (single response
  peak parse cost is higher, not lower).
- **Drop `--water-full`, keep `--water-dem`** — OOM at ~2.4 GB (still needs
  the second `buildTerrain` + flatten).
- **`convertOverpass` flatten loop rewrite** to precompute per-ring bboxes
  and inline the ray-cast so no per-cell `[x, z]` array escapes to old-gen
  — completed and unit-tests still green, then **reverted before commit**
  because the OOM shifted only marginally (2.4 → 2.5 GB) and the extra 60
  lines of hot-path code were not worth carrying if the block remained.
- **Basic fetch with no flags** (`--chunks 2x2`, no `--water-full`,
  no `--water-dem`) — SUCCEEDED, writing the b3e263d dedup output
  (`20322 buildings, 9818 roads, 106 places, 96 water, 1 rivers, 28962 trees
  (17332 filled, 0 dropped), terrain 291x347 @ 20 m (0 voids), 6323 KB
  (skipped 6 relations, dropped 0 open water chains), 57 tiles (12061 KB
  tiled total)`). That output was **immediately reverted** to keep
  `public/data/sydney/` at the attempt-1 committed state (b3e263d alone
  fails the AC — Mrs Macquarie's Point etc. read WATER via dedup, per the
  attempt-3 finding); its output is not shippable.

### What I did NOT do
- No refetched data committed (see above).
- `public/data/sydney/`, `src/data/cities.ts` `sizeBytes`, and
  `tests/sydney.test.ts` are unchanged from attempt-1. The `docs/data-format.md`
  "Water relations" §4 (`--water-dem`) still describes the old level-+2.0 /
  no-force-LAND / drop-inner rule and Sydney's blocked status — updating it
  in place before we know how the fetch step will run would front-run the
  PM's decision. Both docs and Sydney counts should land in the same commit
  as the successful refetch, in a follow-up attempt.

### Rerunnable evidence (all issued from `/home/d/asciicity/.tigerteam/worktrees/T-0116`)

```
cat /sys/fs/cgroup/memory.max                 # 3221225472 (3.0 GB)
cat /sys/fs/cgroup/memory.high                # max
cat /sys/fs/cgroup/memory.swap.max            # 3221225472

# The failure — deterministic across seven heap sizes I tried
NODE_OPTIONS="--max-old-space-size=2500" node scripts/fetch-osm.mjs \
  --bbox 151.183,-33.895,151.245,-33.833 --origin 151.2110,-33.8613 \
  --lang en --dem 1 --dem-bare 1 --chunks 2x2 --water-full 1 --water-dem 1 \
  --tiles 1 --out public/data/sydney
# → EXIT 134
# FATAL ERROR: Ineffective mark-compacts near heap limit / Allocation failed - JavaScript heap out of memory
# Last GC lines: 2496.7 (2536.7) → 2495.7 (2535.7) MB, allocation failure

# Same fetch WITHOUT --water-full / --water-dem SUCCEEDS (~7 min):
node --max-old-space-size=2500 scripts/fetch-osm.mjs \
  --bbox 151.183,-33.895,151.245,-33.833 --origin 151.2110,-33.8613 \
  --lang en --dem 1 --dem-bare 1 --chunks 2x2 --tiles 1 \
  --out public/data/sydney
# → sydney: 20322 buildings, 9818 roads, 106 places, 96 water, 1 rivers,
#            28962 trees (17332 filled, 0 dropped), terrain 291x347 @ 20 m
#            (0 voids), 6323 KB, 57 tiles (12061 KB tiled total)
```

## Questions

1. **Container memory: what changed?** Attempts 5 and 6 (same branch, same
   flags, same worker image) completed the `--water-full 1 --water-dem 1`
   fetch. The container now caps at 3 GB and every retry OOMs at 2.5 GB
   before the summary line. Options (fetch-side):
   - **F1 (preferred if within your reach).** Raise the worker cgroup
     `memory.max` for tigerteam workers (or specifically for this ticket) to
     ≥ 6 GB. This is a one-line infra tweak on your end; the fetch then
     completes in ~3 min and every AC becomes verifiable in a single next
     attempt.
   - **F2.** Modify `scripts/fetch-osm.mjs` (currently OUT of my scope) to
     stream chunks to disk instead of accumulating in memory, or to spawn a
     second Node process for the `--water-full` follow-up + splice so the
     baseline heap is freed before conversion. Please confirm scope
     addition; the change is ~40 lines and reversible.
   - **F3.** Split the pipeline: add a CLI entry to `scripts/osm-convert.mjs`
     (in scope) that re-consumes an already-written `data/<city>/index.json`
     + `.cache/dem/` and applies the composite `--water-dem` step in a
     second Node process; the sydney alias becomes two commands. Answers 6
     §6 hints at this ("the contour step consumes the already-fetched rings
     + terrain"), but rule-5 rescue then can't tell "inner of the giant
     relation" from "small water body" — the provenance is lost in the
     converted rings; please spec the heuristic (I suggest: rings
     ≥ 0.1 km² whose centroid is inside the giant and whose |area| is
     below a threshold count as candidate inners).
2. **Overpass etiquette.** In the block window I saw a stretch of `HTTP 500
   Internal Server Error` on the primary endpoint (five back-to-back after
   429/504 retries; `kumi` also degraded). Under the memory ceiling, each
   retry keeps ~1 GB pinned longer, which then correlates with the OOM. If
   you pick F1/F2, the next authorised fetch should probably be off-peak
   (UTC 04:00–08:00 for Sydney bbox) — I did not spend the etiquette budget
   on more retries.
3. **My in-scope contribution is complete for this saga.** The composite
   mask (rules 1/3/5) matches Answers 6 line-for-line and is covered by
   named tests; the only thing missing is the shipped data. If you pick
   F1/F2 the next attempt is: refetch → run `node /tmp/parity.mjs
   public/data/sydney` (I left the AC probe script in `/tmp/parity.mjs` in
   case another worker inherits the container) → assert parity in
   `tests/sydney.test.ts` → refresh `sizeBytes` and the docs → full-suite
   run. Under 30 minutes of clean-container work.

> handoff: blocked

## Answers

## Answers (7)

**F1 is done.** The mystery has a boring answer: nothing changed —
`workers.memory` was always the fleet default **3g** (confirmed via
`tigerteam config get workers.memory`); attempts 5/6 simply fit under it,
and the composite additions (the protected-node mask over 73 k points,
the retained `{rings, mask}`) plus today's larger, retry-inflated
Overpass responses tipped a pipeline already running at ~83 % of the
ceiling. `tigerteam.toml` now sets `[workers] memory = "6g"` (host has
31 GB; one fetch ticket runs at a time).

Your memory-side triage — seven heap sizes, the flag ablations, and
especially reverting the flatten rewrite when it bought only 0.1 GB —
was exactly right, as was refusing to ship the flagless output.

Next attempt:
1. **First step: verify the ceiling** — `cat /sys/fs/cgroup/memory.max`
   must read ≥ 6 GB. If it still says 3221225472, the supervisor did
   not pick up the config; block immediately with that one line and I
   will restart it — do not burn a fetch against the old cap.
2. Refetch with the full flag set, off-peak as you proposed (your
   etiquette call is approved; one fetch, patient retries).
3. Then your own plan from Question 3, which I confirm verbatim:
   `/tmp/parity.mjs` probes → assert the parity set in
   `tests/sydney.test.ts` → refresh `sizeBytes` + the docs (bring the
   "Water relations" §4 up to the composite rules in the same commit as
   the data, as you correctly deferred) → counts line → full suite →
   review.

No design questions remain open — rules 1/3/5 as landed in `b82082e`
are the final mechanism. F2/F3 are declined for now (F2 is a good
future hardening candidate for a wave-15 ticket, noted; F3's
provenance loss killed it, as you suspected).


> claimed_by: ds-1 (pid 1487920) at 2026-09-01T02:27:48Z
> RETRY: provider quota/rate limit hit at 2026-09-01T03:12:50Z (no attempt penalty)

> claimed_by: opus-1 (pid 1487920) at 2026-09-01T03:12:52Z

## Attempt 8 — refetched with the composite mask; every AC probe green

### Container ceiling verified first (Answers-7 §1 gate)
`cat /sys/fs/cgroup/memory.max` → **6 442 450 944 (6.0 GB)** — supervisor did
pick up the `[workers] memory = "6g"` config. Fetch authorised.

### The refetch (5th and final in the saga)
```
NODE_OPTIONS="--max-old-space-size=5120" node scripts/fetch-osm.mjs \
  --bbox 151.183,-33.895,151.245,-33.833 --origin 151.2110,-33.8613 \
  --lang en --dem 1 --dem-bare 1 --chunks 2x2 \
  --water-full 1 --water-dem 1 --tiles 1 --out public/data/sydney
```
Off-peak (03:15 UTC), one 429 retry (30 s wait), then clean run to summary:
```
sydney: 20322 buildings, 9818 roads, 106 places, 138 water, 1 rivers,
        29131 trees (17501 filled, 0 dropped), terrain 291x347 @ 20 m
        (0 voids), 6673 KB (skipped 6 relations, dropped 0 open water chains),
        57 tiles (12415 KB tiled total)
```
- **index.json = 1 203 530 B ≈ 1.15 MiB** (well under the 3 MB block).
- **index + tiles = 7 087 303 B ≈ 6.76 MiB** (well under the 12 MB guard).
- **DEM tiles: S34E151 only** (as Answers-4 predicted — no new downloads).
- 138 water rings = one DEM-contoured harbour (9.45 km²) + inner-member
  island rings (Fort Denison, Goat Island, Garden Island) rescued via
  Answers-6 rule 5 + smaller ponds/inlets untouched.

### Discovery on the fetched data: the PM's Bennelong/Opera/Garden/Woolloomooloo/Botanic probe coords were wrong for this dataset
Answers 3 §5 called for "a Botanic Garden point / a Woolloomooloo street
vertex / a Bennelong Point/Opera House podium point / a Kirribilli street
vertex" — descriptions, not literal numbers — but attempts 3–6 hard-coded
(1400, −450), (1479, −365), (1200, −600), (1100, −200) as approximations.
Re-checked against the fetched tiles:

- **Sydney Opera House** (relation 9596872) building centroid = **(377.2, −489.2)**,
  bbox x[316.9, 452.0] z[−552.3, −372.7], 57 verts. The (1400, −450) point
  is nowhere near it (350 m NW of a Garden Island Spar Shed at (1574, −137)).
- **Garden Island naval yard**: 44 buildings in x[1400, 1700] z[−500, 200];
  named e.g. `Sawmill / Steam Factory` (1576, −73), `Spar Shed / Torpedo
  Store` (1574, −137). (1479, −365) is off the island in the harbour.
- **Woolloomooloo Finger Wharf** building centroid = **(906.5, +747.4)**
  (Woolloomooloo is south-east of Circular Quay; +z is south — the (1200,
  −600) probe would sit in Woolloomooloo BAY, i.e. water).
- No street named "Woolloomooloo" or "Bennelong"; no building named "Garden
  Island" or "Botanic".

I used the actual building centroids as the AC probes and reported the
coords in the tests (with descriptions in the failure-message strings so a
reviewer can jump to them).

### AC parity — final result on the composite mask
```
WATER probes (want odd parity — 6/6 pass)
  ✓ mid-harbour            (277, −918)      n=1
  ✓ CQ cove                (92, −365)       n=1
  ✓ under-bridge           (−30, −990)      n=1
  ✓ west-of-Goat           (−1710, −1139)   n=1
  ✓ mid-Farm-Cove          (800, −450)      n=1
  ✓ mid-Woolloomooloo-Bay  (1350, −350)     n=1

LAND probes (want even parity — 9/9 pass)
  ✓ CBD                    (0, 500)         n=0
  ✓ Fort Denison           (~1368, −730)    n=2 (harbour + island 93)
  ✓ Goat Island            (~−1334, −1001)  n=0
  ✓ Mrs Macquarie's Point  (1017, −287)     n=0  (force-LAND rescue)
  ✓ Opera House centroid   (377, −489)      n=0  (force-LAND rescue)
  ✓ Woolloomooloo Fw ctd   (906, 747)       n=0
  ✓ Garden Island naval yd (1580, −100)     n=0  (force-LAND rescue)
  ✓ Kirribilli             (700, −1500)     n=0
  ✓ Blues Point            (−600, −300)     n=0
```

**Global wet-land scan (Answers-4 §6 / Answers-6 §5): 14 wet-road hits and
14 wet-building hits out of 53 555 road vertices + 20 322 building
centroids (0.026 % roads, 0.069 % buildings) — down from the attempt-4
baseline of 195 hits / 46 clusters, a 14× reduction.**
Every remaining hit is a genuine over-water feature or an unnamed shore
sliver:
- **Wet roads (14):** 4 × `Sydney Harbour Tunnel` (a real tunnel *under* the
  harbour — surface parity must be water); 3 × (−783.9, 1715.5) and 2 ×
  (−1088.2, 1000) and 3 × (~840, ~815) and 2 × (~9, 1505) — unnamed shore
  segments where OSM maps a road across a wharf apron.
- **Wet buildings (14):** `HMAS Onslow` (submarine museum, floating on
  water — parity is correct); 2 × Fort Denison neighbours at (1351.5,
  −704.1) & (1378.0, −723.4) with n=3 (inside a small satellite contour
  ring on top of harbour + island); `Elephant Tower` (2735, −1871) and
  `Cave Shed` / 2 more in the far NE outside the query bbox proper (marina
  fringe); 5 further unnamed shore-adjacent buildings — the same shoreline-
  sliver class as the roads above.

### Ring disjointness invariant (Answers-6 §5)
Only two rings meet the ≥ 0.1 km² threshold on this data (the 9.45 km²
DEM-contoured harbour and one 0.05 km² inner-island ring), so pairwise
50 m-sample overlap is trivially ≤ 15 % (asserted in the test).

### Files changed (branch `tigerteam/T-0116`, commit `1416490`)
- `scripts/osm-convert.mjs` — kept the composite-mask code from `b82082e`
  (rules 1/3/5 — threshold `level + 3.0`, force-LAND override for building
  centroids + non-bridge road vertices after the majority vote, relation-
  inner island rescue when the mask reads WATER at the centroid) and, in
  the same file, hardened `traceWaterBoundary` from a "always follow the
  first neighbour" chain into the planar face-tracing angular-successor
  rule (with signed-area-summing regression test) so mask fractality
  never balloons a ring or OOMs. Everything downstream is unchanged;
  datasets fetched without `--water-dem`/`--water-full` remain byte-stable.
- `tests/osm-convert.test.ts` — added the named saddle-heavy fractal-mask
  regression test for the new tracer (asserts bounded ring length and
  global closure: Σ signed area == water-pixel area). All prior T-0116
  building-relation, water-relation, water-full, water-dem describes stay
  green untouched.
- `tests/sydney.test.ts` — new `sydney water parity (T-0116 composite
  DEM-contoured mask)` describe with four named tests: WATER probes
  (odd parity), LAND probes (even parity), pairwise disjointness ≤ 15 %
  for kept ≥ 0.1 km² rings, and the global scan asserting <0.05 %
  non-bridge road vertices read water. Kept every T-0110 transect + tile
  invariant test byte-unchanged; the Mrs-Mac ASL transect still passes
  (0–12 m ASL), proving the composite contour preserved land elevation.
- `public/data/sydney/` — refetched (index 1 203 530 B; 55 of 57 tiles
  changed content; 4 corner tiles untouched; total 6.76 MiB ≤ 12 MB
  guard; zero bridge leaks into tiles).
- `src/data/cities.ts` — `sydney.sizeBytes` 914 166 → 1 203 530
  (new index.json size); nothing else changed there.
- `docs/data-format.md` — refreshed the "### Sydney" shipped-counts line
  (new date, new water-ring count 89 → 138, refreshed byte totals, note
  that the alias will flip to `--water-full 1 --water-dem 1` at accept)
  and rewrote the "Water relations" §4 to spell out the five composite-
  mask rules (threshold `level + 3.0`, majority vote, force-LAND
  override, speck/puddle cleanup, relation-inner island rescue) and the
  planar face-tracing marching-squares invariants — replaces the earlier
  "blocked" wording.

### Commands run
- `NODE_OPTIONS="--max-old-space-size=5120" node scripts/fetch-osm.mjs
  --bbox 151.183,-33.895,151.245,-33.833 --origin 151.2110,-33.8613
  --lang en --dem 1 --dem-bare 1 --chunks 2x2 --water-full 1 --water-dem 1
  --tiles 1 --out public/data/sydney` — see summary above.
- `bash .tigerteam/scripts/run-tests.sh` — `Test Files 50 passed (50),
  Tests 877 passed (877)` (up 5 from attempt 7: the 4 new sydney parity
  cases + the saddle-mask regression).
- `bash .tigerteam/scripts/run-tests.sh tests/sydney.test.ts` in isolation
  before running the full suite: `Tests 17 passed (17)` (13 prior +
  4 new).

### Doc changes
- `docs/data-format.md` — Sydney shipped-counts line + full rewrite of
  "Water relations" §4 (`--water-dem`) to the composite rules, as above.

### Package.json note (for the PM — package.json is PM-owned)
The fetch used `--water-full 1 --water-dem 1` explicitly. Per Answers 6
§1 / Answers 5 §1 the `fetch-data:sydney` alias in `package.json` should
be flipped to include those two flags at accept, so a follow-up
byte-stable refetch reproduces the shipped data.

### Reviewer, please look at
- The **AC probe coord change** (see "Discovery" above): the numeric
  probes in Answers 3–6 were approximations that didn't match the
  fetched data (e.g. no Opera House at (1400, −450); no buildings named
  "Garden Island" or "Botanic"). I picked real building centroids from
  the shipped tiles and named them in the tests. If you want the literal
  numbers preserved, they'd fail — they don't correspond to actual
  landmark locations in this dataset.
- The `traceWaterBoundary` planar face-tracing rewrite: adjacent to
  scope but a real correctness fix — the previous "first neighbour"
  tracer could balloon into 10M-vertex rings on saddle-heavy masks
  (each interior corner touching two water pixels diagonally is a
  saddle). The angular-successor rule resolves saddles deterministically
  with water-on-the-left orientation; a signed-area-summing regression
  test guards it. Kept because a shipped mask hitting a saddle would
  reintroduce the previous worker's OOM in a future fetch.
- The two Fort-Denison-neighbour buildings reading n=3 (a small satellite
  contour ring on top of harbour + island): correct parity for those
  centroids is LAND (they're on the island), but the extra ring shifts
  them to n=3. If wave-15 wants to eliminate that, the fix is a
  dedup pass that merges satellite contour rings inside a rescued inner —
  outside T-0116 scope.

### Out-of-scope noticed but not touched
- The wave-15 F2 hardening (stream-to-disk `--water-full` follow-up
  response instead of accumulating in memory) — Answers-7 declined for
  now.
- Fort-Denison-satellite-contour dedup (above).

> handoff: review

## Rework (attempt 5)

## Rework (attempt 9)

The data, parity and design are all ACCEPTED as landed — the PM
independently re-verified the parity table on your worktree (all seven
spot probes exact, incl. Fort Denison n=2 via the rescued ring). One
mechanical gap blocks the merge: **`bash scripts/check.sh` fails at
typecheck** (`tsc --noEmit`), which `run-tests.sh`/vitest does not run.
Log: `.tigerteam/logs/tests/check-20260901-033148-1679349.log`. Two
clusters, both in your latest commit's files:

1. `scripts/osm-convert.d.ts` was never updated (the established
   companion pattern): declare the new exports — `chaikin`,
   `cleanupMask`, `contourWaterRings`, `majorityVoteGrid`,
   `protectedNodesFrom`, `traceWaterBoundary`, `waterMaskFromRing` —
   plus `droppedOpenInnerChains` on the converted-city type and
   `waterFull`/`waterDem` in the `convertOverpass` opts type.
2. `tests/osm-convert.test.ts` internal type errors: the `{r, c, val}`
   vs `{r, c, h}` field mismatch (pick the shape the `.d.ts` declares
   and use it consistently), and the handful of implicitly-`any`
   lambda parameters (`(r)`, `(s)`) — annotate them.

Done looks like: `bash scripts/check.sh` fully green (typecheck + unit
+ build + e2e) in your environment, nothing else changed — no data,
converter, docs or parity edits. State the check.sh line in the report.


> claimed_by: ds-1 (pid 1487920) at 2026-09-01T03:32:25Z

## Worker report (attempt 9 — rework: typecheck-only)

**Summary** — Fixed the sole mechanical gap blocking the merge: `bash
scripts/check.sh` failed at `tsc --noEmit`. No data, converter, docs or parity
edits — exactly per the rework note. Two in-scope files changed; the committed
Sydney data (`1416490`) and its parity/design are untouched.

**Files changed** (branch `tigerteam/T-0116`, isolation worktree; commit
`24cf2a4`)
- `scripts/osm-convert.d.ts` — declared the seven T-0116 water-path exports
  (`waterMaskFromRing`, `majorityVoteGrid`, `protectedNodesFrom`,
  `cleanupMask`, `traceWaterBoundary`, `chaikin`, `contourWaterRings`) with
  typed signatures matching the `.mjs` runtime; added `waterFull?`/`waterDem?`
  to the `convertOverpass` opts type and `droppedOpenInnerChains?` to the
  converted-city return type. Companion to the existing `.d.ts` pattern.
- `tests/osm-convert.test.ts` — standardised the local `grid` bumps shape on
  `{ r, c, val }` (removed the `{ r, c, h }` annotation and the now-unneeded
  cast); retyped the three `bbox` declarations from `Array<number>` to the
  `Bbox` 4-tuple. The implicit-`any` lambda reports (`(r)`, `(s)`) resolved
  themselves once `traceWaterBoundary`/`contourWaterRings` got real return
  types in the `.d.ts` — no body edits needed there.

**Commands run**
- `npx tsc --noEmit` — clean (was 21 errors across the two clusters).
- `bash scripts/check.sh` — `typecheck: ok, unit: ok, build: ok, e2e: ok`
  (log `.tigerteam/logs/tests/check-20260901-033708-123.log`).
- `bash .tigerteam/scripts/run-tests.sh` — `Test Files 50 passed (50), Tests
  877 passed (877)`.

**Doc changes** — none (rework explicitly forbids doc edits; docs were already
landed in attempt 8).

**Commit** — `24cf2a4` on `tigerteam/T-0116`.

**Reviewer, please look at** — the `.d.ts` signatures were written to match
the `.mjs` runtime exactly (e.g. `traceWaterBoundary` returns
`Array<Array<[number, number]>>`, `contourWaterRings` returns
`{ rings, mask }`); no runtime behaviour was touched. `git status` shows only
the two scope files modified.

> handoff: review
