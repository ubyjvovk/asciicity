---
id: T-0048
title: Movement speeds — walk 9 m/s (old sprint), sprint 27 m/s
priority: P1
complexity: C1
depends_on: []
scope: [src/player/controls.ts, tests/controls.test.ts, docs/controls.md]
---

## Goal
User feedback: walking is too slow. Walk becomes the old sprint speed and
sprint three times that: `WALK_SPEED = 9`, `SPRINT_SPEED = 27` (m/s).
Contract already updated: `docs/architecture.md` §4.7.

## Context
- `src/player/controls.ts` exports the two constants; nothing else changes.
- `tests/controls.test.ts` uses the constants symbolically — keep those
  tests untouched and add one explicit assertion of the new values.
- `docs/controls.md`: update every mention of 3 / 9 m/s.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/controls.test.ts` exits 0
- [ ] New test by name: `WALK_SPEED === 9` and `SPRINT_SPEED === 27`; existing tests untouched
- [ ] `bash scripts/check.sh` exits 0 (e2e still moves ≥ 0.5 m); `docs/controls.md` updated
- [ ] No files changed outside scope

## Out of scope
- Fly mode (T-0049), touch, buses.

## Worker report

> claimed_by: ds-1 (pid 484850) at 2026-08-26T22:18:00Z

### Summary
Set walk speed to the old sprint value and sprint to three times that: `WALK_SPEED`
from 3 → 9, `SPRINT_SPEED` from 9 → 27 (m/s). Nothing else in `stepPlayer` changed.

### Files changed
- `src/player/controls.ts` — `WALK_SPEED = 9`, `SPRINT_SPEED = 27`.
- `tests/controls.test.ts` — added an explicit `describe('movement speeds')` block
  with `WALK_SPEED === 9` and `SPRINT_SPEED === 27`; existing tests untouched
  (they reference the constants symbolically).
- `docs/controls.md` — updated both mentions of the 3 / 9 defaults (Exports block
  and Changing sensitivity section).

### Acceptance criteria check
- [x] `bash .tigerteam/scripts/run-tests.sh tests/controls.test.ts` → **exit 0**, 26 passed.
- [x] New tests by name `WALK_SPEED === 9` and `SPRINT_SPEED === 27`; existing tests untouched.
- [x] `bash scripts/check.sh` → **exit 0** (typecheck ok, unit ok, build ok, e2e ok —
      e2e still moves ≥ 0.5 m). `docs/controls.md` updated.
- [x] No files changed outside scope (scope: controls.ts, controls.test.ts, controls.md).

### Commands run
- `bash .tigerteam/scripts/run-tests.sh tests/controls.test.ts` → `Test Files 1 passed (1); Tests 26 passed (26)`.
- `bash scripts/check.sh` → all gates ok.

### Commit
- `7549a9c` on branch `tigerteam/T-0048`.

### Notes for reviewer
- `docs/architecture.md` §4.7 was already updated by the PM as noted in the ticket;
  it is a PM-owned file and was not touched.
- Confirmed by full gate (which runs the e2e move-distance check) that the faster
  speeds still satisfy the e2e threshold.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 1m 43s | 151.43k (116.74k) | 2.63k | $0.0057 | engine | review |

total: 1 attempt · 1m 43s · 151.43k in (116.74k cached) / 2.63k out · $0.0057 (accepted 2026-08-26T22:22:10Z)
