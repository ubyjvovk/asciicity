---
id: T-0026
title: Data-driven landmark spawns — stand on the nearest road facing the named building
priority: P2
complexity: C2
depends_on: [T-0025, T-0028, T-0030]
scope: [src/data/spawn.ts, tests/spawn.test.ts, src/main.ts, docs/integration.md, README.md]
---

## Goal
Hand-picked preset coordinates land in side streets with the landmark out of
frame (`?at=gherkin` today). Resolve landmark presets from the dataset
instead: find the named building, pick a road point about 70 m from its
centroid, and face it.

## Context
- `src/data/spawn.ts` (T-0022): keep `parseAt`, `resolveSpawn`'s signature
  compatibility for the coordinate form, and the +x walk rule. Change
  `SpawnPreset` to `{ building: string; label: string }` where `building` is
  a case-insensitive substring matched against `Building.name`:
  `bank`, `bigben`, `parliament`, `trafalgar`, `embankment` → keep as fixed coordinate presets (T-0028); the default stays `bigben` since
  "Bank" is a place, not a building; `stpauls` → "St Paul's Cathedral";
  `gherkin` → "30 St Mary Axe"; `monument` → "Monument"; `tower` → "Tower of London"
  (may be absent from the bbox — fall back to `bank` when no building
  matches, logging nothing); `barbican` → "Barbican"; `liverpoolst` →
  "Liverpool Street"; `leadenhall` → "Leadenhall Market"; `walkietalkie` →
  "20 Fenchurch Street"; `lloyds` → "Lloyd's". Allow `SpawnPreset` to carry
  either `{ building }` or `{ lon, lat, bearingDeg }` (a discriminated union).
- New pure export `landmarkSpawn(name: string, city: Pick<CityData, 'buildings' | 'roads'>, targetDist = 70): SpawnPoint | null`:
  1. building = first `buildings[]` whose `name` contains `name`
     (case-insensitive); centroid `c` = mean of its `poly`; null if none.
  2. candidate points = every `roads[].pts` vertex (all classes) within
     `[targetDist − 40, targetDist + 60]` metres of `c`; if none, all road
     vertices within 200 m; if still none, return null.
  3. pick the candidate with the smallest `|dist − targetDist|`.
  4. yaw = `atan2(c.x − p.x, −(c.z − p.z))` (faces the centroid; consistent
     with forward = `(sin yaw, −cos yaw)`).
- `resolveSpawn(param, origin, blocked, city?)` gains an optional 4th arg;
  when a preset has `building` and `city` is given, use `landmarkSpawn`,
  falling back to the `bigben` coordinate preset when it returns null.
  `main.ts` passes `city`.
- Update `docs/integration.md` preset table (names → matched building) and
  README.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/spawn.test.ts` exits 0 (existing coordinate/`bank`/blocked-walk cases still pass)
- [ ] New cases with a tiny hand-made city (one named building "Test Tower" as a 20×20 square at (100, 0), roads: a vertex ring at distances 30/70/120 m): `landmarkSpawn('test tower', city)` picks the 70 m vertex and its yaw faces the centroid (forward vector dot direction > 0.99); case-insensitive substring match; unknown name → null; no roads in range → the 200 m fallback, then null; `resolveSpawn('gherkin', ORIGIN, () => false, city)` falls back to bigben when the building is absent
- [ ] `bash scripts/check.sh` exits 0 (incl. `e2e: ok`); JSDoc on every export; docs + README updated
- [ ] No files changed outside scope

## Out of scope
- UI to choose presets, camera animation, changing `geo.ts`.

## Worker report

> claimed_by: ds-1 (pid 3364115) at 2026-08-24T00:38:39Z

**Summary** — Reworked `src/data/spawn.ts` so named-building presets resolve
data-driven from `city.json` instead of hand-picked coordinates; new pure
`landmarkSpawn` finds the named building, picks a road vertex ~70 m away, and
faces its centroid.

**Files changed**
- `src/data/spawn.ts` — `SpawnPreset` is now a discriminated union
  (`{ building, label }` | `{ lon, lat, bearingDeg, label }`); added pure
  `landmarkSpawn`; `resolveSpawn` gained an optional `city` arg and resolves
  building presets via `landmarkSpawn`, falling back to `bigben`. `parseAt`
  unchanged; coordinate form and +x walk rule preserved. New presets
  `walkietalkie` and `lloyds`; `stpauls`/`gherkin`/`monument`/`tower`/
  `barbican`/`liverpoolst`/`leadenhall` now building-based.
- `src/main.ts` — passes `city` as the 4th arg to `resolveSpawn`.
- `tests/spawn.test.ts` — added `landmarkSpawn` suite (70 m pick + yaw faces
  centroid, case-insensitive substring, unknown→null, 200 m fallback, null
  with no roads) and `resolveSpawn`-with-city cases (landmark wins, absent
  building→bigben); updated the old `gherkin`-no-city test to assert the
  `bigben` fallback and the keys/labels test for the two new presets.
- `docs/integration.md` — preset table now shows building↔matched-building
  mapping; fixed-coordinate presets documented with their bearings.
- `README.md` — `?at=` list extended with `walkietalkie`/`lloyds` and a note
  that building presets are data-driven, falling back to `bigben`.

**Commands run**
- `bash .tigerteam/scripts/run-tests.sh tests/spawn.test.ts` →
  `Test Files  1 passed (1)`, `Tests  27 passed (27)`.
- `bash scripts/check.sh` → `typecheck: ok / unit: ok / build: ok / e2e: ok`
  (222 unit tests passed).

**Doc changes** — `docs/integration.md` preset table + data-driven note;
`README.md` URL-parameter list. JSDoc added on every exported function/type/
const in `spawn.ts` (`landmarkSpawn`, `resolveSpawn`, `parseAt`, `SpawnPoint`,
`SpawnPreset`, `SPAWN_PRESETS`).

**Commit** — `aea6bc0` on branch `tigerteam/T-0026` (`[T-0026] Data-driven
landmark spawns`, 5 files, +283/−69).

**For the reviewer** — A minor doc/spec discrepancy in the ticket Context:
"`tower` … may be absent from the bbox — fall back to `bank`". I implemented
and documented the `bigben` fallback instead, because the operative `resolveSpawn`
spec and Acceptance criterion both say "falling back to the `bigben` coordinate
preset when it returns null" (and the acceptance test asserts `gherkin`→`bigben`
when the building is absent). If `bank` was intended for `tower` specifically,
the resolver would need per-preset fallbacks; flagging rather than guessing.
Non-blocking. No out-of-scope issues noticed.

> handoff: review
