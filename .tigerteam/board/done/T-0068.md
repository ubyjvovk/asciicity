---
id: T-0068
title: Gear button only on touch devices; ESC MENU help; credits as a real footer bar below the view
priority: P2
complexity: C1
depends_on: [T-0066]
scope: [src/main.ts, src/style.css, index.html, tests/hud.test.ts, e2e/smoke.spec.ts, docs/integration.md, docs/hud.md]
---

## Goal
User: "there's no way to press the gear icon on desktop" — under pointer
lock nothing is clickable and after Esc the menu overlay covers it, so it
is redundant there. Per the revised `docs/architecture.md` §4.12: show
`#gear` only on touch devices; on desktop the HUD help line ends with
`· ESC MENU`.

## Context
- **Footer bar** (user: "the bottom tagline should be its own footer line
  rather than overlaying the main view"): per the revised §4.12, `#credits`
  becomes a 20 px black bar at the bottom of the page; `#view` gets
  `height: calc(100vh - 20px)` (and `top: 0`), `applySize()` passes
  `window.innerHeight − CREDITS_BAR_PX` to `ascii.setSize`/camera aspect;
  `#gear` and `#toast` sit above the bar (`bottom: 20px + margin`). On the
  ≤ 700 px breakpoint the bar stays 20 px (text may ellipsize). e2e: assert
  the canvas bottom edge ≤ the `#credits` top edge and that `#credits`'s box
  height is 20.
- Detection: the same expression `main.ts` already uses to create
  `TouchControls`; set `gear.hidden = !touch` (and keep the CSS).
- Help line: desktop default becomes
  `'WASD MOVE · MOUSE LOOK · SHIFT RUN · F FLY · R STYLE · ESC MENU'`; the
  touch variant unchanged.
- e2e: the existing `smoke: panels (touch …)` keeps its gear tap; add to the
  desktop smoke: `#gear` is hidden and the HUD contains `ESC MENU`.
- Docs: `docs/integration.md`, `docs/hud.md`.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/hud.test.ts` exits 0 (help-line test updated)
- [ ] `bash scripts/check.sh` exits 0 with the e2e additions (gear hidden on desktop; ESC MENU; canvas/footer non-overlap on desktop and 390×844)
- [ ] Headless evidence: bounding boxes of `#view`, `#credits`, `#gear`, `#toast` on both viewports — no overlap with the bar
- [ ] JSDoc; docs updated
- [ ] No files changed outside scope

## Out of scope
- Any change to the menu itself or to touch behaviour.

## Worker report

> claimed_by: ds-1 (pid 484850) at 2026-08-27T10:27:55Z

**Implementer:** ds-1 · branch `tigerteam/T-0068` · commit `25d1a28`

### Files changed (all in scope)
- `src/main.ts` — desktop help line gains `· ESC MENU`; `gear.hidden = !touch`
  (touch-only detection); defined `CREDITS_BAR_PX = 20` module constant and
  `applySize()` now passes `innerHeight − CREDITS_BAR_PX` to `post.setSize`
  and the camera aspect (viewW/viewH stay full for the full-window tags
  overlay).
- `src/style.css` — `#credits` is now a 20 px black bar across the bottom
  (flex-centred, 11 px monospace, 55 % opacity, the whole bar is the link);
  `#view` is `height: calc(100vh − 20px)` with `top/left: 0`;
  `#gear`/`#toast` moved to `bottom: calc(20px + 16px)` so nothing overlaps
  the bar. `#gear` CSS kept (the `hidden` attribute does the hiding).
- `tests/hud.test.ts` — added `Hud help line` describe: appends the desktop
  line (ends in `ESC MENU`) and the touch line verbatim; fake DOM now
  captures `root.append` args.
- `e2e/smoke.spec.ts` — `panelBoxes` now also reads `#view`/`#toast`; desktop
  `smoke: panels` asserts gear hidden (w=0), credits height 20,
  view/toast-vs-credits non-overlap, HUD contains `ESC MENU`, credits href;
  the desktop menu-open portion was removed because the gear is touch-only
  and headless Chromium cannot exercise pointer-lock Escape (menu navigation
  is covered by the touch `panels`/`fast travel` tests, which keep the gear
  tap). Desktop fast-travel test now asserts the API `travel('lavra')` path
  (URL `at=lavra`) instead of gear→LANDMARKS→Golden Gate; `SPAWN_PRESETS`
  import removed (now unused).
- `docs/integration.md` — gear is touch-only; credits is a 20 px footer bar;
  `CREDITS_BAR_PX` lives in `main.ts`; `#view`/`applySize()` subtract it.
- `docs/hud.md` — help line supplied by `main.ts` (desktop ends `ESC MENU`,)
  touch unchanged; gear touch-only; credits bar layout.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh tests/hud.test.ts` → `Test Files 1
  passed (1)`, `Tests 35 passed (35)`, exit 0.
- `bash .tigerteam/scripts/run-tests.sh` → `Test Files 34 passed (34)`, `Tests
  555 passed (555)`, exit 0.
- `bash scripts/check.sh` → `typecheck: ok · unit: ok · build: ok · e2e: ok`;
  e2e `11 passed` (all green, ~2.0 m).

### Headless bounding-box evidence (printed by e2e, `check-20260827-105624`)
- **desktop 1280×720**: `view {0,0,1280,700}` · `credits {0,700,1280,20}`
  (h=20; view bottom 700 ≤ credits top 700) · `gear {0,0,0,0}` (hidden) ·
  `toast {16,650,157,34}` (bottom 684 < 700). No overlap with the bar.
- **touch 390×844**: `view {0,0,390,824}` · `credits {0,824,390,20}`
  (h=20; view bottom 824 ≤ credits top 824) · `gear {334,768,40,40}`
  (bottom 808 < 824) · `toast {16,774,157,34}` (bottom 808 < 824). No
  overlap with the bar.

### Acceptance walkthrough
- [x] `run-tests.sh tests/hud.test.ts` exits 0 (help-line test added) — 35
  tests.
- [x] `check.sh` exits 0; e2e asserts gear hidden on desktop / ESC MENU in
  HUD / canvas+footer non-overlap on desktop and 390×844 (credits height 20).
- [x] Headless evidence above (boxes captured from the e2e log).
- [x] JSDoc on `CREDITS_BAR_PX`; docs/integration.md + docs/hud.md updated.
- [x] Changed files are exactly the 6 in scope: main.ts, style.css,
  tests/hud.test.ts, e2e/smoke.spec.ts, docs/integration.md, docs/hud.md.

### Notes for the reviewer
- `src/credits.ts` and `src/hud/hud.ts` are **not** in this ticket's scope, so
  `CREDITS_BAR_PX` was placed in `main.ts` (a module const) and the Hud
  constructor default help line was left untouched — the desktop help string
  is passed by `main.ts`. If the PM prefers `CREDITS_BAR_PX` exported from
  `credits.ts` per architecture.md §4.12, that would require widening the
  scope to include `src/credits.ts`.
- Desktop menu-open coverage (gear tap) was moved to the touch tests because
  the gear is now touch-only and headless Chromium cannot acquire pointer
  lock (Escape) — this is the intended behaviour change of the ticket.
- No files changed outside scope.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 31m 58s | 6.6M (6.46M) | 53.52k | $0.14 | engine | review |

total: 1 attempt · 31m 58s · 6.6M in (6.46M cached) / 53.52k out · $0.14 (accepted 2026-08-27T11:02:35Z)
