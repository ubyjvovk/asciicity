---
id: T-0077
title: Converter — map highway=motorway/motorway_link to primary; re-fetch sf.json so the Golden Gate Bridge roadway exists
priority: P1
complexity: C2
depends_on: []
scope: [scripts/osm-convert.mjs, scripts/fetch-osm.mjs, tests/osm-convert.test.ts, tests/sf-data.test.ts, public/data/sf.json, docs/integration.md]
---

## Goal
The Golden Gate Bridge roadway is `highway=motorway` (OSM ways 537838948,
595194543, 1321812310; `bridge=yes`, 3–4 lanes) and the converter drops it,
so the bridge is only its two sidewalks. Map `motorway` and `motorway_link`
to `cls: 'primary'` (contract: `docs/data-format.md` "Roads — highway
mapping", wave-9 note) and re-fetch `public/data/sf.json` so the deck
carries a road (and buses).

## Context
- `scripts/osm-convert.mjs` `ROAD_CLASS` (≈ line 49): add `motorway:
  'primary'`, `motorway_link: 'primary'`. The `bridge` handling for mapped
  classes already exists (`bridge: true` when `bridge≠no`).
- `scripts/fetch-osm.mjs` line ≈ 45 builds the Overpass `highway` regex —
  add `motorway|motorway_link` to it (same regex is quoted in
  data-format.md; the PM already updated the prose there — do not edit
  data-format.md).
- Re-fetch: `npm run fetch-data:sf`. Guard: if the JSON exceeds 16 MB stop
  and file a blocked question. Expect ≈ +0.1 MB over the committed
  14 638 126 bytes. Record the fetch summary line and the counts in the
  report. Do NOT re-fetch London/Kyiv (their bboxes hold no motorways;
  their files must not change).
- NEW `tests/sf-data.test.ts`: read `public/data/sf.json` (see how
  `tests/landmarks.test.ts` loads it) and assert: at least one road named
  exactly `Golden Gate Bridge` with `cls === 'primary'` and `bridge ===
  true`; every such road's points fall inside the sf bbox; the two sidewalk
  roads still exist (`Golden Gate Bridge East Sidewalk` / `West Sidewalk`,
  `pedestrian`, `bridge: true`).
- `tests/osm-convert.test.ts`: add a unit case that a `motorway` way with
  `bridge=yes` converts to `cls: 'primary', bridge: true`, and
  `motorway_link` to `primary`.
- `docs/integration.md`: update the SF counts sentence if present; one line
  noting the roadway is now a `primary` bridge road.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/osm-convert.test.ts tests/sf-data.test.ts` exits 0
- [ ] Tests by name: `'maps motorway and motorway_link to primary (bridge flag kept)'`, `'sf.json carries the Golden Gate Bridge roadway as a primary bridge road'`, `'sf.json still carries both Golden Gate Bridge sidewalks'`
- [ ] `bash scripts/check.sh` exits 0
- [ ] `public/data/sf.json` re-fetched and committed, ≤ 16 MB; report has the fetch summary line, counts, and byte size; `public/data/city.json` and `public/data/kyiv.json` unchanged (`git diff --stat` shows neither)
- [ ] JSDoc on changed exports; docs/integration.md line added
- [ ] No files changed outside scope (`package.json`, `src/**` must show no diff)

## Out of scope
Any bridge geometry (T-0080), ships (T-0081), traffic tuning, other cities.

## Worker report

> claimed_by: ds-1 (pid 3900) at 2026-08-28T22:46:05Z

## Worker report

**Files changed (all in scope):**
- `scripts/osm-convert.mjs` — added `motorway: 'primary'` / `motorway_link: 'primary'` to `ROAD_CLASS`; the existing per-element `bridged` logic already sets `bridge: true` for `bridge≠no` moved roads.
- `scripts/fetch-osm.mjs` — added `motorway|motorway_link` to the Overpass `highway` regex so the GGB roadway is fetched.
- `public/data/sf.json` — re-fetched with `npm run fetch-data:sf` (GGB roadway now present as three `primary` bridge roads).
- `tests/osm-convert.test.ts` — added `motorway`/`motorway_link` rows to the mapping-table test; added unit case `'maps motorway and motorway_link to primary (bridge flag kept)'` (motorway+bridge=yes → `cls: 'primary', bridge: true`; motorway_link → `primary`, no bridge key).
- `tests/sf-data.test.ts` (new) — asserts the GGB roadway is a `primary` bridge road, all its points fall inside the sf bbox, and both sidewalks survive; also a ≤16 MB + validateCity check.
- `docs/integration.md` — added a line noting the GGB roadway is now a `primary` bridge road (carrying buses) alongside the pedestrian sidewalks.

**Commands run / results:**
- `npm run fetch-data:sf` → `sf.json: 44181 buildings, 11267 roads, 92 places, 45 water, 1 rivers, 34037 trees (28153 filled, 0 dropped), terrain 461x396 @ 20 m (0 voids), 14347 KB (skipped 14 relations, dropped 0 open water chains)`
- `bash .tigerteam/scripts/run-tests.sh tests/osm-convert.test.ts tests/sf-data.test.ts` → exit 0; `Test Files 2 passed (2)`, `Tests 73 passed (73)`
- `bash scripts/check.sh` → `typecheck: ok / unit: ok / build: ok / e2e: ok` (19 e2e passed)

**Counts / sizes:**
- sf.json = **14 691 673 bytes** (≈ 14.3 MB, well under the 16 MB guard), up +53 547 bytes over the committed 14 638 126 (~+0.05 MB; ticket estimated ~+0.1 MB — the motorway records are small).
- Roads: 10 935 → **11 267** (+332 motorway/motorway_link records).
- `public/data/city.json` and `public/data/kyiv.json` unchanged (`git diff` shows neither).

**Doc changes:** `docs/integration.md` §San Francisco presets — one line noting the roadway is now a `primary` bridge road (buses ride the deck). No data-format.md edits (PM-owned prose already updated).

**Commit:** `a36ada4` on branch `tigerteam/T-0077`. Scope-clean (only the 6 in-scope files changed; no `package.json`/`src/**` diffs).

**Reviewer should check:** from `?city=sf&at=ggb` the deck should now carry a road (and buses); the two sidewalk corridors still exist as `pedestrian` bridges. No out-of-scope issues noticed.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 5m 35s | 1.51M (1.42M) | 10.06k | $0.04 | engine | review |

total: 1 attempt · 5m 35s · 1.51M in (1.42M cached) / 10.06k out · $0.04 (accepted 2026-08-28T22:54:48Z)
