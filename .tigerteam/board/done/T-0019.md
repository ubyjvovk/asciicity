---
id: T-0019
title: Ignore the pointer-lock entry mouse jump and spike deltas in Controls
priority: P1
complexity: C1
depends_on: [T-0013]
scope: [src/player/controls.ts, tests/controls.test.ts, docs/controls.md]
---

## Goal
When pointer lock is acquired, Chromium delivers a first `mousemove` whose
`movementX/Y` equals the jump from the last pointer position (observed:
640/360 px in headless tests) — the camera spins on every first click.
Ignore the first `mousemove` after lock acquisition and any single-event
delta beyond a spike limit.

## Context
- `src/player/controls.ts` (T-0008/T-0013). Keep the public API unchanged.
- Add an exported pure helper `isMouseSpike(dx: number, dy: number, limit = 300): boolean`
  (true when `|dx| > limit || |dy| > limit`).
- `Controls`: listen for `document` `pointerlockchange`; when
  `document.pointerLockElement === target` set a private `skipNextMove = true`.
  In `onMouseMove` (while locked): if `skipNextMove`, clear it and return
  without accumulating; else if `isMouseSpike(e.movementX, e.movementY)`
  return without accumulating; else accumulate. Remove the listener in
  `dispose()`.
- Document the behaviour in `docs/controls.md` ("Pointer-lock entry" paragraph).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/controls.test.ts` exits 0 with all existing cases untouched
- [ ] New cases: `isMouseSpike(640, 360)` true; `isMouseSpike(20, -15)` false; `isMouseSpike(301, 0)` true; `isMouseSpike(0, 300)` false (limit is exclusive); custom limit honoured
- [ ] Code review: `pointerlockchange` handler + `skipNextMove` + spike check present and the listener removed in `dispose()`
- [ ] `docs/controls.md` updated; `bash scripts/check.sh` exits 0; no files changed outside scope

## Out of scope
- Sensitivity changes, smoothing/acceleration, touch input.

## Worker report

> claimed_by: grok-1 (pid 3364115) at 2026-08-23T23:28:37Z

### Files changed
- `src/player/controls.ts` — exported `isMouseSpike`; `Controls` now skips the first locked `mousemove` after `pointerlockchange` and discards spike deltas; listener removed in `dispose()`.
- `tests/controls.test.ts` — five new `isMouseSpike` cases (existing `stepPlayer` / `yawToBearingDeg` / `axesFromHeld` cases untouched).
- `docs/controls.md` — documented `isMouseSpike` in Exports and added the "Pointer-lock entry" paragraph.

### Commands run
`bash .tigerteam/scripts/run-tests.sh tests/controls.test.ts` (exit 0):
```
 ✓ tests/controls.test.ts (24 tests) 34ms

 Test Files  1 passed (1)
      Tests  24 passed (24)
   Start at  23:29:38
   Duration  861ms (transform 230ms, setup 0ms, import 305ms, tests 34ms, environment 0ms)
```
19 existing cases + 5 new `isMouseSpike` cases.

`bash scripts/check.sh` (exit 0):
```
typecheck: ok
unit: ok
build: ok
e2e: skipped (no e2e/)
log: .tigerteam/logs/tests/check-20260823-232942-157.log
```

### Doc changes
`docs/controls.md`: added `isMouseSpike` to the exports block; noted `pointerlockchange` in `dispose()`; new **Pointer-lock entry** section covering skip-first-move and exclusive spike limit 300.

### Commit
- hash: `1618a90d19d91733230e255e5b084a1583915ebc`
- branch: `tigerteam/T-0019`

### Reviewer notes
- Public `Controls` / `stepPlayer` API unchanged aside from the new exported helper.
- Spike check is exclusive of the limit (`300` is not a spike); `skipNextMove` is set only when lock is acquired on `target`, not cleared on unlock (next acquire sets it again).
- `check.sh` skipped e2e because this worktree has no `e2e/` directory — out of scope, not touched.
- Out of scope (untouched): sensitivity, smoothing/acceleration, touch input.

> handoff: review
