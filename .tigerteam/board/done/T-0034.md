---
id: T-0034
title: Sky — astronomically correct Sun and Moon discs, stars at night, ?time= override
priority: P1
complexity: C2
depends_on: [T-0033]
scope: [src/world/sky.ts, tests/sky.test.ts, src/main.ts, docs/world.md, docs/integration.md]
---

## Goal
Put the Sun and Moon where they really are over London (no relighting — just
visible discs on the black sky), plus ~300 faint stars when the sun is down.
Time defaults to the real clock; `?time=` pins it for testing.

## Context
- Coordinates: architecture.md §3. Azimuth is degrees clockwise from north;
  a direction from (azimuthDeg A, altitudeDeg h) is
  `dir = (sin A·cos h, sin h, −cos A·cos h)` (degrees → radians first).
- New pure module `src/world/sky.ts`:
  1. `export function sunPosition(date: Date, latDeg: number, lonDeg: number): { azimuthDeg: number; altitudeDeg: number }`
     — NOAA low-precision, implement exactly:
     `jd = date.getTime()/86400000 + 2440587.5`; `n = jd − 2451545.0`;
     `L = (280.460 + 0.9856474·n) mod 360`; `g = (357.528 + 0.9856003·n) mod 360` (radians for trig);
     `λ = L + 1.915·sin g + 0.020·sin 2g`; `ε = 23.439 − 4e-7·n`;
     `δ = asin(sin ε·sin λ)`; `α = atan2(cos ε·sin λ, cos λ)`;
     `GMSThours = (18.697374558 + 24.06570982441908·n) mod 24`;
     `H = (GMSThours·15 + lonDeg − α_deg)` normalised to (−180, 180];
     `alt = asin(sin φ·sin δ + cos φ·cos δ·cos H)`;
     `az = (atan2(sin H, cos H·sin φ − tan δ·cos φ)·180/π + 180) mod 360`.
  2. `export function moonPosition(date: Date, latDeg: number, lonDeg: number): { azimuthDeg: number; altitudeDeg: number; fraction: number }`
     — low-precision lunar: with the same `n`:
     `Lm = 218.316 + 13.176396·n`; `Mm = 134.963 + 13.064993·n`; `F = 93.272 + 13.229350·n`;
     `λm = Lm + 6.289·sin Mm`; `βm = 5.128·sin F`;
     equatorial via `δ = asin(sin βm·cos ε + cos βm·sin ε·sin λm)`,
     `α = atan2(sin λm·cos ε − tan βm·sin ε, cos λm)`; then alt/az as above.
     `fraction = (1 − cos(λm − λsun)) / 2` with `λsun` from the sun formulas.
  3. `export function makeSky(date: Date, origin: { lat: number; lon: number }, seed = 5): THREE.Group`
     and `export function updateSky(group: THREE.Group, date: Date, origin): void`:
     sun = `CircleGeometry(45, 24)` `MeshBasicMaterial({ color: 0xfff2b0, fog: false })`
     placed at `dir·1200` looking at the origin (`mesh.lookAt(0,0,0)`), visible
     only when `altitudeDeg > −2`; moon = `CircleGeometry(32, 24)` colour
     `0xd9dbe4`, material `opacity = 0.25 + 0.75·fraction` (transparent),
     same placement rule; stars = one `THREE.Points` of 300 mulberry32-seeded
     directions with altitude 5°–85°, radius 1300, `PointsMaterial({ size: 3, color: 0x9fb4c8, fog: false, sizeAttenuation: false })`,
     `visible` only when sun altitude < −6. `updateSky` moves/toggles the
     existing children — no allocation.
- `main.ts`: `parseUrlOptions` gains `time: Date | null` (accept ISO strings
  and `HH:MM` meaning today local; invalid → null); build the sky with
  `opts.time ?? new Date()` and `city.origin`; `setInterval` every 10 s calls
  `updateSky` with (fixed time if pinned, else `new Date()`).
- Docs: `docs/world.md` Sky section (formulas cited, accuracy note ±2°),
  `docs/integration.md` (`time` URL row, update cadence).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/sky.test.ts` exits 0
- [ ] `tests/sky.test.ts` covers (London 51.5074, −0.1278, tolerance ±2.5°): 2026-06-21T12:00Z → sun alt ≈ 61.9, az ≈ 180 (±6° az); 2026-12-21T12:00Z → alt ≈ 15.1; 2026-06-21T00:00Z → alt < −10; azimuth in [0,360) and increasing through the morning (09:00Z < 12:00Z az… assert 09:00Z az < 12:00Z az); `moonPosition` fraction always in [0,1] across 30 daily samples and spans < 0.15 and > 0.85 at least once in 30 days; moon altitude finite
- [ ] `makeSky`/`updateSky` reviewed: fog disabled, placement radii, visibility rules, no per-call allocation in `updateSky`
- [ ] `bash scripts/check.sh` exits 0 (incl. `e2e: ok`); JSDoc on every export; both docs updated
- [ ] No files changed outside scope

## Out of scope
- Clouds, relighting/ambient changes, gloom-mode special-casing, moon phase *shape* (disc brightness only).

## Worker report

> claimed_by: ds-1 (pid 3475038) at 2026-08-24T13:06:53Z

### Files changed (all in scope)
- `src/world/sky.ts` (new) — pure NOOA low-precision `sunPosition`/`moonPosition`
  (formulas exactly as specced) plus `makeSky`/`updateSky` wrappers: always-fogless
  sun disc (`CircleGeometry(45,24)`) and moon disc (`CircleGeometry(32,24)`,
  opacity `0.25 + 0.75·fraction`), and a `THREE.Points` of 300 mulberry32-seeded
  stars (seed 5, alt 5°–85°, radius 1300).
- `tests/sky.test.ts` (new) — covers every named acceptance case for sun
  positions, moon fraction bounds/spans/finiteness, and the makeSky/updateSky
  contract (fog off, radius 1200, visibility rules, no child reallocation).
- `src/main.ts` — `UrlOptions.time: Date | null`, `parseTimeParam` (ISO or
  `HH:MM` today-local, invalid → null), builds the sky with
  `opts.time ?? new Date()` and `city.origin`, and a 10 s `setInterval` calling
  `updateSky` with fixed-or-live time.
- `docs/world.md` — new **Sky** section (formulas cited, ±2° accuracy note,
  child/material/placement/visibility table, no-allocation updateSky).
- `docs/integration.md` — `time` URL row in the table and a **Sky cadence**
  note (10 s interval, pinned-vs-live).

### Commands run
- `bash .tigerteam/scripts/run-tests.sh tests/sky.test.ts` → `exit: 0`;
  `Test Files  1 passed (1)   Tests  11 passed (11)`.
- `bash scripts/check.sh` → `typecheck: ok`, `unit: ok`, `build: ok`,
  `e2e: ok`.

### Doc changes
- `docs/world.md` Sky section; `docs/integration.md` `time` row + cadence note.

### Commit
- `edb31cf` on branch `tigerteam/T-0034` — `[T-0034] Sky — astronomically
  placed sun/moon discs, stars at night, ?time= override`.

### For the reviewer
- Formulas pre-validated in node before writing: 2026-06-21T12:00Z alt 61.92 /
  az 178.86, 2026-12-21T12:00Z alt 15.06, 2026-06-21T00:00Z alt −15.06, morning
  az increasing (111 < 179), moon fraction spans 0.0002–0.9999 over 30 days —
  all within acceptance tolerances.
- `updateSky` repositions the existing sun/moon meshes and toggles visibility/
  opacity; stars are a static geometry (only visibility toggles) — no per-call
  allocation. Children are stashed in `group.userData` and read back via a cast
  to a local `SkyChildren` interface (no `any`).
- Out of scope, not touched: gloom/relighting (sky is unlit basic material per
  spec), moon phase *shape*, clouds. The sky discs sit below the camera far
  plane (2000) — radius 1200/1300.
- Visual/GPU placement not verified by me (no GPU in this worker); verified via
  the unit tests + `__asciicity`/HUD path instead — PM GPU review recommended.
> handoff: review

## Rework (attempt 2)

PM verification on the GPU: aimed frames at the computed sun azimuth
(2026-08-24T12:00 local, az≈158, alt≈45, camera yaw 152° pitch 31.5°) and at
the night sky show NO sun/moon/stars. Root cause is a **spec bug in the
ticket, not your implementation**: the Context said to place discs at
`dir·1200` (implicitly from the world origin) — but the origin is Bank and
the player spawns 2.7 km west on Westminster Bridge, so the discs are up to
3.9 km from the camera: outside the 2000 m far plane (culled) and at the
wrong bearing. Your formulas and tests are correct; keep them.

What done looks like (spec correction):
1. The sky `THREE.Group` must ride with the camera. In `main.ts`'s frame
   loop add exactly one no-allocation line after the camera update:
   `sky.position.set(state.x, EYE_HEIGHT, state.z);`
   Children keep their `dir·radius` positions relative to the group, so the
   discs stay 1200 m from the player everywhere on the map.
2. In `updateSky`, discs must face the group centre, not the world origin:
   replace `sun.lookAt(0, 0, 0)` / `moon.lookAt(0, 0, 0)` with a lookAt at
   the group's world position (e.g. `group.getWorldPosition(tmp)` into a
   module-level reused `THREE.Vector3`, then `sun.lookAt(tmp)`). No
   allocation per call.
3. Add one unit test: after `group.position.set(100, 0, 100)` and
   `updateGroupWorldMatrix`, `updateSky` leaves each visible disc's world
   position within `radius ± 1` of the group position (use
   `getWorldPosition` in the test).
4. Everything else (formulas, tests, docs) stays. Update
   `docs/world.md`/`docs/integration.md` only where they describe placement.


> claimed_by: grok-1 (pid 3475038) at 2026-08-24T13:16:23Z

## Worker report

### Files changed (all in scope)
- `src/world/sky.ts` — discs `lookAt` the group's world position via a
  module-level reused `THREE.Vector3` (`_lookAtTarget`); `direction()` now
  writes into a reused `_dir` so `updateSky` allocates no Vector3s. Children
  still sit at `dir·radius` relative to the group.
- `src/main.ts` — one no-allocation line after the camera update:
  `sky.position.set(state.x, EYE_HEIGHT, state.z)` so the sky group rides
  with the camera.
- `tests/sky.test.ts` — new case `visible discs stay at radius from a moved
  group`: `position.set(100,0,100)` + `updateMatrixWorld` + `updateSky`, then
  each visible disc's `getWorldPosition` is within `1200 ± 1` of the group.
- `docs/world.md` — Sky placement now describes camera-follow + lookAt group
  world position (formulas unchanged).
- `docs/integration.md` — frame-loop sky follow line + cadence note that the
  group rides with the camera.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh tests/sky.test.ts` → `exit: 0`;
  `Test Files  1 passed (1)   Tests  12 passed (12)`.
- `bash scripts/check.sh` → `typecheck: ok`, `unit: ok`, `build: ok`,
  `e2e: ok`.

### Doc changes
- `docs/world.md` Sky section: group rides with camera, `dir·radius`
  relative to the group, `lookAt(group world position)`.
- `docs/integration.md`: camera-follow line in the frame loop; cadence note.

### Commit
- `d11467f` on branch `tigerteam/T-0034` — `[T-0034] Sky group rides with
  the camera so discs stay in view`. (Prior attempt: `edb31cf`.)

### For the reviewer
- Spec correction only: formulas/visibility/fog/radii unchanged. The discs
  are now 1200 m from the *player* (group at eye position) rather than from
  Bank, so they stay inside the 2000 m far plane on Westminster Bridge.
- `updateSky` gets the group world position once into `_lookAtTarget` then
  `sun.lookAt` / `moon.lookAt` that vector. `direction()` reuses `_dir`.
- New test uses `updateMatrixWorld()` — three.js has no
  `updateGroupWorldMatrix`.
- Visual/GPU placement still not verified by this worker (no GPU); unit
  tests confirm world-space radius after a 100,0,100 group move. PM GPU
  review recommended (same 2026-08-24T12:00 local / night frames as the
  rework note).
- Out of scope, not touched: gloom/relighting, moon phase shape, clouds.

> handoff: review
