---
id: T-0065
title: Trees data — OSM tree nodes/rows + seeded wood/park fills → trees/woods in both datasets; validator
priority: P1
complexity: C2
depends_on: []
scope: [scripts/fetch-osm.mjs, scripts/osm-convert.mjs, scripts/osm-convert.d.ts, tests/osm-convert.test.ts, tests/fixtures/overpass-small.json, src/data/validate.ts, tests/data.test.ts, public/data/kyiv.json, public/data/city.json, docs/fetch-osm.md]
---

## Goal
Kyiv "looks very weird with no trees". Implement `docs/data-format.md`
§Trees exactly: fetch tree nodes, tree rows and wood/forest/park polygons;
emit `trees` (`[x, z, h, r]`) and `woods` rings; validate them; regenerate
**both** datasets (`npm run fetch-data:kyiv` and `npm run fetch-data`).

## Context
- Converter rules, densities, exclusion buckets, PRNG and the 40 000 cap are
  all in data-format.md §Trees — implement them literally; `mulberry32` is
  the same function as `src/data/synthetic.ts` (copy it into the script).
- Validator: `src/data/validate.ts` — optional `trees` (finite quads,
  `3 ≤ h ≤ 40`, `1 ≤ r ≤ 15`; paths `trees`, `trees[i]`) and `woods`
  (ring rules; `woods[i]`), after `rivers`.
- Fixture: add two `natural=tree` nodes (one with `height=12`), one
  `tree_row` way (~30 m), one `natural=wood` closed way (~60×60 m) containing
  one building and crossed by one road; tests: node trees emitted with
  their heights (12 vs seeded 6–14), the row yields 3–4 trees ≈ 8 m apart,
  the wood fill has ~24 ± 8 trees and none inside the building or within
  6 m of the road, output is byte-identical across two runs, the cap thins
  fills only (unit test with a tiny cap parameter exposed for tests).
- Datasets: both regenerated; report the summary lines and tree counts
  (expect Kyiv in the tens of thousands — Trukhaniv/Hydropark forests — and
  London a few thousand); files stay < 10 MB.
- Docs: `docs/fetch-osm.md` (selectors, summary line, trees section).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/osm-convert.test.ts tests/data.test.ts` exits 0
- [ ] Tests by name as in Context; validator accept/reject cases for `trees` and `woods`
- [ ] Both datasets regenerated, committed, validating; sizes and counts quoted
- [ ] `bash scripts/check.sh` exits 0; JSDoc; docs updated
- [ ] No files changed outside scope

## Out of scope
- Rendering (T-0066), minimap (T-0067), `src/main.ts`.

## Worker report

> claimed_by: grok-1 (pid 484850) at 2026-08-27T09:50:09Z

### Files changed
- `scripts/osm-convert.mjs` — OSM tree nodes, tree_row samples, wood/forest/park rings + seeded jittered-grid fills, 40k cap (`treeCap` / `TREE_CAP`), `mulberry32(42)` copy.
- `scripts/osm-convert.d.ts` — `TREE_CAP`, `treeCap` opt, `treesFilled` / `treesDropped` on the convert result.
- `scripts/fetch-osm.mjs` — eight tree/wood/park Overpass selectors; summary line `T trees (F filled, D dropped)`.
- `src/data/validate.ts` — optional `trees` (finite `[x,z,h,r]`, `3≤h≤40`, `1≤r≤15`; paths `trees` / `trees[i]`) and `woods` (poly rules; `woods[i]`), after `rivers`.
- `tests/fixtures/overpass-small.json` — two tree nodes (one `height=12`), ~30 m `tree_row`, ~62×62 m wood containing a building and crossed by a road.
- `tests/osm-convert.test.ts` — named cases from Context (heights, row spacing, wood fill + exclusions, byte-identity, cap thins fills only).
- `tests/data.test.ts` — validator accept/reject cases for `trees` and `woods`.
- `public/data/kyiv.json` / `public/data/city.json` — regenerated.
- `docs/fetch-osm.md` — selectors, summary line, Trees section.

### Commands run
```
bash .tigerteam/scripts/run-tests.sh tests/osm-convert.test.ts tests/data.test.ts
```
```
 ✓ tests/osm-convert.test.ts (68 tests) 230ms
 ✓ tests/data.test.ts (59 tests) 1053ms
 Test Files  2 passed (2)
      Tests  127 passed (127)
```
(`20260827-100307-498.log`)

```
npm run fetch-data:kyiv
```
```
kyiv.json: 8183 buildings, 6746 roads, 193 places, 51 water, 19 rivers, 23214 trees (19455 filled, 19454 dropped), terrain 323x270 @ 20 m (0 voids), 3369 KB (skipped 51 relations, dropped 0 open water chains)
```
woods: 351 rings. File 3.290 MB. Validates.

```
npm run fetch-data
```
(first attempt: Overpass 504 then 502; retry succeeded)
```
city.json: 9061 buildings, 8115 roads, 99 places, 31 water, 20 rivers, 10160 trees (1841 filled, 0 dropped), 3154 KB (skipped 18 relations, dropped 0 open water chains)
```
woods: 96 rings. File 3.080 MB. Validates.

```
bash scripts/check.sh
```
```
typecheck: ok
unit: ok
build: ok
e2e: ok
log: .tigerteam/logs/tests/check-20260827-100311-562.log
```

### Doc changes
`docs/fetch-osm.md`: Overpass tree/wood/park selectors, summary-line `T trees (F filled, D dropped)`, new Trees section (densities, exclusions, PRNG, cap, `treeCap`).

### Commit
`2b0dc479e46876f654b5732368f04d8e1b16128b` on `tigerteam/T-0065` (10 files, all in scope).

### Reviewer notes
- Kyiv is tens of thousands as expected (23 214 after the cap). Raw total was ~42 668 (3 759 mapped + 38 909 fills); `k = ceil(n/40000) = 2` so half the fills were dropped. The specified k-step is coarse — the file lands well under 40 000 rather than hugging the cap.
- London is 10 160 trees (8 319 mapped street trees + 1 841 park fills), denser than "a few thousand" because the Westminster bbox has a lot of `natural=tree` nodes. No cap. Both files stay < 10 MB.
- Rendering / instancing is T-0066; minimap woods T-0067. Not touched.
- `docs/data-format.md` (PM-owned) Overpass QL sample at the top of the OSM→JSON section still omits water/rivers/trees; the live query in `fetch-osm.mjs` is the one that ran.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | grok-1 | grok-4.6-build | 16m 0s | 3.56M (3.13M) | 45.27k | $0.46 | engine | review |

total: 1 attempt · 16m 0s · 3.56M in (3.13M cached) / 45.27k out · $0.46 (accepted 2026-08-27T10:08:28Z)
