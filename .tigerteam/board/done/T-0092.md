---
id: T-0092
title: Cloudflare Web Analytics beacon in index.html
priority: P2
complexity: C1
depends_on: []
assignee: ds
scope: [index.html, README.md]
---

## Goal
Add the Cloudflare Web Analytics beacon to the site so page views on the
GitHub Pages deployment are counted. This is a verbatim snippet insertion —
no design decisions.

## Context
- `index.html` is the single page (Vite entry). Insert the snippet below
  **immediately before the closing `</body>` tag**, on its own lines, after
  the existing `<script type="module" src="/src/main.ts"></script>` line,
  indented like its neighbours (4 spaces). Copy it EXACTLY — same token,
  same attributes, single quotes and all; do not add `async`/`defer`, do
  not gate it on hostname, do not move it into `src/`:

```html
    <!-- Cloudflare Web Analytics --><script type='module' src='https://static.cloudflareinsights.com/beacon.min.js' data-cf-beacon='{"token": "810b464b788a4ab6a437744ac7348a0c"}'></script><!-- End Cloudflare Web Analytics -->
```

- Vite leaves `<script>` tags with absolute `https://` URLs untouched at
  build time, so the built page carries the tag verbatim. Verify that
  rather than assuming it.
- `README.md`: add a short `## Analytics` section right after the
  `## Social preview` section: one or two sentences saying the page loads
  the Cloudflare Web Analytics beacon (`static.cloudflareinsights.com/beacon.min.js`,
  cookieless, client-side only) from `index.html`, and that removing that
  one `<script>` line disables it. Do not paste the token into the README.

## Acceptance criteria
- [ ] `grep -c 'static.cloudflareinsights.com/beacon.min.js' index.html` prints `1`, and the line matches the snippet above byte-for-byte (`grep -F` with the full snippet exits 0)
- [ ] The snippet line is the last element before `</body>`
- [ ] `npm run build` exits 0 and `grep -F 'data-cf-beacon' dist/index.html` exits 0
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (unit tests unchanged)
- [ ] `bash scripts/check.sh` exits 0 (typecheck + unit + build + e2e — the external script is simply blocked/ignored under headless Playwright; nothing in e2e asserts on it)
- [ ] README `## Analytics` section present as described
- [ ] No files changed outside scope; `dist/` is not committed

## Out of scope
Any other change to `index.html` (meta tags, CSP), any code in `src/`,
hostname gating, a consent banner, other analytics providers.

## Worker report

**Files changed**
- `index.html` — inserted the Cloudflare Web Analytics beacon snippet verbatim immediately before `</body>`, on its own line after the `/src/main.ts` module script, indented 4 spaces. No `async`/`defer`, no hostname gating, nothing moved into `src/`.
- `README.md` — added a `## Analytics` section right after `## Social preview` (one-line JSDoc-style, matches surrounding tone; no token pasted): page loads the beacon (`static.cloudflareinsights.com/beacon.min.js`, cookieless, client-side only) from `index.html`, removing that one `<script>` line disables it.

**Acceptance criteria — all run, all green**
1. `grep -c 'static.cloudflareinsights.com/beacon.min.js' index.html` → `1`; `grep -F -x` with the full snippet exits 0 (byte-for-byte match confirmed).
2. Snippet line is the last element before `</body>` (confirmed via `tail -4 index.html`).
3. `npm run build` exits 0; `grep -F 'data-cf-beacon' dist/index.html` exits 0 — Vite carries the absolute-URL `<script>` tag verbatim into the built page, immediately before `</body>` (confirmed in `dist/index.html`).
4. `bash .tigerteam/scripts/run-tests.sh` exits 0 — summary: `Test Files 43 passed (43) / Tests 723 passed (723)`.
5. `bash scripts/check.sh` exits 0 — `typecheck: ok, unit: ok, build: ok, e2e: ok` (log `.tigerteam/logs/tests/check-20260829-214838-502.log`).
6. README `## Analytics` section present as described, no token.
7. Only `index.html` + `README.md` modified (`git status --porcelain`); `dist/` is gitignored and not committed.

**Commands run**
- `bash .tigerteam/scripts/run-tests.sh` → `exit: 0` / `Test Files 43 passed (43) / Tests 723 passed (723)`
- `bash scripts/check.sh` → `typecheck: ok, unit: ok, build: ok, e2e: ok` / `CHECK_EXIT=0`
- `npm run build` → `✓ built in 959ms` / `BUILD_EXIT=0`

**Doc changes**
- `README.md` `## Analytics` section (per spec).

**Commit**
- `bd1286c38839f5a541212dfd5cc06449300cca44` on branch `tigerteam/T-0092` — `[T-0092] Cloudflare Web Analytics beacon in index.html` (2 files changed, 7 insertions).

**Notes for the reviewer**
- The first `npm run build` attempt resolved vite from the parent read-only `/home/d/asciicity/node_modules` and failed (EROFS on `.vite-temp`); after the run-tests wrapper installed a local `node_modules` via `npm ci`, the build succeeds cleanly. Not a code issue — just the fresh-worktree install ordering.
- Out of scope noticed but untouched: none.

> claimed_by: ds-1 (pid 676558) at 2026-08-29T21:47:02Z
> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 5m 45s | 274.68k (229.63k) | 5.47k | $0.0092 | engine | review |

total: 1 attempt · 5m 45s · 274.68k in (229.63k cached) / 5.47k out · $0.0092 (accepted 2026-08-29T21:54:06Z)
