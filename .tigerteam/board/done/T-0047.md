---
id: T-0047
title: Cycleway bridges (Parkovyi + Klitschko) — fetch, convert, regenerate kyiv.json, fix the two bridge presets
priority: P2
complexity: C2
depends_on: [T-0046]
scope: [scripts/fetch-osm.mjs, scripts/osm-convert.mjs, tests/osm-convert.test.ts, tests/fixtures/overpass-small.json, public/data/kyiv.json, tests/data.test.ts, src/data/spawn.ts, tests/spawn.test.ts, docs/fetch-osm.md, docs/integration.md]
---

## Goal
Kyiv's two famous pedestrian bridges — the Parkovyi Bridge to Trukhaniv
Island (OSM way 163254636) and the Klitschko "glass" bridge (way 660559170,
name:en "Bridge over Volodymyrskyi Descent") — are tagged
`highway=cycleway` + `bridge=yes`, so the Overpass query never fetched them
and the `parkbridge` / `glassbridge` presets currently drop the player on the
river bank. Fetch cycleways, apply the existing footway-bridge rule to them,
regenerate `kyiv.json`, and place the two presets on the decks.

## Context
- `docs/data-format.md` (updated 2026-08-26): the Overpass highway regex now
  includes `cycleway`; the road table says footway **and cycleway** are
  dropped unless `bridge` ≠ `no`, in which case they become `pedestrian` +
  `bridge: true`. Implement exactly that in `scripts/fetch-osm.mjs`
  (`buildQuery`) and `scripts/osm-convert.mjs` (the T-0040 footway exception —
  extend it to `cycleway`; `roadClassOf('cycleway')` stays `null`).
- Fixture: add one `highway=cycleway` + `bridge=yes` way and one plain
  `highway=cycleway` way; tests: the bridged one → `pedestrian`/`bridge: true`
  named as in the fixture, the plain one dropped.
- Regenerate with `npm run fetch-data:kyiv` (network; ~1 min + DEM cache).
  Expect the two bridges to appear as `pedestrian` roads with `bridge: true`,
  ≈ 400 m (Parkovyi) and ≈ 200 m (Klitschko); counts otherwise within ±5 %
  of `8183 / 6740 / 193 / 51 / 19`. London `city.json` untouched.
- Presets (`src/data/spawn.ts`): set `parkbridge` and `glassbridge` to the
  **first vertex** of the respective bridge polyline (unproject it with
  `src/geo.ts` `unproject`, 5 dp) with `bearingDeg` = the bearing from that
  vertex to the last vertex (`atan2(dx, −dz)` in degrees, 0–360, rounded).
  Labels: "Parkovyi Bridge, facing Trukhaniv Island" and "Klitschko glass
  bridge, facing the Arch". Derive the numbers from the regenerated dataset
  and quote them in the report.
- Tests (`tests/spawn.test.ts`): for each of the two presets, `resolveSpawn`
  against the committed `kyiv.json` (with a `CollisionGrid` built the way
  `main.ts` builds it — water rings as footprints, bridge roads as corridors,
  see `src/main.ts`) returns a point that is **not blocked**, lies within
  3 m of a `bridge: true` `pedestrian` polyline, and stays unblocked 20 m
  further along its bearing (`resolve(from, to)` returns `to`).
- Docs: `docs/fetch-osm.md` (cycleway rule), `docs/integration.md` (the two
  preset rows).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/osm-convert.test.ts tests/spawn.test.ts tests/data.test.ts` exits 0
- [ ] New tests by name: bridged cycleway → `pedestrian` + `bridge: true`; plain cycleway dropped; `parkbridge` and `glassbridge` spawn on an unblocked bridge vertex and can walk 20 m along the bearing (see Context); committed `kyiv.json` still validates
- [ ] `public/data/kyiv.json` regenerated; summary line + the two bridge lengths + the derived preset numbers quoted in the report; `git diff --stat` shows `public/data/city.json` unchanged
- [ ] `bash scripts/check.sh` exits 0; JSDoc; both docs updated
- [ ] No files changed outside scope

## Out of scope
- Any other preset, `main.ts`, terrain code, look tuning.

## Worker report

> claimed_by: ds-1 (pid 484850) at 2026-08-26T21:42:28Z

**Files changed** (commit `10c74ce`, branch `tigerteam/T-0047`):
- `scripts/fetch-osm.mjs` — `buildQuery` highway regex now includes `cycleway` (matches the already-updated `docs/data-format.md`).
- `scripts/osm-convert.mjs` — the T-0040 footway-bridge exception extended to `cycleway` (a `footway` **or** `cycleway` with `bridge ≠ no` → `pedestrian` + `bridge: true`); `roadClassOf('cycleway')` stays `null`. Comments updated.
- `tests/fixtures/overpass-small.json` — added way 40 (`highway=cycleway` + `bridge=yes`, named `Trukhaniv Cycle Bridge`) and way 41 (plain `highway=cycleway`).
- `tests/osm-convert.test.ts` — new `describe('osm-convert cycleway bridges (T-0047)')`: bridged cycleway → `pedestrian`+`bridge: true` named as in fixture; plain cycleway dropped; `roadClassOf('cycleway')` null. Updated road-id/`bridge`-presence/count assertions for the extra fixture road (roads 7 → 8; `bridgedIds` `[17,19]` → `[17,19,40]`).
- `public/data/kyiv.json` — **regenerated** with `npm run fetch-data:kyiv` (now includes Parkovyi way 163254636 and Klitschko way 660559170 as `pedestrian`+`bridge: true`).
- `src/data/spawn.ts` — `parkbridge` and `glassbridge` presets set to the **first vertex** of the respective bridge polylines, unprojected (5 dp), with `bearingDeg` = bearing from first → last vertex.
- `tests/spawn.test.ts` — new `describe('Kyiv bridge presets (T-0047)')` resolving both presets against kyiv.json with a `CollisionGrid` built exactly as `main.ts` does (water rings as footprints, bridge roads as corridors, `ROAD_WIDTH`): not blocked, within 3 m of a `bridge: true` `pedestrian` polyline, and `resolve(from, to)` returns `to` 20 m along the bearing.
- `tests/data.test.ts` — new kyiv.json test asserting both cycleway bridges are present as `pedestrian` + `bridge: true`.
- `docs/fetch-osm.md` — Roads bullet updated to document the cycleway/`bridge≠no` rule (footway + cycleway).
- `docs/integration.md` — the two Kyiv-preset table rows updated (Parkovyi bearing 33°, Klitschko bearing 286°).

**Commands run / summaries:**
- `bash .tigerteam/scripts/run-tests.sh tests/osm-convert.test.ts` → `cmd: ...` `exit: 0` / `Test Files 1 passed (1)` / `Tests 62 passed (62)`.
- `bash .tigerteam/scripts/run-tests.sh tests/spawn.test.ts tests/data.test.ts tests/osm-convert.test.ts` → `exit: 0` / `Test Files 3 passed (3)` / `Tests 146 passed (146)` (acceptance-criteria command).
- `bash scripts/check.sh` → `typecheck: ok` / `unit: ok` / `build: ok` / `e2e: ok`.

**Kyiv regeneration** — summary line:
```
kyiv.json: 8183 buildings, 6746 roads, 193 places, 51 water, 19 rivers, terrain 323x270 @ 20 m (0 voids), 2633 KB (skipped 51 relations, dropped 0 open water chains)
```
Baseline was `8183 / 6740 / 193 / 51 / 19`; only roads changed 6740 → 6746 (+6), within ±5 %. `git diff --stat` shows `public/data/city.json` UNCHANGED (verified `git diff --quiet public/data/city.json` → clean).

**Bridge lengths** (sum of polyline segments in kyiv.json): Parkovyi = **434 m** (≈ 400), Klitschko = **209 m** (≈ 200), matching the ticket's expectation. Both are `cls: pedestrian` + `bridge: true`.

**Derived preset numbers** (first vertex unprojected with `src/geo.ts` `unproject`, 5 dp; bearing = `atan2(dx, −dz)` in degrees 0–360, rounded):
- `parkbridge` → lon `30.5324`, lat `50.45498`, `bearingDeg: 33`, label "Parkovyi Bridge, facing Trukhaniv Island"
- `glassbridge` → lon `30.52974`, lat `50.45489`, `bearingDeg: 286`, label "Klitschko glass bridge, facing the Arch"

**Reviewer should note:** none blocking. The cycleway fixture way 19 (`footway`+`bridge`) and way 40 (`cycleway`+`bridge`) both become `pedestrian`, so the pre-existing footway rule is fully preserved. kyiv.json is minified (2-line diff) but correct. Visual/GPU check of the two presets was not run by this worker (no GPU here); verified purely via unit tests against the committed dataset — the PM does the GPU visual review.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 8m 37s | 3M (2.9M) | 26.45k | $0.07 | engine | review |

total: 1 attempt · 8m 37s · 3M in (2.9M cached) / 26.45k out · $0.07 (accepted 2026-08-26T21:52:57Z)
