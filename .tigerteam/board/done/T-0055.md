---
id: T-0055
title: Render style — pico8 (16-colour palette)
priority: P1
complexity: C2
depends_on: [T-0050]
scope: [src/render/styles/pico8.ts, tests/styles/pico8.test.ts, docs/styles/pico8.md]
---

## Goal
Replace the stub `src/render/styles/pico8.ts` with the real **pico8 (16-colour palette)** look,
exactly as specified in `docs/architecture.md` §4.11 "pico8". One file,
one shader, pure helpers unit-tested in node.

## Context
- Read `src/render/style.ts` (PM-owned: `RenderStyle`, `STYLE_PRELUDE`
  helpers — `sampleSub`, `cellMean`, `bright`, `shaped`, `tintOf`,
  `linearDepth`) and `src/render/styles/ascii.ts` (the reference style:
  how the atlas, `makeUniforms` and the fragment fit together) before
  writing code. Keep the module's `export const STYLES` and the id(s) the
  stub declares; keep cell/sub/needsDepth per §4.11.
- The shader must mirror the pure helpers term for term (the tests are the
  spec for the shader); pure helpers never touch `document`.
- Atlases are procedural (canvas rectangles/arcs/lines), never font glyphs,
  unless §4.11 says otherwise; build them in `makeUniforms` via
  `ctx.makeCanvas`; `dispose` frees the texture.
- `docs/styles/pico8.md`: the algorithm in prose, the pure exports, and a
  note on what the shader does per pixel.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/styles/pico8.test.ts tests/post.test.ts` exits 0
- [ ] Tests by name: `PICO8_PALETTE` has the 16 §4.11 colours in order; `nearestPico8` of each palette colour returns its own index; black → 0, white → 7 (#FFF1E8), pure green → 11 (#00E436); `bayer4` has 16 distinct values in (0, 1)
- [ ] `bash scripts/check.sh` exits 0 (the e2e style loop still visits every id and saves `e2e/__shots__/style-<id>.png` — mention the file for this style in the report)
- [ ] Headless evidence: `/?synthetic=1&render=<id>&cell=3x6` renders > 2 % non-black pixels and `__asciicity.render` is the id; no WebGL shader compile errors in the console (quote the console tail)
- [ ] JSDoc on every export; `docs/styles/pico8.md` written
- [ ] No files changed outside scope

## Out of scope
- Any other style, `post.ts`, `style.ts`, `main.ts`, HUD, docs outside `docs/styles/`.

## Worker report

> claimed_by: opus-2 (pid 484850) at 2026-08-26T23:25:53Z
> INCOMPLETE attempt by opus-2 at 2026-08-26T23:58:43Z (engine exit 1, no handoff) — see /home/d/asciicity/.tigerteam/logs/workers/opus-2.log

> claimed_by: ds-2 (pid 484850) at 2026-08-26T23:58:46Z

## Worker report

**Context:** this ticket had an INCOMPLETE attempt by opus-2 (rate-limited mid-gate;
came close — code, tests and docs were already drafted). I took over the existing
work, audited it against §4.11, found and fixed one real shader bug, verified every
criterion, and committed. No files changed outside scope.

**Files changed (all in scope):**
- `src/render/styles/pico8.ts` — real pico8 style: cell 4×4 / sub 1×1, `PICO8_PALETTE`,
  `bayer4`, `nearestPico8`, the §4.11 fragment (`uniform vec3 palette[16]` nearest-snap
  with Bayer offset), `makeUniforms`/`STYLES`. Fixed the shader's Bayer construction:
  it was computing the **transpose** of the §4.11 matrix (`4·M2[high]+M2[low]`);
  changed to `M2[high]+4·M2[low]` so the shader mirrors `bayer4()`/`BAYER4_M` term for term.
- `tests/styles/pico8.test.ts` — unit tests for the pure helpers.
- `docs/styles/pico8.md` — algorithm in prose, pure exports, per-pixel shader note;
  updated the Bayer-construction line to the correct orientation.

**Commands run:**
- `bash .tigerteam/scripts/run-tests.sh tests/styles/pico8.test.ts tests/post.test.ts`
  → `Test Files 2 passed (2) · Tests 8 passed (8) · exit: 0`
- `bash scripts/check.sh` → full clean pass `typecheck: ok / unit: ok / build: ok / e2e: ok`
  (log `check-20260827-001417-1728.log`). e2e is **flaky** in this SwiftShader env: the
  R-cycle style loop intermittently times out at `page.screenshot` (`Test timeout of 60000ms
  exceeded`; `painted > 0.02` assertion already passed) — observed on the HATCH/other
  style a few times and clean ~2 of 3 runs; pre-existing, unrelated to pico8. The e2e
  style loop saved `e2e/__shots__/style-pico8.png`.
- Headless evidence `/tmp/pico8-probe.mjs` (temporary, removed) via the Vite server:
  `?synthetic=1&render=pico8&cell=3x6` → `__asciicity.render = pico8`, `styles` includes
  `pico8`, canvas 960×540 non-black **92.26%** (> 2%), and no shader compile errors in the
  console. Console tail (quoted):
  ```
  [GroupMarkerNotSet(crbug.com/242999)!:A0B02000340F0000]Automatic fallback to software WebGL has been deprecated...
  [.WebGL-0x...]GL Driver Message (OpenGL, Performance, GL_CLOSE_PATH_NV, High): GPU stall due to ReadPixels (this message will no longer repeat)
  ```
  (The GPU-stall messages are from my probe's own `readPixels`; no shader compile errors.)

**Doc changes:** `docs/styles/pico8.md` written; JSDoc present on every export
(`PICO8_PALETTE`, `bayer4`, `nearestPico8`, `STYLES`).

**Commit:** `2910dac` on branch `tigerteam/T-0055` — `[T-0055] Render style — pico8 (16-colour palette)`.

**For the reviewer:** the key finding is the shader-vs-pure Bayer-matrix orientation fix
(the previous drafting had the dither matrix transposed, which tests can't catch since they
only exercise the pure helper). If the GPU visual review sees the dither grain mirrored
across the diagonal in `style-pico8.png`, it's stale from a pre-fix run — the committed
shader now matches §4.11 exactly.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-2 | <synthetic> | 32m 50s | 6.04M (6.04M) | 74.46k | $6.12 | engine | todo |
| 2 | ds-2 | deepseek-ai/DeepSeek-V4-Flash-0731 | 22m 38s | 2.02M (1.3M) | 21.92k | $0.09 | engine | review |

total: 2 attempts · 55m 28s · 8.05M in (7.34M cached) / 96.38k out · $6.21 (accepted 2026-08-27T00:44:38Z)
