---
id: T-0056
title: Render style — edges (depth-based wireframe)
priority: P1
complexity: C2
depends_on: [T-0050]
scope: [src/render/styles/edges.ts, tests/styles/edges.test.ts, docs/styles/edges.md]
---

## Goal
Replace the stub `src/render/styles/edges.ts` with the real **edges (depth-based wireframe)** look,
exactly as specified in `docs/architecture.md` §4.11 "edges". One file,
one shader, pure helpers unit-tested in node.

## Context
- Read `src/render/style.ts` (PM-owned: `RenderStyle`, `STYLE_PRELUDE`
  helpers — `sampleSub`, `cellMean`, `bright`, `shaped`, `tintOf`,
  `linearDepth`) and `src/render/styles/ascii.ts` (the reference style:
  how the atlas, `makeUniforms` and the fragment fit together) before
  writing code. Keep the module's `export const STYLES` and the id(s) the
  stub declares; keep cell/sub/needsDepth per §4.11.
- The shader must mirror the pure helpers term for term (the tests are the
  spec for the shader); pure helpers never touch `document`.
- Atlases are procedural (canvas rectangles/arcs/lines), never font glyphs,
  unless §4.11 says otherwise; build them in `makeUniforms` via
  `ctx.makeCanvas`; `dispose` frees the texture.
- `needsDepth: true`; the prelude's `linearDepth(uv)` gives metres; neighbours are one sub-sample (`1 / sceneSize`) away in uv.
- `docs/styles/edges.md`: the algorithm in prose, the pure exports, and a
  note on what the shader does per pixel.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/styles/edges.test.ts tests/post.test.ts` exits 0
- [ ] Tests by name: `isEdge(100, [100,100,100,100], 2000)` false; a 3 % step on one side true; a 1 % step false; centre sky (≥ 0.98·far) with a non-sky neighbour true; all sky false; k is respected (`k = 0.05` makes the 3 % step false)
- [ ] `bash scripts/check.sh` exits 0 (the e2e style loop still visits every id and saves `e2e/__shots__/style-<id>.png` — mention the file for this style in the report)
- [ ] Headless evidence: `/?synthetic=1&render=<id>&cell=3x6` renders > 2 % non-black pixels and `__asciicity.render` is the id; no WebGL shader compile errors in the console (quote the console tail)
- [ ] JSDoc on every export; `docs/styles/edges.md` written
- [ ] No files changed outside scope

## Out of scope
- Any other style, `post.ts`, `style.ts`, `main.ts`, HUD, docs outside `docs/styles/`.

## Worker report

> claimed_by: opus-1 (pid 484850) at 2026-08-26T23:25:53Z

**Files changed**
- `src/render/styles/edges.ts` — replaced the stub with the real style: pure
  `isEdge(dC, neighbours, far, k = 0.02)`, constants `EDGE_COLOUR / FLOOR_GAIN /
  SKY_FRACTION`, and the fragment shader that mirrors `isEdge` term for term
  (four inlined pair tests, `linearDepth` centre + L/R/U/D at `1/sceneSize`
  step). `cellW/cellH = 2`, `subX/subY = 1`, `needsDepth: true`, `makeUniforms`
  returns `{}` (no atlas needed → no `dispose`).
- `tests/styles/edges.test.ts` — new node-only test file covering every
  named case from the acceptance criteria plus the min-of-pair tolerance
  and every neighbour position, and the `STYLES` shape (id/label/cell/sub/
  needsDepth/main()/linearDepth/empty uniforms).
- `docs/styles/edges.md` — new; algorithm in prose, pure exports table,
  per-pixel shader summary.

**Commands run**
- `bash .tigerteam/scripts/run-tests.sh tests/styles/edges.test.ts tests/post.test.ts` →
  `Test Files 2 passed (2)`, `Tests 19 passed (19)` (`.tigerteam/logs/tests/20260826-232847-107.log`).
- `bash scripts/check.sh` → `typecheck: ok / unit: ok / build: ok / e2e: ok`
  (`.tigerteam/logs/tests/check-20260826-232852-197.log`). The
  "R cycles every render style and each paints" e2e visited edges and saved
  `e2e/__shots__/style-edges.png` (42 KB).
- Headless verification against a local static server serving `dist/` (script
  removed after use): `/?synthetic=1&render=edges&cell=3x6` → `render:
  "edges"`, `nonBlackFraction: 0.4702` (≫ 2 %). Console tail contained no
  WebGL shader compile errors — only benign SwiftShader-fallback and
  `GL_CLOSE_PATH_NV`/`ReadPixels` performance warnings:
  ```
  [warning] Automatic fallback to software WebGL has been deprecated. Please use the --enable-unsafe-swiftshader … flag …
  [warning] [.WebGL-…]GL Driver Message (OpenGL, Performance, GL_CLOSE_PATH_NV, High): GPU stall due to ReadPixels
  [warning] [.WebGL-…]GL Driver Message (OpenGL, Performance, GL_CLOSE_PATH_NV, High): GPU stall due to ReadPixels
  [warning] [.WebGL-…]GL Driver Message (OpenGL, Performance, GL_CLOSE_PATH_NV, High): GPU stall due to ReadPixels
  [warning] [.WebGL-…]GL Driver Message (…): GPU stall due to ReadPixels (this message will no longer repeat)
  ```

**Docs**
- `docs/styles/edges.md` written per the ticket. Doc directory `docs/styles/`
  did not exist previously; this ticket creates it.

**Commit**
- Branch `tigerteam/T-0056`, commit `5e3fc72`
  `[T-0056] Render style — edges (depth-based wireframe)`.

**Notes for the reviewer**
- The shader hard-codes `k = 0.02` and `SKY_FRAC = 0.98` in GLSL constants;
  `isEdge` mirrors them via the `k` default and `SKY_FRACTION`. Kept it
  hard-coded rather than adding uniforms because §4.11 pins the numbers and
  no ticket asks for run-time tuning.
- Neighbour order in the pure helper is intentionally documented as
  "any order" — pairs are evaluated independently — while the shader inlines
  L/R/U/D. The tests cover this by permuting the non-sky neighbour position.
- Screenshot for PM visual review: `e2e/__shots__/style-edges.png`.

> handoff: review

## Rework (attempt 2)

Gate green and the silhouettes/creases look great on the GPU (moon, stars, building corners, 60 fps). One spec defect — mine, not yours — blocks acceptance:

**What failed:** the first-difference test `|dC − dN| > k·min(dC, dN)` fires on every grazing surface: on the GPU frame from `/?city=kyiv&at=maidan&render=edges` the entire mid-distance ground band (rows ~320–450 of 720) is a solid green slab and the road/ground seam draws as a long diagonal line, because neighbouring depth samples on a plane differ by more than 2 % at distance.

**What done looks like** (`docs/architecture.md` §4.11 "edges", revised 2026-08-27 — read it):
1. Test the **second difference of inverse depth**: `w = 1/d`; edge when `|wL + wR − 2·wC| > k·wC` or `|wU + wD − 2·wC| > k·wC`, `k = 0.02`, over non-sky samples; the sky rule stays (centre/neighbour disagree on `sky` → edge; sky/sky never). Same in `isEdge` (neighbours ordered `[dL, dR, dU, dD]` — pairs matter now) and in the shader.
2. Tests, by name: a plane — inverse depths linear along each axis (e.g. `dC = 20`, `dL = 1/(1/20 − 0.004)`, `dR = 1/(1/20 + 0.004)`, same for U/D) → **false** at k = 0.02; a wall in front of ground (`dC = 20`, `dR = 40`, others 20) → true; a crease (left pair linear with slope 0.004, right pair slope 0) → true; the four sky cases from your current tests unchanged; `k = 1` makes the crease false.
3. GPU-independent evidence you can produce: `e2e/__shots__/style-edges.png` from the style loop plus your headless pixel count — the fraction of "edge green" pixels on `/?synthetic=1&render=edges&cell=3x6` must be **under 15 %** (report the number; with the old rule it is well above that).
4. `bash scripts/check.sh` green; `docs/styles/edges.md` updated to the new rule.


> claimed_by: ds-2 (pid 484850) at 2026-08-26T23:35:08Z
## Worker report (rework — attempt 2, ds-2)

**Files changed**
- `src/render/styles/edges.ts` — replaced the attempt-1 first-difference rule
  with the revised §4.11 rule: `isEdge(dC, neighbours: [dL, dR, dU, dD], far, k = EDGE_K)`
  now tests the **second difference of inverse depth** (`w = 1/d`):
  `|wL + wR − 2·wC| > k·wC` or `|wU + wD − 2·wC| > k·wC`, over non-sky samples,
  with the sky rule unchanged (centre/neighbour sky disagreement → edge;
  all-sky never). Added `EDGE_K = 0.02` export; the fragment shader mirrors
  `isEdge` term for term (inlined sky tests + two inlined inverse-depth
  second-difference tests, `k = 0.02` hard-coded). Wrote JSDoc on every export.
- `tests/styles/edges.test.ts` — rewrote the pure-helper tests to the new
  rule, by name: plane → false; wall-in-front-of-ground → true; crease → true;
  k = 1 → crease false; the four sky cases unchanged; plus retained
  flat / 3 % step / 1 % step / k-respected cases (still valid under the new
  rule) and constants/STYLES shape.
- `docs/styles/edges.md` — updated algorithm to the inverse-depth
  second-difference rule, revised export table (`EDGE_K`, ordered 4-tuple
  neighbours), and a note on why the first-difference rule was scrapped.

**Commands run**
- `bash .tigerteam/scripts/run-tests.sh tests/styles/edges.test.ts tests/post.test.ts` →
  `Test Files  2 passed (2)` / `Tests  23 passed (23)` (`.tigerteam/logs/tests/20260826-234507-47.log`).
- `bash scripts/check.sh` → `typecheck: ok / unit: ok / build: ok / e2e: ok`
  (`.tigerteam/logs/tests/check-20260826-234935-682.log`). The style loop
  visited every id and saved `e2e/__shots__/style-edges.png` (45 KB).
  Note: a first `check.sh` run timed out in that same e2e style loop at
  `pico8` (FPS 17 on the SwiftShader worker, before `edges` is reached) — a
  flaky perf timeout; the re-run passed and the failure is unrelated to this
  change (it never reached the edges style).
- Headless evidence (throwaway Playwright script against a local vite server
  on `/?synthetic=1&render=edges&cell=3x6`, 1280×720): `render: "edges"`;
  `nonBlackFraction: 0.9362` (≫ 2 %); **edgeGreenFraction: 0.0542 (5.42 %,
  under the 15 % rework threshold)**. Console tail contained no WebGL shader
  compile errors:
  ```
  [debug] [vite] connecting...
  [debug] [vite] connected.
  ```

**Docs**
- `docs/styles/edges.md` rewritten for the revised inverse-depth rule.

**Commit**
- Branch `tigerteam/T-0056`, commit `ffe142e`
  `[T-0056] edges: inverse-depth second-difference rule (rework)`.

**Notes for the reviewer**
- Screenshot for PM GPU visual review: `e2e/__shots__/style-edges.png`.
- The earlier comment in attempt-1's test "takes the smaller of the pair"
  described the old `min(dC, dN)` tolerance; the new rule's tolerance is
  `k·wC` (centre-based), so that test was replaced by the wall/crease cases.
- Out-of-scope observation: the e2e style-loop can exceed its 60 s budget on
  this SwiftShader worker (timed out once at `pico8`). Not touched — it is a
  pre-existing perf/flakiness concern in `e2e/smoke.spec.ts`, unchanged by
  this ticket.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-4-7 | 5m 50s | 3.21M (3.21M) | 17.14k | $2.68 | engine | review |
| 2 | ds-2 | deepseek-ai/DeepSeek-V4-Flash-0731 | 19m 1s | 1.2M (662.02k) | 22.68k | $0.06 | engine | review |

total: 2 attempts · 24m 51s · 4.41M in (3.87M cached) / 39.83k out · $2.75 (accepted 2026-08-27T00:44:17Z)
