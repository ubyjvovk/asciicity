---
id: T-0032
title: Always-visible idle joystick ring so touch movement is discoverable
priority: P1
complexity: C1
depends_on: []
scope: [src/player/touch.ts, src/player/touch.css, docs/controls.md]
---

## Goal
The virtual joystick works but is invisible until touched — players don't
find it. Show a faint idle ring anchored bottom-left; touching anywhere in
the left half still starts the joystick at the touch point (unchanged).

## Context
- `TouchControls` (`src/player/touch.ts`, T-0025) creates `.touch-joystick-ring`
  / `.touch-joystick-knob`, shown only while a joystick pointer is active.
- Changes:
  1. New CSS state `.touch-joystick-ring.idle`: `display: block; opacity: 0.28;`
     positioned at `left: 18vw; bottom: 90px; top: auto; transform: translate(-50%, 50%)`
     (the active state keeps today's behaviour and full opacity; knob hidden
     while idle).
  2. `TouchControls`: on construction add the `idle` class; on joystick
     `pointerdown` swap `idle` → active placement (existing code); on
     `pointerup`/`pointercancel` return to the idle state instead of hiding.
  3. Keep `hideStick`/`showStick` internals free to change, but the public
     API (`readInput`, `dispose`) and all existing behaviour stay identical.
- `docs/controls.md`: update the Touch section (idle hint, unchanged input model).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/touch.test.ts` exits 0 unchanged
- [ ] `bash scripts/check.sh` exits 0
- [ ] Code review: idle class + CSS as specified; `dispose()` removes both elements; no change to axes/look logic
- [ ] `docs/controls.md` updated; JSDoc intact
- [ ] No files changed outside scope

## Out of scope
- Changing the joystick input model, second-touch/long-press movement schemes, `main.ts`.

## Worker report

> claimed_by: ds-1 (pid 3475038) at 2026-08-24T12:55:51Z

### Summary

Implemented an always-visible idle joystick ring so touch movement is
discoverable. The faint ring is anchored bottom-left; touching anywhere in the
left half still starts the joystick at the touch point (input model unchanged).

### Files changed (all in scope)

- `src/player/touch.ts` — `showStick()` now removes the `idle` class before
  activating (active placement & full opacity unchanged); `hideStick()` now adds
  the `idle` class to the ring instead of leaving it fully hidden. The constructor
  already calls `hideStick()`, so the overlay starts in the idle state. Public API
  (`readInput`, `dispose`) and axes/look logic untouched.
- `src/player/touch.css` — added `.touch-joystick-ring.idle:not(.is-active)` rule:
  `display: block; opacity: 0.28; left: 18vw; bottom: 90px; top: auto;
  transform: translate(-50%, 50%)`. Knob stays hidden while idle (it only gains
  `is-active`). Active state keeps today's origin-anchored behavior and full
  opacity.
- `docs/controls.md` — updated the Touch section's Visuals bullet to describe the
  idle hint and the idle→active→idle state transition; input model noted unchanged.

### Commands run

- `bash .tigerteam/scripts/run-tests.sh tests/touch.test.ts` → exit 0:
  `Test Files  1 passed (1)` / `Tests  5 passed (5)` (unchanged)
- `bash scripts/check.sh` → exit 0: `typecheck: ok / unit: ok / build: ok / e2e: ok`

### Doc changes

- `docs/controls.md` Touch section (idle hint + unchanged input model). JSDoc on
  exported helpers and `TouchControls` is intact; no signature changed.

### Commit

- `6b02792` on branch `tigerteam/T-0032` — `[T-0032] Always-visible idle joystick ring`

### Reviewer notes

- The e2e smoke ran on SwiftShader; the idle ring is a visual state best
  confirmed on the GPU via `window.__asciicity` / HUD during PM visual review.
- No changes outside scope; only the three listed files were committed.

> handoff: review
