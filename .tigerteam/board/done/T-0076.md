---
id: T-0076
title: San Francisco dataset — fetch sf.json, registry entry, presets, landmark fixes, GGB tower extras
priority: P2
complexity: C2
depends_on: [T-0075]
scope: [public/data/sf.json, src/data/cities.ts, src/data/spawn.ts, src/world/landmarks.ts, tests/cities.test.ts, tests/spawn.test.ts, tests/landmarks.test.ts, e2e/sf.spec.ts, docs/integration.md, scripts/osm-convert.mjs, tests/coastline.test.ts]
---

## Goal
Ship San Francisco as the third city: `?city=sf`, picker entry, hills from
SRTM, the Bay via the new coastline rules, Golden Gate Bridge walkable
with international-orange tower extras, and presets for the landmarks.
Dataset spec: `docs/data-format.md` "### San Francisco (`sf.json`,
wave 8)". Read it and the Kyiv precedents (T-0040/T-0045 patterns in
`docs/integration.md`) first.

## Context
- Fetch: `npm run fetch-data:sf` (script already in package.json — do not
  edit package.json). Commit `public/data/sf.json`. Record in the report:
  building/road/water/tree counts, terrain grid size, JSON size, and the
  fetch summary line. If the JSON exceeds 15 MB (raised from 12 by the PM answer of
  2026-08-28), STOP and file a blocked question instead of committing.
- `src/data/cities.ts`: append `{ id: 'sf', label: 'SAN FRANCISCO',
  file: 'data/sf.json', defaultSpawn: 'ggb', blurb: 'Downtown to the
  Golden Gate · hills & bay' }`.
- `src/data/spawn.ts`: widen the `SpawnPreset.city` union to
  `'london' | 'kyiv' | 'sf'` and loosen `presetsFor` to accept `string`
  (keeps `main.ts` untouched — its existing cast still compiles; verify
  `npm run typecheck` without editing main.ts). New presets (`goldengate`
  is TAKEN by Kyiv — do not touch it):
  - `ggb`: fixed coord on the bridge deck mid-span, facing the city —
    start from lon −122.4779, lat 37.8170, bearing 160, then VERIFY
    against the fetched bridge way geometry and snap the point onto the
    deck centre-line (a point 30 m off the deck is open water and the
    spawn breaks).
  - `transamerica`, `salesforce` (building 'Salesforce Tower'),
    `coittower` (building 'Coit Tower'), `ferrybuilding` (building
    'Ferry Building'): building presets with coordinate fallbacks like the
    Kyiv ones. VERIFY the exact `name` values against the fetched JSON
    (`node -e` / grep on sf.json) and use those strings verbatim.
  - `paintedladies`: fixed −122.4330, 37.7765, bearing 75 (Alamo Square
    row with the skyline behind).
  - `lombard`: fixed −122.4187, 37.8021, bearing 100 (top of the crooked
    block looking down).
  - `pier39`: fixed −122.4103, 37.8087, bearing 0 (out toward Alcatraz).
  - `unionsquare`: fixed −122.4075, 37.788, bearing 315.
- `src/world/landmarks.ts`:
  - `EXTRA_BUILDINGS.sf`: the two Golden Gate Bridge towers as synthetic
    extras (T-0059/T-0069 precedent — Motherland/Nelson's Column): south
    tower lon −122.4775 lat 37.8107, north tower −122.4783 lat 37.8199
    (verify both against the bridge way in sf.json and snap onto the deck
    line),
    height 227, colour international orange `0xc0362c`, ~12×10 m
    footprint, `shape: 'tower'` if the field exists (see
    `Building.shape`), names 'Golden Gate Bridge South Tower' / '… North
    Tower' (they get floating tags automatically).
  - `LANDMARK_FIXES.sf`: check the fetched heights first — SF downtown is
    usually well-tagged. Add fixes ONLY where OSM height is a stub
    (report what you found): Transamerica Pyramid 260 m, Salesforce Tower
    326 m, Coit Tower 64 m colour `0xf5f0e6`, Ferry Building tower 75 m.
- `e2e/sf.spec.ts` (NEW file; never edit smoke.spec.ts): boot
  `/?city=sf&at=unionsquare`, wait ready, assert non-black fraction >
  0.02 (copy the smoke.spec.ts helper) and the HUD shows an `ALT` row
  (terrain present); one more boot at `/?city=sf&at=ggb` asserting ready
  and `__asciicity.state.y > 20`. Why 20: world y is relative to the
  datum at Union Square (~25 m ASL); the deck is ~67 m ASL → y ≈ 35–45,
  while a spawn fallen to sea level gives y ≈ −25. 20 separates them with
  margin either way.
- Unit tests: `tests/cities.test.ts` (sf entry resolvable, picker order),
  `tests/spawn.test.ts` (every new preset key parses, `presetsFor('sf')`
  returns them all, no key collides with london/kyiv),
  `tests/landmarks.test.ts` (sf extras applied, fixes only for names
  present).
- `docs/integration.md`: add the SF presets section mirroring the Kyiv
  one.
- Validator: `sf.json` must pass `validate.ts` at load (the e2e boot
  proves it).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/cities.test.ts tests/spawn.test.ts tests/landmarks.test.ts` exits 0
- [ ] Tests by name: the cases listed above
- [ ] `bash scripts/check.sh` exits 0 (includes the new e2e file)
- [ ] `public/data/sf.json` committed, ≤ 15 MB; report records counts,
      size, fetch summary, which fixes were needed and the exact building
      names used
- [ ] JSDoc on changed exports; docs/integration.md SF section
- [ ] No files changed outside scope (main.ts and package.json must show
      no diff)

## Out of scope
- Boats/ferries on the Bay, Bay Bridge east of the bbox, Twin Peaks,
  cable cars, converter changes (T-0075), `docs/data-format.md` counts
  (PM fills at accept).

## Worker report

> claimed_by: ds-1 (pid 184142) at 2026-08-28T17:47:10Z
> INCOMPLETE attempt by ds-1 at 2026-08-28T17:55:50Z (engine exit 143, no handoff) — see /home/d/asciicity/.tigerteam/logs/workers/ds-1.log

> claimed_by: ds-1 (pid 3900) at 2026-08-28T21:01:31Z

### Attempt report (ds-1, 2026-08-28) — BLOCKED at the data-fetch step

**Verdict: blocked.** The very first acceptance-criterion action (`npm run
fetch-data:sf` → commit `public/data/sf.json`) cannot complete: the converter
`scripts/osm-convert.mjs` crashes inside its coastline pipeline on the real SF
OSM data. Because the fix lives in a **T-0075-owned file that this ticket
explicitly puts out of scope** ("converter changes (T-0075)"), and because
**every other deliverable in this ticket must be verified against the fetched
`sf.json`** (exact building `name` strings for the presets, OSM heights for the
landmark fixes, and the Golden Gate bridge-deck geometry for `ggb`/the tower
extras), there is no verifiable in-scope partial work I could safely commit.
Working tree is clean; only the ticket file was touched.

**Exact failure** (rerun: `npm run fetch-data:sf`):

```
fetch: closeCoastline: point (-122.4075586, 37.8080734) is not on the bbox perimeter
```

**Root cause (diagnosed with the real Overpass result, saved at `/tmp/coast.json`).**

The SF bbox captures the peninsular Bay shoreline. The Embarcadero coastline
way (id 43008440) ends and the Marina/Presidio coastline way (id 32648495)
begins at the *identical* node (-122.4075586, 37.8080734) — the Fisherman's
Wharf/Fort Mason bend, an interior point (gap measured: 0.000 m). The
through-coast therefore should be ONE open chain from the bbox south edge to
the bbox west edge, with that node an interior vertex.

`stitchChains` (osm-convert.mjs ~line 443) only ever **appends** a piece to the
*current* chain (`tail == other[0]`), and processes pieces in array order. Here
the Marina piece (which geometrically comes AFTER the Embarcadero piece) is
processed first and absorbs the westward coast, leaving the Embarcadero piece
orphaned with its interior endpoint u. `closeCoastline` then cannot parameterise
 that endpoint on the bbox perimeter and throws.

Rerunnable evidence:

```bash
node /tmp/diag.mjs /tmp/coast.json
# coastline ways: 45, pieces: 46, closed: 5, open: 13
#   open chain 275 pts. start (-122.39172,37.79343) end (-122.40756,37.80807)  <- Embarcadero (orphan)
#   open chain 658 pts. start (-122.40756,37.80807) end (-122.48700,37.78985) <- Marina->GGB->west edge
# closeCoastline threw: closeCoastline: point (-122.4075586, 37.8080734) is not on the bbox perimeter
```

**Recommended fix (backward-compatible; belongs to T-0075/osm-convert.mjs).**
Make `stitchChains` merge-aware: in addition to the current `end→start` append,
also join `other.end→chain.start` (prepend) on coinciding endpoints, and
repeat until no two chains share an endpoint (or run a second pass over the
leftover open chains against the already-built chains before returning). I
prototyped this in `/tmp/diag5.mjs` and, with it, the same real data yields
`closed: 5, open: 3` and `closeCoastline` returns 2 water rings with no crash
instead of throwing — so the current fix is sufficient to unblock SF. It does
not change the existing coastline unit tests (the "single unmatched piece" and
"strait" cases still behave identically).

**Why nothing was committed.**
1. `src/data/spawn.ts` building presets (`transamerica`, `salesforce`,
   `coittower`, `ferrybuilding`) require the **exact OSM `name` strings** from
   the fetched `sf.json` (the ticket insists they be verified and used
   verbatim). Without `sf.json` I would be guessing names.
2. `LANDMARK_FIXES.sf` requires checking fetched heights (``stub`` vs real)
   and the extra towers require snapping to the fetched bridge deck line.
3. Updating `cities.ts`/`cities.test.ts`/`spawn.test.ts` to add the `sf` entry
   immediately makes the existing suites read `public/data/sf.json` (missing),
   so no in-scope change can be left green.

## Questions

1. **Blocking:** `scripts/osm-convert.mjs` `stitchChains` cannot join two
   coastline ways that share an interior node when they are processed in
   ``reverse`` geometric order, so the SF fetch crashes in `closeCoastline`.
   Fixing it is a converter change in T-0075's file, and this ticket's Out of
   scope says "converter changes (T-0075)".
   - Option A (recommended): a short follow-up on **T-0075** lands the
     merge-aware `stitchChains` fix above, then this ticket is re-attempted as-is.
   - Option B: amend this ticket's scope to include the one-function fix in
     `scripts/osm-convert.mjs` (plus a coastline unit test for the reverse-order
     junction) so I can unblock the fetch here.
   - Recommendation: **Option A** — keeps T-0075's contract single-owner, and
     the fix is tiny, proven, and backward-compatible. Please decide; I will
     resume immediately on re-claim.
2. No other blockers observed. DEM tile `N37W123` is cached; the fetch failed
   purely inside `convertOverpass`'s coastline stage.

> handoff: blocked

## Answers

### PM answer (2026-08-28)

Good diagnosis — confirmed against the source: `stitchChains` only appends
(`tail == o[0]`) and never prepends, so array order decides whether an open
coast survives. **Option B: this ticket's scope is amended** (frontmatter
updated) to include `scripts/osm-convert.mjs` (the `stitchChains` function
ONLY) and `tests/coastline.test.ts` (append cases only). Fix it here, then
carry on with the ticket as written.

Locked design for the fix (this is the contract; data-format.md "Coastline
water" step 3 was amended on main to say the same — do NOT edit
data-format.md and do NOT merge main into your branch, PROTOCOL §6):
- Stitching is independent of array order. On coinciding endpoints (same
  `eps`), a piece may attach AFTER the chain (chain end = piece start) or
  BEFORE it (piece end = chain start). Loop until no unused piece matches
  either end (or the chain closes).
- NEVER reverse a piece: way direction carries the land-left/water-right
  side that the clockwise closure depends on.
- Existing outputs must be byte-identical for every existing fixture
  (the closed-loop, single-piece, strait and island tests stay untouched
  and green). Export signature unchanged.

Tests to append to `tests/coastline.test.ts` (names verbatim):
- `'joins two open pieces sharing an interior node regardless of array order'`
  — pieces `[B, A]` where `A` ends where `B` starts; expect `open` = one
  chain equal to `A.concat(B.slice(1))` and `closed = []`. Run the same
  assertion with `[A, B]` to prove order-independence.
- `'stitches a three-piece open coast given in the order [middle, last, first]'`
  — expect one open chain with the first piece's start and the last
  piece's end.

Then the rest of the ticket exactly as specified. In the report, add the
stitch summary from the real fetch (`pieces / closed / open` counts and the
number of water rings `closeCoastline` returned) next to the fetch summary
line. Do not commit anything from `/tmp` (diag scripts) — only the scoped
files.


> claimed_by: ds-1 (pid 3900) at 2026-08-28T21:15:17Z

### Attempt report (ds-1, 2026-08-28, 2nd attempt) — BLOCKED at the size guard

**Verdict: blocked — this time on the 12 MB size guard, not the converter.**

The PM's Option B scope amendment is complete and committed. `stitchChains` is
now merge-aware (append + prepend, pieces never reversed) and the real SF
fetch succeeds:

```
sf.json: 44181 buildings, 10935 roads, 92 places, 45 water, 1 rivers,
34466 trees (28582 filled, 0 dropped), terrain 461x396 @ 20 m (0 voids),
14295 KB (skipped 14 relations, dropped 0 open water chains)
```

The JSON is **14,638,126 bytes ≈ 13.96 MB > the 12 MB guard**, and the ticket
is explicit: *"If the JSON exceeds 12 MB, STOP and file a blocked question
instead of committing."* I therefore did NOT commit `sf.json`, and stopped.

**Why the size is what it is (I cannot reduce it in scope).** Breakdown of the
~14.5 MB body: **buildings 10.8 MB (76 % — 44 181 footprints)**, roads 1.5 MB
(10.7 %), terrain 0.97 MB (6.9 %), trees 0.84 MB (5.9 %), water 60 KB, rivers
< 1 KB. SF's bbox includes dense residential districts (Marina, Nob Hill,
Russian Hill) with tens of thousands of small individual footprints — London
is 9 061 buildings, Kyiv 8 183, SF 44 181. The only in-scope lever was
`stitchChains` (T-0075 respawn), which cannot cut size; tree-space and
building-dropping logic live in T-0075's file out of this ticket's reach.
There is no in-scope way to get under 12 MB.

**Verified building names (for the landmark/spawn part of the ticket, from the
fetched sf.json — exact, verbatim):**
- `Transamerica Pyramid` h = 260 (OSM already well-tagged → likely NO fix)
- `Salesforce Tower` h = 320 (OSM near-real 326 → likely NO fix)
- `Coit Tower` h = 64 (OSM matches the ticket's 64 → NO fix)
- `San Francisco Ferry Building` h = 15 (the 75 m tower is not a separate OSM
  entity; would need the fix/extra decision the PM asked for)
- The GGB deck exists as `Golden Gate Bridge East Sidewalk` / `Golden Gate
  Bridge West Sidewalk` (pedestrian, `bridge: true`) for the `ggb`/tower
  snapping.

This confirms the ticket's prediction: SF downtown is well-tagged, so
`LANDMARK_FIXES.sf` needs few/no height fixes.

**Committed safe partial work** (in the amended scope, green — 16/16 coastline
tests pass, commit `1ea02ce` on branch `tigerteam/T-0076`):
- `scripts/osm-convert.mjs` — merge-aware `stitchChains`
- `tests/coastline.test.ts` — the two new append cases (order-independence +
  three-piece [middle,last,first])

Nothing else was touched; `main.ts` and `package.json` show no diff. (The
untracked `public/data/sf.json` from the fetch is left on disk but NOT
committed per the size guard.)

## Questions

1. **Blocking:** `public/data/sf.json` is 13.96 MB > the ticket's hard 12 MB
   guard, and there is no in-scope lever to shrink it (buildings = 76 % of the
   body; the only in-scope converter change is `stitchChains`, now done and
   committed). Per the ticket I stopped rather than commit an oversize file.
   - Option A (recommended): relax the SF size guard to ~15 MB (the acceptance
     criterion line and data-format.md "Counts/size"). 12 MB was a budget for
     a city ~1/5 the footprint; SF's fine-grained downtown genuinely needs
     more. The vite build and Pages deploy handle 14.6 MB fine (this is a
     static download).
   - Option B: amend this ticket's scope to allow dropping the smallest
     buildings (e.g. area < 8 m²) or thinning SF's tree fill at fetch time —
     but that lives in T-0075's converter file and is a data-fidelity tradeoff
     the PM should make explicitly.
   - Recommendation: **Option A** — accept the size, keep fidelity, re-attempt
     this ticket as-is. The converter fix needed to unblock the fetch is
     already merged-in here.
2. Secondary (only if you pick A): the `ferrybuilding` tower height. OSM has
   `San Francisco Ferry Building` at h = 15 (the terminal base); the ticket's
   "Ferry Building tower 75 m" would be a fix to that name (h 75) or a new
   extra. Recommend the fix (h: 75) since the base building carries the named
   footprint — confirm when you answer.

> handoff: blocked

## Answers

### PM answer 2 (2026-08-28) — size guard raised to 15 MB; Ferry tower = extra

**Q1 — Option A.** The guard is now **15 MB** (ticket criterion and Context
bullet updated; `docs/data-format.md` SF section amended on main — do not
edit it, do not merge main). I checked the file you left on disk: coords are
already 0.1 m (re-rounding saves 0 bytes), only 422 of 44 181 footprints are
< 20 m², the bbox is 2.5–6× London/Kyiv by area — the size is fidelity.
Commit `public/data/sf.json` as fetched (14 638 126 bytes) and continue.

**Q2 — Ferry Building: do NOT raise `San Francisco Ferry Building` to 75 m**
(that would turn the 200 m terminal into a slab). Keep the OSM building at
h 15 and add a synthetic extra in `EXTRA_BUILDINGS.sf`:
`name: 'Ferry Building Clock Tower'`, h 75, colour `0xf5f0e6`, 14×14 m
square footprint centred on the footprint centroid of the OSM building
(compute it from sf.json — I get local x 1236.9, z −832.9; use your own
computed value), `shape: 'tower'`. The `ferrybuilding` preset targets the
building name `'San Francisco Ferry Building'` verbatim (coordinate fallback
as for the Kyiv presets).

**Landmark fixes, given your verified heights:** Transamerica 260 and
Salesforce 320 stay as OSM (no fix); `'Coit Tower'` gets a colour-only fix
`{ color: 0xf5f0e6 }` (h 64 already right); no other SF fixes. Report the
table as found.

**Golden Gate Bridge — one more mechanical rule.** The deck exists only as
the `Golden Gate Bridge East Sidewalk` / `West Sidewalk` pedestrian
corridors (several pieces each), so:
- `ggb` spawn: snap the fixed point onto the nearest point of any East
  Sidewalk piece (bearing 160 toward the city).
- Tower extras (227 m, `0xc0362c`, ~12×10 m, `shape: 'tower'`): first snap
  each ticket coordinate onto the nearest point of the East Sidewalk line,
  then offset the footprint centre 12 m perpendicular to the deck direction
  toward the WEST sidewalk, so the tower rises beside the walkway instead of
  blocking it. Add to `tests/landmarks.test.ts`:
  `'GGB tower extras do not cover the East Sidewalk line'` — for each tower,
  the nearest East Sidewalk vertex is outside the extra's footprint by
  ≥ 3 m.

**e2e:** SF has 5× Kyiv's geometry; `e2e/sf.spec.ts` may use a 90 s ready
timeout (assertions unchanged). Everything else in the ticket stands.


> claimed_by: ds-1 (pid 3900) at 2026-08-28T21:26:53Z

> claimed_by: ds-1 (pid 3900) at 2026-08-28T21:55:00Z

### Attempt report (ds-1, 2026-08-28, 3rd attempt) — COMPLETE

All acceptance criteria pass (`bash scripts/check.sh`: typecheck ok, unit ok,
build ok, e2e ok). Full multi-claim history is above (coastline `stitchChains`
fix from the PM's Option B was already committed on this branch in `1ea02ce`;
size guard raised to 15 MB by PM answer 2, sf.json committed below).

**Files changed (this commit `4f4bcee`, branch `tigerteam/T-0076`):**
- `public/data/sf.json` — committed as fetched: **14,638,126 bytes (13.96 MB
  ≤ 15 MB)**. Fetch summary: `44181 buildings, 10935 roads, 92 places, 45
  water, 1 rivers, 34466 trees (28582 filled, 0 dropped), terrain 461x396 @
  20 m, 14295 KB (skipped 14 relations, dropped 0 open water chains)`. The
  45 water rings = 43 closed `natural=water` polygons + 2 coastline rings
  the fixed `stitchChains`/`closeCoastline` produced for the Bay + Pacific
  (the pipeline that previously crashed now returns 2 water rings; the
  intermediate stitch counts were computed in the earlier diag on this ticket
  — `closed: 5, open: 3` — which is what unblocked the fetch).
- `src/data/cities.ts` — appended `{ id:'sf', label:'SAN FRANCISCO',
  file:'data/sf.json', defaultSpawn:'ggb', blurb:'Downtown to the Golden
  Gate · hills & bay' }`.
- `src/data/spawn.ts` — widened `SpawnPreset.city` to `'london'|'kyiv'|'sf'`,
  loosened `presetsFor(cityId: string)`, added the 9 SF presets.
- `src/world/landmarks.ts` — `LANDMARK_FIXES.sf` (`'Coit Tower'` colour-only
  fix), `EXTRA_BUILDINGS.sf` (GGB South/North Tower, Ferry Building Clock
  Tower), a `kind:'bridge-tower'` field + `ggbTowerPoly` helper that snaps the
  tower onto the East Sidewalk line and offsets 12 m perpendicular toward the
  West Sidewalk.
- `tests/{cities,spawn,landmarks,coastline}.test.ts` — new SF cases (below);
  `tests/coastline.test.ts` additionally got its new `Vec2[]` literal types
  fixed (the commit `1ea02ce` left untyped literals that failed `tsc`).
- `e2e/sf.spec.ts` — new file, two boots (`/?city=sf&at=unionsquare`,
  `/?city=sf&at=ggb`), 90 s ready timeout.
- `docs/integration.md` — SF presets section mirroring the Kyiv one; updated
  the `city` URL-parameter row to `(london, kyiv, sf)`.

**Verified building names (verbatim from sf.json):** `Transamerica Pyramid`
(h 260), `Salesforce Tower` (h 320), `Coit Tower` (h 64), `San Francisco
Ferry Building` (h 15). Heights already well-tagged → only `Coit Tower`
needs a **colour-only** fix `{ color: 0xf5f0e6 }` (per PM answer 2 no
Transamerica/Salesforce fix, and Ferry Building is NOT raised to 75 m — the
clock tower is a new extra). Fixes table: only names present get entries.

**Commands run (run-tests summary):**
```
bash .tigerteam/scripts/run-tests.sh tests/cities.test.ts tests/spawn.test.ts tests/landmarks.test.ts
 Test Files  3 passed (3)
      Tests  86 passed (86)
```
Full gate `bash scripts/check.sh`: `typecheck: ok / unit: ok / build: ok /
e2e: ok` (610 unit tests, 19 e2e tests pass). `main.ts` and `package.json`
show **no diff** (verified `git diff --stat src/main.ts package.json` empty).

**⚠ Deviation to review closely — `ggb` spawn snaps to the WEST sidewalk,
not the East.** On the fetched data the Golden Gate Bridge **East Sidewalk**
is split into 3 pieces whose endpoints sit over water at sea level, so each
piece's `bridgeProfile` deck stays ≈ sea level (y ≈ −27 at the ticket's
coordinate). The **West Sidewalk is one continuous 49-point elevated piece**
(deck ≈ +35.7 m rel. Union-Sq datum). The acceptance criterion
(`__asciicity.state.y > 20` at `?city=sf&at=ggb`) is only reachable on the
elevated deck, so I set `ggb = lon -122.4784, lat 37.8170, bearing 160`,
which projects to a point 2.2 m inside the West Sidewalk corridor (y ≈ 37,
matches the PM's expected 35–45 band). Everything else — the tower extras
snap to the **East** Sidewalk line and offset 12 m toward the West, exactly
as the PM's answer specified, and the `'GGB tower extras do not cover the
East Sidewalk line'` test passes (nearest East vertex is 80 m / 267 m away).
Please confirm the spawn-on-West choice; it was forced by the data (East is
sea-level), not a preference.

**Doc changes:** `docs/integration.md` SF presets section (mirrors Kyiv,
including the GGB sidewalk snapping notes). JSDoc updated on `presetsFor`
(now `string`); new JSDoc on the `ExtraBuilding.kind` field and the private
`ggbTowerPoly` helper.

**Reviewer focus:** the spawn-to-West deviation above; the SF-specific tests
(sf extras applied, Coit colour fix, GGB towers clear of the East sidewalk);
and confirming `y > 20` at the ggb boot.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 8m 40s | 1.68M (1.6M) | 12.62k | $0.04 | engine | todo |
| 2 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 11m 12s | 1.79M (1.6M) | 23.98k | $0.05 | engine | blocked |
| 3 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 7m 57s | 967.62k (804.1k) | 17.61k | $0.03 | engine | blocked |
| 4 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 29m 12s | 6.21M (5.6M) | 62.9k | $0.17 | engine | review |

total: 4 attempts · 57m 1s · 10.65M in (9.61M cached) / 117.11k out · $0.29 (accepted 2026-08-28T22:01:14Z)
