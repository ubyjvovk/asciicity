---
id: T-0021
title: Minimap contrast for the real city (dim named buildings, brighter roads)
priority: P2
complexity: C1
depends_on: [T-0014]
scope: [src/hud/minimap.ts, docs/minimap.md]
---

## Goal
In the real dataset ~1/3 of buildings are named, so the minimap is a wall of
bright green. Make footprints dim and roads the bright element so the street
pattern reads at a glance.

## Context
- `src/hud/minimap.ts` (T-0014) `update()` draws roads then buildings.
- New colours: unnamed footprint `#143019`, named footprint `#245c2f`, roads
  `#3fb85a` at 1 px, player triangle and `N` marker unchanged (`#8aff9e`).
  Draw order becomes **buildings first, then roads on top** so roads are
  never hidden by footprints.
- Update the colour table in `docs/minimap.md`.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/minimap.test.ts` exits 0 (pure helpers unchanged, tests untouched)
- [ ] Code review: the four colour constants above and the buildings-then-roads order are present in `update()`
- [ ] `docs/minimap.md` colour table updated; `bash scripts/check.sh` exits 0
- [ ] No files changed outside scope

## Out of scope
- Zoom, labels, wiring changes in `main.ts`.

## Worker report

> claimed_by: opus-1 (pid 3364115) at 2026-08-23T23:48:05Z

### Files changed
- `src/hud/minimap.ts` — in `Minimap.update()`, swapped draw order to
  buildings-then-roads and updated the four colour constants: unnamed
  footprint `#143019`, named footprint `#245c2f`, road stroke `#3fb85a`.
  Player triangle and N marker (`#8aff9e`) unchanged.
- `docs/minimap.md` — updated the "Colours (draw order)" table to reflect
  the new palette and layer order, added one paragraph explaining why
  (roads now stroke on top of dim footprints).

### Commands run
- `bash .tigerteam/scripts/run-tests.sh tests/minimap.test.ts` → `exit: 0`,
  Test Files 1 passed (1), Tests 7 passed (7) (pure helpers unchanged).
- `bash scripts/check.sh` → `typecheck: ok`, `unit: ok`, `build: ok`,
  `e2e: ok`.

### Docs
- `docs/minimap.md` colour table + one clarifying paragraph on the new
  contrast intent.

### Commit
- `14576d8` on branch `tigerteam/T-0021`.

### For the reviewer
- Draw order in `src/hud/minimap.ts:126-160` is now buildings first
  (fill), then roads (stroke on top). Colours match the ticket exactly.
- No behavioural change to `worldToMinimap` / `nearbyCells`, so the
  unit-test suite was left untouched.

> handoff: review
