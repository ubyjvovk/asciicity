---
id: T-0028
title: Default spawn on Westminster Bridge facing Big Ben, plus Westminster presets
priority: P0
complexity: C1
depends_on: [T-0027, T-0024]
scope: [src/data/spawn.ts, tests/spawn.test.ts, docs/integration.md, README.md]
---

## Goal
Start the game with Big Ben straight ahead: the default spawn (no `?at=`)
becomes a point on Westminster Bridge looking west at the Elizabeth Tower,
and a few Westminster presets join the list.

## Context
- `src/data/spawn.ts` (T-0022): `SPAWN_PRESETS` is `Record<string, SpawnPreset>`
  with `{ lon, lat, bearingDeg, label }`; `resolveSpawn` falls back to the
  `bank` preset when `param` is null/unknown.
- Add presets (lon, lat, bearingDeg, label):
  `bigben` (−0.12235, 51.50085, 268, "Westminster Bridge, facing Big Ben"),
  `parliament` (−0.12655, 51.50060, 90, "Parliament Square, facing the Palace of Westminster"),
  `trafalgar` (−0.12800, 51.50790, 180, "Trafalgar Square, facing Whitehall"),
  `embankment` (−0.12200, 51.50740, 120, "Victoria Embankment, facing the London Eye").
- Change the default: `resolveSpawn` uses `bigben` when `param` is null,
  empty, or unknown (`bank` stays available as `?at=bank`). The
  `synthetic=1` path in `main.ts` is unaffected (it never calls `resolveSpawn`).
- Adjust existing tests accordingly (the "defaults to the bank preset"
  case becomes "defaults to bigben": expected point = `project(-0.12235, 51.50085, ORIGIN)`
  and yaw = `normalizeAngle(268°)`), add one case per new preset key, keep
  all coordinate/blocked-walk cases.
- Docs: `docs/integration.md` preset table + default; README "How to play".
- Visual check (throwaway Playwright, GPU flags if available:
  `--use-angle=gl-egl --ignore-gpu-blocklist`, else SwiftShader with `?cell=3x6`):
  load `/` with no params and confirm the HUD ZONE reads a Westminster
  street/bridge and the LANDMARK row or the view shows the tower; describe it.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/spawn.test.ts` exits 0
- [ ] Tests: default (null / '' / 'nowhere') resolves to the `bigben` point and yaw; each new preset key parses and resolves within 1e-6 of its projected point; `?at=bank` still resolves to the origin
- [ ] `bash scripts/check.sh` exits 0 (incl. `e2e: ok`); docs + README updated; JSDoc intact
- [ ] No files changed outside scope

## Out of scope
- Data-driven landmark spawns (T-0026), `main.ts` changes, camera pitch presets.

## Worker report

> claimed_by: ds-1 (pid 3364115) at 2026-08-24T00:14:48Z
> INCOMPLETE attempt by ds-1 at 2026-08-24T00:18:25Z (engine exit 1, no handoff) — see /home/d/asciicity/.tigerteam/logs/workers/ds-1.log

> claimed_by: ds-1 (pid 3364115) at 2026-08-24T00:18:25Z
> claimed_by: ds-1 (pid 26406) at 2026-08-24T00:19:40Z

## Details

Commit: `662c700` on `tigerteam/T-0028` (4 scope files: `src/data/spawn.ts`,
`tests/spawn.test.ts`, `docs/integration.md`, `README.md`).

### Files changed (one line each)
- `src/data/spawn.ts` — added the four Westminster presets (`bigben`,
  `parliament`, `trafalgar`, `embankment`) to `SPAWN_PRESETS`; changed the
  `resolveSpawn` fallback from `bank` → `bigben` so null/empty/unknown
  `?at=` defaults to Westminster Bridge facing Big Ben (bank still reachable
  as `?at=bank`). JSDoc updated to match.
- `tests/spawn.test.ts` — default case flipped to bigben (point =
  `project(-0.12235, 51.50085, ORIGIN)`, yaw `normalizeAngle(268°)`), added
  empty/unknown default case, `?at=bank` still resolves to the origin, one
  resolve case per new preset (1e-6 precision) + a parseAt case per new key,
  preset-key list assertion extended; blocked-walk cases now pin `'bank'` so
  the +x search starts at x0 = 0.
- `docs/integration.md` — preset table gains the four Westminster rows; note
  that no `?at=` spawns at `bigben` and `bank` stays available.
- `README.md` — "How to play" `?at=` list extended with the four new preset
  names; default-spawn sentence added.

Note: the earlier "INCOMPLETE attempt" failed at the visual step because the
DS worker model (DeepSeek V4 Flash) rejects image input when given a
screenshot; no spec/code contradiction caused it — the implementation had
already landed in the worktree and only needed commit + report. No stray files
remain.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh tests/spawn.test.ts` → exit 0;
  summary: `Test Files 1 passed (1)`, `Tests 20 passed (20)`.
- `bash scripts/check.sh` → exit 0: `typecheck: ok`, `unit: ok`, `build: ok`,
  `e2e: ok`.
- Throwaway Playwright against `/` (no params, real data) — HUD ZONE =
  `WESTMINSTER BRIDGE ROAD`, BEARING = `268 DEG / WEST`, WORLD = −2166.21 /
  1376.65 (the projected bigben point west of origin). Confirms the default
  spawn lands on Westminster Bridge facing west toward Big Ben. (Model can't
  ingest the screenshot, so the rendered tower is confirmed indirectly by the
  e2e render smoke + this ZONE/BEARING read; PM visual review on the GPU
  remains the arbiter.)

### Documentation
- `docs/integration.md` preset table + default; `README.md` updated.
- `spawn.ts` JSDoc for `resolveSpawn` updated to say `bigben` is the default;
  the four new preset rows carry their label in the preset object itself.

### Acceptance criteria walk-through
1. `run-tests.sh tests/spawn.test.ts` exits 0 — ✅ (20 passed).
2. Default (null / '' / 'nowhere') → bigben point + yaw; each new preset key
   parses and resolves within 1e-6 of its projected point; `?at=bank` still
   resolves to the origin — ✅ all covered by name in `tests/spawn.test.ts`.
3. `check.sh` exits 0 incl. `e2e: ok`; docs + README updated; JSDoc intact — ✅.
4. No files changed outside scope — ✅ (working tree clean after commit; the
   throwaway check script was deleted).

### Out-of-scope / reviewer notes
- The thrown `?at=nowhere` default lands the walk search at the bigben point;
  with the real map the WESTMINSTER BRIDGE ROAD cell is free so no walk
  needed. Didn't touch `main.ts` (synthetic path) or add data-driven landmark
  spawns (T-0026) — both out of scope.
- Visual confirmation of the tower in-frame relies on PM GPU review
  (`scripts/pm-shot.mjs` or `--use-angle=gl-egl`); checked-in e2e uses
  `?synthetic=1` by design and doesn't cover the default real-data spawn.

> handoff: review
