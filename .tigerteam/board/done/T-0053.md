---
id: T-0053
title: Render style — teletext (Ceefax sixel mosaic)
priority: P1
complexity: C2
depends_on: [T-0050]
scope: [src/render/styles/teletext.ts, tests/styles/teletext.test.ts, docs/styles/teletext.md]
---

## Goal
Replace the stub `src/render/styles/teletext.ts` with the real **teletext (Ceefax sixel mosaic)** look,
exactly as specified in `docs/architecture.md` §4.11 "teletext". One file,
one shader, pure helpers unit-tested in node.

## Context
- Read `src/render/style.ts` (PM-owned: `RenderStyle`, `STYLE_PRELUDE`
  helpers — `sampleSub`, `cellMean`, `bright`, `shaped`, `tintOf`,
  `linearDepth`) and `src/render/styles/ascii.ts` (the reference style:
  how the atlas, `makeUniforms` and the fragment fit together) before
  writing code. Keep the module's `export const STYLES` and the id(s) the
  stub declares; keep cell/sub/needsDepth per §4.11.
- The shader must mirror the pure helpers term for term (the tests are the
  spec for the shader); pure helpers never touch `document`.
- Atlases are procedural (canvas rectangles/arcs/lines), never font glyphs,
  unless §4.11 says otherwise; build them in `makeUniforms` via
  `ctx.makeCanvas`; `dispose` frees the texture.
- `docs/styles/teletext.md`: the algorithm in prose, the pure exports, and a
  note on what the shader does per pixel.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/styles/teletext.test.ts tests/post.test.ts` exits 0
- [ ] Tests by name: `sixelBits` of a bottom-bright column lights bits 0 and 1 only; all-equal → 63; `teletextIndex` maps pure red → 1, yellow → 3, cyan → 6, white → 7, a dim grey (bright < 0.15) → 0, and an orange `(1, 0.6, 0)` → yellow (3); the palette has 8 entries with components 0/1
- [ ] `bash scripts/check.sh` exits 0 (the e2e style loop still visits every id and saves `e2e/__shots__/style-<id>.png` — mention the file for this style in the report)
- [ ] Headless evidence: `/?synthetic=1&render=<id>&cell=3x6` renders > 2 % non-black pixels and `__asciicity.render` is the id; no WebGL shader compile errors in the console (quote the console tail)
- [ ] JSDoc on every export; `docs/styles/teletext.md` written
- [ ] No files changed outside scope

## Out of scope
- Any other style, `post.ts`, `style.ts`, `main.ts`, HUD, docs outside `docs/styles/`.

## Worker report

> claimed_by: grok-2 (pid 484850) at 2026-08-26T23:25:53Z

### Files changed
- `src/render/styles/teletext.ts` — replaced the ascii stub with the §4.11 Ceefax sixel style: `TELETEXT_PALETTE`, `teletextIndex`, `sixelBits`, analytic 2×3 fragment, `STYLES` id `teletext` at cell 6×12 / sub 2×3 / no depth.
- `tests/styles/teletext.test.ts` — named cases for sixelBits (bottom-bright → bits 0+1; all-equal → 63), teletextIndex (red/yellow/cyan/white/dim grey/orange), palette 8×{0,1}.
- `docs/styles/teletext.md` — algorithm, pure exports, per-pixel shader note.

### Commands run
```
bash .tigerteam/scripts/run-tests.sh tests/styles/teletext.test.ts tests/post.test.ts
```
```
 Test Files  2 passed (2)
      Tests  15 passed (15)
   Start at  23:31:58
   Duration  359ms
```
(`✓ tests/styles/teletext.test.ts (11 tests)`, `✓ tests/post.test.ts (4 tests)`)

```
bash scripts/check.sh
```
```
typecheck: ok
unit: ok
build: ok
e2e: ok
log: .tigerteam/logs/tests/check-20260826-233203-220.log
```
e2e: `✓ smoke: R cycles every render style and each paints (58.7s)` — 5 passed. Screenshot: `e2e/__shots__/style-teletext.png`.

Headless probe `/?synthetic=1&render=teletext&cell=3x6` (Playwright Chromium + SwiftShader against vite :4175):
- `__asciicity.render` = `teletext`
- non-black fraction = **0.236** (> 2 %)
- canvas 1280×720, `ready: true`
- console tail (2 lines, no shader compile errors):
  ```
  debug: [vite] connecting...
  debug: [vite] connected.
  ```
  `shaderish` filter (shader|WebGL|THREE|error|compile) matched nothing.

### Doc changes
Wrote `docs/styles/teletext.md` (algorithm, exports, shader). JSDoc on every export (`TELETEXT_PALETTE`, `teletextIndex`, `sixelBits`, `STYLES`, plus `TeletextRgb`).

### Commit
- hash: `5f3716fc7903cb553deac6ff98137dbf2741570c`
- branch: `tigerteam/T-0053`
- message: `[T-0053] Render style — teletext (Ceefax sixel mosaic)`

### Reviewer notes
- Visual: `e2e/__shots__/style-teletext.png` shows a 2×3 mosaic, 8-colour (0/1) buildings, toast `RENDER: TELETEXT`. Overlay still up (e2e loop does not click through). SwiftShader HUD FPS 21 — expected; GPU ≥ 30 fps is a PM check (`?city=kyiv&render=teletext`).
- Shader is analytic (no atlas); `makeUniforms` returns `{}`. Sixel order is bottom-first (`sampleSub` y=0 = bit 0/1).
- Out of scope, not touched: `docs/render.md` still says `teletext` is “Stub until T-0053”.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | grok-2 | grok-4.6-build | 10m 12s | 1.75M (1.47M) | 31.19k | $0.25 | engine | review |

total: 1 attempt · 10m 12s · 1.75M in (1.47M cached) / 31.19k out · $0.25 (accepted 2026-08-26T23:41:28Z)
