---
id: T-0062
title: Landmark silhouettes — dome / spire / tower caps from the landmark table
priority: P2
complexity: C2
depends_on: [T-0059]
scope: [src/world/buildings.ts, src/world/landmarks.ts, tests/buildings.test.ts, tests/landmarks.test.ts, docs/world.md]
---

## Goal
Make the key landmarks recognisable from across the river: per
`docs/architecture.md` §4.13 "Shapes", a `shape` in the landmark table
(`dome` / `spire` / `tower`) becomes a procedural cap on the building.

## Context
- `Building.shape?` already exists in `src/data/types.ts` (PM-owned).
  `applyLandmarks` (T-0059) must copy `shape` from the fix onto the building
  (and the extra Motherland Monument gets `tower`); extend `LANDMARK_FIXES`
  with the shapes listed in §4.13 for Kyiv and London.
- `buildBuildingsMesh`: after emitting a building's roof, emit its cap in
  **group 0** (walls material) with the wall colour, per the exact geometry
  in §4.13 (dome: 8 segments × 4 rings hemisphere; spire: 8-segment cone;
  tower: half-size box of `0.5·h`). Normals outward (compute per face), UV
  `(0,0)`. Buildings without `shape` are byte-identical to today.
- Tests: `tests/buildings.test.ts` — a `dome` building adds exactly
  `8·4·2 = 64` triangles (192 vertices) in group 0 above roof height with
  every normal unit-length and pointing away from the dome centre; a
  `spire` adds 8 side triangles + 8 base triangles (or none — say which)
  reaching `roofY + 0.6·h` at the apex; `tower` adds a box whose top is
  `roofY + 0.5·h`; no `shape` → identical output to before (compare arrays).
  `tests/landmarks.test.ts` — Kyiv fixes carry the §4.13 shapes; Motherland
  extra is `tower`.
- `docs/world.md`: shapes section.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/buildings.test.ts tests/landmarks.test.ts` exits 0
- [ ] Tests by name as in Context
- [ ] `bash scripts/check.sh` exits 0
- [ ] Report the vertex-count delta of `buildBuildingsMesh(applyLandmarks(kyiv).buildings)` versus without shapes (expect a few hundred extra vertices, not thousands)
- [ ] JSDoc; docs updated
- [ ] No files changed outside scope

## Out of scope
- Labels (T-0063), colours/heights (T-0059), any non-landmark geometry.

## Worker report

> claimed_by: ds-2 (pid 484850) at 2026-08-27T09:25:57Z

## Worker report

**Claimed by ds-2.**

### Acceptance criteria — status

All six pass:
- `bash .tigerteam/scripts/run-tests.sh tests/buildings.test.ts tests/landmarks.test.ts` exits 0.
  Summary: `Test Files 2 passed (2)` · `Tests 31 passed (31)`.
- Tests by name as in Context: buildings `dome` (64 triangles / 192 vertices, group 0, outward unit normals), `spire` (8 side triangles, apex `roofY + 0.6·h`, base omitted — documented), `tower` (box top `roofY + 0.5·h`), no-shape → byte-identical (compare arrays); landmarks: Kyiv §4.13 shapes, Motherland extra `tower`.
- `bash scripts/check.sh` exits 0: `typecheck: ok · unit: ok · build: ok · e2e: ok`.
- Vertex-count delta `buildBuildingsMesh(applyLandmarks(kyiv).buildings)` vs shapes stripped: **918 extra vertices** (4 domes ×192 + 5 spires ×24 + 1 tower ×30 = 768+120+30). Not thousands.
- JSDoc on every new export/helper; `docs/world.md` shapes section added.
- No files changed outside scope (git status shows only the 5 scope files).

### Files changed
- `src/world/landmarks.ts` — `LandmarkFix.shape` field; `applyLandmarks` copies `shape` onto matching buildings and stamps `shape:'tower'` on the Motherland Monument extra; `LANDMARK_FIXES` extended with the §4.13 shapes (Kyiv domes/spires; London St Paul's `dome` + Elizabeth Tower `spire`, shape-only so heights/counts unchanged).
- `src/world/buildings.ts` — `emitCap` (dome/ spire/ tower) emitted into group 0 (walls material, wall colour) after each building's walls; cap helpers (`emitTri`/`emitQuad` orient normals outward per face). Buildings without `shape` are byte-identical.
- `tests/buildings.test.ts` — 4 new shape tests (dome count/group/outward-normals, spire apex, tower top, no-shape identity).
- `tests/landmarks.test.ts` — Kyiv shape test, Motherland `tower` test; updated the two old London tests (the London table is no longer empty after T-0062).
- `docs/world.md` — new "Silhouette caps (`Building.shape`, T-0062)" section.

### Commands
- `bash .tigerteam/scripts/run-tests.sh tests/buildings.test.ts tests/landmarks.test.ts` → `Test Files 2 passed (2)`, `Tests 31 passed (31)`.
- `bash scripts/check.sh` → `typecheck: ok / unit: ok / build: ok / e2e: ok`.
- Delta measured with a throwaway `tests/delta.test.ts` (deleted afterwards) → 918 extra vertices.

### Commit
`0a1c04b` on branch `tigerteam/T-0062` — `[T-0062] Landmark silhouettes — dome/spire/tower caps from the landmark table`.

### Notes for reviewer
- **London table is no longer empty** (this is a deliberate, doc-driven change — architecture.md §4.13 explicitly gives London `St Paul's → dome`, `Elizabeth Tower → spire`). I updated the two T-0059-era tests that asserted an empty London table ("London table is empty", "London unchanged deep-equal") to reflect this. The shape-only fixes leave heights/counts untouched and register no colours. Flag if the PM intended London to stay empty.
- The dome's top band emits 8 **degenerate** (zero-area, invisible) closing triangles so the count is exactly `8·4·2 = 64`, per the Context. Their normal falls back to `(0,1,0)` and still points away from the dome centre, so the normal test passes.
- Spire base is omitted (coplanar with roof, would z-fight); documented — the Context allowed "or none — say which".
- Visual (GPU) review of the caps themselves is left to the PM; geometry correctness is covered by the unit tests.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-2 | deepseek-ai/DeepSeek-V4-Flash-0731 | 24m 18s | 2.03M (1.79M) | 41.03k | $0.06 | engine | review |

total: 1 attempt · 24m 18s · 2.03M in (1.79M cached) / 41.03k out · $0.06 (accepted 2026-08-27T09:52:51Z)
