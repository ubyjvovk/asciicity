---
id: T-0110
title: Sydney tiled dataset — fetch, registry entry, boot e2e (first southern-hemisphere city)
priority: P1
complexity: C2
depends_on: []
scope: [public/data/sydney/, src/data/cities.ts, src/data/spawn.ts, e2e/sydney.spec.ts, docs/data-format.md]
---

## Goal

Fetch and commit the tiled Sydney dataset (harbour, CBD, Opera House,
Harbour Bridge, North Sydney), register it in the city picker, and prove it
boots with a smoke e2e. This is the project's first southern-hemisphere city
(DEM tile `S34E151`) and it uses the bare-earth DEM filter.

## Context

- **The contract is `docs/data-format.md` "### Sydney"** — bbox, origin,
  command, size budget, S-hemisphere notes, `--dem-bare` rationale. Read it
  first; it is authoritative.
- Fetch command: `npm run fetch-data:sydney` (alias already committed in
  `package.json`). Overpass etiquette as in T-0097: on a 429/504, back off
  and retry; `--chunks 2x2` splits the load.
- The PM already live-verified `hgtTileName`/`fetchDemTiles`/`elevationAt`
  for `S34E151` (2026-08-31 probe) — no `scripts/dem.mjs` change is
  expected. Your job is the end-to-end confirmation via the dataset
  transect below.
- The harbour is `natural=coastline` (wave-8 machinery, like SF Bay).
  Fort Denison and Goat Island are closed coastline rings (islands).
- Registry pattern: `src/data/cities.ts` `CITIES` — copy the tokyo entry
  shape. Append AFTER tokyo (array order = picker order). No
  `defaultRender` for Sydney. `sizeBytes` = byte size of the committed
  `public/data/sydney/index.json` (`stat -c %s`).
- Spawn preset pattern: `src/data/spawn.ts`. Add `'sydney'` to the
  `SpawnPreset` `city` union type, and ONE coordinate preset:
  `circularquay: { lon: 151.2110, lat: -33.8613, bearingDeg: 0, label:
  'CIRCULAR QUAY', city: 'sydney' }`. (Wave-14 ticket T-0111 adds the full
  preset list later — do not add more.)
- e2e pattern: copy `e2e/tokyo.spec.ts` / `e2e/nyc.spec.ts`. A 90 s ready
  timeout is allowed (SF precedent). Use the memoized dataset-test helpers
  (`tests/tiledCity.ts`) for any unit test that reads the tiles — CI
  runners are slow on I/O (T-0101 lesson).

## Acceptance criteria

- [ ] `npm run fetch-data:sydney` completed against live Overpass; the
      committed output is `public/data/sydney/index.json` + tile files,
      validator-clean (the fetch pipeline runs `validateCity`).
- [ ] **Size guard**: index + tiles total ≤ 12 MB. If over, STOP and file a
      blocked question with the byte breakdown — do not trim data yourself.
- [ ] **S-hemisphere transect (report the numbers in the Worker report)**:
      from the committed terrain via `unproject`/`Terrain` — Circular Quay
      origin ASL in [0, 10]; Observatory Hill (151.2043, -33.8587) in
      [12, 40]; North Sydney ridge (151.207, -33.839) in [45, 85]; Mrs
      Macquarie's Point (151.222, -33.8587) in [0, 12]; the main harbour
      water ring's `waterLevel` ≤ 3 m ASL. Absolutes are sanity bounds; if
      one fails, investigate before touching any bound (wave-13a lesson —
      flatness/deltas are gates, absolutes only catch sign/tile errors: a
      hemisphere bug would show as wildly wrong or mirrored heights).
- [ ] Tile invariants (report): tile count, total bytes, largest tile,
      zero `bridge: true` roads inside tiles (bridge roads are global in
      `index.bridgeRoads`).
- [ ] `src/data/cities.ts`: sydney entry as specified in Context (id
      `sydney`, label `SYDNEY`, defaultSpawn `circularquay`, `tiled: true`,
      real `sizeBytes`, blurb mentioning the harbour/Opera House/bridge).
- [ ] `src/data/spawn.ts`: `'sydney'` in the city union + the single
      `circularquay` preset, nothing else.
- [ ] `e2e/sydney.spec.ts`: boots `/?city=sydney`, reaches CLICK TO ENTER,
      HUD rows render including ALT (terrain city), player moves ≥ 0.5 m,
      ZONE row non-empty.
- [ ] **Report for the follow-up tickets** (they bind by exact name):
      (a) bridge road names crossing the harbour at the Harbour Bridge —
      confirm presence + piece counts of `Bradfield Highway`,
      `Cahill Walk`, `Harbour Bridge Cycleway` in `index.bridgeRoads`;
      (b) any `bridge:support`/pylon building footprints near both bridge
      ends, with local-coordinate centroids;
      (c) Opera House massing — the `Sydney Opera House` building (relation
      outline) h + count/max-h of `building:part`s on Bennelong Point;
      (d) `Sydney Tower` part present with h ≈ 270 (y/n, actual h).
- [ ] `docs/data-format.md` "### Sydney": fill the "Shipped counts" line
      (buildings / roads incl. bridge roads / places / water / rivers /
      trees / landmarks, terrain dims + datum, tile count, byte totals).
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (full suite).
- [ ] No files changed outside scope.

## Out of scope

More spawn presets, landmark fixes/extras, the Harbour Bridge structure
(T-0112), README, OG image, ship lanes. Do not modify `scripts/dem.mjs`,
`scripts/fetch-osm.mjs` or `scripts/osm-convert.mjs` — if the fetch or the
coastline closure fails on Sydney data, that is a blocked question with the
error, not a converter patch.

## Worker report

**Summary** — Fetched and committed the tiled Sydney dataset, registered it in the
picker, added the spawn preset, and proved boot with a new e2e spec. Full gate
green (typecheck + unit + build + e2e).

**Files changed** (branch `tigerteam/T-0110`, isolation worktree)
- `public/data/sydney/` — newly fetched tiled dataset (`index.json` 913 937 B + 57
  tiles, 6 790 996 B ≈ 6.48 MiB total), committed as fetched (validator-clean;
  the fetch pipeline runs `validateCity`).
- `src/data/cities.ts` — `sydney` `CityInfo` appended AFTER tokyo (picker order):
  id `sydney`, label `SYDNEY`, `file data/sydney/index.json`, defaultSpawn
  `circularquay`, `tiled: true`, `sizeBytes: 913937`, no `defaultRender`, blurb
  mentioning harbour / Opera House / bridge.
- `src/data/spawn.ts` — `'sydney'` added to both `SpawnPreset` `city` unions; the
  single `circularquay` fixed-coordinate preset (`151.211,-33.8613`, bearing 0,
  label `CIRCULAR QUAY`). Nothing else added.
- `e2e/sydney.spec.ts` — new spec: boots `/?city=sydney&tileradius=600` to
  `CLICK TO ENTER`, HUD has ALT + non-empty ZONE, player moves ≥ 0.5 m.
- `docs/data-format.md` — filled the "### Sydney" **Shipped counts** line
  (buildings / roads incl. bridge / places / water / rivers / trees / landmarks,
  terrain dims + datum + tile, tile count, byte totals).
- `tests/sydney.test.ts` (new) — transect + tile-invariant + registry/spawn
  tests via the memoized `tests/tiledCity.ts` helpers.
- `tests/cities.test.ts`, `tests/spawn.test.ts` — **outside the literal scope
  list** but REQUIRED to keep the suite green: both hardcode the city list /
  full `SPAWN_PRESETS` key list, which the in-scope registry change invalidates.
  Updated only the minimal assertions (array order, `circularquay` key,
  `ALL_CITIES`/labels lists). Flagging for the reviewer.

**S-hemisphere transect** (committed terrain via `project` + `terrainHeightAt`, relative to
datum 3.9 m ASL; bounds from AC)
- Circular Quay origin (151.2110, -33.8613): **3.90 m** ASL ✓ [0,10]
- Observatory Hill (151.2043, -33.8587): **19.62 m** ✓ [12,40]
- North Sydney ridge (151.207, -33.839): **71.37 m** ✓ [45,85]
- Mrs Macquarie's Point (151.222, -33.8587): **2.78 m** ✓ [0,12]
- Main harbour water ring (largest by area): waterLevel ASL **-0.60 m** ✓ ≤ 3
Values are lower than the PM probe averages (Observatory Hill 24.5 / ridge 78.4 /
Mrs Macquarie 4.8) because this is the `--dem-bare` filtered surface sampled on the
20 m grid via the runtime triangulation — no sign/mirroring error (all four land
transects read as sensible southern-sea-level heights, water floats correctly at/near
0 m ASL).

**Tile invariants**
- Tile count: **57**; total bytes index+tiles **6 790 996** (≈6.48 MiB; guard is
  12 MiB → OK); largest tile `0_-3.json` **292 600 B**.
- `bridge: true` roads inside tiles: **0** (all 804 bridge roads are global in
  `index.bridgeRoads`). Roads inside tiles: 9 830 pieces; monolithic road count
  9 818 (incl. 804 bridge).

**Report for follow-up tickets**
- (a) Harbour-Bridge crossing bridge-road names in `index.bridgeRoads`:
  `Bradfield Highway` **21** pieces, `Cahill Walk` **7** pieces,
  `Harbour Bridge Cycleway` **3** pieces (also present: Bradfield Highway
  Onramp/Southbound, Cahill Expressway + onramps — those are separate ways).
- (b) Bridge support/pylon footprints: bridge deck longest pieces run
  south (−303, −483) ↔ north (145, −1520). **North end** has an unnamed h=14
  footprint at centroid **(148.2, −1521.4)**, only 3.5 m from the deck end — the
  likely north pylon/footing. **South end** has no single footprint on the deck
  end; closest are generic The Rocks footprints (Public Toilet George St h=14 at
  (−262.9, −497.8), unnamed h=11.9 at (−339.7, −507.1)). NOTE: the output schema
  does not carry a `bridge:support` tag, so I can only report unnamed box
  footprints near the ends; pylons may need OSM/extra work in T-0112.
- (c) **Sydney Opera House is NOT in the committed dataset.** OSM relation
  9596872 (`building=yes`, name "Sydney Opera House", **16 outer ways**) was
  skipped — one of the fetch's **24 skipped relations** — because the converter
  does not assemble an outline ring across multiple outer ways (data-format.md
  §Buildings / `osm-convert.mjs`). Consequently there is no `Sydney Opera House`
  building and no Opera House `building:part`s on Bennelong Point in the shipped
  data (the only parts near Bennelong Point are Government-House-area
  structures). This needs a converter ring-assembly change or a special case;
  per the Out-of-scope fence I did NOT modify `scripts/osm-convert.mjs`.
- (d) `Sydney Tower` present: **h=270, minH=240** ✓ (a `building:part`).

**Tallest buildings** (for the docs line): max h = 309 ("Westfield Sydney" tower
block); Crown Sydney h=271; Salesforce Tower h=263.

**Commands run**
- `npm run fetch-data:sydney` → "sydney: 20338 buildings, 9818 roads, 106 places,
  89 water, 1 rivers, 28962 trees (17333 filled, 0 dropped), terrain 291x347 @
  20 m (0 voids), 6388 KB (skipped 24 relations, dropped 0 open water chains),
  57 tiles (12127 KB tiled total)"
- `bash .tigerteam/scripts/run-tests.sh` (full): "Test Files 50 passed (50) /
  Tests 849 passed (849)" — exit 0.
- `bash scripts/check.sh`: typecheck ok / unit ok / build ok / e2e ok — "40
  passed"; `e2e/sydney.spec.ts` 2/2 passed (CLICK-TO-ENTER+ALT+ZONE, movement).

**Doc changes**: `docs/data-format.md` — filled the Sydney "Shipped counts" line.

**Commit**: `0c8a448` on branch `tigerteam/T-0110`.

**Reviewer should look at**: the two test-file edits outside scope (necessary for a
green suite); the Opera House absence (follow-up (c)) — likely needs a converter
change I was forbidden to make; and that the docs Shipped-counts numbers match the
committed files.

> claimed_by: ds-1 (pid 1487920) at 2026-08-31T21:24:28Z
> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 19m 16s | 5.66M (5.37M) | 42.73k | $0.13 | engine | review |

total: 1 attempt · 19m 16s · 5.66M in (5.37M cached) / 42.73k out · $0.13 (accepted 2026-08-31T21:48:40Z)
