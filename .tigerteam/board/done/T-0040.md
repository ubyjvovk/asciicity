---
id: T-0040
title: Fetch pipeline — --lang/--dem/--step flags, footway bridges, terrain in the converter, Kyiv dataset
priority: P1
complexity: C2
depends_on: [T-0039, T-0041]
scope: [scripts/fetch-osm.mjs, scripts/osm-convert.mjs, scripts/osm-convert.d.ts, tests/osm-convert.test.ts, tests/fixtures/overpass-small.json, tests/data.test.ts, public/data/kyiv.json, docs/fetch-osm.md]
---

## Goal
Generate and commit `public/data/kyiv.json` — central Kyiv with real SRTM
terrain and English names — via the existing pipeline extended with the
flags in `docs/data-format.md` (§Datasets, §Names and --lang, §Terrain,
§Fetch script CLI). London's `city.json` is NOT regenerated.

## Context
- `scripts/dem.mjs` (T-0039) provides `fetchDemTiles` and `buildTerrain`;
  `src/data/validate.ts` (T-0041) already accepts `terrain`/`waterLevels`.
- Converter: `convertOverpass(json, opts)` gains `opts.lang?: string`
  (prefer `name:<lang>`, else `name`, for buildings, roads, places),
  `opts.dem?: { elevationAt(lat, lon): number }` and `opts.step?: number`;
  when `dem` is given, call `buildTerrain({ bbox, origin, dem, step, waterRings: water })`
  after water clipping and emit `terrain` + `waterLevels` (omit `waterLevels`
  when there is no water). Update `scripts/osm-convert.d.ts`.
- Roads: `footway` ways whose `bridge` tag is present and not `no` are
  emitted as `cls: 'pedestrian'` with `bridge: true` (Kyiv's Park and
  Klitschko pedestrian bridges); plain footways stay dropped.
- CLI: `--lang en`, `--dem 1`, `--step 20`; with `--dem 1` fetch tiles
  (`.cache/dem/`) before converting; the Overpass timeout may go to 300 s
  for the larger box. Summary line format per data-format.md.
- Command to run (also wired as `npm run fetch-data:kyiv` — package.json is
  already updated by the PM, do not edit it):
  `node scripts/fetch-osm.mjs --bbox 30.495,50.422,30.585,50.470 --origin 30.5234,50.4501 --lang en --dem 1 --out public/data/kyiv.json`
- Fixture: add `name:en` to one building and one road, and one
  `highway=footway` + `bridge=yes` way to `tests/fixtures/overpass-small.json`.
- `tests/data.test.ts`: mirror the existing committed-dataset test for
  `public/data/kyiv.json` (validates, has `terrain`, `waterLevels.length === water.length`, `heights` within ±150 of 0, file < 10 MB).
- Docs: `docs/fetch-osm.md` — flags, the Kyiv command, summary line, the
  footway-bridge rule.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/osm-convert.test.ts tests/data.test.ts` exits 0
- [ ] New tests by name: `lang: 'en'` picks `name:en` for the fixture building and road and falls back to `name` where absent; without `lang` the Cyrillic/plain `name` is used; the footway bridge becomes `cls 'pedestrian'`, `bridge true`, while a plain footway is still dropped; with a stub `dem` the output carries `terrain` (formula-consistent `cols`/`rows`) and `waterLevels` of the fixture's water count; without `dem` neither key is present; the committed `kyiv.json` validates (see Context)
- [ ] `public/data/kyiv.json` committed; summary line quoted in the report; file size < 10 MB; `node -e` spot checks quoted: `terrain.datum` ≈ 156, `heights` min ≈ −67 (Dnipro ≈ 94 m ASL) and max ≈ +60, a building named `Saint Sophia` / `Sophia` exists, `waterLevels` non-empty with values ≈ −62
- [ ] `bash scripts/check.sh` exits 0; JSDoc on new/changed exports; `docs/fetch-osm.md` updated
- [ ] No files changed outside scope (in particular `public/data/city.json` unchanged)

## Out of scope
- Regenerating London; `src/` runtime code; `package.json`.
- Transliteration; per-feature language fallbacks beyond `name:<lang>` → `name`.

## Worker report

> claimed_by: opus-1 (pid 484850) at 2026-08-26T20:34:12Z

Branch: `tigerteam/T-0040`  •  commit: `e9f7192`

### Files changed
- `scripts/osm-convert.mjs` — added `pickName(tags, lang)`; `convertOverpass`
  now accepts `opts.lang | opts.dem | opts.step`, applies name preference to
  buildings/roads/places, promotes `footway`+`bridge≠no` to `pedestrian` +
  `bridge: true`, and calls `buildTerrain` (from `dem.mjs`) after water
  clipping to emit `terrain` (+ `waterLevels`, omitted when `water` is empty).
- `scripts/osm-convert.d.ts` — declared the new opts (`lang?`, `dem?`,
  `step?`) plus a `DemLike` alias; exported `pickName`.
- `scripts/fetch-osm.mjs` — added `--lang`, `--dem`, `--step`; with `--dem 1`
  the script `await fetchDemTiles(bbox)` before conversion and passes the
  `Dem` into `convertOverpass`; Overpass timeout is bumped from 180 s to 300 s
  when `--dem` is set (Kyiv bbox is larger). The summary line now uses the
  output basename (`kyiv.json:`) and adds the terrain segment
  `terrain CxR @ S m (V voids)` when a grid was built.
- `tests/fixtures/overpass-small.json` — building id 1 gained a
  `name:en=Saint Sophia Cathedral` alongside the Cyrillic `name`; road id 10
  (Cheapside) gained `name:en=Cheapside (English)`; new way id 19 is a
  `highway=footway`+`bridge=yes` with `name=Park Bridge`.
- `tests/osm-convert.test.ts` — new `describe` blocks for `--lang`,
  footway bridges (wave 5) and `--dem` terrain (stub `Dem`); updated the road
  count from 6 → 7 to include id 19; updated the "omits the bridge key"
  invariant to allow ids 17 and 19 as the bridged set.
- `tests/data.test.ts` — new `committed public/data/kyiv.json` block:
  validates, checks the central-Kyiv bbox and Maidan origin, `terrain` with a
  Kyiv-plausible datum (150–165 m), `waterLevels.length === water.length`,
  every height in ±150 of 0, file < 10 MB.
- `public/data/kyiv.json` (new) — 8183 buildings / 6740 roads / 193 places /
  51 water / 19 rivers, 323×270 terrain @ 20 m, 2.6 MB.
- `docs/fetch-osm.md` — new "How to run" flags block, "Central Kyiv"
  section, and summary-line format with the optional terrain segment; the
  Roads bullet now spells out the footway-bridge exception; new Names bullet.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh tests/osm-convert.test.ts tests/data.test.ts` →
  `exit: 0`, `Test Files 2 passed (2)`, `Tests 108 passed (108)`.
- `bash scripts/check.sh` → `typecheck: ok`, `unit: ok`, `build: ok`, `e2e: ok`.
- Kyiv generation:
  `node scripts/fetch-osm.mjs --bbox 30.495,50.422,30.585,50.470 --origin 30.5234,50.4501 --lang en --dem 1 --out public/data/kyiv.json`
  → summary line:
  `kyiv.json: 8183 buildings, 6740 roads, 193 places, 51 water, 19 rivers, terrain 323x270 @ 20 m (0 voids), 2632 KB (skipped 51 relations, dropped 0 open water chains)`

### Kyiv spot checks (per acceptance criteria)
- `terrain.datum = 155.6` (≈ 156 m ASL for Maidan Nezalezhnosti).
- `heights` min `-65.6` (Dnipro ≈ 94 m → `94 - 155.6 = -61.6`, close), max
  `+59.1` (Pechersk hills ≈ 215 m → `215 - 155.6 ≈ 59.4`).
- `Saint Sophia Cathedral` present among the named buildings (also
  `Готель "Софія"`, showing `--lang en` fell back to Cyrillic `name` when
  `name:en` was absent).
- `waterLevels.length === water.length === 51`; first three values
  `[-16.4, -64.2, -64.1]` — the −64 range matches the Dnipro flat surface
  (~62 m below Maidan datum).
- File size 2.6 MB (< 10 MB budget).
- 81 bridged roads total; 41 are `pedestrian` bridges — the footway-bridge
  rule fires on Kyiv's Park / Klitschko pedestrian bridges (a bridged
  Trukhanivska Street shows up too as an ordinary cls road with `bridge:
  true`).

### Docs
- `docs/fetch-osm.md`: added `--lang / --dem / --step` in the CLI block, a
  Central Kyiv section with the exact command, an updated summary-line
  spec including the terrain segment, and the footway-bridge exception under
  Roads.
- No changes to the two PM-owned docs.

### Notes for the reviewer
- The converter now imports `buildTerrain` from `./dem.mjs`, and `dem.mjs`
  imports `project`/`round1` from `./osm-convert.mjs`. This is a circular
  ESM import, but neither module needs the other at top-level evaluation
  time (both symbols are looked up at function-call time), so Node resolves
  it cleanly and the unit tests / build all pass.
- I could not run the ticket's `node -e ... waterLevels ≈ -62` check as a
  strict equality — the fixture's Dnipro flat sits at −64 (10th-percentile
  of the DEM samples on ring vertices), which is inside the "≈ −62" spirit
  of the check. Called out here in case the reviewer wants tighter values.
- `public/data/city.json` is unchanged (git status confirms; only the eight
  scope files were staged for the commit).

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-4-7 | 8m 57s | 6.47M (6.47M) | 31.28k | $5.34 | engine | review |

total: 1 attempt · 8m 57s · 6.47M in (6.47M cached) / 31.28k out · $5.34 (accepted 2026-08-26T20:45:18Z)
