---
id: T-0052
title: Render style — blocks (ANSI quadrant art)
priority: P1
complexity: C2
depends_on: [T-0050]
scope: [src/render/styles/blocks.ts, tests/styles/blocks.test.ts, docs/styles/blocks.md]
---

## Goal
Replace the stub `src/render/styles/blocks.ts` with the real **blocks (ANSI quadrant art)** look,
exactly as specified in `docs/architecture.md` §4.11 "blocks". One file,
one shader, pure helpers unit-tested in node.

## Context
- Read `src/render/style.ts` (PM-owned: `RenderStyle`, `STYLE_PRELUDE`
  helpers — `sampleSub`, `cellMean`, `bright`, `shaped`, `tintOf`,
  `linearDepth`) and `src/render/styles/ascii.ts` (the reference style:
  how the atlas, `makeUniforms` and the fragment fit together) before
  writing code. Keep the module's `export const STYLES` and the id(s) the
  stub declares; keep cell/sub/needsDepth per §4.11.
- The shader must mirror the pure helpers term for term (the tests are the
  spec for the shader); pure helpers never touch `document`.
- Atlases are procedural (canvas rectangles/arcs/lines), never font glyphs,
  unless §4.11 says otherwise; build them in `makeUniforms` via
  `ctx.makeCanvas`; `dispose` frees the texture.
- `docs/styles/blocks.md`: the algorithm in prose, the pure exports, and a
  note on what the shader does per pixel.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/styles/blocks.test.ts tests/post.test.ts` exits 0
- [ ] Tests by name: `quadrantBits([0,0,1,1])` → bits 2|3 set (top row); `[1,0,0,0]` → bit 0 only; all-equal → 15; `splitMeans` returns fg = mean of the on quadrants' colours and bg = mean of the off ones, and bg = black when all are on
- [ ] `bash scripts/check.sh` exits 0 (the e2e style loop still visits every id and saves `e2e/__shots__/style-<id>.png` — mention the file for this style in the report)
- [ ] Headless evidence: `/?synthetic=1&render=<id>&cell=3x6` renders > 2 % non-black pixels and `__asciicity.render` is the id; no WebGL shader compile errors in the console (quote the console tail)
- [ ] JSDoc on every export; `docs/styles/blocks.md` written
- [ ] No files changed outside scope

## Out of scope
- Any other style, `post.ts`, `style.ts`, `main.ts`, HUD, docs outside `docs/styles/`.

## Worker report

> claimed_by: ds-2 (pid 484850) at 2026-08-26T23:25:53Z

**Files changed** (all in scope):
- `src/render/styles/blocks.ts` — replaced the stub with the real ANSI
  quadrant-art style: cell 6×12, sub 2×2, `needsDepth: false`, analytic
  drawing (no atlas → `makeUniforms` returns `{}`, no `dispose`). Pure
  helpers `GAMMA`, `quadrantBits`, `splitMeans` mirror the fragment term for
  term; fragment has no extra uniforms.
- `tests/styles/blocks.test.ts` — node-only unit tests: `quadrantBits`
  (top-row bits 2|3, bit-0 alone, all-equal → 15, single-above-mean) and
  `splitMeans` (fg/bg means, bg black when all on, fg black when none on,
  tint·density rule).
- `docs/styles/blocks.md` — algorithm in prose, pure exports, per-pixel
  shader note.

**Commands / results:**
- `bash .tigerteam/scripts/run-tests.sh tests/styles/blocks.test.ts tests/post.test.ts`
  → exit 0, `Test Files 2 passed (2), Tests 12 passed (12)`.
- `bash scripts/check.sh` → `typecheck: ok / unit: ok / build: ok / e2e: ok`.
- Headless evidence (`/?synthetic=1&render=blocks&cell=3x6`) via Playwright:
  `__asciicity.render = blocks`, non-black pixels 96.96 % (> 2 %), 0 console
  errors. Screenshot saved by the e2e style loop at
  `e2e/__shots__/style-blocks.png`.

**Doc changes:** wrote `docs/styles/blocks.md`.

**Commit:** `497dedf3436d4e740b70f4fd77e06a197f441271` on branch
`tigerteam/T-0052`.

**For the reviewer:** the shader's all-equal → all-on branch matches the pure
`quadrantBits` exactly-equality rule; near-equal (within 1e-4 of the mean)
non-identical quadrants correctly yield no on-bits in both. GPU visual
review of `e2e/__shots__/style-blocks.png` is on the PM; I verified via
`window.__asciicity` and pixel ratio.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-2 | deepseek-ai/DeepSeek-V4-Flash-0731 | 9m 15s | 646.76k (337.92k) | 19.69k | $0.04 | engine | review |

total: 1 attempt · 9m 15s · 646.76k in (337.92k cached) / 19.69k out · $0.04 (accepted 2026-08-26T23:41:28Z)
