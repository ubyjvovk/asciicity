---
id: T-0004
title: Building mesh builder, palette and window texture
priority: P1
complexity: C2
depends_on: [T-0001]
scope: [src/world/buildings.ts, src/world/palette.ts, src/world/textures.ts, tests/buildings.test.ts, tests/mesh.test.ts, docs/world.md]
---

## Goal
Turn building footprints into one merged, lit, vertex-coloured mesh with
window-textured walls and flat roofs — the dominant visual of the game.

## Context
- Contract: `docs/architecture.md` §4.2 (walls/roofs/uv/normals/winding/
  groups/materials — implement exactly), §4.3 (palette, exact hex values and
  `colorFor`), §4.4 (`makeWindowTexture`, browser-only, seeded). Use the
  PM-owned `MeshBuilder`/`toGeometry` from `src/world/mesh.ts` (read it first).
- Exports: `buildBuildingsMesh(buildings: Building[]): MeshData`,
  `makeBuildingsObject(buildings: Building[], windowTex: THREE.Texture): THREE.Mesh`,
  `PALETTE`, `LANDMARK_PALETTE`, `colorFor`, `makeWindowTexture`.
  Also export the helper `normalizeRing(poly: Vec2[]): Vec2[]` (returns a copy
  with positive `ShapeUtils.area`).
- `makeWindowTexture` may only be called in a browser; keep it in
  `textures.ts` with no top-level side effects so importing the module in
  node is safe (`buildings.ts` must not import `textures.ts`).
- `tests/mesh.test.ts` covers the PM-owned `MeshBuilder` (see criteria).
- Write `docs/world.md`: how a footprint becomes geometry (with the uv scale
  and group layout), the palette, and the texture.

## Acceptance criteria
- [x] `bash .tigerteam/scripts/run-tests.sh tests/buildings.test.ts tests/mesh.test.ts` exits 0
- [x] `tests/buildings.test.ts` covers: a 10×10 square footprint at `h=5` yields 4 walls (24 vertices) + 2 roof triangles (6 vertices), groups `[ {start:0,count:24,materialIndex:0}, {start:24,count:6,materialIndex:1} ]`; every wall normal is unit length, horizontal, and points away from the centroid (`dot(n, mid - centroid) > 0`); every wall triangle's geometric normal `cross(b-a, c-a)` has positive dot with its stored normal; roof normals are `(0,1,0)` and roof triangles wind upward; wall uvs — `u` runs 0 → 40/24 around the ring, `v` is 0 at the base and `5/24` at the top; a clockwise input ring gives identical geometry to its reversed copy (`normalizeRing`); rings with `|area| < 1` produce no vertices; vertex colours equal the linear rgb of `colorFor` (`new THREE.Color(hex)`); `colorFor` picks `LANDMARK_PALETTE[id % 4]` for named buildings and `PALETTE[id % 8]` otherwise
- [x] `tests/mesh.test.ts` covers: `vertexCount`, `triangle` pushes 3 vertices with the given normal/colour, `quad` pushes 6, `endGroup` records start/count/materialIndex, `build()` closes a trailing group as material 0, `toGeometry` sets the four attributes with the right item sizes and the groups
- [x] Every exported function has a JSDoc line; `docs/world.md` written
- [x] `bash scripts/check.sh` exits 0
- [x] No files changed outside scope

## Out of scope
- Roads/ground (T-0005), collision (T-0007), scene wiring (T-0010), editing `mesh.ts` or `types.ts`.

## Worker report

> claimed_by: grok-1 (pid 3364115) at 2026-08-23T23:01:14Z
> RETRY: killed by operator at 2026-08-23T23:03:00Z (no attempt penalty)

> claimed_by: grok-1 (pid 3364115) at 2026-08-23T23:03:50Z

### Files changed
- `src/world/buildings.ts` — `normalizeRing`, `buildBuildingsMesh` (walls then roofs), `makeBuildingsObject`; does not import `textures.ts`
- `src/world/palette.ts` — `PALETTE`, `LANDMARK_PALETTE`, `colorFor` (named → landmark `[id%4]`, else `[id%8]`)
- `src/world/textures.ts` — browser-only `makeWindowTexture` (mulberry32 seed 7, no top-level side effects)
- `tests/buildings.test.ts` — all listed building/palette cases by name
- `tests/mesh.test.ts` — MeshBuilder `vertexCount`/`triangle`/`quad`/`endGroup`/`build` + `toGeometry` attributes/groups
- `docs/world.md` — footprint→geometry, UV scale 24 m, group layout, palette, window texture

### Commands run
```
bash .tigerteam/scripts/run-tests.sh tests/buildings.test.ts tests/mesh.test.ts
```
Test Files  2 passed (2)
Tests  15 passed (15)

```
bash scripts/check.sh
```
typecheck: ok
unit: ok
build: ok
e2e: skipped (no e2e/)
log: .tigerteam/logs/tests/check-20260823-231014-276.log

### Doc changes
Wrote `docs/world.md` (how a ring becomes walls+roof, UV `/24`, groups 0=walls / 1=roofs, palettes, seeded 64×64 window atlas). One-line JSDoc on every exported function.

### Commit
`eaa5a13fcfc24409e94ee4dcfd7fe00afd7ec86c` on `tigerteam/T-0004`

### Reviewer notes
- Window inner 4×5 is inset `(2, 1)` inside each 8×8 cell (integer-centred, 2 px sill). Spec does not pin the inset; documented in `docs/world.md`.
- Roof faces from `ShapeUtils.triangulateShape` are swapped when `cross.y < 0` so they wind upward even though positive `ShapeUtils.area` on `Vector2(x, z)` is CCW in x/z (downward in Y-up).
- Zero-length wall edges are skipped (avoids NaN normals); not exercised by tests.
- Did not touch `mesh.ts` / `types.ts` (PM-owned, out of scope). Roads/ground/collision/scene wiring left for later tickets.

> handoff: review
