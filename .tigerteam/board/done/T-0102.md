---
id: T-0102
title: Tokyo bbox v2 — extend west over Shibuya and Shinjuku, refetch tiles
priority: P1
complexity: C2
depends_on: []
scope: [public/data/tokyo/, docs/fetch-osm.md, tests/spawn.test.ts, src/data/spawn.ts, src/data/cities.ts]
---

## Goal
Refetch the Tokyo tiled dataset with the wave-12 bbox v2
`139.692,35.645,139.820,35.715` (the v1 box plus the west strip holding
Shibuya and Shinjuku). Origin stays Tokyo Station, so the tile grid and
every existing local coordinate are unchanged — new tiles simply appear
to the west.

## Context
- **Read first**: data-format.md "Central Tokyo … bbox v2 wave 12" — the
  bbox, chunking, counts and stability guarantees are all there.
- Command: `npm run fetch-data:tokyo` (already updated to bbox v2 +
  `--chunks 4x3`). Delete the old `public/data/tokyo/` first
  (`rm -rf public/data/tokyo` then fetch) so stale tiles cannot linger.
- West-strip counts at boarding: 57 539 buildings / 17 230 highways —
  expect ≈ 170 k buildings and ≈ 25–35 MB total. Budget stays **60 MB**
  (blocked question if over).
- Overpass etiquette per T-0097: retry a failed chunk after 60 s; if both
  endpoints keep failing, `## Questions`, do not hammer.
- `src/data/spawn.ts` and `tests/spawn.test.ts` are in scope ONLY for
  this: a refetch can shift a road vertex an existing preset sits on. If
  a wave-11 preset assertion fails on the new data, adjust that preset's
  `lon`/`lat`/`bearingDeg` minimally (nearest passing road vertex, same
  tile and view intent, per the T-0098 derivation rules) and justify each
  adjustment line-by-line in your report. Do NOT add new presets.
- `src/data/cities.ts` is in scope ONLY for the tokyo `sizeBytes` value
  (exact `stat -c %s` of the new index.json).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` (full suite) exits 0 against
      the new committed dataset.
- [ ] `bash scripts/check.sh` green (e2e/tokyo.spec.ts passes — spawn
      tiles/landmarks unchanged, or minimally adjusted and flagged).
- [ ] `node -e` spot-checks in the report: bbox equals v2; `tiled === true`;
      tile count > 99; Σ tile `bytes` equals the on-disk tiles total;
      landmarks still include "Tokyo Skytree" and "Tokyo Tower" AND at
      least one named entry each within 700 m of Shibuya Station
      (≈ local −5 950, 2 400) and Shinjuku Station (≈ local −6 030,
      −1 000) — report the names found (they seed T-0103).
- [ ] Total committed size ≤ 60 MB; exact totals + the fetch summary line
      in the report (the PM refreshes data-format.md from it at accept).
- [ ] `docs/fetch-osm.md` Tokyo section updated to the v2 command/counts.
- [ ] `src/data/cities.ts` tokyo `sizeBytes` matches the new index.
- [ ] No files changed outside scope.

## Out of scope
New presets and default-spawn change (T-0103), default render (T-0104),
matrix style (T-0105), converter/tiler code (flag bugs via `## Questions`),
docs/data-format.md (PM-owned).

## Worker report

> claimed_by: ds-2 (pid 912208) at 2026-08-31T07:41:25Z

**Tokyo bbox v2 refetch — complete. Commit `897a903` on branch `tigerteam/T-0102`.**

### What I did
- Deleted the old `public/data/tokyo/` and refetched with `npm run fetch-data:tokyo` (already bbox v2 `139.692,35.645,139.820,35.715` + `--chunks 4x3`). First attempt hit Overpass overloading (504→500 on overpass-api.de; the kumi fallback is unreachable from this host) — waited the T-0097 60 s and retried once; the second pass succeeded (no hammering).
- `src/data/cities.ts`: `tokyo.sizeBytes` 1979748 → **2701381** (exact `stat -c %s` of the new index.json).
- `docs/fetch-osm.md`: updated the Tokyo section (heading now "wave 11; bbox v2 wave 12, T-0102"), the v2 command (`--chunks 4x3`), a note that origin/grid/v1 coordinates are unchanged and new tiles appear west, and the new shipped-snapshot counts. Also updated the "Fetch-time tiling" example command to v2.
- **No change needed to `src/data/spawn.ts` or `tests/spawn.test.ts`** (in scope only as a fallback): the v1 region data is unchanged (Overpass elements deduped by `type`+`id` across the new chunk grid, origin/grid fixed), so all seven wave-11 presets still resolve — 11 of the 99 v1 tile files are byte-identical; the full 79-test spawn suite passes on the new data.

### Fetch summary line (verbatim)
`tokyo: 170619 buildings, 24991 roads, 1087 places, 276 water, 141 rivers, 39547 trees (30340 filled, 0 dropped), terrain 583x391 @ 20 m (0 voids), 26330 KB (skipped 526 relations, dropped 0 open water chains), 129 tiles (51358 KB tiled total)`

**True on-disk committed size:** index.json 2 701 381 B + tiles 25 628 014 B = **28 329 395 B ≈ 28.3 MB (27.0 MiB)** — well under the 60 MB budget (the v2 estimate said 25–35 MB; this lands at the low end).

⚠️ **PM note (data-format.md refresh):** the summary-line "tiled total" (51 358 KB) OVERSTATES the committed size — it adds the *monolithic* city `bytes` (26 330 KB) + tile bytes, not index + tiles. A pre-existing `fetch-osm.mjs` summary quirk (out of scope — it's data-pipeline code). Use the on-disk figures above for data-format.md: **129 tiles, index 2 701 381 B + tiles 25 628 014 B ≈ 28.3 MB, 170 619 buildings / 24 991 roads / 1 087 places / 276 water / 141 rivers / 39 547 trees / terrain 583×391 @ 20 m / 526 skipped relations / 0 dropped open water chains**. Budget remains 60 MB.

### Acceptance-criteria spot-checks (`node -e`, run against the committed data)
- bbox equals v2 (numeric): **true** (`139.692, 35.645, 139.82, 35.715`)
- `tiled === true`: **true**; tileSize 1000
- tile count **129 > 99** (30 new west-strip tiles at `i ≤ −5`, matching "i ≤ −6-ish west")
- Σ `index.tiles[].bytes` = 25 628 014 = on-disk tiles total: **true**
- landmarks: "Tokyo Skytree" @ (3944.019, −3189.625) and "Tokyo Tower" @ (−1958.971, 2514.657) both present (anchors byte-identical to v1).
- Shibuya Station (≈ local −5 950, 2 400): named entries within 700 m incl. **MAGNET SHIBUYA (d=52), FPG Links Shibuya (61), Q-FRONT (91), Shibuya (104)**, Shibuya Parkside Kyodo (49), … → **seed T-0103 Shibuya preset**.
- Shinjuku Station (≈ local −6 030, −1 000): named entries within 700 m incl. **Shinjuku (d=70), ルミネ新宿 LUMINE2 (92), Flags (103), Keio Department Store Shinjuku (107)**, 新宿東口ＮＯＷＡビル (87) → **seed T-0103 Shinjuku preset**.

### Commands run
- `npm run fetch-data:tokyo` → fetched (success on 2nd attempt).
- `bash .tigerteam/scripts/run-tests.sh` full suite → `Test Files 47 passed (47)`, `Tests 787 passed (787)`, exit 0.
- `bash .tigerteam/scripts/run-tests.sh tests/spawn.test.ts` → `Test Files 1 passed (1)`, `Tests 79 passed (79)`.
- `bash scripts/check.sh` → `typecheck: ok / unit: ok / build: ok / e2e: ok`, exit 0. e2e: **34 passed** incl. both `e2e/tokyo.spec.ts` tests (default-spawn boot + `skytree` preset on tile 4_-3).

### Notes for the reviewer
- No preset adjustments were needed — the v1 region data is snapshot-identical to wave 11 (dedup + fixed origin/grid), so `spawn.ts`/`spawn.test.ts` are untouched (both in scope, no changes required).
- The full gate (`check.sh`) is green in-worktree. I could not do a GPU visual review (no image view here); verified via the `__asciicity`/unit/e2e surfaces as instructed — the PM does the visual pass.
- Out-of-scope observation (not a block): the fetch summary's "tiled total" double-counts the monolithic city bytes (see ⚠️ above).

> handoff: review
