---
id: T-0074
title: Amber render style — high-contrast warm phosphor theme in the ascii family
priority: P2
complexity: C2
depends_on: []
scope: [src/render/styles/ascii.ts, tests/styles/amber.test.ts, e2e/amber.spec.ts]
---

## Goal
A fourth ascii-family look, `amber` (`R`-cycle position 4, `?render=amber`):
near-black frame, amber/gold glyphs with olive-green accents, crushed darks,
hot cells (lamps, lit windows, sun/moon) blooming warm white. The formula is
locked in `docs/architecture.md` §4.11, subsection "`amber` (wave 8)" — read
it first; it is the contract. The stub already ships
(`asciiStyle('amber', 'AMBER', 3)` currently renders the solarized branch).

## Context
- Only `ASCII_FRAGMENT` and the pure mirrors in `src/render/styles/ascii.ts`
  change: add the amber density (`aV`/`aDens`) and colour branch per §4.11,
  make the glyph index use the theme-selected density
  (`theme < 2.5 ? dens : aDens`), and keep the raw tint available for the
  amber branch (the existing in-place luminance fold feeds themes 0–2).
- HARD constraint: themes 0–2 stay pixel-identical. Do not edit `themeMix`,
  its tests (`tests/ascii.test.ts`), or any term of the existing branches —
  restructure only by introducing new locals.
- New pure exports mirroring the shader term-for-term (like
  `themeMix`/`glyphIndex` do): `amberDensity(v, gamma): number`,
  `amberMix(rawTint, v, mask, gamma): [r, g, b]`.
- New unit file `tests/styles/amber.test.ts` (do not touch
  `tests/ascii.test.ts`): amberDensity(0.06, 0.45) === 0 and
  amberDensity(1, 0.45) === 1, strictly monotone on a grid, matches
  `pow((v − 0.06)/0.94, 0.675)` at v = 0.5; amberMix: grey rawTint
  `[1,1,1]`, v 0.5, mask 1 → r > g > b (amber order); green rawTint
  `[0.3,1,0.3]` → g ≥ r (olive); v = 1 → every channel ≥ 0.5 (bloom);
  mask 0 → `[0,0,0]`.
- New e2e file `e2e/amber.spec.ts` (copy the boot helpers from
  `smoke.spec.ts`; never edit smoke.spec.ts). On
  `/?synthetic=1&render=amber&cell=3x6&time=23:00`, after ready + two
  double-rAF waits, compute in-page pixel stats exactly like the
  `nonBlackFraction` helper in `smoke.spec.ts` (offscreen 2d canvas +
  `drawImage(#view)` + `getImageData`) and assert:
  1. non-black fraction > 0.02 (it renders at all);
  2. dark floor: fraction of pixels with max(r,g,b) < 13/255 ≥ 0.35
     (crushed blacks);
  3. warm dominance: over pixels with max(r,g,b) > 38/255, mean(r) ≥
     1.4 × mean(b).
- The style loop in `smoke.spec.ts` already cycles 13 styles and saves
  `e2e/__shots__/style-amber.png` — that artifact is the PM's visual
  review; make sure it is generated by your final `check.sh` run.
- §4.11 says constants are a starting point: if a criterion fails, tune
  constants (black point, exponent, chroma endpoints) until criteria pass,
  and record the final constants in the Worker report. Do NOT edit
  `docs/architecture.md` (PM-owned) — report instead.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/styles/amber.test.ts` exits 0
- [ ] Tests by name: the amberDensity and amberMix cases listed above;
      `tests/ascii.test.ts` passes UNMODIFIED (git diff shows no change)
- [ ] e2e/amber.spec.ts assertions 1–3 green
- [ ] `bash scripts/check.sh` exits 0
- [ ] JSDoc on `amberDensity`/`amberMix`; Worker report records the final
      constants if tuned
- [ ] No files changed outside scope

## Out of scope
- New STYLE_ORDER entries or registry changes (already committed), CRT
  overlay, other styles' modules, `src/render/style.ts` (PM-owned),
  `docs/architecture.md`.

## Worker report

> claimed_by: grok-1 (pid 184142) at 2026-08-28T17:02:36Z

Implementation is in the ticket scope and the amber criteria pass. `check.sh` does not exit 0 because of a **pre-existing** smoke assertion that this ticket is forbidden to edit — see Questions.

### Files changed
- `src/render/styles/ascii.ts` — `amberDensity` / `amberMix` (JSDoc, term-for-term mirrors of §4.11) plus `aV`/`aDens`/`rawTint`/`amberCol` locals in `ASCII_FRAGMENT`. Glyph index uses `theme < 2.5 ? dens : aDens`. Existing `themeMix` body and themes 0–2 shader terms are untouched; `outCol` keeps the original ternary, then `outCol = theme < 2.5 ? outCol : amberCol`.
- `tests/styles/amber.test.ts` — named `amberDensity` / `amberMix` cases from the ticket.
- `e2e/amber.spec.ts` — boot helpers copied from `smoke.spec.ts`; pixel stats on `/?synthetic=1&render=amber&cell=3x6&time=23:00` after ready + two double-rAF waits.

### Commands run
`bash .tigerteam/scripts/run-tests.sh tests/styles/amber.test.ts tests/ascii.test.ts`

```
 Test Files  2 passed (2)
      Tests  24 passed (24)
   Start at  17:07:05
   Duration  312ms
```

(`tests/ascii.test.ts` 16 tests, `tests/styles/amber.test.ts` 8 tests. `git diff -- tests/ascii.test.ts` is empty.)

`bash scripts/check.sh`

```
typecheck: ok
unit: ok
build: ok
e2e: FAIL (see .tigerteam/logs/tests/check-20260828-170711-216.log)
```

e2e log: `e2e/amber.spec.ts` **passed** (3.2s); `smoke: R cycles every render style and each paints` **passed** (1.2m, 13 styles, `style-amber.png` generated); `smoke: ?render= aliases and R / Shift+R cycle` **failed** — 60s timeout waiting for `render === 'braille'` after one `KeyR` from `solarized`.

### Doc changes
JSDoc on `amberDensity` and `amberMix`. No `docs/*.md` (ticket forbids editing `docs/architecture.md`; no other doc is in scope).

### Constants (untuned)
§4.11 starting point; amber e2e criteria 1–3 passed so nothing was retuned:
- black point `aV = clamp((v − 0.06) / 0.94, 0, 1)`
- `aDens = pow(aV, gamma · 1.5)`
- chroma amber `(1.00, 0.62, 0.18)` ↔ olive `(0.75, 0.85, 0.32)` via `gr`
- `aHot = smoothstep(0.82, 1.0, v)`
- `glyphC = mix(chroma · (0.18 + 0.82·aDens), (1.00, 0.88, 0.58), aHot)`
- `amberCol = glyphC · mask`

### Commit
`d8f5d5312110ce26495c6c6a78bf9c4fdda2e6d9` on branch `tigerteam/T-0074` (isolated checkout).

### Reviewer notes
- Themes 0–2: `themeMix` byte-identical; shader 0–2 path still uses `dens` and the original `outCol` ternary.
- SwiftShader visual of `e2e/__shots__/style-amber.png` is for PM GPU review; I did not inspect the PNG.

## Questions

`bash scripts/check.sh` cannot exit 0 without editing `e2e/smoke.spec.ts`, which this ticket explicitly forbids (`never edit smoke.spec.ts`; not in scope).

**Blocker.** Wave-8 contract `0ac005e` inserted `amber` into `STYLE_ORDER` (solarized → **amber** → braille) and bumped the 13-style loop, but left this assertion from T-0050:

```
await page.keyboard.press('KeyR');
await page.waitForFunction(() => …render === 'braille');  // smoke.spec.ts:281–286
```

One `R` from `solarized` now lands on `amber`, so the wait times out at 60s. This is independent of the amber shader — I did not touch `STYLE_ORDER`, `style.ts`, or `smoke.spec.ts`. The two Shift+R steps after that still land on `gloom` even with amber in the cycle.

**Options**
1. PM (or a one-line follow-up) changes that wait to `=== 'amber'`. Then `check.sh` should go green on this branch.
2. Authorise this ticket to edit `e2e/smoke.spec.ts` (currently out of scope) and I make the same one-line change.

**Recommendation:** (1). Amber unit + `e2e/amber.spec.ts` + the 13-style screenshot loop already pass.

## Answers

Good catch — this is a PM contract bug: commit 0ac005e bumped the 13-style
count but missed the alias/cycle test's adjacency assumption. Taking your
option 2 (the fix must live on your branch so `check.sh` evidence is
green there), with option 1's exact line:

**Scope amendment**: `e2e/smoke.spec.ts` is added to this ticket's scope
for EXACTLY ONE change — in `smoke: ?render= aliases and R / Shift+R
cycle`, the first `waitForFunction` after `KeyR`:
`render === 'braille'` → `render === 'amber'`.

Nothing else in the file. Do NOT touch the `'gloom'` wait two lines down:
with amber between solarized and braille, one `R` from `solarized` lands
on `amber`, and the existing two `Shift+KeyR` then walk amber → solarized
→ gloom, so that assertion is correct again as written.

Then rerun `bash scripts/check.sh`; if e2e fails under fleet load, note it
and paste the failing test name — the PM gates on the host at accept.
Everything else in your attempt stands; keep the commits.


> claimed_by: grok-1 (pid 184142) at 2026-08-28T17:15:35Z

## Worker report

Implementation plus the PM-authorised one-line smoke adjacency fix. `check.sh` exits 0.

### Files changed
- `src/render/styles/ascii.ts` — `amberDensity` / `amberMix` (JSDoc, term-for-term mirrors of §4.11) plus `aV`/`aDens`/`rawTint`/`amberCol` locals in `ASCII_FRAGMENT`. Glyph index uses `theme < 2.5 ? dens : aDens`. Existing `themeMix` body and themes 0–2 shader terms are untouched; `outCol` keeps the original ternary, then `outCol = theme < 2.5 ? outCol : amberCol`. (commit d8f5d53)
- `tests/styles/amber.test.ts` — named `amberDensity` / `amberMix` cases from the ticket. (commit d8f5d53)
- `e2e/amber.spec.ts` — boot helpers copied from `smoke.spec.ts`; pixel stats on `/?synthetic=1&render=amber&cell=3x6&time=23:00` after ready + two double-rAF waits. (commit d8f5d53)
- `e2e/smoke.spec.ts` — **exactly one change** (PM Answers option 2 / scope amendment): in `smoke: ?render= aliases and R / Shift+R cycle`, first `waitForFunction` after `KeyR` is `render === 'amber'` (was `'braille'`). `'gloom'` wait after two `Shift+KeyR` is untouched. (commit ce9254c)

### Commands run
`bash .tigerteam/scripts/run-tests.sh tests/styles/amber.test.ts tests/ascii.test.ts`

```
 Test Files  2 passed (2)
      Tests  24 passed (24)
   Start at  17:16:24
   Duration  297ms (transform 50ms, setup 0ms, import 109ms, tests 16ms, environment 0ms)
```

(`tests/ascii.test.ts` 16 tests, `tests/styles/amber.test.ts` 8 tests. `git diff -- tests/ascii.test.ts` is empty.)

`bash scripts/check.sh`

```
typecheck: ok
unit: ok
build: ok
e2e: ok
log: .tigerteam/logs/tests/check-20260828-171629-140.log
```

e2e (from that log): `e2e/amber.spec.ts` **passed** (2.7s, paints / crushed blacks / warm-dominant); `smoke: R cycles every render style and each paints` **passed** (1.1m, 13 styles, `e2e/__shots__/style-amber.png` generated, 135607 bytes); `smoke: ?render= aliases and R / Shift+R cycle` **passed** (6.1s).

### Doc changes
JSDoc on `amberDensity` and `amberMix`. No `docs/*.md` (ticket forbids editing `docs/architecture.md`; no other doc is in scope).

### Constants (untuned)
§4.11 starting point; amber e2e criteria 1–3 passed so nothing was retuned:
- black point `aV = clamp((v − 0.06) / 0.94, 0, 1)`
- `aDens = pow(aV, gamma · 1.5)`
- chroma amber `(1.00, 0.62, 0.18)` ↔ olive `(0.75, 0.85, 0.32)` via `gr`
- `aHot = smoothstep(0.82, 1.0, v)`
- `glyphC = mix(chroma · (0.18 + 0.82·aDens), (1.00, 0.88, 0.58), aHot)`
- `amberCol = glyphC · mask`

### Commit
- `d8f5d5312110ce26495c6c6a78bf9c4fdda2e6d9` — amber shader/helpers/tests/e2e
- `ce9254c4fb35e569550749fb8011fa24c3125180` — smoke R-cycle wait (`braille` → `amber`)
on branch `tigerteam/T-0074` (isolated checkout).

### Reviewer notes
- Themes 0–2: `themeMix` byte-identical; shader 0–2 path still uses `dens` and the original `outCol` ternary.
- SwiftShader visual of `e2e/__shots__/style-amber.png` is for PM GPU review; I did not inspect the PNG.
- `e2e/amber.spec.ts` header still says smoke.spec.ts is not edited (true of the first commit; the later smoke one-liner is the PM amendment).

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | grok-1 | grok-4.6-build | 10m 56s | 1.78M (1.54M) | 25.43k | $0.24 | engine | blocked |
| 2 | grok-1 | grok-4.6-build | 4m 59s | 622.21k (361.22k) | 6.79k | $0.13 | engine | review |

total: 2 attempts · 15m 55s · 2.4M in (1.9M cached) / 32.22k out · $0.36 (accepted 2026-08-28T17:28:40Z)
