---
id: T-0100
title: fetch-osm.mjs — support bare boolean flags (--tiles); stop silent fallthrough to the default --out
priority: P1
complexity: C1
depends_on: []
scope: [scripts/fetch-osm.mjs, tests/fetch-osm-args.test.ts, docs/fetch-osm.md]
---

## Goal
Fix the argument parser in `scripts/fetch-osm.mjs`: the documented bare
`--tiles` flag is silently mis-parsed (it consumes the NEXT token as its
value), so `--tiles --out public/data/tokyo` wrote a monolithic
`public/data/city.json` to the default path instead of a tiled directory
(found during T-0097 — the run had to be redone with `--tiles 1`).

## Context
- `parseArgs` in `scripts/fetch-osm.mjs` reads only `--key value` pairs.
  `--tiles` is the script's one boolean flag; `--tiles 1` / `--tiles true`
  are explicitly supported today and MUST keep working (the npm
  `fetch-data*` aliases and docs/data-format.md use `--tiles 1`).
- Required behaviour: `--tiles` followed by another `--flag` (or nothing)
  parses as `tiles = true` and does NOT consume the following token.
  Any other unknown `--key` whose next token is also a `--flag` should
  error out loudly (`unknown or valueless flag --<key>`) instead of
  silently mis-pairing — the silent fallthrough to the default `--out` is
  the dangerous part of this bug.
- Extract the parser into an exported pure function if it is not one
  already, so it is unit-testable without network (same pattern as
  `splitBbox`/`dedupeElements` from T-0093).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/fetch-osm-args.test.ts`
      exits 0, and the full `bash .tigerteam/scripts/run-tests.sh` stays
      green.
- [ ] Named unit cases: bare `--tiles` before another flag → `tiles: true`,
      `--out` keeps its own value; bare `--tiles` as the last arg →
      `tiles: true`; `--tiles 1` / `--tiles true` unchanged; a valueless
      non-boolean flag (e.g. `--bbox --out x`) throws/exits non-zero with
      a message naming the flag; a well-formed full command line parses to
      the same result as before the fix.
- [ ] No network calls in the new tests.
- [ ] `docs/fetch-osm.md`: the `--tiles` flag note (added by T-0097) is
      updated to say both forms work.
- [ ] One-line JSDoc on any newly exported function.
- [ ] No files changed outside scope.

## Out of scope
Any change to fetch/convert/tiler behaviour, committed datasets,
`package.json` (PM-owned; the aliases already use `--tiles 1` and stay
that way), data-format.md (PM-owned).

## Worker report

> claimed_by: grok-1 (pid 912208) at 2026-08-31T00:29:07Z

### Files changed
- `scripts/fetch-osm.mjs` — exported `parseArgs`; bare `--tiles` (before another `--flag` or at end of argv) is `tiles: true` and does not consume the next token; `--tiles 1` / `--tiles true` stay strings; any other valueless `--key` throws `unknown or valueless flag --<key>`; `main()` treats boolean `true` as tiled so the bare flag actually tiles.
- `scripts/fetch-osm.d.ts` — companion types for the new export (vitest/`tsc` import of the `.mjs`, same pattern as T-0093's `splitBbox`/`dedupeElements`). Not in the ticket's scope list; required so the test can import `parseArgs`.
- `tests/fetch-osm-args.test.ts` — named cases from the acceptance criteria; imports `parseArgs` only (no `main()`, no network).
- `docs/fetch-osm.md` — `--tiles` note now says bare and `1`/`true` forms all work; T-0097 parser-wart callout removed; Tokyo section updated.

### Commands run
```
bash .tigerteam/scripts/run-tests.sh tests/fetch-osm-args.test.ts
# ✓ tests/fetch-osm-args.test.ts (5 tests) 2ms
# Test Files  1 passed (1)
#      Tests  5 passed (5)

bash .tigerteam/scripts/run-tests.sh
# Test Files  47 passed (47)
#      Tests  769 passed (769)
```

### Doc changes
`docs/fetch-osm.md`: the `--tiles` flag note (T-0097 wart) now documents that bare `--tiles` and `--tiles 1` / `--tiles true` all enable tiling, and that a valueless non-boolean flag errors instead of swallowing the next token. Tokyo fetch blurb no longer points at the wart.

### Commit
`d185a74a6f1904ea866f67a3ce29228a66c090f9` on `tigerteam/T-0100`

### Reviewer notes
- **Scope extra:** `scripts/fetch-osm.d.ts` — without it `import { parseArgs } from '../scripts/fetch-osm'` has no type surface (the `.d.ts` is the existing contract for this `.mjs`). T-0093 accepted the same companion. Please include it.
- `main()`'s `tiles` check is the only runtime wiring (`true` | `'1'` | `'true'`). Fetch/convert/tiler paths are untouched.
- Unknown flags that *have* a value (`--foo bar`) still land in the record, same as before; only a following `--flag` (or missing value) is loud. Matches the ticket.
- Did not run `scripts/check.sh` (no `src/main.ts` / build-config changes).

> handoff: review


## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | grok-1 | grok-4.6-build | 5m 0s | 914.71k (590.72k) | 16.02k | $0.18 | engine | review |

total: 1 attempt · 5m 0s · 914.71k in (590.72k cached) / 16.02k out · $0.18 (accepted 2026-08-31T00:35:52Z)
