---
id: T-0118
title: Deck humps apply only to each name's LONGEST chain (Cahill Walk fix)
priority: P1
complexity: C1
depends_on: [T-0112]
scope: [src/world/terrain.ts, src/world/roads.ts, tests/terrain.test.ts, docs/architecture.md]
---

## Goal

T-0112 found (and flagged, per its ticket) that deck humps match by road
NAME, so Sydney's `Cahill Walk` — whose name covers BOTH the Harbour
Bridge east walkway (1503 m chain) and the elevated Circular Quay walkway
(661 m chain) — gets the 49 m-ASL bridge apex applied to the Circular
Quay chain too: that walkway now floats ~45 m over the quay, right where
the default spawn stands. Rule change: a hump entry applies, per matching
name, only to that name's LONGEST chain; other chains keep the plain
abutment lerp.

## Context

- The hump→chain association happens in two places, both via
  `humpApex(roadName, humps)` after `chainBridgeRoads`:
  `BridgeDecks` in `src/world/terrain.ts` (constructor, ~line 336) and
  the ribbon builder in `src/world/roads.ts` (its own `humpApex` copy,
  ~line 46). Add ONE exported helper in `terrain.ts` — given the full
  chain list, the chain, and the humps, return the apex only when the
  chain is the longest (by polyline arc length; ties: first wins) among
  chains matching that name — and use it from both files (drop the
  roads.ts copy).
- Chain lengths for the motivating case are in T-0112's report
  (`.tigerteam/board/done/T-0112.md`, "Cahill Walk chain finding").
- The rule is a no-op for every existing bridge: GGB/NYC sidewalk and
  roadway names chain into a single chain each (their tests must pass
  unchanged), and Sydney's `Bradfield Highway` / `Harbour Bridge
  Cycleway` keep their aprons because their longest chains ARE the deck.
- `docs/architecture.md`: amend the §4.9 deck-humps wording (and the
  §4.16b as-built note that flagged this) with the longest-chain rule —
  one or two sentences each.

## Acceptance criteria

- [ ] Exported helper in `terrain.ts` per Context; `roads.ts` uses it
      (no duplicated name-matching logic remains).
- [ ] Unit tests (`tests/terrain.test.ts`): a fixture with two chains
      sharing one name (a long and a short one, mirroring Cahill Walk) —
      the apex shapes only the longer chain, the shorter one equals the
      plain `bridgeProfile` with no apex; a single-chain name behaves
      exactly as before (existing expectation reused).
- [ ] Sydney assertion (memoized tiles + `ARCH_BRIDGES` humps): the deck
      y at the midpoint of the ~661 m Circular Quay `Cahill Walk` chain
      is within 3 m of its no-apex `bridgeProfile` value, and the deck y
      at the harbour-crossing chain's midpoint is (49 − datum) ± 1.
- [ ] Existing `tests/bridge.test.ts`, `tests/arch.test.ts` and every
      GGB/NYC hump test green and unmodified.
- [ ] `docs/architecture.md` §4.9 + §4.16b as-built note updated per
      Context.
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (full suite).
- [ ] No files changed outside scope.

## Out of scope

Renaming any road in the data, touching `chainBridgeRoads`, bridge.ts,
main.ts, spawn/preset changes, any dataset file.

## Worker report

**Summary** — Added `chainHumpApex(chain, chains, humps)` in
`src/world/terrain.ts` and used it from both `BridgeDecks` (constructor)
and `buildRoadsMesh` (in `roads.ts`), dropping the duplicated `humpApex`
helper from `roads.ts` (the name-matching logic now lives in exactly one
place). The new helper returns the hump apex only when the passed chain
is the LONGEST bridge chain sharing that name by polyline arc length
(ties: first encountered wins); shorter same-name chains fall back to
the plain abutment lerp. Added `chainHumpApex` to the terrain.ts
`export`s, kept the `DeckHump` shape and existing signatures unchanged.

**Files changed** (branch `tigerteam/T-0118`, commit `c29b618`,
isolation worktree)
- `src/world/terrain.ts` — new `polylineLength` (private) +
  `chainHumpApex` (exported); `BridgeDecks` constructor now builds
  `chains = chainBridgeRoads(roads)` once and calls
  `chainHumpApex(road, chains, humps)` in place of the private
  name-only `humpApex`. `humpApex` deleted (no callers).
- `src/world/roads.ts` — deleted the duplicated private `humpApex`;
  `buildRoadsMesh` collects `chains = chainBridgeRoads(roads)` and uses
  `chainHumpApex(road, chains, humps)` instead. Import list adds
  `chainHumpApex`; nothing else moved.
- `tests/terrain.test.ts` — three additions:
  1. `BridgeDecks` describe: "a hump named on two disconnected same-name
     chains applies only to the LONGEST chain (T-0118, Cahill Walk
     fix)" — two `Humped` bridges (100 m long chain at z=0, 20 m short
     chain at z=200, no chaining because they're 200 m apart); the long
     chain apex reads 10 at mid-span, the short chain deck reads 0
     throughout; asserts `chainHumpApex` directly (long → apex, short →
     undefined).
  2. `BridgeDecks` describe: "a hump on a single-chain name behaves
     exactly as pre-T-0118 (backwards compatible)" — same shape as the
     pre-existing single-chain expectation, restated to make the
     backwards-compatibility guarantee explicit.
  3. New describe `Sydney Cahill Walk longest-chain rule (T-0118)` on
     the memoized tiled sydney globals + `ARCH_BRIDGES` humps (via
     `deckHumps('sydney', ...)`): the two long `Cahill Walk` chains
     (harbour crossing and Circular Quay) are identified; long chain
     midpoint deck y = (49 − datum) ± 1 (hump lands here), short chain
     midpoint deck y is within 3 m of its no-apex `bridgeProfile`
     value (plain lerp on the Circular Quay walkway).
- `docs/architecture.md` — §4.9 amended: the deck-humps paragraph now
  names `chainHumpApex` as the shared lookup and adds a "Wave 11
  amendment (T-0118)" clause with the longest-chain rule; the API
  signature block gains `chainHumpApex` and shows the `apexY?` param on
  `bridgeProfile` and the `humps?` param on the `BridgeDecks`
  constructor (both already existed, spec was stale). §4.16b as-built
  Cahill Walk paragraph rewritten from "T-0118 (queued) fixes it" to
  the as-built explanation of what T-0118 did (harbour chain gets the
  apex, Circular Quay + stub keep the plain lerp; Bradfield /
  Cycleway unaffected because their longest chain IS the deck).

**Commands run**
- `bash .tigerteam/scripts/run-tests.sh tests/terrain.test.ts` —
  **32 passed** (was 29; +3 for the T-0118 additions).
- `bash .tigerteam/scripts/run-tests.sh` (full) — "Test Files 51 passed
  (51) / Tests 874 passed (874)", exit 0.
- `npx tsc --noEmit` — clean (exit 0).

**Doc changes**: `docs/architecture.md` §4.9 (deck-humps paragraph + API
signature block) and §4.16b (Cahill Walk as-built paragraph) per
Context.

**No-op verification for existing bridges** — GGB East/West Sidewalk,
Brooklyn/Manhattan/Williamsburg walkways+roadway, Bradfield Highway
and Harbour Bridge Cycleway all chain into a single same-name chain
each (per the wave-9 chaining tests and T-0112's `deckHumps` test), so
`chainHumpApex` finds exactly one candidate and returns the apex
unchanged. `tests/bridge.test.ts` (15 tests) and `tests/arch.test.ts`
(13 tests) are green and unmodified.

**Reviewer should look at**:
- `chainHumpApex` matches by chain reference identity (`longest === chain`).
  Callers must pass the same `chains` array they iterate — both
  `BridgeDecks` and `buildRoadsMesh` do exactly that (build once,
  iterate the same array).
- The two disconnected `Cahill Walk` chains in the sydney dataset stay
  disconnected because their endpoints are ~170 m apart in world
  coords (well beyond the 0.5 m coincidence threshold in
  `chainBridgeRoads`) — the test guards this with a `≥ 2 chains`
  assertion so a future data change that accidentally joins them will
  surface here.
- No dataset, `chainBridgeRoads`, `bridge.ts`, `main.ts`, spawn or
  preset files touched — everything is Out of scope-clean.

**Commit**: `c29b618` on branch `tigerteam/T-0118`.

> claimed_by: opus-1 (pid 1487920) at 2026-08-31T22:36:39Z
> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-4-7 | 6m 26s | 4.35M (4.35M) | 23.74k | $3.76 | engine | review |

total: 1 attempt · 6m 26s · 4.35M in (4.35M cached) / 23.74k out · $3.76 (accepted 2026-08-31T22:46:09Z)
