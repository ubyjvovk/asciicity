---
id: T-0093
title: Tiler (tile-city.mjs), --tiles/--chunks fetch flags, clamp 650, validateTileIndex
priority: P0
complexity: C2
depends_on: []
scope: [scripts/tile-city.mjs, scripts/fetch-osm.mjs, scripts/osm-convert.mjs, src/data/validate.ts, tests/tile-city.test.ts, tests/validate.test.ts, docs/fetch-osm.md]
---

## Goal
Build the data-side half of sector streaming (wave 11): a pure tiler that
turns a monolithic `CityData` into `index.json` + `tiles/<i>_<j>.json`, a
`--tiles` mode and a `--chunks NxM` chunked-fetch mode in `fetch-osm.mjs`,
the height clamp raised to 650, and `validateTileIndex` in `validate.ts`.
No runtime code — the app does not load tiled data yet (that is T-0094/95).

## Context
- **Read first**: docs/data-format.md section "Tiled datasets (wave 11 —
  sector streaming)" — it is the complete spec for this ticket (tile grid,
  the 7 tiler rules, `--chunks`, clamp). Types: `TileIndexData`, `TileData`,
  `TileStat`, `LandmarkEntry` in `src/data/types.ts` (PM-owned — import,
  never edit).
- `scripts/tile-city.mjs` is NEW: a pure `tileCity(city, tileSize)` →
  `{ index, tiles: Map<key, TileData> }` plus a thin CLI
  `node scripts/tile-city.mjs <city.json> <outdir>` that validates input
  with `validateCity` (import from `src/data/validate.ts` the way
  `osm-convert.mjs` structures its imports — it is an .mjs/zero-dep script;
  if importing the TS validator from an .mjs script is not workable, put an
  equivalent structural check in tile-city.mjs and say so in your report).
- `fetch-osm.mjs --tiles`: after conversion, run `tileCity` and write the
  directory named by `--out` instead of one JSON. `--chunks NxM`: split the
  bbox into N×M sub-bboxes, fetch sequentially (5 s pause, existing
  endpoint-fallback logic), concatenate `elements`, dedupe by `type`+`id`
  BEFORE conversion. Keep both flags orthogonal to every existing flag.
- Clamp: `[3, 600]` → `[3, 650]` in `osm-convert.mjs` (the `clampHeight`
  path around line 128) and in `src/data/validate.ts`. `types.ts` and the
  docs already say 650. No shipped dataset has h > 600, so committed JSONs
  stay byte-identical — do NOT re-fetch or touch `public/data/`.
- The wave-9 ordering lesson applies to road splitting: the boundary
  crossing point is computed ONCE and appended to BOTH pieces, and the
  result must be independent of vertex order / travel direction.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh tests/tile-city.test.ts tests/validate.test.ts` exits 0, and the full
      `bash .tigerteam/scripts/run-tests.sh` stays green.
- [ ] `tests/tile-city.test.ts` covers, by name: (1) union round-trip — tiling
      the synthetic city (`syntheticCity()`) and concatenating all tiles
      reproduces every building/tree/woods element exactly once, input order
      preserved within a tile; (2) building assignment by unrounded vertex-mean
      centroid, deterministic; (3) a road crossing a boundary is split with
      exactly coincident endpoints in both pieces, and splitting the reversed
      polyline yields the same crossing point; (4) a segment that cuts a tile
      corner (no vertex inside) still produces a piece in that tile; (5) a
      U-shaped road re-entering a tile yields two pieces with the same `id` in
      that tile; (6) roads with `bridge: true` land whole in
      `index.bridgeRoads` and appear in NO tile; (7) `landmarks` is in tile
      scan order (j asc, i asc, input order) with one entry per named
      building; (8) empty tiles are absent from `tiles` and no file is
      written; (9) `tiles[key].bytes` equals the written file's byte length;
      (10) single-point degenerate road pieces are dropped; (11) negative
      tile indices key as `"-3_2"` style.
- [ ] CLI determinism: running `node scripts/tile-city.mjs` twice on the same
      input produces byte-identical files (test via temp dirs, or a
      determinism unit test over serialized output).
- [ ] Chunk logic is tested as pure functions with NO network: bbox N×M split
      covers the bbox exactly with no overlap gaps, and dedupe by `type`+`id`
      keeps one copy of a seam element.
- [ ] `tests/validate.test.ts` gains: `validateTileIndex` accepts a good
      index; rejects a bad tile key, non-positive `tileSize`, missing
      `bridgeRoads`; `validateCity` accepts h = 650 and rejects h = 651.
- [ ] `docs/fetch-osm.md` documents `--tiles`, `--chunks NxM`, and the
      standalone `tile-city.mjs` migration command.
- [ ] Every new exported function has a one-line JSDoc.
- [ ] No files changed outside scope.

## Out of scope
Any runtime/loading code (`src/main.ts`, `src/world/`, `src/ui/`,
`src/hud/`), migrating any committed dataset in `public/data/`, fetching
Tokyo, `package.json` (PM-owned — block with a question if you need a dep).

## Worker report

> claimed_by: ds-1 (pid 912208) at 2026-08-30T22:21:20Z

**Files changed (all listed scope + 3 necessary support files — flagged below):**
- `scripts/tile-city.mjs` (new) — pure `tileCity(city, tileSize)` → `{index, tiles: Map}`, the 7 data-format rules (exclusive anchor assignment, DDA road splitting at tile boundaries with once-computed shared endpoints, bridges → global, landmarks in scan order, `bytes` = serialized tile length, empty tiles omitted) and the standalone CLI `node scripts/tile-city.mjs <city.json> <outdir>`. Input validated with `validateCity` (rule 7).
- `scripts/tile-city.d.ts` (new, support) — TS declaration so the in-scope `tests/tile-city.test.ts` typechecks (same pattern as `osm-convert.d.ts`/`dem.d.ts`).
- `scripts/fetch-osm.mjs` — added `--tiles` (writes the `--out` city DIRECTORY via `tileCity`), `--chunks NxM` (sequential sub-bbox fetch, 5 s pause, same `fetchJson` endpoint fallback; concat + `dedupeElements` by type+id before conversion), exported pure `splitBbox` + `dedupeElements`, and guarded `main()` so importing the module (from tests) no longer fires a network fetch.
- `scripts/fetch-osm.d.ts` (new, support) — TS declaration for `splitBbox`/`dedupeElements`.
- `scripts/osm-convert.mjs` — height clamp `[3, 600]` → `[3, 650]` (`heightOf` + doc comment). No shipped dataset has h > 600, so committed JSONs stay byte-identical; nothing re-fetched.
- `src/data/validate.ts` — clamp `[3, 600]` → `[3, 650]` in `validateCity`; added exported `validateTileIndex` (tiled discriminant, positive `tileSize`, `bridgeRoads`/`landmarks`/`places`/`tiles` presence, tile keys `/-?\d+_-?\d+/`, integer non-negative `TileStat` fields, optional terrain/water/waterLevels/rivers reused from `validateCity` via extracted shared helpers). Extracting helpers preserved every existing error message exactly.
- `tests/tile-city.test.ts` (new) — all 11 named acceptance criteria + CLI double-run byte-identical test (temp dirs, `node scripts/tile-city.mjs`) + `splitBbox`/`dedupeElements` pure chunk tests (no network).
- `tests/validate.test.ts` — clamp test now `h up to 650 is valid` (650 ok / 651 throws); added `validateTileIndex` accept/reject (bad key, non-positive `tileSize`, missing `bridgeRoads`, non-tiled discriminate) cases.
- `tests/osm-convert.test.ts` — **out of the literal scope list**: the mandated clamp change to `osm-convert.mjs` necessarily flipped the `heights clamp to [3, 600]` assertion (601/1000 now → 650). Updated to `[3, 650]` — the only way the "full suite stays green" criterion holds.
- `docs/fetch-osm.md` — added `--tiles`/`--chunks NxM` to the flag list, a new "Tiling and chunked fetch" section documenting `--tiles`, `--chunks NxM` and the standalone `tile-city.mjs` migration command, and bumped the clamp doc to `[3, 650]`.

**Commands run (run-tests summaries):**
- `bash .tigerteam/scripts/run-tests.sh tests/tile-city.test.ts tests/validate.test.ts` → exit 0, `Test Files 2 passed (2)`, `Tests 20 passed (20)`.
- `bash .tigerteam/scripts/run-tests.sh` → exit 0, `Test Files 44 passed (44)`, `Tests 741 passed (741)`.
- `bash scripts/check.sh` → `typecheck: ok / unit: ok / build: ok / e2e: ok`.

**Decisions / flags for the reviewer:**
- `tile-city.mjs` imports the `validateCity` TS validator directly (verified working on this node 22.23.2 via built-in type stripping; host node 24 also fine). This keeps the height clamp single-sourced in `validate.ts` (I raised each independently too, so no drift either way). If a 3rd-party environment ever downgrades below node 22.18 this import breaks — flag only.
- The 3 out-of-scope-but-required files: two `.d.ts` support declarations (identical pattern to the existing `osm-convert.d.ts`/`dem.d.ts`, otherwise `tsc --noEmit` fails on the in-scope test) and `tests/osm-convert.test.ts` (required to keep the suite green after the mandated clamp change). No other files outside scope were touched; `public/data/` untouched.
- Road splitting is a DDA walk through the tile grid; boundary crossing points are computed once and written to both pieces (wave-9 lesson). The `t===0` and segment-start re-location cases (a vertex sitting exactly on a shared edge, or a boundary-parallel continuation) are handled; single-point pieces (a road just touching a tile corner) are dropped. The corner-cut (criterion 4), U-shape (5) and degenerate (10) tests are exact.
- `out of scope` items noted but not touched: no runtime loader (T-0094/95), no committed dataset migration, no Tokyo fetch, `package.json` untouched (no new deps).

**Commit:** `37e49a6` on branch `tigerteam/T-0093`.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 35m 48s | 5.63M (5.32M) | 111.13k | $0.14 | engine | review |

total: 1 attempt · 35m 48s · 5.63M in (5.32M cached) / 111.13k out · $0.14 (accepted 2026-08-30T23:01:55Z)
