---
id: T-0097
title: Central Tokyo tiled dataset (fetch --chunks 3x3 --tiles, DEM N35E139)
priority: P1
complexity: C2
depends_on: [T-0093]
scope: [public/data/tokyo/, docs/fetch-osm.md]
---

## Goal
Fetch, convert and commit the Central Tokyo tiled dataset — the first
streamed-only city. Data only: no app code. Expect the largest fetch so
far (112 184 building ways counted at boarding).

## Context
- **Read first**: data-format.md "Central Tokyo (`data/tokyo/`, wave 11)"
  — bbox, origin, flags, budget — and "Tiled datasets" above it.
- Command: `node scripts/fetch-osm.mjs --bbox 139.730,35.645,139.820,35.715
  --origin 139.7671,35.6812 --lang en --dem 1 --chunks 3x3 --tiles --out
  public/data/tokyo`. Off-peak Overpass etiquette: if a chunk 429s/504s,
  wait 60 s and retry that chunk (the endpoints fall back automatically);
  if it keeps failing across both endpoints, file `## Questions` rather
  than hammering.
- Origin is Tokyo Station from the PM's notes — VERIFY it against the
  fetched `railway=station` node named Tokyo (or `name:en` `Tokyo`); if
  the node is > 100 m away, use the node's coordinates instead and state
  the correction in your report (spec-vs-data reconciliation is wanted
  here, per AGENTS.md).
- Verify landmark coverage in the OUTPUT (node one-liners over the tiles
  or index): `index.landmarks` contains entries whose names identify
  Tokyo Skytree and Tokyo Tower (report the exact name strings — with
  `--lang en` they should be the `name:en` values), and max building `h`
  is ≈ 634 (Skytree; the clamp is 650).
- Sanity: `index.tiles` has ≈ 40–72 non-empty keys (8×7 km at 1000 m);
  terrain datum should be ≈ 0–10 m ASL (Marunouchi is near sea level);
  water should include the Sumida river and bay fragments (coastline
  closure) — report ring/river counts.
- **Size budget 60 MB** for index + tiles combined. Over budget → STOP and
  file `## Questions` with the actual numbers (do not trim the bbox or
  thin data on your own).
- Committed data is binary-ish: never `cat` it; inspect with `node -e`.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` (full suite) exits 0 — this
      ticket must not change behaviour for any existing test.
- [ ] `public/data/tokyo/index.json` + `public/data/tokyo/tiles/*.json`
      committed; `node -e` spot-checks in the report show: `tiled === true`,
      `tileSize === 1000`, non-empty `tiles`, `bridgeRoads.length > 0`
      (Sumida bridges), `landmarks` includes Skytree + Tokyo Tower entries,
      max `h` in any tile ≤ 650 and ≥ 600.
- [ ] Total committed size ≤ 60 MB; report the exact byte totals, per the
      summary line: buildings / roads / places / water / rivers / trees /
      terrain dims / tile count / largest tile bytes.
- [ ] The origin-verification result (station node distance, any
      correction) is in the report.
- [ ] `docs/fetch-osm.md` gains the Tokyo command line (copy from
      data-format.md; the PM updates data-format.md itself at accept).
- [ ] No files changed outside scope (`package.json` npm alias is PM-owned
      — document the raw command only).

## Out of scope
Any `src/` change (registry/presets/landmarks are T-0098), converter fixes
(problems in scripts/ → `## Questions`, they belong to T-0093's authors),
trimming or restyling the data.

## Worker report

> claimed_by: ds-1 (pid 912208) at 2026-08-30T23:01:59Z
> INCOMPLETE attempt by ds-1 at 2026-08-30T23:47:00Z (engine exit 137, no handoff) — see /home/d/asciicity/.tigerteam/logs/workers/ds-1.log

> claimed_by: ds-1 (pid 912208) at 2026-08-30T23:47:00Z

### Files changed (and why)
- `public/data/tokyo/` — the Central Tokyo tiled dataset: `index.json` (1 979 748 B) + 99 `tiles/<i>_<j>.json` (16 650 053 B). Fetched via `node scripts/fetch-osm.mjs --bbox 139.730,35.645,139.820,35.715 --origin 139.7671,35.6812 --lang en --dem 1 --chunks 3x3 --tiles 1 --out public/data/tokyo`.
- `docs/fetch-osm.md` — added a **Central Tokyo (`tokyo/`)** section (working command, origin-verification result, shipped counts/sizes) and a note under the `--tiles` flag documenting a T-0093 parser bug (see below).

### Commands run / test summary
- `bash .tigerteam/scripts/run-tests.sh` (full suite): **exit 0 — 45 test files, 756 tests passed** (from the wrapper's bounded summary; log `.tigerteam/logs/tests/20260831-002302-268.log`).
- Data fetch (twice): first run wrote a **monolithic `public/data/city.json`** because the bare `--tiles` flag was not parsed (see below); restored `city.json` from git and re-ran with `--tiles 1` → tiled output, summary line:
  `tokyo: 112743 buildings, 16902 roads, 847 places, 214 water, 111 rivers, 25426 trees (19004 filled, 0 dropped), terrain 411x391 @ 20 m (0 voids), 17228 KB (skipped 332 relations, dropped 0 open water chains), 99 tiles (33488 KB tiled total)`

### Origin verification
Given origin `139.7671,35.6812` (Tokyo Station). Re-queried Overpass for `railway=station` named Tokyo/東京: the main node **11329319854** (`name:en` = `Tokyo`) sits at `139.7674549, 35.6811412` ≈ **33 m** from the given origin (≈ 32 m E, 6.5 m S) — well inside the 100 m tolerance, so the given origin stands, **no correction needed**.

### Acceptance spot-checks (via `node -e` over the committed files)
- `tiled === true`, `v === 1`, `tileSize === 1000`, `origin {35.6812,139.7671}`, `bbox [139.73,35.645,139.82,35.715]`.
- `index.tiles` has **99** non-empty keys; sum of `tiles[k].bytes` = 16 650 053 B == actual tiles bytes (index stats consistent with files).
- `bridgeRoads.length === 1712` (Sumida crossings included, e.g. `Izumihashi Bridge`).
- `landmarks` (11 528 entries) includes **"Tokyo Skytree"** (@ 3944,-3190) and **"Tokyo Tower"** — exact `name:en` strings, plus "Tokyo Skytree East Tower".
- Max building `h` across all tiles = **634** (Tokyo Skytree, tile `3_-4.json`) — within [600, 650] and the 650 clamp.
- Terrain `datum 9.5` m ASL (Marunouchi ≈ sea level ✓), heights min/max −37.0 / 76.7, grid 411×391 @ 20 m, 0 voids.
- Water: **214** rings (Sumida river + bay/coastline fragments present), **111** rivers, waterLevels per ring.
- **Size: index 1 979 748 B + tiles 16 650 053 B = 18 629 801 B ≈ 17.77 MB ≤ 60 MB** ✓ (biggest tile `1_-3.json` 646 790 B).
- Report summary line totals: buildings 112 743 / roads 16 902 (monolithic; 17 317 counting boundary-split pieces across tiles) / places 847 / water 214 / rivers 111 / trees 25 426 / terrain 411×391 @ 20 m / tiles 99 / largest tile 646 790 B.

### Doc changes
- `docs/fetch-osm.md`: new **Central Tokyo (`tokyo/`)** section + `--tiles`-flag note. The PM owns `docs/data-format.md` (the ticket says it updates itself at accept).

### Commit
- `b4c2e72` on branch `tigerteam/T-0097` — `[T-0097] Central Tokyo tiled dataset (fetch --chunks 3x3 --tiles, DEM N35E139)` (101 files: index + 99 tiles + docs). Working tree clean; London `city.json` untouched.

### Out-of-scope issues for the reviewer (spec-vs-code discrepancy — flagging, not fixing)
- **`--tiles` bare flag is broken in `scripts/fetch-osm.mjs` (T-0093's parser, not mine).** `parseArgs` only consumes `--key value` pairs, so the documented bare `--tiles` is skipped and its value is consumed from the next arg: with the documented command (`... --tiles --out public/data/tokyo`) the run silently wrote the monolithic **`public/data/city.json`** instead of a tiled directory. I restored `city.json` from git and produced the tiled dataset via the value form **`--tiles 1`** (`tiles === '1' || 'true'` is explicitly supported by the script). data-format.md / the doc command should be reconciled (likely the PM updates data-format.md and the T-0093 authors fix the parser).
- First run took ~4 min and hit one 429 + one 504 retry before succeeding; both Overpass endpoints were failing on chunk-scale `out geom` queries during the earlier window (overpass-api.de 504, kumi 500) — a load lull (≈ 00:08–00:19 UTC) was needed. No data was trimmed or restyled.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 45m 0s | 611.51k (556.8k) | 7.33k | $0.02 | engine | todo |
| 2 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 38m 57s | 2.39M (2.09M) | 32.06k | $0.07 | engine | review |

total: 2 attempts · 1h 23m · 3M in (2.65M cached) / 39.38k out · $0.09 (accepted 2026-08-31T00:28:01Z)
